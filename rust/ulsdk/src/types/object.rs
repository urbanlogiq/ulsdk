// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use flatbuffers::{WIPOffset, UnionWIPOffset};
use bitflags::bitflags;
use core::ops::Deref;

use crate::types::Schema::{
    Binary,
    Bool,
    Buffer,
    Date,
    DateUnit,
    Decimal,
    DictionaryEncoding,
    DictionaryKind,
    Duration,
    Endianness,
    Feature,
    Field,
    FixedSizeBinary,
    FixedSizeList,
    FloatingPoint,
    Int,
    Interval,
    IntervalUnit,
    KeyValue,
    LargeBinary,
    LargeList,
    LargeUtf8,
    List,
    Map,
    MetadataVersion,
    Null,
    Precision,
    Schema,
    Struct_,
    Time,
    TimeUnit,
    Timestamp,
    Type,
    Union,
    UnionMode,
    Utf8,
};
use crate::types::data::{
    AttributePair,
    DayOfWeek,
    DirectionAndRoadName,
    DirectionAndRoadNames,
    DirectionTy,
    NamedParameter,
    NamedParameterFlags,
    RoadUserTy,
    Source,
    StatisticTy,
    TimeGranularity,
    TurnTy,
};
use crate::types::id::{
    B2cId,
    ColumnGroupId,
    ContentId,
    DataStateId,
    GenericId,
    GraphNodeId,
    ObjectId,
    ObjectNamespace,
    StreamId,
};
use crate::types::job::{
    DeprecatedRunSpec,
    DeprecatedTaskParameter,
    Edge,
    EmbeddedTable,
    Job,
    Node,
    ParamIndices,
    RunSpec,
    Schematic,
    Status,
    Task,
    TaskErrorTy,
    TaskList,
    TaskParameter,
    TaskParameterValue,
    TaskPriority,
    TaskRunFlags,
};
use crate::types::reflection::{
    ReflectionAdvancedFeatures,
    ReflectionBaseType,
    ReflectionEnum,
    ReflectionEnumVal,
    ReflectionField,
    ReflectionKeyValue,
    ReflectionObject,
    ReflectionRPCCall,
    ReflectionSchema,
    ReflectionSchemaFile,
    ReflectionService,
    ReflectionType,
};
use crate::types::stream::{
    AxisType,
    FormatFlags,
    Stream,
    StreamFlags,
};
use crate::types::value::{
    Point2D,
    Tri2D,
    VArray,
    VBool,
    VBytes,
    VChar,
    VF32,
    VF64,
    VFixedSizeBytes,
    VI16,
    VI32,
    VI64,
    VI8,
    VIsize,
    VNull,
    VPlaceholder,
    VStr,
    VTimestampMs,
    VTimestampMsUtc,
    VTimestampNs,
    VTimestampNsUtc,
    VTri2D,
    VU16,
    VU32,
    VU64,
    VU8,
    VUnit,
    VUsize,
    Value,
    ValueInstance,
    ValueTy,
};
use crate::types::worklog::{
    AggregationTy,
    ByteArray,
    ChartTypeTy,
    Layout,
    ParameterFlags,
    ParameterValue,
    TileData,
    TileSettings,
    UserSettings,
    ValuesFormatTy,
    WorkLog,
    WorklogParameter,
};
use crate::types::generated::Schema_generated::{
    Binary as FbsBinary,
    Bool as FbsBool,
    Buffer as FbsBuffer,
    Date as FbsDate,
    Decimal as FbsDecimal,
    DictionaryEncoding as FbsDictionaryEncoding,
    Duration as FbsDuration,
    Field as FbsField,
    FixedSizeBinary as FbsFixedSizeBinary,
    FixedSizeList as FbsFixedSizeList,
    FloatingPoint as FbsFloatingPoint,
    Int as FbsInt,
    Interval as FbsInterval,
    KeyValue as FbsKeyValue,
    LargeBinary as FbsLargeBinary,
    LargeList as FbsLargeList,
    LargeUtf8 as FbsLargeUtf8,
    List as FbsList,
    Map as FbsMap,
    Null as FbsNull,
    Schema as FbsSchema,
    Struct_ as FbsStruct_,
    Time as FbsTime,
    Timestamp as FbsTimestamp,
    Union as FbsUnion,
    Utf8 as FbsUtf8,
    DateUnit as FbsDateUnit,
    DictionaryKind as FbsDictionaryKind,
    Endianness as FbsEndianness,
    Feature as FbsFeature,
    IntervalUnit as FbsIntervalUnit,
    MetadataVersion as FbsMetadataVersion,
    Precision as FbsPrecision,
    TimeUnit as FbsTimeUnit,
    Type as FbsType,
    UnionMode as FbsUnionMode,
};
use crate::types::generated::data_generated::{
    AttributePair as FbsAttributePair,
    DirectionAndRoadName as FbsDirectionAndRoadName,
    DirectionAndRoadNames as FbsDirectionAndRoadNames,
    NamedParameter as FbsNamedParameter,
    Source as FbsSource,
    DayOfWeek as FbsDayOfWeek,
    DirectionTy as FbsDirectionTy,
    NamedParameterFlags as FbsNamedParameterFlags,
    RoadUserTy as FbsRoadUserTy,
    StatisticTy as FbsStatisticTy,
    TimeGranularity as FbsTimeGranularity,
    TurnTy as FbsTurnTy,
};
use crate::types::generated::id_generated::{
    B2cId as FbsB2cId,
    ColumnGroupId as FbsColumnGroupId,
    ContentId as FbsContentId,
    DataStateId as FbsDataStateId,
    GenericId as FbsGenericId,
    GraphNodeId as FbsGraphNodeId,
    ObjectId as FbsObjectId,
    StreamId as FbsStreamId,
    ObjectNamespace as FbsObjectNamespace,
};
use crate::types::generated::job_generated::{
    DeprecatedRunSpec as FbsDeprecatedRunSpec,
    DeprecatedTaskParameter as FbsDeprecatedTaskParameter,
    Edge as FbsEdge,
    EmbeddedTable as FbsEmbeddedTable,
    Job as FbsJob,
    Node as FbsNode,
    ParamIndices as FbsParamIndices,
    RunSpec as FbsRunSpec,
    Schematic as FbsSchematic,
    Task as FbsTask,
    TaskList as FbsTaskList,
    TaskParameter as FbsTaskParameter,
    Status as FbsStatus,
    TaskErrorTy as FbsTaskErrorTy,
    TaskParameterValue as FbsTaskParameterValue,
    TaskPriority as FbsTaskPriority,
    TaskRunFlags as FbsTaskRunFlags,
};
use crate::types::generated::object_generated::{
    DataCatalogObject as FbsDataCatalogObject,
    ObjectIdList as FbsObjectIdList,
    ObjectIdPair as FbsObjectIdPair,
    ObjectIdPairList as FbsObjectIdPairList,
    ObjectSummary as FbsObjectSummary,
    ObjectSummaryList as FbsObjectSummaryList,
    DataCatalogObjectFlags as FbsDataCatalogObjectFlags,
    DataCatalogObjectTy as FbsDataCatalogObjectTy,
};
use crate::types::generated::reflection_generated::{
    reflection::Enum as FbsReflectionEnum,
    reflection::EnumVal as FbsReflectionEnumVal,
    reflection::Field as FbsReflectionField,
    reflection::KeyValue as FbsReflectionKeyValue,
    reflection::Object as FbsReflectionObject,
    reflection::RPCCall as FbsReflectionRPCCall,
    reflection::Schema as FbsReflectionSchema,
    reflection::SchemaFile as FbsReflectionSchemaFile,
    reflection::Service as FbsReflectionService,
    reflection::Type as FbsReflectionType,
    reflection::AdvancedFeatures as FbsReflectionAdvancedFeatures,
    reflection::BaseType as FbsReflectionBaseType,
};
use crate::types::generated::stream_generated::{
    Stream as FbsStream,
    AxisType as FbsAxisType,
    FormatFlags as FbsFormatFlags,
    StreamFlags as FbsStreamFlags,
};
use crate::types::generated::value_generated::{
    Point2D as FbsPoint2D,
    Tri2D as FbsTri2D,
    VArray as FbsVArray,
    VBool as FbsVBool,
    VBytes as FbsVBytes,
    VChar as FbsVChar,
    VF32 as FbsVF32,
    VF64 as FbsVF64,
    VFixedSizeBytes as FbsVFixedSizeBytes,
    VI16 as FbsVI16,
    VI32 as FbsVI32,
    VI64 as FbsVI64,
    VI8 as FbsVI8,
    VIsize as FbsVIsize,
    VNull as FbsVNull,
    VPlaceholder as FbsVPlaceholder,
    VStr as FbsVStr,
    VTimestampMs as FbsVTimestampMs,
    VTimestampMsUtc as FbsVTimestampMsUtc,
    VTimestampNs as FbsVTimestampNs,
    VTimestampNsUtc as FbsVTimestampNsUtc,
    VTri2D as FbsVTri2D,
    VU16 as FbsVU16,
    VU32 as FbsVU32,
    VU64 as FbsVU64,
    VU8 as FbsVU8,
    VUnit as FbsVUnit,
    VUsize as FbsVUsize,
    ValueInstance as FbsValueInstance,
    Value as FbsValue,
    ValueTy as FbsValueTy,
};
use crate::types::generated::worklog_generated::{
    ByteArray as FbsByteArray,
    Layout as FbsLayout,
    ParameterFlags as FbsParameterFlags,
    TileData as FbsTileData,
    TileSettings as FbsTileSettings,
    UserSettings as FbsUserSettings,
    WorkLog as FbsWorkLog,
    WorklogParameter as FbsWorklogParameter,
    AggregationTy as FbsAggregationTy,
    ChartTypeTy as FbsChartTypeTy,
    ParameterValue as FbsParameterValue,
    ValuesFormatTy as FbsValuesFormatTy,
};

bitflags! {
    #[derive(Default)]
    pub struct DataCatalogObjectFlags: u32 {
        /// objects generated by a job
        const _GENERATED = 1;
        /// Object contents are encrypted. When this is set the `obj` field is an
        /// EncryptedObject instance.
        const _ENCRYPTED = 2;
        /// Object has a cryptographic signature
        const _SIGNED = 4;
    }
}

impl From<DataCatalogObjectFlags> for FbsDataCatalogObjectFlags {
    fn from(val: DataCatalogObjectFlags) -> Self {
        Self::from_bits_truncate(val.bits())
    }
}

impl From<FbsDataCatalogObjectFlags> for DataCatalogObjectFlags {
    fn from(fbs: FbsDataCatalogObjectFlags) -> Self {
        Self::from_bits_truncate(fbs.bits())
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum DataCatalogObjectTy {
    #[default]
    Invalid = 0,
    WorkLog = 1,
    Schematic = 2,
    Node = 3,
    Stream = 4,
    Metadata = 5,
    Source = 6,
    UserProject = 7,
    UseCase = 8,
    UserPreferences = 9,
    AccessControlList = 10,
    DirectoryEntry = 11,
    Notification = 12,
    Model = 13,
    Ingestion = 14,
    View = 15,
}

impl From<DataCatalogObjectTy> for FbsDataCatalogObjectTy {
    fn from(val: DataCatalogObjectTy) -> Self {
        match val {
            DataCatalogObjectTy::Invalid => FbsDataCatalogObjectTy::Invalid,
            DataCatalogObjectTy::WorkLog => FbsDataCatalogObjectTy::WorkLog,
            DataCatalogObjectTy::Schematic => FbsDataCatalogObjectTy::Schematic,
            DataCatalogObjectTy::Node => FbsDataCatalogObjectTy::Node,
            DataCatalogObjectTy::Stream => FbsDataCatalogObjectTy::Stream,
            DataCatalogObjectTy::Metadata => FbsDataCatalogObjectTy::Metadata,
            DataCatalogObjectTy::Source => FbsDataCatalogObjectTy::Source,
            DataCatalogObjectTy::UserProject => FbsDataCatalogObjectTy::UserProject,
            DataCatalogObjectTy::UseCase => FbsDataCatalogObjectTy::UseCase,
            DataCatalogObjectTy::UserPreferences => FbsDataCatalogObjectTy::UserPreferences,
            DataCatalogObjectTy::AccessControlList => FbsDataCatalogObjectTy::AccessControlList,
            DataCatalogObjectTy::DirectoryEntry => FbsDataCatalogObjectTy::DirectoryEntry,
            DataCatalogObjectTy::Notification => FbsDataCatalogObjectTy::Notification,
            DataCatalogObjectTy::Model => FbsDataCatalogObjectTy::Model,
            DataCatalogObjectTy::Ingestion => FbsDataCatalogObjectTy::Ingestion,
            DataCatalogObjectTy::View => FbsDataCatalogObjectTy::View,
        }
    }
}

impl From<FbsDataCatalogObjectTy> for DataCatalogObjectTy {
    fn from(fbs: FbsDataCatalogObjectTy) -> Self {
        match fbs.0 {
            0 => Self::Invalid,
            1 => Self::WorkLog,
            2 => Self::Schematic,
            3 => Self::Node,
            4 => Self::Stream,
            5 => Self::Metadata,
            6 => Self::Source,
            7 => Self::UserProject,
            8 => Self::UseCase,
            9 => Self::UserPreferences,
            10 => Self::AccessControlList,
            11 => Self::DirectoryEntry,
            12 => Self::Notification,
            13 => Self::Model,
            14 => Self::Ingestion,
            15 => Self::View,
            _ => panic!("Invalid value {} when constructing DataCatalogObjectTy", fbs.0)
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct DataCatalogObject {
    attributes: Option<Vec<AttributePair>>,
    /// Optional change log comment
    comment: Option<String>,
    /// Default protection mode (see PermissionTy in the permissions module).
    /// Purpose is to determine what happens when an object is navigated to
    /// (ie: a directory in the drive). Defaults to 0 (ie: no access)
    default_mode: u32,
    flags: u32,
    /// This field is either an embedded flatbuffer containing the actual object
    /// content (ie: worklog, schematic, ...) if the Encrypted flag is unset, or
    /// an EncryptedObject where the obj field of the EncryptedObject table is
    /// the embedded flatbuffer of the object if it is set.
    obj: Vec<u8>,
    /// Parent nodes of this commit. To handle the cases of multiple parents (ie:
    /// in cases of parallel mutation), this field allows multiple IDs to be specified.
    parents: Vec<ContentId>,
    signature: Option<Vec<u8>>,
    tags: Option<Vec<String>>,
    /// UTC timestamp (in ms) when this change was made.
    time: u64,
    ty: DataCatalogObjectTy,
    /// User ID of the person committing the change.
    user: B2cId,
    version: u32,
}

impl DataCatalogObject {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsDataCatalogObject<'a>> {
        use crate::types::generated::object_generated::DataCatalogObjectBuilder as FbsDataCatalogObjectBuilder;

        let attributes_offset = self.attributes.as_ref().map(|v| {
            let mut attributes_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                attributes_offsets.push(offset);
            }
            let attributes_offset = builder.create_vector(&attributes_offsets);
            attributes_offset
        });
        let comment_offset = self.comment.as_ref().map(|s| builder.create_string(s));
        let obj_offset = builder.create_vector(&self.obj);
        let mut parents_offsets = Vec::with_capacity(self.parents.len());
        for val in self.parents.iter() {
            let offset = val.serialize_to(builder);
            parents_offsets.push(offset);
        }
        let parents_offset = builder.create_vector(&parents_offsets);
        let signature_offset = self.signature.as_ref().map(|v| {
            let signature_offset = builder.create_vector(&v);
            signature_offset
        });
        let tags_offset = self.tags.as_ref().map(|v| {
            let mut tags_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = builder.create_string(val);
                tags_offsets.push(offset);
            }
            let tags_offset = builder.create_vector(&tags_offsets);
            tags_offset
        });
        let user_offset = self.user.serialize_to(builder);

        let mut bldr = FbsDataCatalogObjectBuilder::new(builder);
        if let Some(offset) = attributes_offset {
            bldr.add_attributes(offset);
        }
        if let Some(offset) = comment_offset {
            bldr.add_comment(offset);
        }
        bldr.add_default_mode(self.default_mode);
        bldr.add_flags(self.flags);
        bldr.add_obj(obj_offset);
        bldr.add_parents(parents_offset);
        if let Some(offset) = signature_offset {
            bldr.add_signature(offset);
        }
        if let Some(offset) = tags_offset {
            bldr.add_tags(offset);
        }
        bldr.add_time(self.time);
        bldr.add_ty(FbsDataCatalogObjectTy::from(self.ty));
        bldr.add_user(user_offset);
        bldr.add_version(self.version);
        bldr.finish()
    }
}

impl From<FbsDataCatalogObject<'_>> for DataCatalogObject {
    fn from(fbs: FbsDataCatalogObject<'_>) -> Self {
        let attributes = if let Some(val) = fbs.attributes() {
            let mut attributes = Vec::new();
            for elem in val {
                attributes.push(elem.into());
            }

            Some(attributes)
        } else {
            None
        };

        let comment = fbs.comment().map(ToOwned::to_owned);
        let default_mode = fbs.default_mode();
        let flags = fbs.flags();
        let mut obj = Vec::new();
        for elem in fbs.obj() {
            obj.push(elem.into());
        }

        let mut parents = Vec::new();
        for elem in fbs.parents() {
            parents.push(elem.into());
        }

        let signature = if let Some(val) = fbs.signature() {
            let mut signature = Vec::new();
            for elem in val {
                signature.push(elem.into());
            }

            Some(signature)
        } else {
            None
        };

        let tags = if let Some(val) = fbs.tags() {
            let mut tags = Vec::new();
            for elem in val {
                tags.push(elem.into());
            }

            Some(tags)
        } else {
            None
        };

        let time = fbs.time();
        let ty = DataCatalogObjectTy::from(fbs.ty());
        let user = B2cId::from(fbs.user());
        let version = fbs.version();
        Self {
            attributes,
            comment,
            default_mode,
            flags,
            obj,
            parents,
            signature,
            tags,
            time,
            ty,
            user,
            version,
        }
    }
}

impl TryFrom<&[u8]> for DataCatalogObject {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsDataCatalogObject>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<DataCatalogObject> for Vec<u8> {
    fn from(obj: DataCatalogObject) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectIdList {
    ids: Vec<ObjectId>,
}

impl ObjectIdList {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsObjectIdList<'a>> {
        use crate::types::generated::object_generated::ObjectIdListBuilder as FbsObjectIdListBuilder;

        let mut ids_offsets = Vec::with_capacity(self.ids.len());
        for val in self.ids.iter() {
            let offset = val.serialize_to(builder);
            ids_offsets.push(offset);
        }
        let ids_offset = builder.create_vector(&ids_offsets);

        let mut bldr = FbsObjectIdListBuilder::new(builder);
        bldr.add_ids(ids_offset);
        bldr.finish()
    }
}

impl From<FbsObjectIdList<'_>> for ObjectIdList {
    fn from(fbs: FbsObjectIdList<'_>) -> Self {
        let mut ids = Vec::new();
        for elem in fbs.ids() {
            ids.push(elem.into());
        }

        Self {
            ids,
        }
    }
}

impl TryFrom<&[u8]> for ObjectIdList {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectIdList>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectIdList> for Vec<u8> {
    fn from(obj: ObjectIdList) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectIdPair {
    id: ObjectId,
    object: Option<Vec<u8>>,
}

impl ObjectIdPair {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsObjectIdPair<'a>> {
        use crate::types::generated::object_generated::ObjectIdPairBuilder as FbsObjectIdPairBuilder;

        let id_offset = self.id.serialize_to(builder);
        let object_offset = self.object.as_ref().map(|v| {
            let object_offset = builder.create_vector(&v);
            object_offset
        });

        let mut bldr = FbsObjectIdPairBuilder::new(builder);
        bldr.add_id(id_offset);
        if let Some(offset) = object_offset {
            bldr.add_object(offset);
        }
        bldr.finish()
    }
}

impl From<FbsObjectIdPair<'_>> for ObjectIdPair {
    fn from(fbs: FbsObjectIdPair<'_>) -> Self {
        let id = ObjectId::from(fbs.id());
        let object = if let Some(val) = fbs.object() {
            let mut object = Vec::new();
            for elem in val {
                object.push(elem.into());
            }

            Some(object)
        } else {
            None
        };

        Self {
            id,
            object,
        }
    }
}

impl TryFrom<&[u8]> for ObjectIdPair {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectIdPair>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectIdPair> for Vec<u8> {
    fn from(obj: ObjectIdPair) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectIdPairList {
    pairs: Vec<ObjectIdPair>,
}

impl ObjectIdPairList {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsObjectIdPairList<'a>> {
        use crate::types::generated::object_generated::ObjectIdPairListBuilder as FbsObjectIdPairListBuilder;

        let mut pairs_offsets = Vec::with_capacity(self.pairs.len());
        for val in self.pairs.iter() {
            let offset = val.serialize_to(builder);
            pairs_offsets.push(offset);
        }
        let pairs_offset = builder.create_vector(&pairs_offsets);

        let mut bldr = FbsObjectIdPairListBuilder::new(builder);
        bldr.add_pairs(pairs_offset);
        bldr.finish()
    }
}

impl From<FbsObjectIdPairList<'_>> for ObjectIdPairList {
    fn from(fbs: FbsObjectIdPairList<'_>) -> Self {
        let mut pairs = Vec::new();
        for elem in fbs.pairs() {
            pairs.push(elem.into());
        }

        Self {
            pairs,
        }
    }
}

impl TryFrom<&[u8]> for ObjectIdPairList {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectIdPairList>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectIdPairList> for Vec<u8> {
    fn from(obj: ObjectIdPairList) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectSummary {
    acl: Option<ObjectId>,
    drive_size: u64,
    head_revision: ContentId,
    id: ObjectId,
    time: u64,
    ty: DataCatalogObjectTy,
}

impl ObjectSummary {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsObjectSummary<'a>> {
        use crate::types::generated::object_generated::ObjectSummaryBuilder as FbsObjectSummaryBuilder;

        let acl_offset = self.acl.as_ref().map(|o| o.serialize_to(builder));
        let head_revision_offset = self.head_revision.serialize_to(builder);
        let id_offset = self.id.serialize_to(builder);

        let mut bldr = FbsObjectSummaryBuilder::new(builder);
        if let Some(offset) = acl_offset {
            bldr.add_acl(offset);
        }
        bldr.add_drive_size(self.drive_size);
        bldr.add_head_revision(head_revision_offset);
        bldr.add_id(id_offset);
        bldr.add_time(self.time);
        bldr.add_ty(FbsDataCatalogObjectTy::from(self.ty));
        bldr.finish()
    }
}

impl From<FbsObjectSummary<'_>> for ObjectSummary {
    fn from(fbs: FbsObjectSummary<'_>) -> Self {
        let acl = fbs.acl().map(ObjectId::from);
        let drive_size = fbs.drive_size();
        let head_revision = ContentId::from(fbs.head_revision());
        let id = ObjectId::from(fbs.id());
        let time = fbs.time();
        let ty = DataCatalogObjectTy::from(fbs.ty());
        Self {
            acl,
            drive_size,
            head_revision,
            id,
            time,
            ty,
        }
    }
}

impl TryFrom<&[u8]> for ObjectSummary {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectSummary>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectSummary> for Vec<u8> {
    fn from(obj: ObjectSummary) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectSummaryList {
    pairs: Vec<ObjectSummary>,
}

impl ObjectSummaryList {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsObjectSummaryList<'a>> {
        use crate::types::generated::object_generated::ObjectSummaryListBuilder as FbsObjectSummaryListBuilder;

        let mut pairs_offsets = Vec::with_capacity(self.pairs.len());
        for val in self.pairs.iter() {
            let offset = val.serialize_to(builder);
            pairs_offsets.push(offset);
        }
        let pairs_offset = builder.create_vector(&pairs_offsets);

        let mut bldr = FbsObjectSummaryListBuilder::new(builder);
        bldr.add_pairs(pairs_offset);
        bldr.finish()
    }
}

impl From<FbsObjectSummaryList<'_>> for ObjectSummaryList {
    fn from(fbs: FbsObjectSummaryList<'_>) -> Self {
        let mut pairs = Vec::new();
        for elem in fbs.pairs() {
            pairs.push(elem.into());
        }

        Self {
            pairs,
        }
    }
}

impl TryFrom<&[u8]> for ObjectSummaryList {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectSummaryList>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectSummaryList> for Vec<u8> {
    fn from(obj: ObjectSummaryList) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_data_catalog_object() {
        let t0 = DataCatalogObject::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = DataCatalogObject::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_id_list() {
        let t0 = ObjectIdList::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectIdList::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_id_pair() {
        let t0 = ObjectIdPair::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectIdPair::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_id_pair_list() {
        let t0 = ObjectIdPairList::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectIdPairList::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_summary() {
        let t0 = ObjectSummary::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectSummary::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_summary_list() {
        let t0 = ObjectSummaryList::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectSummaryList::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

}