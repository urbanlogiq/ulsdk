// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use flatbuffers::{WIPOffset, UnionWIPOffset};
use bitflags::bitflags;
use core::ops::Deref;

use crate::types::generated::value_generated::{
    Point2D as FbsPoint2D,
    Tri2D as FbsTri2D,
    VArray as FbsVArray,
    VBool as FbsVBool,
    VBytes as FbsVBytes,
    VChar as FbsVChar,
    VF32 as FbsVF32,
    VF64 as FbsVF64,
    VFixedSizeBytes as FbsVFixedSizeBytes,
    VI16 as FbsVI16,
    VI32 as FbsVI32,
    VI64 as FbsVI64,
    VI8 as FbsVI8,
    VIsize as FbsVIsize,
    VNull as FbsVNull,
    VPlaceholder as FbsVPlaceholder,
    VStr as FbsVStr,
    VTimestampMs as FbsVTimestampMs,
    VTimestampMsUtc as FbsVTimestampMsUtc,
    VTimestampNs as FbsVTimestampNs,
    VTimestampNsUtc as FbsVTimestampNsUtc,
    VTri2D as FbsVTri2D,
    VU16 as FbsVU16,
    VU32 as FbsVU32,
    VU64 as FbsVU64,
    VU8 as FbsVU8,
    VUnit as FbsVUnit,
    VUsize as FbsVUsize,
    ValueInstance as FbsValueInstance,
    Value as FbsValue,
    ValueTy as FbsValueTy,
};

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum ValueTy {
    #[default]
    Bool = 0,
    Unit = 1,
    Char = 2,
    Null = 3,
    I8 = 4,
    U8 = 5,
    I16 = 6,
    U16 = 7,
    I32 = 8,
    U32 = 9,
    F32 = 10,
    Isize = 11,
    Usize = 12,
    I64 = 13,
    U64 = 14,
    F64 = 15,
    Str = 16,
    Bytes = 17,
    Array = 18,
    Tri2D = 19,
    FixedSizeBytes = 20,
    TimestampMsUtc = 21,
    TimestampMs = 22,
    TimestampNsUtc = 23,
    TimestampNs = 24,
    Placeholder = 25,
}

impl From<ValueTy> for FbsValueTy {
    fn from(val: ValueTy) -> Self {
        match val {
            ValueTy::Bool => FbsValueTy::Bool,
            ValueTy::Unit => FbsValueTy::Unit,
            ValueTy::Char => FbsValueTy::Char,
            ValueTy::Null => FbsValueTy::Null,
            ValueTy::I8 => FbsValueTy::I8,
            ValueTy::U8 => FbsValueTy::U8,
            ValueTy::I16 => FbsValueTy::I16,
            ValueTy::U16 => FbsValueTy::U16,
            ValueTy::I32 => FbsValueTy::I32,
            ValueTy::U32 => FbsValueTy::U32,
            ValueTy::F32 => FbsValueTy::F32,
            ValueTy::Isize => FbsValueTy::Isize,
            ValueTy::Usize => FbsValueTy::Usize,
            ValueTy::I64 => FbsValueTy::I64,
            ValueTy::U64 => FbsValueTy::U64,
            ValueTy::F64 => FbsValueTy::F64,
            ValueTy::Str => FbsValueTy::Str,
            ValueTy::Bytes => FbsValueTy::Bytes,
            ValueTy::Array => FbsValueTy::Array,
            ValueTy::Tri2D => FbsValueTy::Tri2D,
            ValueTy::FixedSizeBytes => FbsValueTy::FixedSizeBytes,
            ValueTy::TimestampMsUtc => FbsValueTy::TimestampMsUtc,
            ValueTy::TimestampMs => FbsValueTy::TimestampMs,
            ValueTy::TimestampNsUtc => FbsValueTy::TimestampNsUtc,
            ValueTy::TimestampNs => FbsValueTy::TimestampNs,
            ValueTy::Placeholder => FbsValueTy::Placeholder,
        }
    }
}

impl From<FbsValueTy> for ValueTy {
    fn from(fbs: FbsValueTy) -> Self {
        match fbs.0 {
            0 => Self::Bool,
            1 => Self::Unit,
            2 => Self::Char,
            3 => Self::Null,
            4 => Self::I8,
            5 => Self::U8,
            6 => Self::I16,
            7 => Self::U16,
            8 => Self::I32,
            9 => Self::U32,
            10 => Self::F32,
            11 => Self::Isize,
            12 => Self::Usize,
            13 => Self::I64,
            14 => Self::U64,
            15 => Self::F64,
            16 => Self::Str,
            17 => Self::Bytes,
            18 => Self::Array,
            19 => Self::Tri2D,
            20 => Self::FixedSizeBytes,
            21 => Self::TimestampMsUtc,
            22 => Self::TimestampMs,
            23 => Self::TimestampNsUtc,
            24 => Self::TimestampNs,
            25 => Self::Placeholder,
            _ => panic!("Invalid value {} when constructing ValueTy", fbs.0)
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VBool {
    pub v: bool,
}

impl VBool {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVBool<'a>> {
        use crate::types::generated::value_generated::VBoolBuilder as FbsVBoolBuilder;

        let mut bldr = FbsVBoolBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVBool<'_>> for VBool {
    fn from(fbs: FbsVBool<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VBool {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVBool>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VBool> for Vec<u8> {
    fn from(obj: VBool) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VUnit {
}

impl VUnit {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVUnit<'a>> {
        use crate::types::generated::value_generated::VUnitBuilder as FbsVUnitBuilder;

        let mut bldr = FbsVUnitBuilder::new(builder);
        bldr.finish()
    }
}

impl From<FbsVUnit<'_>> for VUnit {
    fn from(fbs: FbsVUnit<'_>) -> Self {
        Self {
        }
    }
}

impl TryFrom<&[u8]> for VUnit {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVUnit>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VUnit> for Vec<u8> {
    fn from(obj: VUnit) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VChar {
    pub v: u32,
}

impl VChar {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVChar<'a>> {
        use crate::types::generated::value_generated::VCharBuilder as FbsVCharBuilder;

        let mut bldr = FbsVCharBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVChar<'_>> for VChar {
    fn from(fbs: FbsVChar<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VChar {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVChar>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VChar> for Vec<u8> {
    fn from(obj: VChar) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VNull {
}

impl VNull {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVNull<'a>> {
        use crate::types::generated::value_generated::VNullBuilder as FbsVNullBuilder;

        let mut bldr = FbsVNullBuilder::new(builder);
        bldr.finish()
    }
}

impl From<FbsVNull<'_>> for VNull {
    fn from(fbs: FbsVNull<'_>) -> Self {
        Self {
        }
    }
}

impl TryFrom<&[u8]> for VNull {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVNull>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VNull> for Vec<u8> {
    fn from(obj: VNull) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VI8 {
    pub v: i8,
}

impl VI8 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVI8<'a>> {
        use crate::types::generated::value_generated::VI8Builder as FbsVI8Builder;

        let mut bldr = FbsVI8Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVI8<'_>> for VI8 {
    fn from(fbs: FbsVI8<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VI8 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVI8>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VI8> for Vec<u8> {
    fn from(obj: VI8) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VU8 {
    pub v: u8,
}

impl VU8 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVU8<'a>> {
        use crate::types::generated::value_generated::VU8Builder as FbsVU8Builder;

        let mut bldr = FbsVU8Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVU8<'_>> for VU8 {
    fn from(fbs: FbsVU8<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VU8 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVU8>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VU8> for Vec<u8> {
    fn from(obj: VU8) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VI16 {
    pub v: i16,
}

impl VI16 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVI16<'a>> {
        use crate::types::generated::value_generated::VI16Builder as FbsVI16Builder;

        let mut bldr = FbsVI16Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVI16<'_>> for VI16 {
    fn from(fbs: FbsVI16<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VI16 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVI16>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VI16> for Vec<u8> {
    fn from(obj: VI16) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VU16 {
    pub v: u16,
}

impl VU16 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVU16<'a>> {
        use crate::types::generated::value_generated::VU16Builder as FbsVU16Builder;

        let mut bldr = FbsVU16Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVU16<'_>> for VU16 {
    fn from(fbs: FbsVU16<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VU16 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVU16>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VU16> for Vec<u8> {
    fn from(obj: VU16) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VI32 {
    pub v: i32,
}

impl VI32 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVI32<'a>> {
        use crate::types::generated::value_generated::VI32Builder as FbsVI32Builder;

        let mut bldr = FbsVI32Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVI32<'_>> for VI32 {
    fn from(fbs: FbsVI32<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VI32 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVI32>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VI32> for Vec<u8> {
    fn from(obj: VI32) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VU32 {
    pub v: u32,
}

impl VU32 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVU32<'a>> {
        use crate::types::generated::value_generated::VU32Builder as FbsVU32Builder;

        let mut bldr = FbsVU32Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVU32<'_>> for VU32 {
    fn from(fbs: FbsVU32<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VU32 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVU32>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VU32> for Vec<u8> {
    fn from(obj: VU32) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VF32 {
    pub v: f32,
}

impl VF32 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVF32<'a>> {
        use crate::types::generated::value_generated::VF32Builder as FbsVF32Builder;

        let mut bldr = FbsVF32Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVF32<'_>> for VF32 {
    fn from(fbs: FbsVF32<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VF32 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVF32>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VF32> for Vec<u8> {
    fn from(obj: VF32) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VIsize {
    pub v: i64,
}

impl VIsize {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVIsize<'a>> {
        use crate::types::generated::value_generated::VIsizeBuilder as FbsVIsizeBuilder;

        let mut bldr = FbsVIsizeBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVIsize<'_>> for VIsize {
    fn from(fbs: FbsVIsize<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VIsize {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVIsize>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VIsize> for Vec<u8> {
    fn from(obj: VIsize) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VUsize {
    pub v: u64,
}

impl VUsize {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVUsize<'a>> {
        use crate::types::generated::value_generated::VUsizeBuilder as FbsVUsizeBuilder;

        let mut bldr = FbsVUsizeBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVUsize<'_>> for VUsize {
    fn from(fbs: FbsVUsize<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VUsize {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVUsize>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VUsize> for Vec<u8> {
    fn from(obj: VUsize) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VI64 {
    pub v: i64,
}

impl VI64 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVI64<'a>> {
        use crate::types::generated::value_generated::VI64Builder as FbsVI64Builder;

        let mut bldr = FbsVI64Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVI64<'_>> for VI64 {
    fn from(fbs: FbsVI64<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VI64 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVI64>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VI64> for Vec<u8> {
    fn from(obj: VI64) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VU64 {
    pub v: u64,
}

impl VU64 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVU64<'a>> {
        use crate::types::generated::value_generated::VU64Builder as FbsVU64Builder;

        let mut bldr = FbsVU64Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVU64<'_>> for VU64 {
    fn from(fbs: FbsVU64<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VU64 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVU64>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VU64> for Vec<u8> {
    fn from(obj: VU64) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VF64 {
    pub v: f64,
}

impl VF64 {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVF64<'a>> {
        use crate::types::generated::value_generated::VF64Builder as FbsVF64Builder;

        let mut bldr = FbsVF64Builder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVF64<'_>> for VF64 {
    fn from(fbs: FbsVF64<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VF64 {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVF64>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VF64> for Vec<u8> {
    fn from(obj: VF64) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VStr {
    pub v: String,
}

impl VStr {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVStr<'a>> {
        use crate::types::generated::value_generated::VStrBuilder as FbsVStrBuilder;

        let v_offset = builder.create_string(&self.v);

        let mut bldr = FbsVStrBuilder::new(builder);
        bldr.add_v(v_offset);
        bldr.finish()
    }
}

impl From<FbsVStr<'_>> for VStr {
    fn from(fbs: FbsVStr<'_>) -> Self {
        let v = fbs.v().to_owned();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VStr {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVStr>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VStr> for Vec<u8> {
    fn from(obj: VStr) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VBytes {
    pub v: Vec<u8>,
}

impl VBytes {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVBytes<'a>> {
        use crate::types::generated::value_generated::VBytesBuilder as FbsVBytesBuilder;

        let v_offset = builder.create_vector(&self.v);

        let mut bldr = FbsVBytesBuilder::new(builder);
        bldr.add_v(v_offset);
        bldr.finish()
    }
}

impl From<FbsVBytes<'_>> for VBytes {
    fn from(fbs: FbsVBytes<'_>) -> Self {
        let mut v = Vec::new();
        for elem in fbs.v() {
            v.push(elem.into());
        }

        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VBytes {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVBytes>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VBytes> for Vec<u8> {
    fn from(obj: VBytes) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VArray {
    pub v: Vec<ValueInstance>,
}

impl VArray {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVArray<'a>> {
        use crate::types::generated::value_generated::VArrayBuilder as FbsVArrayBuilder;

        let mut v_offsets = Vec::with_capacity(self.v.len());
        for val in self.v.iter() {
            let offset = val.serialize_to(builder);
            v_offsets.push(offset);
        }
        let v_offset = builder.create_vector(&v_offsets);

        let mut bldr = FbsVArrayBuilder::new(builder);
        bldr.add_v(v_offset);
        bldr.finish()
    }
}

impl From<FbsVArray<'_>> for VArray {
    fn from(fbs: FbsVArray<'_>) -> Self {
        let mut v = Vec::new();
        for elem in fbs.v() {
            v.push(elem.into());
        }

        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VArray {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVArray>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VArray> for Vec<u8> {
    fn from(obj: VArray) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Point2D {
    pub x: f32,
    pub y: f32,
}

impl From<Point2D> for FbsPoint2D {
    fn from(obj: Point2D) -> Self {
        let x = obj.x;
        let y = obj.y;

        Self::new(x, y)
    }
}

impl From<&FbsPoint2D> for Point2D {
    fn from(fbs: &FbsPoint2D) -> Self {
        Self {
            x: fbs.x(),
            y: fbs.y(),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Tri2D {
    pub p0: Point2D,
    pub p1: Point2D,
    pub p2: Point2D,
}

impl From<Tri2D> for FbsTri2D {
    fn from(obj: Tri2D) -> Self {
        let p0 = obj.p0.into();
        let p1 = obj.p1.into();
        let p2 = obj.p2.into();

        Self::new(&p0, &p1, &p2)
    }
}

impl From<&FbsTri2D> for Tri2D {
    fn from(fbs: &FbsTri2D) -> Self {
        Self {
            p0: Point2D::from(fbs.p0()),
            p1: Point2D::from(fbs.p1()),
            p2: Point2D::from(fbs.p2()),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VTri2D {
    pub v: Tri2D,
}

impl VTri2D {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVTri2D<'a>> {
        use crate::types::generated::value_generated::VTri2DBuilder as FbsVTri2DBuilder;

        let v: FbsTri2D = self.v.clone().into();

        let mut bldr = FbsVTri2DBuilder::new(builder);
        bldr.add_v(&v);
        bldr.finish()
    }
}

impl From<FbsVTri2D<'_>> for VTri2D {
    fn from(fbs: FbsVTri2D<'_>) -> Self {
        let v = Tri2D::from(fbs.v());
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VTri2D {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVTri2D>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VTri2D> for Vec<u8> {
    fn from(obj: VTri2D) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VFixedSizeBytes {
    pub sz: i32,
    pub v: Vec<u8>,
}

impl VFixedSizeBytes {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVFixedSizeBytes<'a>> {
        use crate::types::generated::value_generated::VFixedSizeBytesBuilder as FbsVFixedSizeBytesBuilder;

        let v_offset = builder.create_vector(&self.v);

        let mut bldr = FbsVFixedSizeBytesBuilder::new(builder);
        bldr.add_sz(self.sz);
        bldr.add_v(v_offset);
        bldr.finish()
    }
}

impl From<FbsVFixedSizeBytes<'_>> for VFixedSizeBytes {
    fn from(fbs: FbsVFixedSizeBytes<'_>) -> Self {
        let sz = fbs.sz();
        let mut v = Vec::new();
        for elem in fbs.v() {
            v.push(elem.into());
        }

        Self {
            sz,
            v,
        }
    }
}

impl TryFrom<&[u8]> for VFixedSizeBytes {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVFixedSizeBytes>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VFixedSizeBytes> for Vec<u8> {
    fn from(obj: VFixedSizeBytes) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VTimestampMsUtc {
    pub v: i64,
}

impl VTimestampMsUtc {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVTimestampMsUtc<'a>> {
        use crate::types::generated::value_generated::VTimestampMsUtcBuilder as FbsVTimestampMsUtcBuilder;

        let mut bldr = FbsVTimestampMsUtcBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVTimestampMsUtc<'_>> for VTimestampMsUtc {
    fn from(fbs: FbsVTimestampMsUtc<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VTimestampMsUtc {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVTimestampMsUtc>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VTimestampMsUtc> for Vec<u8> {
    fn from(obj: VTimestampMsUtc) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VTimestampMs {
    pub v: i64,
}

impl VTimestampMs {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVTimestampMs<'a>> {
        use crate::types::generated::value_generated::VTimestampMsBuilder as FbsVTimestampMsBuilder;

        let mut bldr = FbsVTimestampMsBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVTimestampMs<'_>> for VTimestampMs {
    fn from(fbs: FbsVTimestampMs<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VTimestampMs {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVTimestampMs>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VTimestampMs> for Vec<u8> {
    fn from(obj: VTimestampMs) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VTimestampNsUtc {
    pub v: i64,
}

impl VTimestampNsUtc {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVTimestampNsUtc<'a>> {
        use crate::types::generated::value_generated::VTimestampNsUtcBuilder as FbsVTimestampNsUtcBuilder;

        let mut bldr = FbsVTimestampNsUtcBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVTimestampNsUtc<'_>> for VTimestampNsUtc {
    fn from(fbs: FbsVTimestampNsUtc<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VTimestampNsUtc {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVTimestampNsUtc>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VTimestampNsUtc> for Vec<u8> {
    fn from(obj: VTimestampNsUtc) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VTimestampNs {
    pub v: i64,
}

impl VTimestampNs {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVTimestampNs<'a>> {
        use crate::types::generated::value_generated::VTimestampNsBuilder as FbsVTimestampNsBuilder;

        let mut bldr = FbsVTimestampNsBuilder::new(builder);
        bldr.add_v(self.v);
        bldr.finish()
    }
}

impl From<FbsVTimestampNs<'_>> for VTimestampNs {
    fn from(fbs: FbsVTimestampNs<'_>) -> Self {
        let v = fbs.v();
        Self {
            v,
        }
    }
}

impl TryFrom<&[u8]> for VTimestampNs {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVTimestampNs>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VTimestampNs> for Vec<u8> {
    fn from(obj: VTimestampNs) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct VPlaceholder {
    pub name: String,
    pub ty: ValueTy,
}

impl VPlaceholder {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsVPlaceholder<'a>> {
        use crate::types::generated::value_generated::VPlaceholderBuilder as FbsVPlaceholderBuilder;

        let name_offset = builder.create_string(&self.name);

        let mut bldr = FbsVPlaceholderBuilder::new(builder);
        bldr.add_name(name_offset);
        bldr.add_ty(FbsValueTy::from(self.ty));
        bldr.finish()
    }
}

impl From<FbsVPlaceholder<'_>> for VPlaceholder {
    fn from(fbs: FbsVPlaceholder<'_>) -> Self {
        let name = fbs.name().to_owned();
        let ty = ValueTy::from(fbs.ty());
        Self {
            name,
            ty,
        }
    }
}

impl TryFrom<&[u8]> for VPlaceholder {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsVPlaceholder>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<VPlaceholder> for Vec<u8> {
    fn from(obj: VPlaceholder) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Clone, Debug, PartialEq)]
pub enum Value {
    VBool(VBool),
    VUnit(VUnit),
    VChar(VChar),
    VNull(VNull),
    VI8(VI8),
    VU8(VU8),
    VI16(VI16),
    VU16(VU16),
    VI32(VI32),
    VU32(VU32),
    VF32(VF32),
    VIsize(VIsize),
    VUsize(VUsize),
    VI64(VI64),
    VU64(VU64),
    VF64(VF64),
    VStr(VStr),
    VBytes(VBytes),
    VArray(VArray),
    VTri2D(VTri2D),
    VFixedSizeBytes(VFixedSizeBytes),
    VTimestampMsUtc(VTimestampMsUtc),
    VTimestampMs(VTimestampMs),
    VTimestampNsUtc(VTimestampNsUtc),
    VTimestampNs(VTimestampNs),
    VPlaceholder(VPlaceholder),
}

impl Default for Value {
    fn default() -> Self {
        Self::VBool(VBool::default())
    }
}

impl Value {
    pub fn serialize_to(&self, builder: &mut flatbuffers::FlatBufferBuilder) -> (WIPOffset<UnionWIPOffset>, FbsValue) {
        match self {
            Self::VBool(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VBool;
                (offset, ty)
            }
            Self::VUnit(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VUnit;
                (offset, ty)
            }
            Self::VChar(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VChar;
                (offset, ty)
            }
            Self::VNull(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VNull;
                (offset, ty)
            }
            Self::VI8(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VI8;
                (offset, ty)
            }
            Self::VU8(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VU8;
                (offset, ty)
            }
            Self::VI16(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VI16;
                (offset, ty)
            }
            Self::VU16(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VU16;
                (offset, ty)
            }
            Self::VI32(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VI32;
                (offset, ty)
            }
            Self::VU32(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VU32;
                (offset, ty)
            }
            Self::VF32(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VF32;
                (offset, ty)
            }
            Self::VIsize(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VIsize;
                (offset, ty)
            }
            Self::VUsize(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VUsize;
                (offset, ty)
            }
            Self::VI64(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VI64;
                (offset, ty)
            }
            Self::VU64(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VU64;
                (offset, ty)
            }
            Self::VF64(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VF64;
                (offset, ty)
            }
            Self::VStr(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VStr;
                (offset, ty)
            }
            Self::VBytes(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VBytes;
                (offset, ty)
            }
            Self::VArray(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VArray;
                (offset, ty)
            }
            Self::VTri2D(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VTri2D;
                (offset, ty)
            }
            Self::VFixedSizeBytes(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VFixedSizeBytes;
                (offset, ty)
            }
            Self::VTimestampMsUtc(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VTimestampMsUtc;
                (offset, ty)
            }
            Self::VTimestampMs(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VTimestampMs;
                (offset, ty)
            }
            Self::VTimestampNsUtc(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VTimestampNsUtc;
                (offset, ty)
            }
            Self::VTimestampNs(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VTimestampNs;
                (offset, ty)
            }
            Self::VPlaceholder(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsValue::VPlaceholder;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ValueInstance {
    pub name: Option<String>,
    pub v: Value,
}

impl ValueInstance {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsValueInstance<'a>> {
        use crate::types::generated::value_generated::ValueInstanceBuilder as FbsValueInstanceBuilder;

        let name_offset = self.name.as_ref().map(|s| builder.create_string(s));
        let (v_offset, v_ty) = self.v.serialize_to(builder);

        let mut bldr = FbsValueInstanceBuilder::new(builder);
        if let Some(offset) = name_offset {
            bldr.add_name(offset);
        }
        bldr.add_v(v_offset);
        bldr.add_v_type(v_ty);
        bldr.finish()
    }
}

impl From<FbsValueInstance<'_>> for ValueInstance {
    fn from(fbs: FbsValueInstance<'_>) -> Self {
        let name = fbs.name().map(ToOwned::to_owned);
        let v = match fbs.v_type() {
            FbsValue::VBool => Value::VBool(VBool::from(fbs.v_as_vbool().unwrap())),
            FbsValue::VUnit => Value::VUnit(VUnit::from(fbs.v_as_vunit().unwrap())),
            FbsValue::VChar => Value::VChar(VChar::from(fbs.v_as_vchar().unwrap())),
            FbsValue::VNull => Value::VNull(VNull::from(fbs.v_as_vnull().unwrap())),
            FbsValue::VI8 => Value::VI8(VI8::from(fbs.v_as_vi8().unwrap())),
            FbsValue::VU8 => Value::VU8(VU8::from(fbs.v_as_vu8().unwrap())),
            FbsValue::VI16 => Value::VI16(VI16::from(fbs.v_as_vi16().unwrap())),
            FbsValue::VU16 => Value::VU16(VU16::from(fbs.v_as_vu16().unwrap())),
            FbsValue::VI32 => Value::VI32(VI32::from(fbs.v_as_vi32().unwrap())),
            FbsValue::VU32 => Value::VU32(VU32::from(fbs.v_as_vu32().unwrap())),
            FbsValue::VF32 => Value::VF32(VF32::from(fbs.v_as_vf32().unwrap())),
            FbsValue::VIsize => Value::VIsize(VIsize::from(fbs.v_as_visize().unwrap())),
            FbsValue::VUsize => Value::VUsize(VUsize::from(fbs.v_as_vusize().unwrap())),
            FbsValue::VI64 => Value::VI64(VI64::from(fbs.v_as_vi64().unwrap())),
            FbsValue::VU64 => Value::VU64(VU64::from(fbs.v_as_vu64().unwrap())),
            FbsValue::VF64 => Value::VF64(VF64::from(fbs.v_as_vf64().unwrap())),
            FbsValue::VStr => Value::VStr(VStr::from(fbs.v_as_vstr().unwrap())),
            FbsValue::VBytes => Value::VBytes(VBytes::from(fbs.v_as_vbytes().unwrap())),
            FbsValue::VArray => Value::VArray(VArray::from(fbs.v_as_varray().unwrap())),
            FbsValue::VTri2D => Value::VTri2D(VTri2D::from(fbs.v_as_vtri_2_d().unwrap())),
            FbsValue::VFixedSizeBytes => Value::VFixedSizeBytes(VFixedSizeBytes::from(fbs.v_as_vfixed_size_bytes().unwrap())),
            FbsValue::VTimestampMsUtc => Value::VTimestampMsUtc(VTimestampMsUtc::from(fbs.v_as_vtimestamp_ms_utc().unwrap())),
            FbsValue::VTimestampMs => Value::VTimestampMs(VTimestampMs::from(fbs.v_as_vtimestamp_ms().unwrap())),
            FbsValue::VTimestampNsUtc => Value::VTimestampNsUtc(VTimestampNsUtc::from(fbs.v_as_vtimestamp_ns_utc().unwrap())),
            FbsValue::VTimestampNs => Value::VTimestampNs(VTimestampNs::from(fbs.v_as_vtimestamp_ns().unwrap())),
            FbsValue::VPlaceholder => Value::VPlaceholder(VPlaceholder::from(fbs.v_as_vplaceholder().unwrap())),
            _ => unreachable!(),
        };

        Self {
            name,
            v,
        }
    }
}

impl TryFrom<&[u8]> for ValueInstance {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsValueInstance>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ValueInstance> for Vec<u8> {
    fn from(obj: ValueInstance) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_v_array() {
        let t0 = VArray::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VArray::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_bool() {
        let t0 = VBool::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VBool::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_bytes() {
        let t0 = VBytes::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VBytes::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_char() {
        let t0 = VChar::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VChar::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vf32() {
        let t0 = VF32::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VF32::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vf64() {
        let t0 = VF64::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VF64::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_fixed_size_bytes() {
        let t0 = VFixedSizeBytes::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VFixedSizeBytes::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vi16() {
        let t0 = VI16::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VI16::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vi32() {
        let t0 = VI32::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VI32::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vi64() {
        let t0 = VI64::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VI64::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vi8() {
        let t0 = VI8::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VI8::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_isize() {
        let t0 = VIsize::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VIsize::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_null() {
        let t0 = VNull::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VNull::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_placeholder() {
        let t0 = VPlaceholder::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VPlaceholder::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_str() {
        let t0 = VStr::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VStr::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_timestamp_ms() {
        let t0 = VTimestampMs::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VTimestampMs::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_timestamp_ms_utc() {
        let t0 = VTimestampMsUtc::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VTimestampMsUtc::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_timestamp_ns() {
        let t0 = VTimestampNs::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VTimestampNs::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_timestamp_ns_utc() {
        let t0 = VTimestampNsUtc::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VTimestampNsUtc::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_tri_2d() {
        let t0 = VTri2D::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VTri2D::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vu16() {
        let t0 = VU16::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VU16::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vu32() {
        let t0 = VU32::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VU32::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vu64() {
        let t0 = VU64::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VU64::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_vu8() {
        let t0 = VU8::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VU8::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_unit() {
        let t0 = VUnit::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VUnit::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_v_usize() {
        let t0 = VUsize::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = VUsize::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_value_instance() {
        let t0 = ValueInstance::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ValueInstance::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

}