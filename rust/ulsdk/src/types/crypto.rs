// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(mismatched_lifetime_syntaxes)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use bitflags::bitflags;
use core::ops::Deref;
use flatbuffers::{UnionWIPOffset, WIPOffset};

use crate::types::generated::crypto_generated::{
    CryptHeader as FbsCryptHeader, Digest as FbsDigest, EncryptedObject as FbsEncryptedObject,
    Sha256 as FbsSha256, Signature as FbsSignature,
};

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Sha256 {
    pub b: Vec<u8>,
}

impl Sha256 {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsSha256<'a>> {
        use crate::types::generated::crypto_generated::Sha256Builder as FbsSha256Builder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsSha256Builder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsSha256<'_>> for Sha256 {
    fn from(fbs: FbsSha256<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl Sha256 {
    pub fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    pub fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let fbs = flatbuffers::size_prefixed_root::<FbsSha256>(bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Clone, Debug, PartialEq)]
pub enum Digest {
    Sha256(Sha256),
}

impl Default for Digest {
    fn default() -> Self {
        Self::Sha256(Sha256::default())
    }
}

impl Digest {
    pub fn serialize_to(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder,
    ) -> (WIPOffset<UnionWIPOffset>, FbsDigest) {
        match self {
            Self::Sha256(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsDigest::Sha256;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct CryptHeader {
    /// An ID for the key used to encrypt this particular encrypted object.
    pub kid: String,
    pub nonce: Vec<u8>,
    pub plaintext_len: u32,
}

impl CryptHeader {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsCryptHeader<'a>> {
        use crate::types::generated::crypto_generated::CryptHeaderBuilder as FbsCryptHeaderBuilder;

        let kid_offset = builder.create_string(&self.kid);
        let nonce_offset = builder.create_vector(&self.nonce);

        let mut bldr = FbsCryptHeaderBuilder::new(builder);
        bldr.add_kid(kid_offset);
        bldr.add_nonce(nonce_offset);
        bldr.add_plaintext_len(self.plaintext_len);
        bldr.finish()
    }
}

impl From<FbsCryptHeader<'_>> for CryptHeader {
    fn from(fbs: FbsCryptHeader<'_>) -> Self {
        let kid = fbs.kid().to_owned();
        let mut nonce = Vec::new();
        for elem in fbs.nonce() {
            nonce.push(elem.into());
        }

        let plaintext_len = fbs.plaintext_len();
        Self {
            kid,
            nonce,
            plaintext_len,
        }
    }
}

impl CryptHeader {
    pub fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    pub fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let fbs = flatbuffers::size_prefixed_root::<FbsCryptHeader>(bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct EncryptedObject {
    pub header: CryptHeader,
    pub obj: Vec<u8>,
}

impl EncryptedObject {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsEncryptedObject<'a>> {
        use crate::types::generated::crypto_generated::EncryptedObjectBuilder as FbsEncryptedObjectBuilder;

        let header_offset = self.header.serialize_to(builder);
        let obj_offset = builder.create_vector(&self.obj);

        let mut bldr = FbsEncryptedObjectBuilder::new(builder);
        bldr.add_header(header_offset);
        bldr.add_obj(obj_offset);
        bldr.finish()
    }
}

impl From<FbsEncryptedObject<'_>> for EncryptedObject {
    fn from(fbs: FbsEncryptedObject<'_>) -> Self {
        let header = CryptHeader::from(fbs.header());
        let mut obj = Vec::new();
        for elem in fbs.obj() {
            obj.push(elem.into());
        }

        Self { header, obj }
    }
}

impl EncryptedObject {
    pub fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    pub fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let fbs = flatbuffers::size_prefixed_root::<FbsEncryptedObject>(bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Signature {
    pub kid: String,
    pub sig: Vec<u8>,
}

impl Signature {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsSignature<'a>> {
        use crate::types::generated::crypto_generated::SignatureBuilder as FbsSignatureBuilder;

        let kid_offset = builder.create_string(&self.kid);
        let sig_offset = builder.create_vector(&self.sig);

        let mut bldr = FbsSignatureBuilder::new(builder);
        bldr.add_kid(kid_offset);
        bldr.add_sig(sig_offset);
        bldr.finish()
    }
}

impl From<FbsSignature<'_>> for Signature {
    fn from(fbs: FbsSignature<'_>) -> Self {
        let kid = fbs.kid().to_owned();
        let mut sig = Vec::new();
        for elem in fbs.sig() {
            sig.push(elem.into());
        }

        Self { kid, sig }
    }
}

impl Signature {
    pub fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    pub fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let fbs = flatbuffers::size_prefixed_root::<FbsSignature>(bytes)?;
        Ok(Self::from(fbs))
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_crypt_header() {
        let t0 = CryptHeader::default();
        let buf = t0.to_fbs_bytes();
        let t1 = CryptHeader::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_encrypted_object() {
        let t0 = EncryptedObject::default();
        let buf = t0.to_fbs_bytes();
        let t1 = EncryptedObject::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_sha_256() {
        let t0 = Sha256::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Sha256::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_signature() {
        let t0 = Signature::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Signature::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }
}
