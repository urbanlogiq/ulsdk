// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(mismatched_lifetime_syntaxes)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use crate::FbsSerde;
use bitflags::bitflags;
use core::ops::Deref;
use flatbuffers::{UnionWIPOffset, WIPOffset};
use ordered_float::OrderedFloat;
use serde::{Deserialize, Serialize};
use strum_macros::FromRepr;

use crate::types::Schema::{
    Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind,
    Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int,
    Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List,
    ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time,
    TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View,
};
use crate::types::api::SortOrder;
use crate::types::data::{
    AttributePair, BinaryYesNo, DayOfWeek, DirectionAndRoadName, DirectionAndRoadNames,
    DirectionTy, NamedParameter, NamedParameterFlags, OutputSchema, RoadUserTy, Source,
    StatisticTy, TimeGranularity, TurnTy,
};
use crate::types::entity::{
    EdgeTy, EntityTy, Geometry, GraphEdge, GraphNode, Line, MultiLine, MultiPoint, MultiPolygon,
    NodeTy, Point, Polygon,
};
use crate::types::fun::Fn_;
use crate::types::generated::Schema_generated::{
    Binary as FbsBinary, BinaryView as FbsBinaryView, Bool as FbsBool, Buffer as FbsBuffer,
    Date as FbsDate, DateUnit as FbsDateUnit, Decimal as FbsDecimal,
    DictionaryEncoding as FbsDictionaryEncoding, DictionaryKind as FbsDictionaryKind,
    Duration as FbsDuration, Endianness as FbsEndianness, Feature as FbsFeature, Field as FbsField,
    FixedSizeBinary as FbsFixedSizeBinary, FixedSizeList as FbsFixedSizeList,
    FloatingPoint as FbsFloatingPoint, Int as FbsInt, Interval as FbsInterval,
    IntervalUnit as FbsIntervalUnit, KeyValue as FbsKeyValue, LargeBinary as FbsLargeBinary,
    LargeList as FbsLargeList, LargeListView as FbsLargeListView, LargeUtf8 as FbsLargeUtf8,
    List as FbsList, ListView as FbsListView, Map as FbsMap, MetadataVersion as FbsMetadataVersion,
    Null as FbsNull, Precision as FbsPrecision, RunEndEncoded as FbsRunEndEncoded,
    Schema as FbsSchema, Struct_ as FbsStruct_, Time as FbsTime, TimeUnit as FbsTimeUnit,
    Timestamp as FbsTimestamp, Type as FbsType, Union as FbsUnion, UnionMode as FbsUnionMode,
    Utf8 as FbsUtf8, Utf8View as FbsUtf8View,
};
use crate::types::generated::api_generated::SortOrder as FbsSortOrder;
use crate::types::generated::data_generated::{
    AttributePair as FbsAttributePair, BinaryYesNo as FbsBinaryYesNo, DayOfWeek as FbsDayOfWeek,
    DirectionAndRoadName as FbsDirectionAndRoadName,
    DirectionAndRoadNames as FbsDirectionAndRoadNames, DirectionTy as FbsDirectionTy,
    NamedParameter as FbsNamedParameter, NamedParameterFlags as FbsNamedParameterFlags,
    OutputSchema as FbsOutputSchema, RoadUserTy as FbsRoadUserTy, Source as FbsSource,
    StatisticTy as FbsStatisticTy, TimeGranularity as FbsTimeGranularity, TurnTy as FbsTurnTy,
};
use crate::types::generated::entity_generated::{
    EdgeTy as FbsEdgeTy, EntityTy as FbsEntityTy, Geometry as FbsGeometry,
    GraphEdge as FbsGraphEdge, GraphNode as FbsGraphNode, Line as FbsLine,
    MultiLine as FbsMultiLine, MultiPoint as FbsMultiPoint, MultiPolygon as FbsMultiPolygon,
    NodeTy as FbsNodeTy, Point as FbsPoint, Polygon as FbsPolygon,
};
use crate::types::generated::fun_generated::Fn as FbsFn;
use crate::types::generated::graph_generated::{
    EdgeList as FbsEdgeList, EdgeQuery as FbsEdgeQuery, Geom as FbsGeom, GeomOp as FbsGeomOp,
    GraphQuery as FbsGraphQuery, NodeIdPair as FbsNodeIdPair, NodeList as FbsNodeList,
    NodeQuery as FbsNodeQuery, OrderBy as FbsOrderBy, Predicate as FbsPredicate,
    Projection as FbsProjection, QueryPathElement as FbsQueryPathElement,
    QueryPathElementUnion as FbsQueryPathElementUnion, ValueTransform as FbsValueTransform,
};
use crate::types::generated::id_generated::{
    B2cId as FbsB2cId, ColumnGroupId as FbsColumnGroupId, ContentId as FbsContentId,
    DataStateId as FbsDataStateId, GenericId as FbsGenericId, GraphNodeId as FbsGraphNodeId,
    ObjectId as FbsObjectId, ObjectNamespace as FbsObjectNamespace,
    PinnedObjectId as FbsPinnedObjectId, StreamId as FbsStreamId,
};
use crate::types::generated::metadata_generated::{
    AggregationFunction as FbsAggregationFunction,
    CategoryRelationshipData as FbsCategoryRelationshipData, ComponentData as FbsComponentData,
    ContactInfo as FbsContactInfo, DatacatalogGeometry as FbsDatacatalogGeometry,
    DatacatalogLatLngGeometry as FbsDatacatalogLatLngGeometry,
    DatasetCategory as FbsDatasetCategory, DatasetSource as FbsDatasetSource, Dates as FbsDates,
    DatetimeRange as FbsDatetimeRange, Document as FbsDocument, Documents as FbsDocuments,
    FieldFlags as FbsFieldFlags, FieldUnit as FbsFieldUnit, FloatAggregate as FbsFloatAggregate,
    FloatBucket as FbsFloatBucket, FloatRange as FbsFloatRange, GeometryData as FbsGeometryData,
    GeometryDataUnion as FbsGeometryDataUnion, GeometrySource as FbsGeometrySource,
    HierarchicalRelationship as FbsHierarchicalRelationship,
    HierarchyRelationshipData as FbsHierarchyRelationshipData, IntAggregate as FbsIntAggregate,
    IntBucket as FbsIntBucket, IntRange as FbsIntRange,
    IntegerDisplayString as FbsIntegerDisplayString, Metadata as FbsMetadata,
    NestedCategoryRelationshipData as FbsNestedCategoryRelationshipData,
    NestedCategoryRelationshipNode as FbsNestedCategoryRelationshipNode,
    NestedHierarchyRelationshipData as FbsNestedHierarchyRelationshipData,
    NestedHierarchyRelationshipNode as FbsNestedHierarchyRelationshipNode,
    NestedStringCategories as FbsNestedStringCategories,
    NestedStringCategoryNode as FbsNestedStringCategoryNode, NoGeometry as FbsNoGeometry,
    NumericalFieldFormat as FbsNumericalFieldFormat,
    NumericalFieldValueType as FbsNumericalFieldValueType, RawGeom as FbsRawGeom,
    StringAggregate as FbsStringAggregate, StringCategories as FbsStringCategories,
    TimeInterval as FbsTimeInterval, UIntAggregate as FbsUIntAggregate,
    UIntBucket as FbsUIntBucket, UlField as FbsUlField,
    UlFieldRelationship as FbsUlFieldRelationship,
    UlFieldRelationshipData as FbsUlFieldRelationshipData, UlFieldType as FbsUlFieldType,
    UpdateCadence as FbsUpdateCadence, WorldGraphGeometry as FbsWorldGraphGeometry,
};
use crate::types::generated::value_generated::{
    Point2D as FbsPoint2D, Tri2D as FbsTri2D, VArray as FbsVArray, VBool as FbsVBool,
    VBytes as FbsVBytes, VChar as FbsVChar, VF32 as FbsVF32, VF64 as FbsVF64,
    VFixedSizeBytes as FbsVFixedSizeBytes, VI8 as FbsVI8, VI16 as FbsVI16, VI32 as FbsVI32,
    VI64 as FbsVI64, VIsize as FbsVIsize, VNull as FbsVNull, VPlaceholder as FbsVPlaceholder,
    VStr as FbsVStr, VTimestampMs as FbsVTimestampMs, VTimestampMsUtc as FbsVTimestampMsUtc,
    VTimestampNs as FbsVTimestampNs, VTimestampNsUtc as FbsVTimestampNsUtc, VTri2D as FbsVTri2D,
    VU8 as FbsVU8, VU16 as FbsVU16, VU32 as FbsVU32, VU64 as FbsVU64, VUnit as FbsVUnit,
    VUsize as FbsVUsize, Value as FbsValue, ValueInstance as FbsValueInstance,
    ValueTy as FbsValueTy,
};
use crate::types::graph::{
    EdgeList, EdgeQuery, Geom, GeomOp, GraphQuery, NodeIdPair, NodeList, NodeQuery, OrderBy,
    Predicate, Projection, QueryPathElement, QueryPathElementUnion, ValueTransform,
};
use crate::types::id::{
    B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId,
    ObjectNamespace, PinnedObjectId, StreamId,
};
use crate::types::value::{
    Point2D, Tri2D, VArray, VBool, VBytes, VChar, VF32, VF64, VFixedSizeBytes, VI8, VI16, VI32,
    VI64, VIsize, VNull, VPlaceholder, VStr, VTimestampMs, VTimestampMsUtc, VTimestampNs,
    VTimestampNsUtc, VTri2D, VU8, VU16, VU32, VU64, VUnit, VUsize, Value, ValueInstance, ValueTy,
};

#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum AggregationFunction {
    #[default]
    Any = 0,
    Sum = 1,
    Average = 2,
    Min = 3,
    Max = 4,
    Latest = 5,
}

impl TryFrom<u32> for AggregationFunction {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        AggregationFunction::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl AggregationFunction {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::Any => Some("Any"),
            Self::Sum => Some("Sum"),
            Self::Average => Some("Average"),
            Self::Min => Some("Min"),
            Self::Max => Some("Max"),
            Self::Latest => Some("Latest"),
            _ => None,
        }
    }
}

impl From<AggregationFunction> for FbsAggregationFunction {
    fn from(val: AggregationFunction) -> Self {
        match val {
            AggregationFunction::Any => FbsAggregationFunction::Any,
            AggregationFunction::Sum => FbsAggregationFunction::Sum,
            AggregationFunction::Average => FbsAggregationFunction::Average,
            AggregationFunction::Min => FbsAggregationFunction::Min,
            AggregationFunction::Max => FbsAggregationFunction::Max,
            AggregationFunction::Latest => FbsAggregationFunction::Latest,
        }
    }
}

impl From<FbsAggregationFunction> for AggregationFunction {
    fn from(fbs: FbsAggregationFunction) -> Self {
        match fbs.0 {
            0 => Self::Any,
            1 => Self::Sum,
            2 => Self::Average,
            3 => Self::Min,
            4 => Self::Max,
            5 => Self::Latest,
            _ => panic!(
                "Invalid value {} when constructing AggregationFunction",
                fbs.0
            ),
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum DatasetCategory {
    #[default]
    DC_BUSINESSES = 0,
    DC_DEMOGRAPHICS = 1,
    DC_ECONOMICS = 2,
    DC_EVENTS = 3,
    DC_INFRASTRUCTURE = 4,
    DC_LAND_USE = 5,
    DC_MOBILITY = 6,
    DC_WEATHER = 7,
    DC_BOUNDARY = 8,
    DC_HEALTH = 9,
    DC_HIDDEN = 4294967295,
}

impl TryFrom<u32> for DatasetCategory {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        DatasetCategory::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl DatasetCategory {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::DC_BUSINESSES => Some("DC_BUSINESSES"),
            Self::DC_DEMOGRAPHICS => Some("DC_DEMOGRAPHICS"),
            Self::DC_ECONOMICS => Some("DC_ECONOMICS"),
            Self::DC_EVENTS => Some("DC_EVENTS"),
            Self::DC_INFRASTRUCTURE => Some("DC_INFRASTRUCTURE"),
            Self::DC_LAND_USE => Some("DC_LAND_USE"),
            Self::DC_MOBILITY => Some("DC_MOBILITY"),
            Self::DC_WEATHER => Some("DC_WEATHER"),
            Self::DC_BOUNDARY => Some("DC_BOUNDARY"),
            Self::DC_HEALTH => Some("DC_HEALTH"),
            Self::DC_HIDDEN => Some("DC_HIDDEN"),
            _ => None,
        }
    }
}

impl From<DatasetCategory> for FbsDatasetCategory {
    fn from(val: DatasetCategory) -> Self {
        match val {
            DatasetCategory::DC_BUSINESSES => FbsDatasetCategory::DC_BUSINESSES,
            DatasetCategory::DC_DEMOGRAPHICS => FbsDatasetCategory::DC_DEMOGRAPHICS,
            DatasetCategory::DC_ECONOMICS => FbsDatasetCategory::DC_ECONOMICS,
            DatasetCategory::DC_EVENTS => FbsDatasetCategory::DC_EVENTS,
            DatasetCategory::DC_INFRASTRUCTURE => FbsDatasetCategory::DC_INFRASTRUCTURE,
            DatasetCategory::DC_LAND_USE => FbsDatasetCategory::DC_LAND_USE,
            DatasetCategory::DC_MOBILITY => FbsDatasetCategory::DC_MOBILITY,
            DatasetCategory::DC_WEATHER => FbsDatasetCategory::DC_WEATHER,
            DatasetCategory::DC_BOUNDARY => FbsDatasetCategory::DC_BOUNDARY,
            DatasetCategory::DC_HEALTH => FbsDatasetCategory::DC_HEALTH,
            DatasetCategory::DC_HIDDEN => FbsDatasetCategory::DC_HIDDEN,
        }
    }
}

impl From<FbsDatasetCategory> for DatasetCategory {
    fn from(fbs: FbsDatasetCategory) -> Self {
        match fbs.0 {
            0 => Self::DC_BUSINESSES,
            1 => Self::DC_DEMOGRAPHICS,
            2 => Self::DC_ECONOMICS,
            3 => Self::DC_EVENTS,
            4 => Self::DC_INFRASTRUCTURE,
            5 => Self::DC_LAND_USE,
            6 => Self::DC_MOBILITY,
            7 => Self::DC_WEATHER,
            8 => Self::DC_BOUNDARY,
            9 => Self::DC_HEALTH,
            4294967295 => Self::DC_HIDDEN,
            _ => panic!("Invalid value {} when constructing DatasetCategory", fbs.0),
        }
    }
}

bitflags! {
    #[derive(Default, Serialize, Deserialize)]
    pub struct FieldFlags: u32 {
        const FILTERABLE = 1;
        const CATEGORY = 2;
        const RANGEABLE = 4;
        const EDITABLE = 8;
        const NULLABLE = 16;
        const AGGREGATED = 32;
        /// This flag indicates to the front end whether a field is required in order
        /// to filter on the dataset. Some datasets may have an unreasonably large
        /// number of results that require a filter in order to make it manageable/interpretable.
        const REQUIREDFORFILTER = 64;
        /// This flag indicates to the BE whether a field should be added to the area report
        const DONOTINCLUDEINAREAREPORT = 128;
        /// When aggregating this data, such as when building histograms for the area
        /// report, aggregate this field into buckets with integer ranges. This allows
        /// us to specify that certain float-type fields, which would normally be
        /// aggregated into float ranges, should be aggregated into integer ranges instead.
        const AGGREGATEINTOINTEGERBUCKETS = 256;
        const READONLY = 512;
    }
}

impl From<FieldFlags> for FbsFieldFlags {
    fn from(val: FieldFlags) -> Self {
        Self::from_bits_truncate(val.bits())
    }
}

impl From<FbsFieldFlags> for FieldFlags {
    fn from(fbs: FbsFieldFlags) -> Self {
        Self::from_bits_truncate(fbs.bits())
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum FieldUnit {
    #[default]
    U_INVALID = 0,
    U_METRES = 1,
    U_DEGREES_CELSIUS = 2,
    U_KILOGRAMS = 3,
    U_SECONDS = 4,
    U_DOLLARS_CAD = 5,
    U_DOLLARS_USD = 6,
    /// Degrees longitude / latitude
    U_DEGREES = 7,
    U_KPH = 8,
    U_MPH = 9,
    U_PERCENTAGE = 10,
    U_ACRES = 11,
    U_SQUARE_FEET = 12,
    U_MILES = 13,
}

impl TryFrom<u32> for FieldUnit {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        FieldUnit::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl FieldUnit {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::U_INVALID => Some("U_INVALID"),
            Self::U_METRES => Some("U_METRES"),
            Self::U_DEGREES_CELSIUS => Some("U_DEGREES_CELSIUS"),
            Self::U_KILOGRAMS => Some("U_KILOGRAMS"),
            Self::U_SECONDS => Some("U_SECONDS"),
            Self::U_DOLLARS_CAD => Some("U_DOLLARS_CAD"),
            Self::U_DOLLARS_USD => Some("U_DOLLARS_USD"),
            Self::U_DEGREES => Some("U_DEGREES"),
            Self::U_KPH => Some("U_KPH"),
            Self::U_MPH => Some("U_MPH"),
            Self::U_PERCENTAGE => Some("U_PERCENTAGE"),
            Self::U_ACRES => Some("U_ACRES"),
            Self::U_SQUARE_FEET => Some("U_SQUARE_FEET"),
            Self::U_MILES => Some("U_MILES"),
            _ => None,
        }
    }
}

impl From<FieldUnit> for FbsFieldUnit {
    fn from(val: FieldUnit) -> Self {
        match val {
            FieldUnit::U_INVALID => FbsFieldUnit::U_INVALID,
            FieldUnit::U_METRES => FbsFieldUnit::U_METRES,
            FieldUnit::U_DEGREES_CELSIUS => FbsFieldUnit::U_DEGREES_CELSIUS,
            FieldUnit::U_KILOGRAMS => FbsFieldUnit::U_KILOGRAMS,
            FieldUnit::U_SECONDS => FbsFieldUnit::U_SECONDS,
            FieldUnit::U_DOLLARS_CAD => FbsFieldUnit::U_DOLLARS_CAD,
            FieldUnit::U_DOLLARS_USD => FbsFieldUnit::U_DOLLARS_USD,
            FieldUnit::U_DEGREES => FbsFieldUnit::U_DEGREES,
            FieldUnit::U_KPH => FbsFieldUnit::U_KPH,
            FieldUnit::U_MPH => FbsFieldUnit::U_MPH,
            FieldUnit::U_PERCENTAGE => FbsFieldUnit::U_PERCENTAGE,
            FieldUnit::U_ACRES => FbsFieldUnit::U_ACRES,
            FieldUnit::U_SQUARE_FEET => FbsFieldUnit::U_SQUARE_FEET,
            FieldUnit::U_MILES => FbsFieldUnit::U_MILES,
        }
    }
}

impl From<FbsFieldUnit> for FieldUnit {
    fn from(fbs: FbsFieldUnit) -> Self {
        match fbs.0 {
            0 => Self::U_INVALID,
            1 => Self::U_METRES,
            2 => Self::U_DEGREES_CELSIUS,
            3 => Self::U_KILOGRAMS,
            4 => Self::U_SECONDS,
            5 => Self::U_DOLLARS_CAD,
            6 => Self::U_DOLLARS_USD,
            7 => Self::U_DEGREES,
            8 => Self::U_KPH,
            9 => Self::U_MPH,
            10 => Self::U_PERCENTAGE,
            11 => Self::U_ACRES,
            12 => Self::U_SQUARE_FEET,
            13 => Self::U_MILES,
            _ => panic!("Invalid value {} when constructing FieldUnit", fbs.0),
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum NumericalFieldValueType {
    #[default]
    None_ = 0,
    Percent = 1,
    Ratio = 2,
}

impl TryFrom<u32> for NumericalFieldValueType {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        NumericalFieldValueType::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl NumericalFieldValueType {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::None_ => Some("None"),
            Self::Percent => Some("Percent"),
            Self::Ratio => Some("Ratio"),
            _ => None,
        }
    }
}

impl From<NumericalFieldValueType> for FbsNumericalFieldValueType {
    fn from(val: NumericalFieldValueType) -> Self {
        match val {
            NumericalFieldValueType::None_ => FbsNumericalFieldValueType::None,
            NumericalFieldValueType::Percent => FbsNumericalFieldValueType::Percent,
            NumericalFieldValueType::Ratio => FbsNumericalFieldValueType::Ratio,
        }
    }
}

impl From<FbsNumericalFieldValueType> for NumericalFieldValueType {
    fn from(fbs: FbsNumericalFieldValueType) -> Self {
        match fbs.0 {
            0 => Self::None_,
            1 => Self::Percent,
            2 => Self::Ratio,
            _ => panic!(
                "Invalid value {} when constructing NumericalFieldValueType",
                fbs.0
            ),
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum UlFieldType {
    #[default]
    FT_UNKNOWN = 0,
    FT_INT = 1,
    FT_FLOAT = 2,
    FT_STRING = 3,
    FT_UUID = 4,
    FT_POINT = 5,
    FT_REGION = 6,
    FT_DATETIME = 7,
    FT_CURRENCY = 8,
    FT_BOOLEAN = 9,
    FT_DOCUMENT = 10,
    FT_GEOMETRY = 11,
    FT_JSON = 12,
    FT_DATE = 13,
    FT_EXTERNAL_URL = 14,
    FT_CONTACT_INFO = 15,
    FT_AGGREGATE_FLOAT = 16,
    FT_AGGREGATE_UINT = 17,
    FT_AGGREGATE_INT = 18,
    FT_AGGREGATE_STRING = 19,
    FT_AGGREGATE_FLOAT32 = 20,
    FT_AGGREGATE_DATETIME = 21,
    FT_BINARY = 22,
    FT_TIME = 23,
    FT_GEOMETRY_OR_NODE = 24,
    FT_STRING_ARRAY = 25,
}

impl TryFrom<u32> for UlFieldType {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        UlFieldType::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl UlFieldType {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::FT_UNKNOWN => Some("FT_UNKNOWN"),
            Self::FT_INT => Some("FT_INT"),
            Self::FT_FLOAT => Some("FT_FLOAT"),
            Self::FT_STRING => Some("FT_STRING"),
            Self::FT_UUID => Some("FT_UUID"),
            Self::FT_POINT => Some("FT_POINT"),
            Self::FT_REGION => Some("FT_REGION"),
            Self::FT_DATETIME => Some("FT_DATETIME"),
            Self::FT_CURRENCY => Some("FT_CURRENCY"),
            Self::FT_BOOLEAN => Some("FT_BOOLEAN"),
            Self::FT_DOCUMENT => Some("FT_DOCUMENT"),
            Self::FT_GEOMETRY => Some("FT_GEOMETRY"),
            Self::FT_JSON => Some("FT_JSON"),
            Self::FT_DATE => Some("FT_DATE"),
            Self::FT_EXTERNAL_URL => Some("FT_EXTERNAL_URL"),
            Self::FT_CONTACT_INFO => Some("FT_CONTACT_INFO"),
            Self::FT_AGGREGATE_FLOAT => Some("FT_AGGREGATE_FLOAT"),
            Self::FT_AGGREGATE_UINT => Some("FT_AGGREGATE_UINT"),
            Self::FT_AGGREGATE_INT => Some("FT_AGGREGATE_INT"),
            Self::FT_AGGREGATE_STRING => Some("FT_AGGREGATE_STRING"),
            Self::FT_AGGREGATE_FLOAT32 => Some("FT_AGGREGATE_FLOAT32"),
            Self::FT_AGGREGATE_DATETIME => Some("FT_AGGREGATE_DATETIME"),
            Self::FT_BINARY => Some("FT_BINARY"),
            Self::FT_TIME => Some("FT_TIME"),
            Self::FT_GEOMETRY_OR_NODE => Some("FT_GEOMETRY_OR_NODE"),
            Self::FT_STRING_ARRAY => Some("FT_STRING_ARRAY"),
            _ => None,
        }
    }
}

impl From<UlFieldType> for FbsUlFieldType {
    fn from(val: UlFieldType) -> Self {
        match val {
            UlFieldType::FT_UNKNOWN => FbsUlFieldType::FT_UNKNOWN,
            UlFieldType::FT_INT => FbsUlFieldType::FT_INT,
            UlFieldType::FT_FLOAT => FbsUlFieldType::FT_FLOAT,
            UlFieldType::FT_STRING => FbsUlFieldType::FT_STRING,
            UlFieldType::FT_UUID => FbsUlFieldType::FT_UUID,
            UlFieldType::FT_POINT => FbsUlFieldType::FT_POINT,
            UlFieldType::FT_REGION => FbsUlFieldType::FT_REGION,
            UlFieldType::FT_DATETIME => FbsUlFieldType::FT_DATETIME,
            UlFieldType::FT_CURRENCY => FbsUlFieldType::FT_CURRENCY,
            UlFieldType::FT_BOOLEAN => FbsUlFieldType::FT_BOOLEAN,
            UlFieldType::FT_DOCUMENT => FbsUlFieldType::FT_DOCUMENT,
            UlFieldType::FT_GEOMETRY => FbsUlFieldType::FT_GEOMETRY,
            UlFieldType::FT_JSON => FbsUlFieldType::FT_JSON,
            UlFieldType::FT_DATE => FbsUlFieldType::FT_DATE,
            UlFieldType::FT_EXTERNAL_URL => FbsUlFieldType::FT_EXTERNAL_URL,
            UlFieldType::FT_CONTACT_INFO => FbsUlFieldType::FT_CONTACT_INFO,
            UlFieldType::FT_AGGREGATE_FLOAT => FbsUlFieldType::FT_AGGREGATE_FLOAT,
            UlFieldType::FT_AGGREGATE_UINT => FbsUlFieldType::FT_AGGREGATE_UINT,
            UlFieldType::FT_AGGREGATE_INT => FbsUlFieldType::FT_AGGREGATE_INT,
            UlFieldType::FT_AGGREGATE_STRING => FbsUlFieldType::FT_AGGREGATE_STRING,
            UlFieldType::FT_AGGREGATE_FLOAT32 => FbsUlFieldType::FT_AGGREGATE_FLOAT32,
            UlFieldType::FT_AGGREGATE_DATETIME => FbsUlFieldType::FT_AGGREGATE_DATETIME,
            UlFieldType::FT_BINARY => FbsUlFieldType::FT_BINARY,
            UlFieldType::FT_TIME => FbsUlFieldType::FT_TIME,
            UlFieldType::FT_GEOMETRY_OR_NODE => FbsUlFieldType::FT_GEOMETRY_OR_NODE,
            UlFieldType::FT_STRING_ARRAY => FbsUlFieldType::FT_STRING_ARRAY,
        }
    }
}

impl From<FbsUlFieldType> for UlFieldType {
    fn from(fbs: FbsUlFieldType) -> Self {
        match fbs.0 {
            0 => Self::FT_UNKNOWN,
            1 => Self::FT_INT,
            2 => Self::FT_FLOAT,
            3 => Self::FT_STRING,
            4 => Self::FT_UUID,
            5 => Self::FT_POINT,
            6 => Self::FT_REGION,
            7 => Self::FT_DATETIME,
            8 => Self::FT_CURRENCY,
            9 => Self::FT_BOOLEAN,
            10 => Self::FT_DOCUMENT,
            11 => Self::FT_GEOMETRY,
            12 => Self::FT_JSON,
            13 => Self::FT_DATE,
            14 => Self::FT_EXTERNAL_URL,
            15 => Self::FT_CONTACT_INFO,
            16 => Self::FT_AGGREGATE_FLOAT,
            17 => Self::FT_AGGREGATE_UINT,
            18 => Self::FT_AGGREGATE_INT,
            19 => Self::FT_AGGREGATE_STRING,
            20 => Self::FT_AGGREGATE_FLOAT32,
            21 => Self::FT_AGGREGATE_DATETIME,
            22 => Self::FT_BINARY,
            23 => Self::FT_TIME,
            24 => Self::FT_GEOMETRY_OR_NODE,
            25 => Self::FT_STRING_ARRAY,
            _ => panic!("Invalid value {} when constructing UlFieldType", fbs.0),
        }
    }
}

/// The update cadence of the dataset. This looks at the maximum timestamp/observation date in the data,
/// and not when the pipeline ran because we can run a pipeline today that only ingests data from 2020.
#[derive(Debug, Clone, Copy, Default, PartialEq, Hash, Eq, FromRepr, Serialize, Deserialize)]
#[repr(u32)]
pub enum UpdateCadence {
    #[default]
    UC_UNSET = 0,
    UC_IRREGULAR = 1,
    UC_DAILY = 2,
    UC_WEEKLY = 3,
    UC_BI_WEEKLY = 4,
    UC_MONTHLY = 5,
    UC_YEARLY = 6,
}

impl TryFrom<u32> for UpdateCadence {
    type Error = crate::error::Error;
    fn try_from(v: u32) -> Result<Self, crate::error::Error> {
        UpdateCadence::from_repr(v).ok_or(crate::error::Error::InvalidEnumValue(v as i64))
    }
}

impl UpdateCadence {
    pub fn variant_name(&self) -> Option<&'static str> {
        match self {
            Self::UC_UNSET => Some("UC_UNSET"),
            Self::UC_IRREGULAR => Some("UC_IRREGULAR"),
            Self::UC_DAILY => Some("UC_DAILY"),
            Self::UC_WEEKLY => Some("UC_WEEKLY"),
            Self::UC_BI_WEEKLY => Some("UC_BI_WEEKLY"),
            Self::UC_MONTHLY => Some("UC_MONTHLY"),
            Self::UC_YEARLY => Some("UC_YEARLY"),
            _ => None,
        }
    }
}

impl From<UpdateCadence> for FbsUpdateCadence {
    fn from(val: UpdateCadence) -> Self {
        match val {
            UpdateCadence::UC_UNSET => FbsUpdateCadence::UC_UNSET,
            UpdateCadence::UC_IRREGULAR => FbsUpdateCadence::UC_IRREGULAR,
            UpdateCadence::UC_DAILY => FbsUpdateCadence::UC_DAILY,
            UpdateCadence::UC_WEEKLY => FbsUpdateCadence::UC_WEEKLY,
            UpdateCadence::UC_BI_WEEKLY => FbsUpdateCadence::UC_BI_WEEKLY,
            UpdateCadence::UC_MONTHLY => FbsUpdateCadence::UC_MONTHLY,
            UpdateCadence::UC_YEARLY => FbsUpdateCadence::UC_YEARLY,
        }
    }
}

impl From<FbsUpdateCadence> for UpdateCadence {
    fn from(fbs: FbsUpdateCadence) -> Self {
        match fbs.0 {
            0 => Self::UC_UNSET,
            1 => Self::UC_IRREGULAR,
            2 => Self::UC_DAILY,
            3 => Self::UC_WEEKLY,
            4 => Self::UC_BI_WEEKLY,
            5 => Self::UC_MONTHLY,
            6 => Self::UC_YEARLY,
            _ => panic!("Invalid value {} when constructing UpdateCadence", fbs.0),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct StringCategories {
    pub categories: Option<Vec<String>>,
}

impl StringCategories {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsStringCategories<'a>> {
        use crate::types::generated::metadata_generated::StringCategoriesBuilder as FbsStringCategoriesBuilder;

        let categories_offset = self.categories.as_ref().map(|v| {
            let mut categories_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = builder.create_string(val);
                categories_offsets.push(offset);
            }
            let categories_offset = builder.create_vector(&categories_offsets);
            categories_offset
        });

        let mut bldr = FbsStringCategoriesBuilder::new(builder);
        if let Some(offset) = categories_offset {
            bldr.add_categories(offset);
        }
        bldr.finish()
    }
}

impl From<FbsStringCategories<'_>> for StringCategories {
    fn from(fbs: FbsStringCategories<'_>) -> Self {
        let categories = if let Some(val) = fbs.categories() {
            let mut categories = Vec::new();
            for elem in val {
                categories.push(elem.into());
            }

            Some(categories)
        } else {
            None
        };

        Self { categories }
    }
}

impl crate::FbsSerde for StringCategories {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsStringCategories>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NumericalFieldFormat {
    pub decimal_places: u32,
    pub offset: OrderedFloat<f64>,
    pub scale: OrderedFloat<f64>,
    pub value_type: NumericalFieldValueType,
}

impl NumericalFieldFormat {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNumericalFieldFormat<'a>> {
        use crate::types::generated::metadata_generated::NumericalFieldFormatBuilder as FbsNumericalFieldFormatBuilder;

        let mut bldr = FbsNumericalFieldFormatBuilder::new(builder);
        bldr.add_decimal_places(self.decimal_places);
        bldr.add_offset(*self.offset);
        bldr.add_scale(*self.scale);
        bldr.add_value_type(FbsNumericalFieldValueType::from(self.value_type));
        bldr.finish()
    }
}

impl From<FbsNumericalFieldFormat<'_>> for NumericalFieldFormat {
    fn from(fbs: FbsNumericalFieldFormat<'_>) -> Self {
        let decimal_places = fbs.decimal_places();
        let offset = OrderedFloat(fbs.offset());
        let scale = OrderedFloat(fbs.scale());
        let value_type = NumericalFieldValueType::from(fbs.value_type());
        Self {
            decimal_places,
            offset,
            scale,
            value_type,
        }
    }
}

impl crate::FbsSerde for NumericalFieldFormat {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsNumericalFieldFormat>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct IntRange {
    pub aggregation_protocol: AggregationFunction,
    pub display_strings: Option<Vec<IntegerDisplayString>>,
    pub enum_name: Option<String>,
    pub field_format: Option<NumericalFieldFormat>,
    pub is_bitmask_enum: bool,
    pub max: i64,
    pub min: i64,
}

impl IntRange {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsIntRange<'a>> {
        use crate::types::generated::metadata_generated::IntRangeBuilder as FbsIntRangeBuilder;

        let display_strings_offset = self.display_strings.as_ref().map(|v| {
            let mut display_strings_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                display_strings_offsets.push(offset);
            }
            let display_strings_offset = builder.create_vector(&display_strings_offsets);
            display_strings_offset
        });
        let enum_name_offset = self.enum_name.as_ref().map(|s| builder.create_string(s));
        let field_format_offset = self.field_format.as_ref().map(|o| o.serialize_to(builder));

        let mut bldr = FbsIntRangeBuilder::new(builder);
        bldr.add_aggregation_protocol(FbsAggregationFunction::from(self.aggregation_protocol));
        if let Some(offset) = display_strings_offset {
            bldr.add_display_strings(offset);
        }
        if let Some(offset) = enum_name_offset {
            bldr.add_enum_name(offset);
        }
        if let Some(offset) = field_format_offset {
            bldr.add_field_format(offset);
        }
        bldr.add_is_bitmask_enum(self.is_bitmask_enum);
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        bldr.finish()
    }
}

impl From<FbsIntRange<'_>> for IntRange {
    fn from(fbs: FbsIntRange<'_>) -> Self {
        let aggregation_protocol = AggregationFunction::from(fbs.aggregation_protocol());
        let display_strings = if let Some(val) = fbs.display_strings() {
            let mut display_strings = Vec::new();
            for elem in val {
                display_strings.push(elem.into());
            }

            Some(display_strings)
        } else {
            None
        };

        let enum_name = fbs.enum_name().map(ToOwned::to_owned);
        let field_format = fbs.field_format().map(NumericalFieldFormat::from);
        let is_bitmask_enum = fbs.is_bitmask_enum();
        let max = fbs.max();
        let min = fbs.min();
        Self {
            aggregation_protocol,
            display_strings,
            enum_name,
            field_format,
            is_bitmask_enum,
            max,
            min,
        }
    }
}

impl crate::FbsSerde for IntRange {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsIntRange>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct FloatRange {
    pub aggregation_protocol: AggregationFunction,
    pub field_format: Option<NumericalFieldFormat>,
    pub max: OrderedFloat<f64>,
    pub min: OrderedFloat<f64>,
}

impl FloatRange {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsFloatRange<'a>> {
        use crate::types::generated::metadata_generated::FloatRangeBuilder as FbsFloatRangeBuilder;

        let field_format_offset = self.field_format.as_ref().map(|o| o.serialize_to(builder));

        let mut bldr = FbsFloatRangeBuilder::new(builder);
        bldr.add_aggregation_protocol(FbsAggregationFunction::from(self.aggregation_protocol));
        if let Some(offset) = field_format_offset {
            bldr.add_field_format(offset);
        }
        bldr.add_max(*self.max);
        bldr.add_min(*self.min);
        bldr.finish()
    }
}

impl From<FbsFloatRange<'_>> for FloatRange {
    fn from(fbs: FbsFloatRange<'_>) -> Self {
        let aggregation_protocol = AggregationFunction::from(fbs.aggregation_protocol());
        let field_format = fbs.field_format().map(NumericalFieldFormat::from);
        let max = OrderedFloat(fbs.max());
        let min = OrderedFloat(fbs.min());
        Self {
            aggregation_protocol,
            field_format,
            max,
            min,
        }
    }
}

impl crate::FbsSerde for FloatRange {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsFloatRange>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct DatetimeRange {
    pub intervals: Option<Vec<TimeInterval>>,
    pub max: i64,
    pub min: i64,
}

impl DatetimeRange {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDatetimeRange<'a>> {
        use crate::types::generated::metadata_generated::DatetimeRangeBuilder as FbsDatetimeRangeBuilder;

        let intervals_offset = self.intervals.as_ref().map(|v| {
            let mut intervals_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                intervals_offsets.push(offset);
            }
            let intervals_offset = builder.create_vector(&intervals_offsets);
            intervals_offset
        });

        let mut bldr = FbsDatetimeRangeBuilder::new(builder);
        if let Some(offset) = intervals_offset {
            bldr.add_intervals(offset);
        }
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        bldr.finish()
    }
}

impl From<FbsDatetimeRange<'_>> for DatetimeRange {
    fn from(fbs: FbsDatetimeRange<'_>) -> Self {
        let intervals = if let Some(val) = fbs.intervals() {
            let mut intervals = Vec::new();
            for elem in val {
                intervals.push(elem.into());
            }

            Some(intervals)
        } else {
            None
        };

        let max = fbs.max();
        let min = fbs.min();
        Self {
            intervals,
            max,
            min,
        }
    }
}

impl crate::FbsSerde for DatetimeRange {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDatetimeRange>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct Dates {
    pub max: i64,
    pub min: i64,
    pub unique_value_counts: Option<Vec<u32>>,
    pub unique_values: Option<Vec<i64>>,
}

impl Dates {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDates<'a>> {
        use crate::types::generated::metadata_generated::DatesBuilder as FbsDatesBuilder;

        let unique_value_counts_offset = self.unique_value_counts.as_ref().map(|v| {
            let unique_value_counts_offset = builder.create_vector(&v);
            unique_value_counts_offset
        });
        let unique_values_offset = self.unique_values.as_ref().map(|v| {
            let unique_values_offset = builder.create_vector(&v);
            unique_values_offset
        });

        let mut bldr = FbsDatesBuilder::new(builder);
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        if let Some(offset) = unique_value_counts_offset {
            bldr.add_unique_value_counts(offset);
        }
        if let Some(offset) = unique_values_offset {
            bldr.add_unique_values(offset);
        }
        bldr.finish()
    }
}

impl From<FbsDates<'_>> for Dates {
    fn from(fbs: FbsDates<'_>) -> Self {
        let max = fbs.max();
        let min = fbs.min();
        let unique_value_counts = if let Some(val) = fbs.unique_value_counts() {
            let mut unique_value_counts = Vec::new();
            for elem in val {
                unique_value_counts.push(elem.into());
            }

            Some(unique_value_counts)
        } else {
            None
        };

        let unique_values = if let Some(val) = fbs.unique_values() {
            let mut unique_values = Vec::new();
            for elem in val {
                unique_values.push(elem.into());
            }

            Some(unique_values)
        } else {
            None
        };

        Self {
            max,
            min,
            unique_value_counts,
            unique_values,
        }
    }
}

impl crate::FbsSerde for Dates {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDates>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedStringCategories {
    pub nesting_tree: Vec<NestedStringCategoryNode>,
}

impl NestedStringCategories {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedStringCategories<'a>> {
        use crate::types::generated::metadata_generated::NestedStringCategoriesBuilder as FbsNestedStringCategoriesBuilder;

        let mut nesting_tree_offsets = Vec::with_capacity(self.nesting_tree.len());
        for val in self.nesting_tree.iter() {
            let offset = val.serialize_to(builder);
            nesting_tree_offsets.push(offset);
        }
        let nesting_tree_offset = builder.create_vector(&nesting_tree_offsets);

        let mut bldr = FbsNestedStringCategoriesBuilder::new(builder);
        bldr.add_nesting_tree(nesting_tree_offset);
        bldr.finish()
    }
}

impl From<FbsNestedStringCategories<'_>> for NestedStringCategories {
    fn from(fbs: FbsNestedStringCategories<'_>) -> Self {
        let mut nesting_tree = Vec::new();
        for elem in fbs.nesting_tree() {
            nesting_tree.push(elem.into());
        }

        Self { nesting_tree }
    }
}

impl crate::FbsSerde for NestedStringCategories {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsNestedStringCategories>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Clone, Debug, PartialEq, Hash, Eq, Serialize, Deserialize)]
pub enum ComponentData {
    StringCategories(StringCategories),
    IntRange(IntRange),
    FloatRange(FloatRange),
    DatetimeRange(DatetimeRange),
    Dates(Dates),
    NestedStringCategories(NestedStringCategories),
}

impl Default for ComponentData {
    fn default() -> Self {
        Self::StringCategories(StringCategories::default())
    }
}

impl ComponentData {
    pub fn serialize_to(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder,
    ) -> (WIPOffset<UnionWIPOffset>, FbsComponentData) {
        match self {
            Self::StringCategories(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::StringCategories;
                (offset, ty)
            }
            Self::IntRange(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::IntRange;
                (offset, ty)
            }
            Self::FloatRange(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::FloatRange;
                (offset, ty)
            }
            Self::DatetimeRange(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::DatetimeRange;
                (offset, ty)
            }
            Self::Dates(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::Dates;
                (offset, ty)
            }
            Self::NestedStringCategories(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsComponentData::NestedStringCategories;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct RawGeom {
    pub geom: Vec<u8>,
}

impl RawGeom {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsRawGeom<'a>> {
        use crate::types::generated::metadata_generated::RawGeomBuilder as FbsRawGeomBuilder;

        let geom_offset = builder.create_vector(&self.geom);

        let mut bldr = FbsRawGeomBuilder::new(builder);
        bldr.add_geom(geom_offset);
        bldr.finish()
    }
}

impl From<FbsRawGeom<'_>> for RawGeom {
    fn from(fbs: FbsRawGeom<'_>) -> Self {
        let mut geom = Vec::new();
        for elem in fbs.geom() {
            geom.push(elem.into());
        }

        Self { geom }
    }
}

impl crate::FbsSerde for RawGeom {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsRawGeom>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Clone, Debug, PartialEq, Hash, Eq, Serialize, Deserialize)]
pub enum GeometryDataUnion {
    RawGeom(RawGeom),
    NodeIdPair(NodeIdPair),
}

impl Default for GeometryDataUnion {
    fn default() -> Self {
        Self::RawGeom(RawGeom::default())
    }
}

impl GeometryDataUnion {
    pub fn serialize_to(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder,
    ) -> (WIPOffset<UnionWIPOffset>, FbsGeometryDataUnion) {
        match self {
            Self::RawGeom(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometryDataUnion::RawGeom;
                (offset, ty)
            }
            Self::NodeIdPair(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometryDataUnion::NodeIdPair;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NoGeometry {}

impl NoGeometry {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNoGeometry<'a>> {
        use crate::types::generated::metadata_generated::NoGeometryBuilder as FbsNoGeometryBuilder;

        let mut bldr = FbsNoGeometryBuilder::new(builder);
        bldr.finish()
    }
}

impl From<FbsNoGeometry<'_>> for NoGeometry {
    fn from(fbs: FbsNoGeometry<'_>) -> Self {
        Self {}
    }
}

impl crate::FbsSerde for NoGeometry {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsNoGeometry>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct DatacatalogGeometry {
    pub column: String,
}

impl DatacatalogGeometry {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDatacatalogGeometry<'a>> {
        use crate::types::generated::metadata_generated::DatacatalogGeometryBuilder as FbsDatacatalogGeometryBuilder;

        let column_offset = builder.create_string(&self.column);

        let mut bldr = FbsDatacatalogGeometryBuilder::new(builder);
        bldr.add_column(column_offset);
        bldr.finish()
    }
}

impl From<FbsDatacatalogGeometry<'_>> for DatacatalogGeometry {
    fn from(fbs: FbsDatacatalogGeometry<'_>) -> Self {
        let column = fbs.column().to_owned();
        Self { column }
    }
}

impl crate::FbsSerde for DatacatalogGeometry {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsDatacatalogGeometry>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

/// For most streams with GeometrySourceType==WorldGraphGeometry, edge_path will
/// be empty and start_stream_id will be unset.
///
/// If edge_path is empty and start_stream_id is unset we just query for nodes
/// whose stream predicate matches this stream's streamId in order to fetch the stream's geometry.
///
/// If edge_path is non-empty and start_stream_id is set: Start at nodes with
/// streamId=start_stream_id and follow the edge_path. Retrieve geometry from
/// the last node on that path.
///
/// If edge_path is non-empty and start_stream_id is unset: Start at nodes with
/// streamId=id of this stream and follow the edge_path to retrieve the geeometry.
///
/// If edge_path is empty and start_stream_id is set: Just query for nodes with
/// streamId == start_stream_id.
#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct WorldGraphGeometry {
    /// Edges to follow to reach the nodes with geometry.
    pub edge_path: Vec<EdgeTy>,
    /// Stream id of the starting node in the query path for the geometry
    pub start_stream_id: Option<ObjectId>,
}

impl WorldGraphGeometry {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsWorldGraphGeometry<'a>> {
        use crate::types::generated::metadata_generated::WorldGraphGeometryBuilder as FbsWorldGraphGeometryBuilder;

        let edge_path_offset =
            builder.create_vector_from_iter(self.edge_path.iter().map(|v| match v {
                EdgeTy::E_INVALID => FbsEdgeTy::E_INVALID,
                EdgeTy::E_POSTAL_CODE => FbsEdgeTy::E_POSTAL_CODE,
                EdgeTy::E_INTERSECTS => FbsEdgeTy::E_INTERSECTS,
                EdgeTy::E_CONTAINS => FbsEdgeTy::E_CONTAINS,
                EdgeTy::E_WITHIN => FbsEdgeTy::E_WITHIN,
                EdgeTy::E_HAS_COUNTS => FbsEdgeTy::E_HAS_COUNTS,
                EdgeTy::E_OCCURS_AT => FbsEdgeTy::E_OCCURS_AT,
                EdgeTy::E_REALM => FbsEdgeTy::E_REALM,
                EdgeTy::E_SEARCH_ATTRIBUTES => FbsEdgeTy::E_SEARCH_ATTRIBUTES,
                EdgeTy::E_ASSOCIATED_TFC_LOOP => FbsEdgeTy::E_ASSOCIATED_TFC_LOOP,
                EdgeTy::E_TRAFFIC_METRICS => FbsEdgeTy::E_TRAFFIC_METRICS,
                EdgeTy::E_TIME_DENSITY => FbsEdgeTy::E_TIME_DENSITY,
                EdgeTy::E_CRASHBOARD_AGGREGATE_BY => FbsEdgeTy::E_CRASHBOARD_AGGREGATE_BY,
                EdgeTy::E_OPPOSING_ROAD_SEGMENT => FbsEdgeTy::E_OPPOSING_ROAD_SEGMENT,
                EdgeTy::E_ASSOCIATED_CAMERA => FbsEdgeTy::E_ASSOCIATED_CAMERA,
            }));
        let start_stream_id_offset = self
            .start_stream_id
            .as_ref()
            .map(|o| o.serialize_to(builder));

        let mut bldr = FbsWorldGraphGeometryBuilder::new(builder);
        bldr.add_edge_path(edge_path_offset);
        if let Some(offset) = start_stream_id_offset {
            bldr.add_start_stream_id(offset);
        }
        bldr.finish()
    }
}

impl From<FbsWorldGraphGeometry<'_>> for WorldGraphGeometry {
    fn from(fbs: FbsWorldGraphGeometry<'_>) -> Self {
        let mut edge_path = Vec::new();
        for elem in fbs.edge_path() {
            edge_path.push(elem.into());
        }

        let start_stream_id = fbs.start_stream_id().map(ObjectId::from);
        Self {
            edge_path,
            start_stream_id,
        }
    }
}

impl crate::FbsSerde for WorldGraphGeometry {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsWorldGraphGeometry>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

/// Point geometry stored as two scalar coordinate columns rather than one
/// geometry column. Common for raw/bronze-level ingests, which land as-is
/// without a transformation step to construct a geom column.
///
/// Consumers compose a point from the pair (e.g. st_makepoint(lng, lat))
/// wherever they would otherwise reference a DatacatalogGeometry column.
#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct DatacatalogLatLngGeometry {
    pub lat_column: String,
    pub lng_column: String,
}

impl DatacatalogLatLngGeometry {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDatacatalogLatLngGeometry<'a>> {
        use crate::types::generated::metadata_generated::DatacatalogLatLngGeometryBuilder as FbsDatacatalogLatLngGeometryBuilder;

        let lat_column_offset = builder.create_string(&self.lat_column);
        let lng_column_offset = builder.create_string(&self.lng_column);

        let mut bldr = FbsDatacatalogLatLngGeometryBuilder::new(builder);
        bldr.add_lat_column(lat_column_offset);
        bldr.add_lng_column(lng_column_offset);
        bldr.finish()
    }
}

impl From<FbsDatacatalogLatLngGeometry<'_>> for DatacatalogLatLngGeometry {
    fn from(fbs: FbsDatacatalogLatLngGeometry<'_>) -> Self {
        let lat_column = fbs.lat_column().to_owned();
        let lng_column = fbs.lng_column().to_owned();
        Self {
            lat_column,
            lng_column,
        }
    }
}

impl crate::FbsSerde for DatacatalogLatLngGeometry {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDatacatalogLatLngGeometry>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

/// Append new variants only — a union member's position is its wire value, so
/// inserting one would silently reinterpret existing stored metadata.
#[derive(Clone, Debug, PartialEq, Hash, Eq, Serialize, Deserialize)]
pub enum GeometrySource {
    NoGeometry(NoGeometry),
    DatacatalogGeometry(DatacatalogGeometry),
    WorldGraphGeometry(WorldGraphGeometry),
    DatacatalogLatLngGeometry(DatacatalogLatLngGeometry),
}

impl Default for GeometrySource {
    fn default() -> Self {
        Self::NoGeometry(NoGeometry::default())
    }
}

impl GeometrySource {
    pub fn serialize_to(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder,
    ) -> (WIPOffset<UnionWIPOffset>, FbsGeometrySource) {
        match self {
            Self::NoGeometry(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometrySource::NoGeometry;
                (offset, ty)
            }
            Self::DatacatalogGeometry(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometrySource::DatacatalogGeometry;
                (offset, ty)
            }
            Self::WorldGraphGeometry(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometrySource::WorldGraphGeometry;
                (offset, ty)
            }
            Self::DatacatalogLatLngGeometry(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsGeometrySource::DatacatalogLatLngGeometry;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct HierarchyRelationshipData {
    pub hierarchy: Option<Vec<HierarchicalRelationship>>,
}

impl HierarchyRelationshipData {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsHierarchyRelationshipData<'a>> {
        use crate::types::generated::metadata_generated::HierarchyRelationshipDataBuilder as FbsHierarchyRelationshipDataBuilder;

        let hierarchy_offset = self.hierarchy.as_ref().map(|v| {
            let mut hierarchy_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                hierarchy_offsets.push(offset);
            }
            let hierarchy_offset = builder.create_vector(&hierarchy_offsets);
            hierarchy_offset
        });

        let mut bldr = FbsHierarchyRelationshipDataBuilder::new(builder);
        if let Some(offset) = hierarchy_offset {
            bldr.add_hierarchy(offset);
        }
        bldr.finish()
    }
}

impl From<FbsHierarchyRelationshipData<'_>> for HierarchyRelationshipData {
    fn from(fbs: FbsHierarchyRelationshipData<'_>) -> Self {
        let hierarchy = if let Some(val) = fbs.hierarchy() {
            let mut hierarchy = Vec::new();
            for elem in val {
                hierarchy.push(elem.into());
            }

            Some(hierarchy)
        } else {
            None
        };

        Self { hierarchy }
    }
}

impl crate::FbsSerde for HierarchyRelationshipData {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsHierarchyRelationshipData>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct CategoryRelationshipData {
    pub associated_fields: Option<Vec<i32>>,
    pub categories: Option<Vec<i32>>,
}

impl CategoryRelationshipData {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsCategoryRelationshipData<'a>> {
        use crate::types::generated::metadata_generated::CategoryRelationshipDataBuilder as FbsCategoryRelationshipDataBuilder;

        let associated_fields_offset = self.associated_fields.as_ref().map(|v| {
            let associated_fields_offset = builder.create_vector(&v);
            associated_fields_offset
        });
        let categories_offset = self.categories.as_ref().map(|v| {
            let categories_offset = builder.create_vector(&v);
            categories_offset
        });

        let mut bldr = FbsCategoryRelationshipDataBuilder::new(builder);
        if let Some(offset) = associated_fields_offset {
            bldr.add_associated_fields(offset);
        }
        if let Some(offset) = categories_offset {
            bldr.add_categories(offset);
        }
        bldr.finish()
    }
}

impl From<FbsCategoryRelationshipData<'_>> for CategoryRelationshipData {
    fn from(fbs: FbsCategoryRelationshipData<'_>) -> Self {
        let associated_fields = if let Some(val) = fbs.associated_fields() {
            let mut associated_fields = Vec::new();
            for elem in val {
                associated_fields.push(elem.into());
            }

            Some(associated_fields)
        } else {
            None
        };

        let categories = if let Some(val) = fbs.categories() {
            let mut categories = Vec::new();
            for elem in val {
                categories.push(elem.into());
            }

            Some(categories)
        } else {
            None
        };

        Self {
            associated_fields,
            categories,
        }
    }
}

impl crate::FbsSerde for CategoryRelationshipData {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsCategoryRelationshipData>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedCategoryRelationshipData {
    pub categories: Option<Vec<NestedCategoryRelationshipNode>>,
}

impl NestedCategoryRelationshipData {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedCategoryRelationshipData<'a>> {
        use crate::types::generated::metadata_generated::NestedCategoryRelationshipDataBuilder as FbsNestedCategoryRelationshipDataBuilder;

        let categories_offset = self.categories.as_ref().map(|v| {
            let mut categories_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                categories_offsets.push(offset);
            }
            let categories_offset = builder.create_vector(&categories_offsets);
            categories_offset
        });

        let mut bldr = FbsNestedCategoryRelationshipDataBuilder::new(builder);
        if let Some(offset) = categories_offset {
            bldr.add_categories(offset);
        }
        bldr.finish()
    }
}

impl From<FbsNestedCategoryRelationshipData<'_>> for NestedCategoryRelationshipData {
    fn from(fbs: FbsNestedCategoryRelationshipData<'_>) -> Self {
        let categories = if let Some(val) = fbs.categories() {
            let mut categories = Vec::new();
            for elem in val {
                categories.push(elem.into());
            }

            Some(categories)
        } else {
            None
        };

        Self { categories }
    }
}

impl crate::FbsSerde for NestedCategoryRelationshipData {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsNestedCategoryRelationshipData>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedHierarchyRelationshipData {
    pub nodes: Option<Vec<NestedHierarchyRelationshipNode>>,
}

impl NestedHierarchyRelationshipData {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedHierarchyRelationshipData<'a>> {
        use crate::types::generated::metadata_generated::NestedHierarchyRelationshipDataBuilder as FbsNestedHierarchyRelationshipDataBuilder;

        let nodes_offset = self.nodes.as_ref().map(|v| {
            let mut nodes_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                nodes_offsets.push(offset);
            }
            let nodes_offset = builder.create_vector(&nodes_offsets);
            nodes_offset
        });

        let mut bldr = FbsNestedHierarchyRelationshipDataBuilder::new(builder);
        if let Some(offset) = nodes_offset {
            bldr.add_nodes(offset);
        }
        bldr.finish()
    }
}

impl From<FbsNestedHierarchyRelationshipData<'_>> for NestedHierarchyRelationshipData {
    fn from(fbs: FbsNestedHierarchyRelationshipData<'_>) -> Self {
        let nodes = if let Some(val) = fbs.nodes() {
            let mut nodes = Vec::new();
            for elem in val {
                nodes.push(elem.into());
            }

            Some(nodes)
        } else {
            None
        };

        Self { nodes }
    }
}

impl crate::FbsSerde for NestedHierarchyRelationshipData {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsNestedHierarchyRelationshipData>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

#[derive(Clone, Debug, PartialEq, Hash, Eq, Serialize, Deserialize)]
pub enum UlFieldRelationshipData {
    HierarchyRelationshipData(HierarchyRelationshipData),
    CategoryRelationshipData(CategoryRelationshipData),
    NestedCategoryRelationshipData(NestedCategoryRelationshipData),
    NestedHierarchyRelationshipData(NestedHierarchyRelationshipData),
}

impl Default for UlFieldRelationshipData {
    fn default() -> Self {
        Self::HierarchyRelationshipData(HierarchyRelationshipData::default())
    }
}

impl UlFieldRelationshipData {
    pub fn serialize_to(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder,
    ) -> (WIPOffset<UnionWIPOffset>, FbsUlFieldRelationshipData) {
        match self {
            Self::HierarchyRelationshipData(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsUlFieldRelationshipData::HierarchyRelationshipData;
                (offset, ty)
            }
            Self::CategoryRelationshipData(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsUlFieldRelationshipData::CategoryRelationshipData;
                (offset, ty)
            }
            Self::NestedCategoryRelationshipData(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsUlFieldRelationshipData::NestedCategoryRelationshipData;
                (offset, ty)
            }
            Self::NestedHierarchyRelationshipData(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsUlFieldRelationshipData::NestedHierarchyRelationshipData;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct ContactInfo {
    pub address: Option<String>,
    pub email: Option<String>,
    pub name: Option<String>,
    pub phone: Option<String>,
    pub url: Option<String>,
}

impl ContactInfo {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsContactInfo<'a>> {
        use crate::types::generated::metadata_generated::ContactInfoBuilder as FbsContactInfoBuilder;

        let address_offset = self.address.as_ref().map(|s| builder.create_string(s));
        let email_offset = self.email.as_ref().map(|s| builder.create_string(s));
        let name_offset = self.name.as_ref().map(|s| builder.create_string(s));
        let phone_offset = self.phone.as_ref().map(|s| builder.create_string(s));
        let url_offset = self.url.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsContactInfoBuilder::new(builder);
        if let Some(offset) = address_offset {
            bldr.add_address(offset);
        }
        if let Some(offset) = email_offset {
            bldr.add_email(offset);
        }
        if let Some(offset) = name_offset {
            bldr.add_name(offset);
        }
        if let Some(offset) = phone_offset {
            bldr.add_phone(offset);
        }
        if let Some(offset) = url_offset {
            bldr.add_url(offset);
        }
        bldr.finish()
    }
}

impl From<FbsContactInfo<'_>> for ContactInfo {
    fn from(fbs: FbsContactInfo<'_>) -> Self {
        let address = fbs.address().map(ToOwned::to_owned);
        let email = fbs.email().map(ToOwned::to_owned);
        let name = fbs.name().map(ToOwned::to_owned);
        let phone = fbs.phone().map(ToOwned::to_owned);
        let url = fbs.url().map(ToOwned::to_owned);
        Self {
            address,
            email,
            name,
            phone,
            url,
        }
    }
}

impl crate::FbsSerde for ContactInfo {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsContactInfo>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct DatasetSource {
    /// Date information about the data source, such as the year it was generated.
    /// This is a free-form text field that isn't interpreted in any means by the
    /// system.
    pub date: Option<String>,
    /// The entity that created the data (Manifold, Government of Canada, Wejo, City
    pub source: Option<String>,
    /// A URL to where the data set can be fetched. Ideally a direct download but
    pub url: Option<String>,
}

impl DatasetSource {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDatasetSource<'a>> {
        use crate::types::generated::metadata_generated::DatasetSourceBuilder as FbsDatasetSourceBuilder;

        let date_offset = self.date.as_ref().map(|s| builder.create_string(s));
        let source_offset = self.source.as_ref().map(|s| builder.create_string(s));
        let url_offset = self.url.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsDatasetSourceBuilder::new(builder);
        if let Some(offset) = date_offset {
            bldr.add_date(offset);
        }
        if let Some(offset) = source_offset {
            bldr.add_source(offset);
        }
        if let Some(offset) = url_offset {
            bldr.add_url(offset);
        }
        bldr.finish()
    }
}

impl From<FbsDatasetSource<'_>> for DatasetSource {
    fn from(fbs: FbsDatasetSource<'_>) -> Self {
        let date = fbs.date().map(ToOwned::to_owned);
        let source = fbs.source().map(ToOwned::to_owned);
        let url = fbs.url().map(ToOwned::to_owned);
        Self { date, source, url }
    }
}

impl crate::FbsSerde for DatasetSource {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDatasetSource>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct Document {
    pub display_name: Option<String>,
    pub filename: Option<String>,
    pub mime_type: Option<String>,
    pub url: Option<String>,
}

impl Document {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDocument<'a>> {
        use crate::types::generated::metadata_generated::DocumentBuilder as FbsDocumentBuilder;

        let display_name_offset = self.display_name.as_ref().map(|s| builder.create_string(s));
        let filename_offset = self.filename.as_ref().map(|s| builder.create_string(s));
        let mime_type_offset = self.mime_type.as_ref().map(|s| builder.create_string(s));
        let url_offset = self.url.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsDocumentBuilder::new(builder);
        if let Some(offset) = display_name_offset {
            bldr.add_display_name(offset);
        }
        if let Some(offset) = filename_offset {
            bldr.add_filename(offset);
        }
        if let Some(offset) = mime_type_offset {
            bldr.add_mime_type(offset);
        }
        if let Some(offset) = url_offset {
            bldr.add_url(offset);
        }
        bldr.finish()
    }
}

impl From<FbsDocument<'_>> for Document {
    fn from(fbs: FbsDocument<'_>) -> Self {
        let display_name = fbs.display_name().map(ToOwned::to_owned);
        let filename = fbs.filename().map(ToOwned::to_owned);
        let mime_type = fbs.mime_type().map(ToOwned::to_owned);
        let url = fbs.url().map(ToOwned::to_owned);
        Self {
            display_name,
            filename,
            mime_type,
            url,
        }
    }
}

impl crate::FbsSerde for Document {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDocument>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct Documents {
    pub documents: Option<Vec<Document>>,
}

impl Documents {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDocuments<'a>> {
        use crate::types::generated::metadata_generated::DocumentsBuilder as FbsDocumentsBuilder;

        let documents_offset = self.documents.as_ref().map(|v| {
            let mut documents_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                documents_offsets.push(offset);
            }
            let documents_offset = builder.create_vector(&documents_offsets);
            documents_offset
        });

        let mut bldr = FbsDocumentsBuilder::new(builder);
        if let Some(offset) = documents_offset {
            bldr.add_documents(offset);
        }
        bldr.finish()
    }
}

impl From<FbsDocuments<'_>> for Documents {
    fn from(fbs: FbsDocuments<'_>) -> Self {
        let documents = if let Some(val) = fbs.documents() {
            let mut documents = Vec::new();
            for elem in val {
                documents.push(elem.into());
            }

            Some(documents)
        } else {
            None
        };

        Self { documents }
    }
}

impl crate::FbsSerde for Documents {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsDocuments>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct FloatAggregate {
    pub count: u64,
    pub histo: Option<Vec<FloatBucket>>,
    pub max: OrderedFloat<f64>,
    pub mean: OrderedFloat<f64>,
    pub min: OrderedFloat<f64>,
    pub sum: OrderedFloat<f64>,
    pub variance: OrderedFloat<f64>,
}

impl FloatAggregate {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsFloatAggregate<'a>> {
        use crate::types::generated::metadata_generated::FloatAggregateBuilder as FbsFloatAggregateBuilder;

        let histo_offset = self.histo.as_ref().map(|v| {
            let histo_offset = builder
                .create_vector_from_iter(v.iter().cloned().map(Into::<FbsFloatBucket>::into));
            histo_offset
        });

        let mut bldr = FbsFloatAggregateBuilder::new(builder);
        bldr.add_count(self.count);
        if let Some(offset) = histo_offset {
            bldr.add_histo(offset);
        }
        bldr.add_max(*self.max);
        bldr.add_mean(*self.mean);
        bldr.add_min(*self.min);
        bldr.add_sum(*self.sum);
        bldr.add_variance(*self.variance);
        bldr.finish()
    }
}

impl From<FbsFloatAggregate<'_>> for FloatAggregate {
    fn from(fbs: FbsFloatAggregate<'_>) -> Self {
        let count = fbs.count();
        let histo = if let Some(val) = fbs.histo() {
            let mut histo = Vec::new();
            for elem in val {
                histo.push(elem.into());
            }

            Some(histo)
        } else {
            None
        };

        let max = OrderedFloat(fbs.max());
        let mean = OrderedFloat(fbs.mean());
        let min = OrderedFloat(fbs.min());
        let sum = OrderedFloat(fbs.sum());
        let variance = OrderedFloat(fbs.variance());
        Self {
            count,
            histo,
            max,
            mean,
            min,
            sum,
            variance,
        }
    }
}

impl crate::FbsSerde for FloatAggregate {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsFloatAggregate>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct FloatBucket {
    pub count: u64,
    pub max: OrderedFloat<f64>,
}

impl From<FloatBucket> for FbsFloatBucket {
    fn from(obj: FloatBucket) -> Self {
        let count = obj.count;
        let max = obj.max.into_inner();

        Self::new(count, max)
    }
}

impl From<&FbsFloatBucket> for FloatBucket {
    fn from(fbs: &FbsFloatBucket) -> Self {
        Self {
            count: fbs.count(),
            max: OrderedFloat(fbs.max()),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct GeometryData {
    pub data: GeometryDataUnion,
}

impl GeometryData {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsGeometryData<'a>> {
        use crate::types::generated::metadata_generated::GeometryDataBuilder as FbsGeometryDataBuilder;

        let (data_offset, data_ty) = self.data.serialize_to(builder);

        let mut bldr = FbsGeometryDataBuilder::new(builder);
        bldr.add_data(data_offset);
        bldr.add_data_type(data_ty);
        bldr.finish()
    }
}

impl From<FbsGeometryData<'_>> for GeometryData {
    fn from(fbs: FbsGeometryData<'_>) -> Self {
        let data = match fbs.data_type() {
            FbsGeometryDataUnion::RawGeom => {
                GeometryDataUnion::RawGeom(RawGeom::from(fbs.data_as_raw_geom().unwrap()))
            }
            FbsGeometryDataUnion::NodeIdPair => {
                GeometryDataUnion::NodeIdPair(NodeIdPair::from(fbs.data_as_node_id_pair().unwrap()))
            }
            _ => unreachable!(),
        };

        Self { data }
    }
}

impl crate::FbsSerde for GeometryData {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsGeometryData>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct HierarchicalRelationship {
    pub children: Option<Vec<i32>>,
    pub parent: i32,
}

impl HierarchicalRelationship {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsHierarchicalRelationship<'a>> {
        use crate::types::generated::metadata_generated::HierarchicalRelationshipBuilder as FbsHierarchicalRelationshipBuilder;

        let children_offset = self.children.as_ref().map(|v| {
            let children_offset = builder.create_vector(&v);
            children_offset
        });

        let mut bldr = FbsHierarchicalRelationshipBuilder::new(builder);
        if let Some(offset) = children_offset {
            bldr.add_children(offset);
        }
        bldr.add_parent(self.parent);
        bldr.finish()
    }
}

impl From<FbsHierarchicalRelationship<'_>> for HierarchicalRelationship {
    fn from(fbs: FbsHierarchicalRelationship<'_>) -> Self {
        let children = if let Some(val) = fbs.children() {
            let mut children = Vec::new();
            for elem in val {
                children.push(elem.into());
            }

            Some(children)
        } else {
            None
        };

        let parent = fbs.parent();
        Self { children, parent }
    }
}

impl crate::FbsSerde for HierarchicalRelationship {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsHierarchicalRelationship>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct IntAggregate {
    pub count: u64,
    pub histo: Option<Vec<UIntBucket>>,
    pub max: i64,
    pub mean: i64,
    pub min: i64,
    pub sum: i64,
    pub variance: i64,
}

impl IntAggregate {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsIntAggregate<'a>> {
        use crate::types::generated::metadata_generated::IntAggregateBuilder as FbsIntAggregateBuilder;

        let histo_offset = self.histo.as_ref().map(|v| {
            let histo_offset =
                builder.create_vector_from_iter(v.iter().cloned().map(Into::<FbsUIntBucket>::into));
            histo_offset
        });

        let mut bldr = FbsIntAggregateBuilder::new(builder);
        bldr.add_count(self.count);
        if let Some(offset) = histo_offset {
            bldr.add_histo(offset);
        }
        bldr.add_max(self.max);
        bldr.add_mean(self.mean);
        bldr.add_min(self.min);
        bldr.add_sum(self.sum);
        bldr.add_variance(self.variance);
        bldr.finish()
    }
}

impl From<FbsIntAggregate<'_>> for IntAggregate {
    fn from(fbs: FbsIntAggregate<'_>) -> Self {
        let count = fbs.count();
        let histo = if let Some(val) = fbs.histo() {
            let mut histo = Vec::new();
            for elem in val {
                histo.push(elem.into());
            }

            Some(histo)
        } else {
            None
        };

        let max = fbs.max();
        let mean = fbs.mean();
        let min = fbs.min();
        let sum = fbs.sum();
        let variance = fbs.variance();
        Self {
            count,
            histo,
            max,
            mean,
            min,
            sum,
            variance,
        }
    }
}

impl crate::FbsSerde for IntAggregate {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsIntAggregate>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct IntBucket {
    pub count: u64,
    pub max: i64,
}

impl From<IntBucket> for FbsIntBucket {
    fn from(obj: IntBucket) -> Self {
        let count = obj.count;
        let max = obj.max;

        Self::new(count, max)
    }
}

impl From<&FbsIntBucket> for IntBucket {
    fn from(fbs: &FbsIntBucket) -> Self {
        Self {
            count: fbs.count(),
            max: fbs.max(),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct IntegerDisplayString {
    pub display_name: String,
    pub value: i64,
}

impl IntegerDisplayString {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsIntegerDisplayString<'a>> {
        use crate::types::generated::metadata_generated::IntegerDisplayStringBuilder as FbsIntegerDisplayStringBuilder;

        let display_name_offset = builder.create_string(&self.display_name);

        let mut bldr = FbsIntegerDisplayStringBuilder::new(builder);
        bldr.add_display_name(display_name_offset);
        bldr.add_value(self.value);
        bldr.finish()
    }
}

impl From<FbsIntegerDisplayString<'_>> for IntegerDisplayString {
    fn from(fbs: FbsIntegerDisplayString<'_>) -> Self {
        let display_name = fbs.display_name().to_owned();
        let value = fbs.value();
        Self {
            display_name,
            value,
        }
    }
}

impl crate::FbsSerde for IntegerDisplayString {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsIntegerDisplayString>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct Metadata {
    /// is to be included in the boundary selection modal
    pub area_selection: bool,
    /// Organizational category for frontend. Defaults to DC_HIDDEN.
    pub dataset_category: DatasetCategory,
    pub description: Option<String>,
    pub display_name: Option<String>,
    /// do not use user's viewport bounding box when fetching this stream's geometry from worldgraph
    pub do_not_filter_geometry_by_viewport: bool,
    pub entity_ty: EntityTy,
    /// Field groupings e.g. age ranges, ethnicities or hierarchical codes like zoning and NAICS
    pub field_relationships: Option<Vec<UlFieldRelationship>>,
    pub fields: Option<Vec<UlField>>,
    pub geometry_source: Option<GeometrySource>,
    /// Many geospatial datasets whose rows correspond to map locations have a
    /// column that should be used as the human-friendly display name of the location.
    /// For example, for a stream containing stores, this field might be "store_name".
    /// If the stream contains multiple rows with the same ul_node_id, then those rows
    /// should have the same value for the location_description_field.
    /// This attribute holds the index of the field in the dataset that should be
    /// used as the location description.
    pub location_description_field: i32,
    /// Indices of fields that are "promoted to metrics"
    pub promoted_metrics: Option<Vec<i32>>,
    /// An optional field that is meant to provide information to the user on
    /// where the data has come from
    pub source: Option<DatasetSource>,
    pub summary: Option<Vec<i32>>,
    pub update_cadence: UpdateCadence,
    /// Indices of non-numeric fields that are "visualized in Explore" — they
    /// appear as a categorical color visualization on Generic layers but, unlike
    /// promoted_metrics, do NOT create an entry in the metric catalog.
    pub visualize_in_explore_fields: Option<Vec<i32>>,
}

impl Metadata {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsMetadata<'a>> {
        use crate::types::generated::metadata_generated::MetadataBuilder as FbsMetadataBuilder;

        let description_offset = self.description.as_ref().map(|s| builder.create_string(s));
        let display_name_offset = self.display_name.as_ref().map(|s| builder.create_string(s));
        let field_relationships_offset = self.field_relationships.as_ref().map(|v| {
            let mut field_relationships_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                field_relationships_offsets.push(offset);
            }
            let field_relationships_offset = builder.create_vector(&field_relationships_offsets);
            field_relationships_offset
        });
        let fields_offset = self.fields.as_ref().map(|v| {
            let mut fields_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                fields_offsets.push(offset);
            }
            let fields_offset = builder.create_vector(&fields_offsets);
            fields_offset
        });
        let geometry_source_offset = self
            .geometry_source
            .as_ref()
            .map(|u| u.serialize_to(builder));
        let promoted_metrics_offset = self.promoted_metrics.as_ref().map(|v| {
            let promoted_metrics_offset = builder.create_vector(&v);
            promoted_metrics_offset
        });
        let source_offset = self.source.as_ref().map(|o| o.serialize_to(builder));
        let summary_offset = self.summary.as_ref().map(|v| {
            let summary_offset = builder.create_vector(&v);
            summary_offset
        });
        let visualize_in_explore_fields_offset =
            self.visualize_in_explore_fields.as_ref().map(|v| {
                let visualize_in_explore_fields_offset = builder.create_vector(&v);
                visualize_in_explore_fields_offset
            });

        let mut bldr = FbsMetadataBuilder::new(builder);
        bldr.add_area_selection(self.area_selection);
        bldr.add_dataset_category(FbsDatasetCategory::from(self.dataset_category));
        if let Some(offset) = description_offset {
            bldr.add_description(offset);
        }
        if let Some(offset) = display_name_offset {
            bldr.add_display_name(offset);
        }
        bldr.add_do_not_filter_geometry_by_viewport(self.do_not_filter_geometry_by_viewport);
        bldr.add_entity_ty(FbsEntityTy::from(self.entity_ty));
        if let Some(offset) = field_relationships_offset {
            bldr.add_field_relationships(offset);
        }
        if let Some(offset) = fields_offset {
            bldr.add_fields(offset);
        }
        if let Some((offset, ty)) = geometry_source_offset {
            bldr.add_geometry_source(offset);
            bldr.add_geometry_source_type(ty);
        }
        bldr.add_location_description_field(self.location_description_field);
        if let Some(offset) = promoted_metrics_offset {
            bldr.add_promoted_metrics(offset);
        }
        if let Some(offset) = source_offset {
            bldr.add_source(offset);
        }
        if let Some(offset) = summary_offset {
            bldr.add_summary(offset);
        }
        bldr.add_update_cadence(FbsUpdateCadence::from(self.update_cadence));
        if let Some(offset) = visualize_in_explore_fields_offset {
            bldr.add_visualize_in_explore_fields(offset);
        }
        bldr.finish()
    }
}

impl From<FbsMetadata<'_>> for Metadata {
    fn from(fbs: FbsMetadata<'_>) -> Self {
        let area_selection = fbs.area_selection();
        let dataset_category = DatasetCategory::from(fbs.dataset_category());
        let description = fbs.description().map(ToOwned::to_owned);
        let display_name = fbs.display_name().map(ToOwned::to_owned);
        let do_not_filter_geometry_by_viewport = fbs.do_not_filter_geometry_by_viewport();
        let entity_ty = EntityTy::from(fbs.entity_ty());
        let field_relationships = if let Some(val) = fbs.field_relationships() {
            let mut field_relationships = Vec::new();
            for elem in val {
                field_relationships.push(elem.into());
            }

            Some(field_relationships)
        } else {
            None
        };

        let fields = if let Some(val) = fbs.fields() {
            let mut fields = Vec::new();
            for elem in val {
                fields.push(elem.into());
            }

            Some(fields)
        } else {
            None
        };

        let geometry_source = if let Some(val) = fbs.geometry_source() {
            let geometry_source = match fbs.geometry_source_type() {
                FbsGeometrySource::NoGeometry => GeometrySource::NoGeometry(NoGeometry::from(
                    fbs.geometry_source_as_no_geometry().unwrap(),
                )),
                FbsGeometrySource::DatacatalogGeometry => {
                    GeometrySource::DatacatalogGeometry(DatacatalogGeometry::from(
                        fbs.geometry_source_as_datacatalog_geometry().unwrap(),
                    ))
                }
                FbsGeometrySource::WorldGraphGeometry => {
                    GeometrySource::WorldGraphGeometry(WorldGraphGeometry::from(
                        fbs.geometry_source_as_world_graph_geometry().unwrap(),
                    ))
                }
                FbsGeometrySource::DatacatalogLatLngGeometry => {
                    GeometrySource::DatacatalogLatLngGeometry(DatacatalogLatLngGeometry::from(
                        fbs.geometry_source_as_datacatalog_lat_lng_geometry()
                            .unwrap(),
                    ))
                }
                _ => unreachable!(),
            };

            Some(geometry_source)
        } else {
            None
        };

        let location_description_field = fbs.location_description_field();
        let promoted_metrics = if let Some(val) = fbs.promoted_metrics() {
            let mut promoted_metrics = Vec::new();
            for elem in val {
                promoted_metrics.push(elem.into());
            }

            Some(promoted_metrics)
        } else {
            None
        };

        let source = fbs.source().map(DatasetSource::from);
        let summary = if let Some(val) = fbs.summary() {
            let mut summary = Vec::new();
            for elem in val {
                summary.push(elem.into());
            }

            Some(summary)
        } else {
            None
        };

        let update_cadence = UpdateCadence::from(fbs.update_cadence());
        let visualize_in_explore_fields = if let Some(val) = fbs.visualize_in_explore_fields() {
            let mut visualize_in_explore_fields = Vec::new();
            for elem in val {
                visualize_in_explore_fields.push(elem.into());
            }

            Some(visualize_in_explore_fields)
        } else {
            None
        };

        Self {
            area_selection,
            dataset_category,
            description,
            display_name,
            do_not_filter_geometry_by_viewport,
            entity_ty,
            field_relationships,
            fields,
            geometry_source,
            location_description_field,
            promoted_metrics,
            source,
            summary,
            update_cadence,
            visualize_in_explore_fields,
        }
    }
}

impl crate::FbsSerde for Metadata {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsMetadata>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

impl Default for Metadata {
    fn default() -> Self {
        Self {
            area_selection: bool::default(),
            dataset_category: DatasetCategory::DC_HIDDEN,
            description: None,
            display_name: None,
            do_not_filter_geometry_by_viewport: bool::default(),
            entity_ty: EntityTy::default(),
            field_relationships: None,
            fields: None,
            geometry_source: None,
            location_description_field: -1,
            promoted_metrics: None,
            source: None,
            summary: None,
            update_cadence: UpdateCadence::default(),
            visualize_in_explore_fields: None,
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedCategoryRelationshipNode {
    pub child_columns: Option<Vec<i32>>,
    pub column: i32,
}

impl NestedCategoryRelationshipNode {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedCategoryRelationshipNode<'a>> {
        use crate::types::generated::metadata_generated::NestedCategoryRelationshipNodeBuilder as FbsNestedCategoryRelationshipNodeBuilder;

        let child_columns_offset = self.child_columns.as_ref().map(|v| {
            let child_columns_offset = builder.create_vector(&v);
            child_columns_offset
        });

        let mut bldr = FbsNestedCategoryRelationshipNodeBuilder::new(builder);
        if let Some(offset) = child_columns_offset {
            bldr.add_child_columns(offset);
        }
        bldr.add_column(self.column);
        bldr.finish()
    }
}

impl From<FbsNestedCategoryRelationshipNode<'_>> for NestedCategoryRelationshipNode {
    fn from(fbs: FbsNestedCategoryRelationshipNode<'_>) -> Self {
        let child_columns = if let Some(val) = fbs.child_columns() {
            let mut child_columns = Vec::new();
            for elem in val {
                child_columns.push(elem.into());
            }

            Some(child_columns)
        } else {
            None
        };

        let column = fbs.column();
        Self {
            child_columns,
            column,
        }
    }
}

impl crate::FbsSerde for NestedCategoryRelationshipNode {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsNestedCategoryRelationshipNode>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedHierarchyRelationshipNode {
    pub child_columns: Option<Vec<i32>>,
    pub child_nodes: Option<Vec<i32>>,
    pub label: Option<String>,
}

impl NestedHierarchyRelationshipNode {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedHierarchyRelationshipNode<'a>> {
        use crate::types::generated::metadata_generated::NestedHierarchyRelationshipNodeBuilder as FbsNestedHierarchyRelationshipNodeBuilder;

        let child_columns_offset = self.child_columns.as_ref().map(|v| {
            let child_columns_offset = builder.create_vector(&v);
            child_columns_offset
        });
        let child_nodes_offset = self.child_nodes.as_ref().map(|v| {
            let child_nodes_offset = builder.create_vector(&v);
            child_nodes_offset
        });
        let label_offset = self.label.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsNestedHierarchyRelationshipNodeBuilder::new(builder);
        if let Some(offset) = child_columns_offset {
            bldr.add_child_columns(offset);
        }
        if let Some(offset) = child_nodes_offset {
            bldr.add_child_nodes(offset);
        }
        if let Some(offset) = label_offset {
            bldr.add_label(offset);
        }
        bldr.finish()
    }
}

impl From<FbsNestedHierarchyRelationshipNode<'_>> for NestedHierarchyRelationshipNode {
    fn from(fbs: FbsNestedHierarchyRelationshipNode<'_>) -> Self {
        let child_columns = if let Some(val) = fbs.child_columns() {
            let mut child_columns = Vec::new();
            for elem in val {
                child_columns.push(elem.into());
            }

            Some(child_columns)
        } else {
            None
        };

        let child_nodes = if let Some(val) = fbs.child_nodes() {
            let mut child_nodes = Vec::new();
            for elem in val {
                child_nodes.push(elem.into());
            }

            Some(child_nodes)
        } else {
            None
        };

        let label = fbs.label().map(ToOwned::to_owned);
        Self {
            child_columns,
            child_nodes,
            label,
        }
    }
}

impl crate::FbsSerde for NestedHierarchyRelationshipNode {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsNestedHierarchyRelationshipNode>(
            &opts, bytes,
        )?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct NestedStringCategoryNode {
    pub child_values: Vec<String>,
    pub value: Option<String>,
}

impl NestedStringCategoryNode {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsNestedStringCategoryNode<'a>> {
        use crate::types::generated::metadata_generated::NestedStringCategoryNodeBuilder as FbsNestedStringCategoryNodeBuilder;

        let mut child_values_offsets = Vec::with_capacity(self.child_values.len());
        for val in self.child_values.iter() {
            let offset = builder.create_string(val);
            child_values_offsets.push(offset);
        }
        let child_values_offset = builder.create_vector(&child_values_offsets);
        let value_offset = self.value.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsNestedStringCategoryNodeBuilder::new(builder);
        bldr.add_child_values(child_values_offset);
        if let Some(offset) = value_offset {
            bldr.add_value(offset);
        }
        bldr.finish()
    }
}

impl From<FbsNestedStringCategoryNode<'_>> for NestedStringCategoryNode {
    fn from(fbs: FbsNestedStringCategoryNode<'_>) -> Self {
        let mut child_values = Vec::new();
        for elem in fbs.child_values() {
            child_values.push(elem.into());
        }

        let value = fbs.value().map(ToOwned::to_owned);
        Self {
            child_values,
            value,
        }
    }
}

impl crate::FbsSerde for NestedStringCategoryNode {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsNestedStringCategoryNode>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct StringAggregate {
    pub count: u64,
    pub str: Option<String>,
}

impl StringAggregate {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsStringAggregate<'a>> {
        use crate::types::generated::metadata_generated::StringAggregateBuilder as FbsStringAggregateBuilder;

        let str_offset = self.str.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsStringAggregateBuilder::new(builder);
        bldr.add_count(self.count);
        if let Some(offset) = str_offset {
            bldr.add_str(offset);
        }
        bldr.finish()
    }
}

impl From<FbsStringAggregate<'_>> for StringAggregate {
    fn from(fbs: FbsStringAggregate<'_>) -> Self {
        let count = fbs.count();
        let str = fbs.str().map(ToOwned::to_owned);
        Self { count, str }
    }
}

impl crate::FbsSerde for StringAggregate {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsStringAggregate>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct TimeInterval {
    pub max: i64,
    pub min: i64,
}

impl TimeInterval {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsTimeInterval<'a>> {
        use crate::types::generated::metadata_generated::TimeIntervalBuilder as FbsTimeIntervalBuilder;

        let mut bldr = FbsTimeIntervalBuilder::new(builder);
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        bldr.finish()
    }
}

impl From<FbsTimeInterval<'_>> for TimeInterval {
    fn from(fbs: FbsTimeInterval<'_>) -> Self {
        let max = fbs.max();
        let min = fbs.min();
        Self { max, min }
    }
}

impl crate::FbsSerde for TimeInterval {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsTimeInterval>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct UIntAggregate {
    pub count: u64,
    pub histo: Option<Vec<UIntBucket>>,
    pub max: u64,
    pub mean: u64,
    pub min: u64,
    pub sum: u64,
    pub variance: u64,
}

impl UIntAggregate {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsUIntAggregate<'a>> {
        use crate::types::generated::metadata_generated::UIntAggregateBuilder as FbsUIntAggregateBuilder;

        let histo_offset = self.histo.as_ref().map(|v| {
            let histo_offset =
                builder.create_vector_from_iter(v.iter().cloned().map(Into::<FbsUIntBucket>::into));
            histo_offset
        });

        let mut bldr = FbsUIntAggregateBuilder::new(builder);
        bldr.add_count(self.count);
        if let Some(offset) = histo_offset {
            bldr.add_histo(offset);
        }
        bldr.add_max(self.max);
        bldr.add_mean(self.mean);
        bldr.add_min(self.min);
        bldr.add_sum(self.sum);
        bldr.add_variance(self.variance);
        bldr.finish()
    }
}

impl From<FbsUIntAggregate<'_>> for UIntAggregate {
    fn from(fbs: FbsUIntAggregate<'_>) -> Self {
        let count = fbs.count();
        let histo = if let Some(val) = fbs.histo() {
            let mut histo = Vec::new();
            for elem in val {
                histo.push(elem.into());
            }

            Some(histo)
        } else {
            None
        };

        let max = fbs.max();
        let mean = fbs.mean();
        let min = fbs.min();
        let sum = fbs.sum();
        let variance = fbs.variance();
        Self {
            count,
            histo,
            max,
            mean,
            min,
            sum,
            variance,
        }
    }
}

impl crate::FbsSerde for UIntAggregate {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsUIntAggregate>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct UIntBucket {
    pub count: u64,
    pub max: u64,
}

impl From<UIntBucket> for FbsUIntBucket {
    fn from(obj: UIntBucket) -> Self {
        let count = obj.count;
        let max = obj.max;

        Self::new(count, max)
    }
}

impl From<&FbsUIntBucket> for UIntBucket {
    fn from(fbs: &FbsUIntBucket) -> Self {
        Self {
            count: fbs.count(),
            max: fbs.max(),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct UlField {
    pub breakdown_display_name: Option<String>,
    pub component_data: Option<ComponentData>,
    pub default: Option<ValueInstance>,
    pub description: Option<String>,
    pub display_name: Option<String>,
    pub field_name: Option<String>,
    pub field_type: UlFieldType,
    pub flags: u32,
    pub storage_type: Option<Type>,
    pub unit: FieldUnit,
}

impl UlField {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsUlField<'a>> {
        use crate::types::generated::metadata_generated::UlFieldBuilder as FbsUlFieldBuilder;

        let breakdown_display_name_offset = self
            .breakdown_display_name
            .as_ref()
            .map(|s| builder.create_string(s));
        let component_data_offset = self
            .component_data
            .as_ref()
            .map(|u| u.serialize_to(builder));
        let default_offset = self.default.as_ref().map(|o| o.serialize_to(builder));
        let description_offset = self.description.as_ref().map(|s| builder.create_string(s));
        let display_name_offset = self.display_name.as_ref().map(|s| builder.create_string(s));
        let field_name_offset = self.field_name.as_ref().map(|s| builder.create_string(s));
        let storage_type_offset = self.storage_type.as_ref().map(|u| u.serialize_to(builder));

        let mut bldr = FbsUlFieldBuilder::new(builder);
        if let Some(offset) = breakdown_display_name_offset {
            bldr.add_breakdown_display_name(offset);
        }
        if let Some((offset, ty)) = component_data_offset {
            bldr.add_component_data(offset);
            bldr.add_component_data_type(ty);
        }
        if let Some(offset) = default_offset {
            bldr.add_default(offset);
        }
        if let Some(offset) = description_offset {
            bldr.add_description(offset);
        }
        if let Some(offset) = display_name_offset {
            bldr.add_display_name(offset);
        }
        if let Some(offset) = field_name_offset {
            bldr.add_field_name(offset);
        }
        bldr.add_field_type(FbsUlFieldType::from(self.field_type));
        bldr.add_flags(self.flags);
        if let Some((offset, ty)) = storage_type_offset {
            bldr.add_storage_type(offset);
            bldr.add_storage_type_type(ty);
        }
        bldr.add_unit(FbsFieldUnit::from(self.unit));
        bldr.finish()
    }
}

impl From<FbsUlField<'_>> for UlField {
    fn from(fbs: FbsUlField<'_>) -> Self {
        let breakdown_display_name = fbs.breakdown_display_name().map(ToOwned::to_owned);
        let component_data = if let Some(val) = fbs.component_data() {
            let component_data = match fbs.component_data_type() {
                FbsComponentData::StringCategories => ComponentData::StringCategories(
                    StringCategories::from(fbs.component_data_as_string_categories().unwrap()),
                ),
                FbsComponentData::IntRange => ComponentData::IntRange(IntRange::from(
                    fbs.component_data_as_int_range().unwrap(),
                )),
                FbsComponentData::FloatRange => ComponentData::FloatRange(FloatRange::from(
                    fbs.component_data_as_float_range().unwrap(),
                )),
                FbsComponentData::DatetimeRange => ComponentData::DatetimeRange(
                    DatetimeRange::from(fbs.component_data_as_datetime_range().unwrap()),
                ),
                FbsComponentData::Dates => {
                    ComponentData::Dates(Dates::from(fbs.component_data_as_dates().unwrap()))
                }
                FbsComponentData::NestedStringCategories => {
                    ComponentData::NestedStringCategories(NestedStringCategories::from(
                        fbs.component_data_as_nested_string_categories().unwrap(),
                    ))
                }
                _ => unreachable!(),
            };

            Some(component_data)
        } else {
            None
        };

        let default = fbs.default().map(ValueInstance::from);
        let description = fbs.description().map(ToOwned::to_owned);
        let display_name = fbs.display_name().map(ToOwned::to_owned);
        let field_name = fbs.field_name().map(ToOwned::to_owned);
        let field_type = UlFieldType::from(fbs.field_type());
        let flags = fbs.flags();
        let storage_type = if let Some(val) = fbs.storage_type() {
            let storage_type = match fbs.storage_type_type() {
                FbsType::Null => Type::Null(Null::from(fbs.storage_type_as_null().unwrap())),
                FbsType::Int => Type::Int(Int::from(fbs.storage_type_as_int().unwrap())),
                FbsType::FloatingPoint => Type::FloatingPoint(FloatingPoint::from(
                    fbs.storage_type_as_floating_point().unwrap(),
                )),
                FbsType::Binary => {
                    Type::Binary(Binary::from(fbs.storage_type_as_binary().unwrap()))
                }
                FbsType::Utf8 => Type::Utf8(Utf8::from(fbs.storage_type_as_utf_8().unwrap())),
                FbsType::Bool => Type::Bool(Bool::from(fbs.storage_type_as_bool().unwrap())),
                FbsType::Decimal => {
                    Type::Decimal(Decimal::from(fbs.storage_type_as_decimal().unwrap()))
                }
                FbsType::Date => Type::Date(Date::from(fbs.storage_type_as_date().unwrap())),
                FbsType::Time => Type::Time(Time::from(fbs.storage_type_as_time().unwrap())),
                FbsType::Timestamp => {
                    Type::Timestamp(Timestamp::from(fbs.storage_type_as_timestamp().unwrap()))
                }
                FbsType::Interval => {
                    Type::Interval(Interval::from(fbs.storage_type_as_interval().unwrap()))
                }
                FbsType::List => Type::List(List::from(fbs.storage_type_as_list().unwrap())),
                FbsType::Struct_ => {
                    Type::Struct_(Struct_::from(fbs.storage_type_as_struct_().unwrap()))
                }
                FbsType::Union => Type::Union(Union::from(fbs.storage_type_as_union().unwrap())),
                FbsType::FixedSizeBinary => Type::FixedSizeBinary(FixedSizeBinary::from(
                    fbs.storage_type_as_fixed_size_binary().unwrap(),
                )),
                FbsType::FixedSizeList => Type::FixedSizeList(FixedSizeList::from(
                    fbs.storage_type_as_fixed_size_list().unwrap(),
                )),
                FbsType::Map => Type::Map(Map::from(fbs.storage_type_as_map().unwrap())),
                FbsType::Duration => {
                    Type::Duration(Duration::from(fbs.storage_type_as_duration().unwrap()))
                }
                FbsType::LargeBinary => Type::LargeBinary(LargeBinary::from(
                    fbs.storage_type_as_large_binary().unwrap(),
                )),
                FbsType::LargeUtf8 => {
                    Type::LargeUtf8(LargeUtf8::from(fbs.storage_type_as_large_utf_8().unwrap()))
                }
                FbsType::LargeList => {
                    Type::LargeList(LargeList::from(fbs.storage_type_as_large_list().unwrap()))
                }
                FbsType::RunEndEncoded => Type::RunEndEncoded(RunEndEncoded::from(
                    fbs.storage_type_as_run_end_encoded().unwrap(),
                )),
                FbsType::BinaryView => {
                    Type::BinaryView(BinaryView::from(fbs.storage_type_as_binary_view().unwrap()))
                }
                FbsType::Utf8View => {
                    Type::Utf8View(Utf8View::from(fbs.storage_type_as_utf_8_view().unwrap()))
                }
                FbsType::ListView => {
                    Type::ListView(ListView::from(fbs.storage_type_as_list_view().unwrap()))
                }
                FbsType::LargeListView => Type::LargeListView(LargeListView::from(
                    fbs.storage_type_as_large_list_view().unwrap(),
                )),
                _ => unreachable!(),
            };

            Some(storage_type)
        } else {
            None
        };

        let unit = FieldUnit::from(fbs.unit());
        Self {
            breakdown_display_name,
            component_data,
            default,
            description,
            display_name,
            field_name,
            field_type,
            flags,
            storage_type,
            unit,
        }
    }
}

impl crate::FbsSerde for UlField {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs = flatbuffers::size_prefixed_root_with_opts::<FbsUlField>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[derive(Default, PartialEq, Debug, Clone, Hash, Eq, Serialize, Deserialize)]
pub struct UlFieldRelationship {
    pub relationship_data: Option<UlFieldRelationshipData>,
    pub relationship_display_name: Option<String>,
}

impl UlFieldRelationship {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsUlFieldRelationship<'a>> {
        use crate::types::generated::metadata_generated::UlFieldRelationshipBuilder as FbsUlFieldRelationshipBuilder;

        let relationship_data_offset = self
            .relationship_data
            .as_ref()
            .map(|u| u.serialize_to(builder));
        let relationship_display_name_offset = self
            .relationship_display_name
            .as_ref()
            .map(|s| builder.create_string(s));

        let mut bldr = FbsUlFieldRelationshipBuilder::new(builder);
        if let Some((offset, ty)) = relationship_data_offset {
            bldr.add_relationship_data(offset);
            bldr.add_relationship_data_type(ty);
        }
        if let Some(offset) = relationship_display_name_offset {
            bldr.add_relationship_display_name(offset);
        }
        bldr.finish()
    }
}

impl From<FbsUlFieldRelationship<'_>> for UlFieldRelationship {
    fn from(fbs: FbsUlFieldRelationship<'_>) -> Self {
        let relationship_data = if let Some(val) = fbs.relationship_data() {
            let relationship_data = match fbs.relationship_data_type() {
                FbsUlFieldRelationshipData::HierarchyRelationshipData => {
                    UlFieldRelationshipData::HierarchyRelationshipData(
                        HierarchyRelationshipData::from(
                            fbs.relationship_data_as_hierarchy_relationship_data()
                                .unwrap(),
                        ),
                    )
                }
                FbsUlFieldRelationshipData::CategoryRelationshipData => {
                    UlFieldRelationshipData::CategoryRelationshipData(
                        CategoryRelationshipData::from(
                            fbs.relationship_data_as_category_relationship_data()
                                .unwrap(),
                        ),
                    )
                }
                FbsUlFieldRelationshipData::NestedCategoryRelationshipData => {
                    UlFieldRelationshipData::NestedCategoryRelationshipData(
                        NestedCategoryRelationshipData::from(
                            fbs.relationship_data_as_nested_category_relationship_data()
                                .unwrap(),
                        ),
                    )
                }
                FbsUlFieldRelationshipData::NestedHierarchyRelationshipData => {
                    UlFieldRelationshipData::NestedHierarchyRelationshipData(
                        NestedHierarchyRelationshipData::from(
                            fbs.relationship_data_as_nested_hierarchy_relationship_data()
                                .unwrap(),
                        ),
                    )
                }
                _ => unreachable!(),
            };

            Some(relationship_data)
        } else {
            None
        };

        let relationship_display_name = fbs.relationship_display_name().map(ToOwned::to_owned);
        Self {
            relationship_data,
            relationship_display_name,
        }
    }
}

impl crate::FbsSerde for UlFieldRelationship {
    fn to_fbs_bytes(&self) -> Vec<u8> {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = self.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }

    fn from_fbs_bytes(bytes: &[u8]) -> Result<Self, flatbuffers::InvalidFlatbuffer> {
        let opts = flatbuffers::VerifierOptions {
            max_tables: 100_000_000,
            ..Default::default()
        };
        let fbs =
            flatbuffers::size_prefixed_root_with_opts::<FbsUlFieldRelationship>(&opts, bytes)?;
        Ok(Self::from(fbs))
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_category_relationship_data() {
        let t0 = CategoryRelationshipData::default();
        let buf = t0.to_fbs_bytes();
        let t1 = CategoryRelationshipData::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_contact_info() {
        let t0 = ContactInfo::default();
        let buf = t0.to_fbs_bytes();
        let t1 = ContactInfo::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_datacatalog_geometry() {
        let t0 = DatacatalogGeometry::default();
        let buf = t0.to_fbs_bytes();
        let t1 = DatacatalogGeometry::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_datacatalog_lat_lng_geometry() {
        let t0 = DatacatalogLatLngGeometry::default();
        let buf = t0.to_fbs_bytes();
        let t1 = DatacatalogLatLngGeometry::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_dataset_source() {
        let t0 = DatasetSource::default();
        let buf = t0.to_fbs_bytes();
        let t1 = DatasetSource::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_dates() {
        let t0 = Dates::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Dates::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_datetime_range() {
        let t0 = DatetimeRange::default();
        let buf = t0.to_fbs_bytes();
        let t1 = DatetimeRange::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_document() {
        let t0 = Document::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Document::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_documents() {
        let t0 = Documents::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Documents::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_float_aggregate() {
        let t0 = FloatAggregate::default();
        let buf = t0.to_fbs_bytes();
        let t1 = FloatAggregate::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_float_range() {
        let t0 = FloatRange::default();
        let buf = t0.to_fbs_bytes();
        let t1 = FloatRange::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_geometry_data() {
        let t0 = GeometryData::default();
        let buf = t0.to_fbs_bytes();
        let t1 = GeometryData::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_hierarchical_relationship() {
        let t0 = HierarchicalRelationship::default();
        let buf = t0.to_fbs_bytes();
        let t1 = HierarchicalRelationship::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_hierarchy_relationship_data() {
        let t0 = HierarchyRelationshipData::default();
        let buf = t0.to_fbs_bytes();
        let t1 = HierarchyRelationshipData::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_int_aggregate() {
        let t0 = IntAggregate::default();
        let buf = t0.to_fbs_bytes();
        let t1 = IntAggregate::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_int_range() {
        let t0 = IntRange::default();
        let buf = t0.to_fbs_bytes();
        let t1 = IntRange::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_integer_display_string() {
        let t0 = IntegerDisplayString::default();
        let buf = t0.to_fbs_bytes();
        let t1 = IntegerDisplayString::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_metadata() {
        let t0 = Metadata::default();
        let buf = t0.to_fbs_bytes();
        let t1 = Metadata::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_category_relationship_data() {
        let t0 = NestedCategoryRelationshipData::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedCategoryRelationshipData::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_category_relationship_node() {
        let t0 = NestedCategoryRelationshipNode::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedCategoryRelationshipNode::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_hierarchy_relationship_data() {
        let t0 = NestedHierarchyRelationshipData::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedHierarchyRelationshipData::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_hierarchy_relationship_node() {
        let t0 = NestedHierarchyRelationshipNode::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedHierarchyRelationshipNode::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_string_categories() {
        let t0 = NestedStringCategories::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedStringCategories::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_nested_string_category_node() {
        let t0 = NestedStringCategoryNode::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NestedStringCategoryNode::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_no_geometry() {
        let t0 = NoGeometry::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NoGeometry::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_numerical_field_format() {
        let t0 = NumericalFieldFormat::default();
        let buf = t0.to_fbs_bytes();
        let t1 = NumericalFieldFormat::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_raw_geom() {
        let t0 = RawGeom::default();
        let buf = t0.to_fbs_bytes();
        let t1 = RawGeom::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_string_aggregate() {
        let t0 = StringAggregate::default();
        let buf = t0.to_fbs_bytes();
        let t1 = StringAggregate::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_string_categories() {
        let t0 = StringCategories::default();
        let buf = t0.to_fbs_bytes();
        let t1 = StringCategories::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_time_interval() {
        let t0 = TimeInterval::default();
        let buf = t0.to_fbs_bytes();
        let t1 = TimeInterval::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_u_int_aggregate() {
        let t0 = UIntAggregate::default();
        let buf = t0.to_fbs_bytes();
        let t1 = UIntAggregate::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_ul_field() {
        let t0 = UlField::default();
        let buf = t0.to_fbs_bytes();
        let t1 = UlField::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_ul_field_relationship() {
        let t0 = UlFieldRelationship::default();
        let buf = t0.to_fbs_bytes();
        let t1 = UlFieldRelationship::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_world_graph_geometry() {
        let t0 = WorldGraphGeometry::default();
        let buf = t0.to_fbs_bytes();
        let t1 = WorldGraphGeometry::from_fbs_bytes(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }
}
