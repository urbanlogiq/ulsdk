// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use flatbuffers::{WIPOffset, UnionWIPOffset};
use bitflags::bitflags;
use core::ops::Deref;

use crate::types::entity::{
    EdgeTy,
    EntityTy,
    Geometry,
    GraphEdge,
    GraphNode,
    Line,
    MultiLine,
    MultiPolygon,
    NodeTy,
    Point,
    Polygon,
};
use crate::types::id::{
    B2cId,
    ColumnGroupId,
    ContentId,
    DataStateId,
    GenericId,
    GraphNodeId,
    ObjectId,
    ObjectNamespace,
    StreamId,
};
use crate::types::generated::entity_generated::{
    GraphEdge as FbsGraphEdge,
    GraphNode as FbsGraphNode,
    Line as FbsLine,
    MultiLine as FbsMultiLine,
    MultiPolygon as FbsMultiPolygon,
    Point as FbsPoint,
    Polygon as FbsPolygon,
    EdgeTy as FbsEdgeTy,
    EntityTy as FbsEntityTy,
    Geometry as FbsGeometry,
    NodeTy as FbsNodeTy,
};
use crate::types::generated::id_generated::{
    B2cId as FbsB2cId,
    ColumnGroupId as FbsColumnGroupId,
    ContentId as FbsContentId,
    DataStateId as FbsDataStateId,
    GenericId as FbsGenericId,
    GraphNodeId as FbsGraphNodeId,
    ObjectId as FbsObjectId,
    StreamId as FbsStreamId,
    ObjectNamespace as FbsObjectNamespace,
};
use crate::types::generated::user_project_generated::{
    CategoryFilter as FbsCategoryFilter,
    ColumnGroup as FbsColumnGroup,
    DataStateGeometrySource as FbsDataStateGeometrySource,
    DeprecatedDataStateJoin as FbsDeprecatedDataStateJoin,
    FieldFilter as FbsFieldFilter,
    JoinStackEntry as FbsJoinStackEntry,
    RangeFilter as FbsRangeFilter,
    RawGeometrySource as FbsRawGeometrySource,
    RawGeometrySourceGeom as FbsRawGeometrySourceGeom,
    RelationshipRangeFilter as FbsRelationshipRangeFilter,
    StackableDataStateJoin as FbsStackableDataStateJoin,
    UserLayer as FbsUserLayer,
    UserLayerDataState as FbsUserLayerDataState,
    UserProject as FbsUserProject,
    AggregateOp as FbsAggregateOp,
    ColumnGroupType as FbsColumnGroupType,
    FieldComparator as FbsFieldComparator,
    Filter as FbsFilter,
    JoinOperation as FbsJoinOperation,
    JoinStackEntryGeometrySourceUnion as FbsJoinStackEntryGeometrySourceUnion,
    JoinType as FbsJoinType,
    LayerCombineMode as FbsLayerCombineMode,
};

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum AggregateOp {
    #[default]
    Average = 0,
    Relative = 1,
}

impl From<AggregateOp> for FbsAggregateOp {
    fn from(val: AggregateOp) -> Self {
        match val {
            AggregateOp::Average => FbsAggregateOp::Average,
            AggregateOp::Relative => FbsAggregateOp::Relative,
        }
    }
}

impl From<FbsAggregateOp> for AggregateOp {
    fn from(fbs: FbsAggregateOp) -> Self {
        match fbs.0 {
            0 => Self::Average,
            1 => Self::Relative,
            _ => panic!("Invalid value {} when constructing AggregateOp", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum ColumnGroupType {
    #[default]
    None_ = 0,
    Relationship = 1,
    Column = 2,
}

impl From<ColumnGroupType> for FbsColumnGroupType {
    fn from(val: ColumnGroupType) -> Self {
        match val {
            ColumnGroupType::None_ => FbsColumnGroupType::None,
            ColumnGroupType::Relationship => FbsColumnGroupType::Relationship,
            ColumnGroupType::Column => FbsColumnGroupType::Column,
        }
    }
}

impl From<FbsColumnGroupType> for ColumnGroupType {
    fn from(fbs: FbsColumnGroupType) -> Self {
        match fbs.0 {
            0 => Self::None_,
            1 => Self::Relationship,
            2 => Self::Column,
            _ => panic!("Invalid value {} when constructing ColumnGroupType", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum FieldComparator {
    #[default]
    Greater = 0,
    GreaterEqual = 1,
    Less = 2,
    LessEqual = 3,
    Equal = 4,
    NotEqual = 5,
}

impl From<FieldComparator> for FbsFieldComparator {
    fn from(val: FieldComparator) -> Self {
        match val {
            FieldComparator::Greater => FbsFieldComparator::Greater,
            FieldComparator::GreaterEqual => FbsFieldComparator::GreaterEqual,
            FieldComparator::Less => FbsFieldComparator::Less,
            FieldComparator::LessEqual => FbsFieldComparator::LessEqual,
            FieldComparator::Equal => FbsFieldComparator::Equal,
            FieldComparator::NotEqual => FbsFieldComparator::NotEqual,
        }
    }
}

impl From<FbsFieldComparator> for FieldComparator {
    fn from(fbs: FbsFieldComparator) -> Self {
        match fbs.0 {
            0 => Self::Greater,
            1 => Self::GreaterEqual,
            2 => Self::Less,
            3 => Self::LessEqual,
            4 => Self::Equal,
            5 => Self::NotEqual,
            _ => panic!("Invalid value {} when constructing FieldComparator", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum JoinOperation {
    #[default]
    Intersect = 0,
    Disjoint = 1,
    BufferedIntersection = 2,
}

impl From<JoinOperation> for FbsJoinOperation {
    fn from(val: JoinOperation) -> Self {
        match val {
            JoinOperation::Intersect => FbsJoinOperation::Intersect,
            JoinOperation::Disjoint => FbsJoinOperation::Disjoint,
            JoinOperation::BufferedIntersection => FbsJoinOperation::BufferedIntersection,
        }
    }
}

impl From<FbsJoinOperation> for JoinOperation {
    fn from(fbs: FbsJoinOperation) -> Self {
        match fbs.0 {
            0 => Self::Intersect,
            1 => Self::Disjoint,
            2 => Self::BufferedIntersection,
            _ => panic!("Invalid value {} when constructing JoinOperation", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum JoinType {
    #[default]
    Inner = 0,
    Outer = 1,
}

impl From<JoinType> for FbsJoinType {
    fn from(val: JoinType) -> Self {
        match val {
            JoinType::Inner => FbsJoinType::Inner,
            JoinType::Outer => FbsJoinType::Outer,
        }
    }
}

impl From<FbsJoinType> for JoinType {
    fn from(fbs: FbsJoinType) -> Self {
        match fbs.0 {
            0 => Self::Inner,
            1 => Self::Outer,
            _ => panic!("Invalid value {} when constructing JoinType", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum LayerCombineMode {
    #[default]
    Union = 0,
    Difference = 1,
}

impl From<LayerCombineMode> for FbsLayerCombineMode {
    fn from(val: LayerCombineMode) -> Self {
        match val {
            LayerCombineMode::Union => FbsLayerCombineMode::Union,
            LayerCombineMode::Difference => FbsLayerCombineMode::Difference,
        }
    }
}

impl From<FbsLayerCombineMode> for LayerCombineMode {
    fn from(fbs: FbsLayerCombineMode) -> Self {
        match fbs.0 {
            0 => Self::Union,
            1 => Self::Difference,
            _ => panic!("Invalid value {} when constructing LayerCombineMode", fbs.0)
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct CategoryFilter {
    column_group_id: ColumnGroupId,
    comparator: FieldComparator,
    include_nulls: bool,
    values: Vec<String>,
}

impl CategoryFilter {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsCategoryFilter<'a>> {
        use crate::types::generated::user_project_generated::CategoryFilterBuilder as FbsCategoryFilterBuilder;

        let column_group_id_offset = self.column_group_id.serialize_to(builder);
        let mut values_offsets = Vec::with_capacity(self.values.len());
        for val in self.values.iter() {
            let offset = builder.create_string(val);
            values_offsets.push(offset);
        }
        let values_offset = builder.create_vector(&values_offsets);

        let mut bldr = FbsCategoryFilterBuilder::new(builder);
        bldr.add_column_group_id(column_group_id_offset);
        bldr.add_comparator(FbsFieldComparator::from(self.comparator));
        bldr.add_include_nulls(self.include_nulls);
        bldr.add_values(values_offset);
        bldr.finish()
    }
}

impl From<FbsCategoryFilter<'_>> for CategoryFilter {
    fn from(fbs: FbsCategoryFilter<'_>) -> Self {
        let column_group_id = ColumnGroupId::from(fbs.column_group_id());
        let comparator = FieldComparator::from(fbs.comparator());
        let include_nulls = fbs.include_nulls();
        let mut values = Vec::new();
        for elem in fbs.values() {
            values.push(elem.into());
        }

        Self {
            column_group_id,
            comparator,
            include_nulls,
            values,
        }
    }
}

impl TryFrom<&[u8]> for CategoryFilter {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsCategoryFilter>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<CategoryFilter> for Vec<u8> {
    fn from(obj: CategoryFilter) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct RangeFilter {
    column_group_id: ColumnGroupId,
    include_nulls: bool,
    max: f32,
    min: f32,
}

impl RangeFilter {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsRangeFilter<'a>> {
        use crate::types::generated::user_project_generated::RangeFilterBuilder as FbsRangeFilterBuilder;

        let column_group_id_offset = self.column_group_id.serialize_to(builder);

        let mut bldr = FbsRangeFilterBuilder::new(builder);
        bldr.add_column_group_id(column_group_id_offset);
        bldr.add_include_nulls(self.include_nulls);
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        bldr.finish()
    }
}

impl From<FbsRangeFilter<'_>> for RangeFilter {
    fn from(fbs: FbsRangeFilter<'_>) -> Self {
        let column_group_id = ColumnGroupId::from(fbs.column_group_id());
        let include_nulls = fbs.include_nulls();
        let max = fbs.max();
        let min = fbs.min();
        Self {
            column_group_id,
            include_nulls,
            max,
            min,
        }
    }
}

impl TryFrom<&[u8]> for RangeFilter {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsRangeFilter>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<RangeFilter> for Vec<u8> {
    fn from(obj: RangeFilter) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct RelationshipRangeFilter {
    aggregate_op: AggregateOp,
    column_group_id: ColumnGroupId,
    columns: Vec<String>,
    include_nulls: bool,
    max: f32,
    min: f32,
}

impl RelationshipRangeFilter {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsRelationshipRangeFilter<'a>> {
        use crate::types::generated::user_project_generated::RelationshipRangeFilterBuilder as FbsRelationshipRangeFilterBuilder;

        let column_group_id_offset = self.column_group_id.serialize_to(builder);
        let mut columns_offsets = Vec::with_capacity(self.columns.len());
        for val in self.columns.iter() {
            let offset = builder.create_string(val);
            columns_offsets.push(offset);
        }
        let columns_offset = builder.create_vector(&columns_offsets);

        let mut bldr = FbsRelationshipRangeFilterBuilder::new(builder);
        bldr.add_aggregate_op(FbsAggregateOp::from(self.aggregate_op));
        bldr.add_column_group_id(column_group_id_offset);
        bldr.add_columns(columns_offset);
        bldr.add_include_nulls(self.include_nulls);
        bldr.add_max(self.max);
        bldr.add_min(self.min);
        bldr.finish()
    }
}

impl From<FbsRelationshipRangeFilter<'_>> for RelationshipRangeFilter {
    fn from(fbs: FbsRelationshipRangeFilter<'_>) -> Self {
        let aggregate_op = AggregateOp::from(fbs.aggregate_op());
        let column_group_id = ColumnGroupId::from(fbs.column_group_id());
        let mut columns = Vec::new();
        for elem in fbs.columns() {
            columns.push(elem.into());
        }

        let include_nulls = fbs.include_nulls();
        let max = fbs.max();
        let min = fbs.min();
        Self {
            aggregate_op,
            column_group_id,
            columns,
            include_nulls,
            max,
            min,
        }
    }
}

impl TryFrom<&[u8]> for RelationshipRangeFilter {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsRelationshipRangeFilter>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<RelationshipRangeFilter> for Vec<u8> {
    fn from(obj: RelationshipRangeFilter) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Clone, Debug, PartialEq)]
pub enum Filter {
    CategoryFilter(CategoryFilter),
    RangeFilter(RangeFilter),
    RelationshipRangeFilter(RelationshipRangeFilter),
}

impl Default for Filter {
    fn default() -> Self {
        Self::CategoryFilter(CategoryFilter::default())
    }
}

impl Filter {
    pub fn serialize_to(&self, builder: &mut flatbuffers::FlatBufferBuilder) -> (WIPOffset<UnionWIPOffset>, FbsFilter) {
        match self {
            Self::CategoryFilter(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsFilter::CategoryFilter;
                (offset, ty)
            }
            Self::RangeFilter(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsFilter::RangeFilter;
                (offset, ty)
            }
            Self::RelationshipRangeFilter(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsFilter::RelationshipRangeFilter;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct DataStateGeometrySource {
    data_state_id: DataStateId,
}

impl DataStateGeometrySource {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsDataStateGeometrySource<'a>> {
        use crate::types::generated::user_project_generated::DataStateGeometrySourceBuilder as FbsDataStateGeometrySourceBuilder;

        let data_state_id_offset = self.data_state_id.serialize_to(builder);

        let mut bldr = FbsDataStateGeometrySourceBuilder::new(builder);
        bldr.add_data_state_id(data_state_id_offset);
        bldr.finish()
    }
}

impl From<FbsDataStateGeometrySource<'_>> for DataStateGeometrySource {
    fn from(fbs: FbsDataStateGeometrySource<'_>) -> Self {
        let data_state_id = DataStateId::from(fbs.data_state_id());
        Self {
            data_state_id,
        }
    }
}

impl TryFrom<&[u8]> for DataStateGeometrySource {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsDataStateGeometrySource>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<DataStateGeometrySource> for Vec<u8> {
    fn from(obj: DataStateGeometrySource) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct RawGeometrySource {
    geoms: Vec<RawGeometrySourceGeom>,
}

impl RawGeometrySource {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsRawGeometrySource<'a>> {
        use crate::types::generated::user_project_generated::RawGeometrySourceBuilder as FbsRawGeometrySourceBuilder;

        let mut geoms_offsets = Vec::with_capacity(self.geoms.len());
        for val in self.geoms.iter() {
            let offset = val.serialize_to(builder);
            geoms_offsets.push(offset);
        }
        let geoms_offset = builder.create_vector(&geoms_offsets);

        let mut bldr = FbsRawGeometrySourceBuilder::new(builder);
        bldr.add_geoms(geoms_offset);
        bldr.finish()
    }
}

impl From<FbsRawGeometrySource<'_>> for RawGeometrySource {
    fn from(fbs: FbsRawGeometrySource<'_>) -> Self {
        let mut geoms = Vec::new();
        for elem in fbs.geoms() {
            geoms.push(elem.into());
        }

        Self {
            geoms,
        }
    }
}

impl TryFrom<&[u8]> for RawGeometrySource {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsRawGeometrySource>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<RawGeometrySource> for Vec<u8> {
    fn from(obj: RawGeometrySource) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Clone, Debug, PartialEq)]
pub enum JoinStackEntryGeometrySourceUnion {
    DataStateGeometrySource(DataStateGeometrySource),
    RawGeometrySource(RawGeometrySource),
}

impl Default for JoinStackEntryGeometrySourceUnion {
    fn default() -> Self {
        Self::DataStateGeometrySource(DataStateGeometrySource::default())
    }
}

impl JoinStackEntryGeometrySourceUnion {
    pub fn serialize_to(&self, builder: &mut flatbuffers::FlatBufferBuilder) -> (WIPOffset<UnionWIPOffset>, FbsJoinStackEntryGeometrySourceUnion) {
        match self {
            Self::DataStateGeometrySource(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsJoinStackEntryGeometrySourceUnion::DataStateGeometrySource;
                (offset, ty)
            }
            Self::RawGeometrySource(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsJoinStackEntryGeometrySourceUnion::RawGeometrySource;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ColumnGroup {
    columns: Vec<String>,
    display_name: String,
    id: ColumnGroupId,
    stream_id: Option<StreamId>,
    ty: ColumnGroupType,
}

impl ColumnGroup {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsColumnGroup<'a>> {
        use crate::types::generated::user_project_generated::ColumnGroupBuilder as FbsColumnGroupBuilder;

        let mut columns_offsets = Vec::with_capacity(self.columns.len());
        for val in self.columns.iter() {
            let offset = builder.create_string(val);
            columns_offsets.push(offset);
        }
        let columns_offset = builder.create_vector(&columns_offsets);
        let display_name_offset = builder.create_string(&self.display_name);
        let id_offset = self.id.serialize_to(builder);
        let stream_id_offset = self.stream_id.as_ref().map(|o| o.serialize_to(builder));

        let mut bldr = FbsColumnGroupBuilder::new(builder);
        bldr.add_columns(columns_offset);
        bldr.add_display_name(display_name_offset);
        bldr.add_id(id_offset);
        if let Some(offset) = stream_id_offset {
            bldr.add_stream_id(offset);
        }
        bldr.add_ty(FbsColumnGroupType::from(self.ty));
        bldr.finish()
    }
}

impl From<FbsColumnGroup<'_>> for ColumnGroup {
    fn from(fbs: FbsColumnGroup<'_>) -> Self {
        let mut columns = Vec::new();
        for elem in fbs.columns() {
            columns.push(elem.into());
        }

        let display_name = fbs.display_name().to_owned();
        let id = ColumnGroupId::from(fbs.id());
        let stream_id = fbs.stream_id().map(StreamId::from);
        let ty = ColumnGroupType::from(fbs.ty());
        Self {
            columns,
            display_name,
            id,
            stream_id,
            ty,
        }
    }
}

impl TryFrom<&[u8]> for ColumnGroup {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsColumnGroup>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ColumnGroup> for Vec<u8> {
    fn from(obj: ColumnGroup) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct DeprecatedDataStateJoin {
    distance: f32,
    from: DataStateId,
    operation: JoinOperation,
    to: DataStateId,
}

impl DeprecatedDataStateJoin {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsDeprecatedDataStateJoin<'a>> {
        use crate::types::generated::user_project_generated::DeprecatedDataStateJoinBuilder as FbsDeprecatedDataStateJoinBuilder;

        let from_offset = self.from.serialize_to(builder);
        let to_offset = self.to.serialize_to(builder);

        let mut bldr = FbsDeprecatedDataStateJoinBuilder::new(builder);
        bldr.add_distance(self.distance);
        bldr.add_from(from_offset);
        bldr.add_operation(FbsJoinOperation::from(self.operation));
        bldr.add_to(to_offset);
        bldr.finish()
    }
}

impl From<FbsDeprecatedDataStateJoin<'_>> for DeprecatedDataStateJoin {
    fn from(fbs: FbsDeprecatedDataStateJoin<'_>) -> Self {
        let distance = fbs.distance();
        let from = DataStateId::from(fbs.from());
        let operation = JoinOperation::from(fbs.operation());
        let to = DataStateId::from(fbs.to());
        Self {
            distance,
            from,
            operation,
            to,
        }
    }
}

impl TryFrom<&[u8]> for DeprecatedDataStateJoin {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsDeprecatedDataStateJoin>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<DeprecatedDataStateJoin> for Vec<u8> {
    fn from(obj: DeprecatedDataStateJoin) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct FieldFilter {
    filter: Filter,
}

impl FieldFilter {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsFieldFilter<'a>> {
        use crate::types::generated::user_project_generated::FieldFilterBuilder as FbsFieldFilterBuilder;

        let (filter_offset, filter_ty) = self.filter.serialize_to(builder);

        let mut bldr = FbsFieldFilterBuilder::new(builder);
        bldr.add_filter(filter_offset);
        bldr.add_filter_type(filter_ty);
        bldr.finish()
    }
}

impl From<FbsFieldFilter<'_>> for FieldFilter {
    fn from(fbs: FbsFieldFilter<'_>) -> Self {
        let filter = match fbs.filter_type() {
            FbsFilter::CategoryFilter => Filter::CategoryFilter(CategoryFilter::from(fbs.filter_as_category_filter().unwrap())),
            FbsFilter::RangeFilter => Filter::RangeFilter(RangeFilter::from(fbs.filter_as_range_filter().unwrap())),
            FbsFilter::RelationshipRangeFilter => Filter::RelationshipRangeFilter(RelationshipRangeFilter::from(fbs.filter_as_relationship_range_filter().unwrap())),
            _ => unreachable!(),
        };

        Self {
            filter,
        }
    }
}

impl TryFrom<&[u8]> for FieldFilter {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsFieldFilter>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<FieldFilter> for Vec<u8> {
    fn from(obj: FieldFilter) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct JoinStackEntry {
    buffer: f32,
    deprecated_data_state_id_do_not_use: Option<DataStateId>,
    geometry_source: JoinStackEntryGeometrySourceUnion,
    mode: LayerCombineMode,
}

impl JoinStackEntry {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsJoinStackEntry<'a>> {
        use crate::types::generated::user_project_generated::JoinStackEntryBuilder as FbsJoinStackEntryBuilder;

        let deprecated_data_state_id_do_not_use_offset = self.deprecated_data_state_id_do_not_use.as_ref().map(|o| o.serialize_to(builder));
        let (geometry_source_offset, geometry_source_ty) = self.geometry_source.serialize_to(builder);

        let mut bldr = FbsJoinStackEntryBuilder::new(builder);
        bldr.add_buffer(self.buffer);
        if let Some(offset) = deprecated_data_state_id_do_not_use_offset {
            bldr.add_deprecated_data_state_id_do_not_use(offset);
        }
        bldr.add_geometry_source(geometry_source_offset);
        bldr.add_geometry_source_type(geometry_source_ty);
        bldr.add_mode(FbsLayerCombineMode::from(self.mode));
        bldr.finish()
    }
}

impl From<FbsJoinStackEntry<'_>> for JoinStackEntry {
    fn from(fbs: FbsJoinStackEntry<'_>) -> Self {
        let buffer = fbs.buffer();
        let deprecated_data_state_id_do_not_use = fbs.deprecated_data_state_id_do_not_use().map(DataStateId::from);
        let geometry_source = match fbs.geometry_source_type() {
            FbsJoinStackEntryGeometrySourceUnion::DataStateGeometrySource => JoinStackEntryGeometrySourceUnion::DataStateGeometrySource(DataStateGeometrySource::from(fbs.geometry_source_as_data_state_geometry_source().unwrap())),
            FbsJoinStackEntryGeometrySourceUnion::RawGeometrySource => JoinStackEntryGeometrySourceUnion::RawGeometrySource(RawGeometrySource::from(fbs.geometry_source_as_raw_geometry_source().unwrap())),
            _ => unreachable!(),
        };

        let mode = LayerCombineMode::from(fbs.mode());
        Self {
            buffer,
            deprecated_data_state_id_do_not_use,
            geometry_source,
            mode,
        }
    }
}

impl TryFrom<&[u8]> for JoinStackEntry {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsJoinStackEntry>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<JoinStackEntry> for Vec<u8> {
    fn from(obj: JoinStackEntry) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct RawGeometrySourceGeom {
    geom: Geometry,
}

impl RawGeometrySourceGeom {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsRawGeometrySourceGeom<'a>> {
        use crate::types::generated::user_project_generated::RawGeometrySourceGeomBuilder as FbsRawGeometrySourceGeomBuilder;

        let (geom_offset, geom_ty) = self.geom.serialize_to(builder);

        let mut bldr = FbsRawGeometrySourceGeomBuilder::new(builder);
        bldr.add_geom(geom_offset);
        bldr.add_geom_type(geom_ty);
        bldr.finish()
    }
}

impl From<FbsRawGeometrySourceGeom<'_>> for RawGeometrySourceGeom {
    fn from(fbs: FbsRawGeometrySourceGeom<'_>) -> Self {
        let geom = match fbs.geom_type() {
            FbsGeometry::Point => Geometry::Point(Point::from(fbs.geom_as_point().unwrap())),
            FbsGeometry::Line => Geometry::Line(Line::from(fbs.geom_as_line().unwrap())),
            FbsGeometry::MultiLine => Geometry::MultiLine(MultiLine::from(fbs.geom_as_multi_line().unwrap())),
            FbsGeometry::Polygon => Geometry::Polygon(Polygon::from(fbs.geom_as_polygon().unwrap())),
            FbsGeometry::MultiPolygon => Geometry::MultiPolygon(MultiPolygon::from(fbs.geom_as_multi_polygon().unwrap())),
            _ => unreachable!(),
        };

        Self {
            geom,
        }
    }
}

impl TryFrom<&[u8]> for RawGeometrySourceGeom {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsRawGeometrySourceGeom>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<RawGeometrySourceGeom> for Vec<u8> {
    fn from(obj: RawGeometrySourceGeom) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct StackableDataStateJoin {
    buffer: f32,
    join_stack: Vec<JoinStackEntry>,
    op: JoinOperation,
    target: DataStateId,
    type_: JoinType,
}

impl StackableDataStateJoin {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsStackableDataStateJoin<'a>> {
        use crate::types::generated::user_project_generated::StackableDataStateJoinBuilder as FbsStackableDataStateJoinBuilder;

        let mut join_stack_offsets = Vec::with_capacity(self.join_stack.len());
        for val in self.join_stack.iter() {
            let offset = val.serialize_to(builder);
            join_stack_offsets.push(offset);
        }
        let join_stack_offset = builder.create_vector(&join_stack_offsets);
        let target_offset = self.target.serialize_to(builder);

        let mut bldr = FbsStackableDataStateJoinBuilder::new(builder);
        bldr.add_buffer(self.buffer);
        bldr.add_join_stack(join_stack_offset);
        bldr.add_op(FbsJoinOperation::from(self.op));
        bldr.add_target(target_offset);
        bldr.add_type_(FbsJoinType::from(self.type_));
        bldr.finish()
    }
}

impl From<FbsStackableDataStateJoin<'_>> for StackableDataStateJoin {
    fn from(fbs: FbsStackableDataStateJoin<'_>) -> Self {
        let buffer = fbs.buffer();
        let mut join_stack = Vec::new();
        for elem in fbs.join_stack() {
            join_stack.push(elem.into());
        }

        let op = JoinOperation::from(fbs.op());
        let target = DataStateId::from(fbs.target());
        let type_ = JoinType::from(fbs.type_());
        Self {
            buffer,
            join_stack,
            op,
            target,
            type_,
        }
    }
}

impl TryFrom<&[u8]> for StackableDataStateJoin {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsStackableDataStateJoin>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<StackableDataStateJoin> for Vec<u8> {
    fn from(obj: StackableDataStateJoin) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct UserLayer {
    data_states: Vec<UserLayerDataState>,
    deprecated_data_joins_do_not_use: Option<Vec<DeprecatedDataStateJoin>>,
    joins: Vec<StackableDataStateJoin>,
    name: Option<String>,
    visible: bool,
}

impl UserLayer {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsUserLayer<'a>> {
        use crate::types::generated::user_project_generated::UserLayerBuilder as FbsUserLayerBuilder;

        let mut data_states_offsets = Vec::with_capacity(self.data_states.len());
        for val in self.data_states.iter() {
            let offset = val.serialize_to(builder);
            data_states_offsets.push(offset);
        }
        let data_states_offset = builder.create_vector(&data_states_offsets);
        let deprecated_data_joins_do_not_use_offset = self.deprecated_data_joins_do_not_use.as_ref().map(|v| {
            let mut deprecated_data_joins_do_not_use_offsets = Vec::with_capacity(v.len());
            for val in v.iter() {
                let offset = val.serialize_to(builder);
                deprecated_data_joins_do_not_use_offsets.push(offset);
            }
            let deprecated_data_joins_do_not_use_offset = builder.create_vector(&deprecated_data_joins_do_not_use_offsets);
            deprecated_data_joins_do_not_use_offset
        });
        let mut joins_offsets = Vec::with_capacity(self.joins.len());
        for val in self.joins.iter() {
            let offset = val.serialize_to(builder);
            joins_offsets.push(offset);
        }
        let joins_offset = builder.create_vector(&joins_offsets);
        let name_offset = self.name.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsUserLayerBuilder::new(builder);
        bldr.add_data_states(data_states_offset);
        if let Some(offset) = deprecated_data_joins_do_not_use_offset {
            bldr.add_deprecated_data_joins_do_not_use(offset);
        }
        bldr.add_joins(joins_offset);
        if let Some(offset) = name_offset {
            bldr.add_name(offset);
        }
        bldr.add_visible(self.visible);
        bldr.finish()
    }
}

impl From<FbsUserLayer<'_>> for UserLayer {
    fn from(fbs: FbsUserLayer<'_>) -> Self {
        let mut data_states = Vec::new();
        for elem in fbs.data_states() {
            data_states.push(elem.into());
        }

        let deprecated_data_joins_do_not_use = if let Some(val) = fbs.deprecated_data_joins_do_not_use() {
            let mut deprecated_data_joins_do_not_use = Vec::new();
            for elem in val {
                deprecated_data_joins_do_not_use.push(elem.into());
            }

            Some(deprecated_data_joins_do_not_use)
        } else {
            None
        };

        let mut joins = Vec::new();
        for elem in fbs.joins() {
            joins.push(elem.into());
        }

        let name = fbs.name().map(ToOwned::to_owned);
        let visible = fbs.visible();
        Self {
            data_states,
            deprecated_data_joins_do_not_use,
            joins,
            name,
            visible,
        }
    }
}

impl TryFrom<&[u8]> for UserLayer {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsUserLayer>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<UserLayer> for Vec<u8> {
    fn from(obj: UserLayer) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct UserLayerDataState {
    active_fields: Vec<ColumnGroup>,
    data_state_id: DataStateId,
    filters: Vec<FieldFilter>,
    stream_id: StreamId,
}

impl UserLayerDataState {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsUserLayerDataState<'a>> {
        use crate::types::generated::user_project_generated::UserLayerDataStateBuilder as FbsUserLayerDataStateBuilder;

        let mut active_fields_offsets = Vec::with_capacity(self.active_fields.len());
        for val in self.active_fields.iter() {
            let offset = val.serialize_to(builder);
            active_fields_offsets.push(offset);
        }
        let active_fields_offset = builder.create_vector(&active_fields_offsets);
        let data_state_id_offset = self.data_state_id.serialize_to(builder);
        let mut filters_offsets = Vec::with_capacity(self.filters.len());
        for val in self.filters.iter() {
            let offset = val.serialize_to(builder);
            filters_offsets.push(offset);
        }
        let filters_offset = builder.create_vector(&filters_offsets);
        let stream_id_offset = self.stream_id.serialize_to(builder);

        let mut bldr = FbsUserLayerDataStateBuilder::new(builder);
        bldr.add_active_fields(active_fields_offset);
        bldr.add_data_state_id(data_state_id_offset);
        bldr.add_filters(filters_offset);
        bldr.add_stream_id(stream_id_offset);
        bldr.finish()
    }
}

impl From<FbsUserLayerDataState<'_>> for UserLayerDataState {
    fn from(fbs: FbsUserLayerDataState<'_>) -> Self {
        let mut active_fields = Vec::new();
        for elem in fbs.active_fields() {
            active_fields.push(elem.into());
        }

        let data_state_id = DataStateId::from(fbs.data_state_id());
        let mut filters = Vec::new();
        for elem in fbs.filters() {
            filters.push(elem.into());
        }

        let stream_id = StreamId::from(fbs.stream_id());
        Self {
            active_fields,
            data_state_id,
            filters,
            stream_id,
        }
    }
}

impl TryFrom<&[u8]> for UserLayerDataState {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsUserLayerDataState>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<UserLayerDataState> for Vec<u8> {
    fn from(obj: UserLayerDataState) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct UserProject {
    id: GenericId,
    layers: Vec<UserLayer>,
    name: Option<String>,
}

impl UserProject {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsUserProject<'a>> {
        use crate::types::generated::user_project_generated::UserProjectBuilder as FbsUserProjectBuilder;

        let id_offset = self.id.serialize_to(builder);
        let mut layers_offsets = Vec::with_capacity(self.layers.len());
        for val in self.layers.iter() {
            let offset = val.serialize_to(builder);
            layers_offsets.push(offset);
        }
        let layers_offset = builder.create_vector(&layers_offsets);
        let name_offset = self.name.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsUserProjectBuilder::new(builder);
        bldr.add_id(id_offset);
        bldr.add_layers(layers_offset);
        if let Some(offset) = name_offset {
            bldr.add_name(offset);
        }
        bldr.finish()
    }
}

impl From<FbsUserProject<'_>> for UserProject {
    fn from(fbs: FbsUserProject<'_>) -> Self {
        let id = GenericId::from(fbs.id());
        let mut layers = Vec::new();
        for elem in fbs.layers() {
            layers.push(elem.into());
        }

        let name = fbs.name().map(ToOwned::to_owned);
        Self {
            id,
            layers,
            name,
        }
    }
}

impl TryFrom<&[u8]> for UserProject {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsUserProject>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<UserProject> for Vec<u8> {
    fn from(obj: UserProject) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_category_filter() {
        let t0 = CategoryFilter::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = CategoryFilter::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_column_group() {
        let t0 = ColumnGroup::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ColumnGroup::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_data_state_geometry_source() {
        let t0 = DataStateGeometrySource::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = DataStateGeometrySource::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_deprecated_data_state_join() {
        let t0 = DeprecatedDataStateJoin::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = DeprecatedDataStateJoin::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_field_filter() {
        let t0 = FieldFilter::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = FieldFilter::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_join_stack_entry() {
        let t0 = JoinStackEntry::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = JoinStackEntry::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_range_filter() {
        let t0 = RangeFilter::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = RangeFilter::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_raw_geometry_source() {
        let t0 = RawGeometrySource::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = RawGeometrySource::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_raw_geometry_source_geom() {
        let t0 = RawGeometrySourceGeom::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = RawGeometrySourceGeom::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_relationship_range_filter() {
        let t0 = RelationshipRangeFilter::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = RelationshipRangeFilter::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_stackable_data_state_join() {
        let t0 = StackableDataStateJoin::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = StackableDataStateJoin::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_user_layer() {
        let t0 = UserLayer::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = UserLayer::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_user_layer_data_state() {
        let t0 = UserLayerDataState::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = UserLayerDataState::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_user_project() {
        let t0 = UserProject::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = UserProject::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

}