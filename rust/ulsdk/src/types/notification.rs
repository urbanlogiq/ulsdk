// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use flatbuffers::{WIPOffset, UnionWIPOffset};
use bitflags::bitflags;
use core::ops::Deref;

use crate::types::id::{
    B2cId,
    ColumnGroupId,
    ContentId,
    DataStateId,
    GenericId,
    GraphNodeId,
    ObjectId,
    ObjectNamespace,
    StreamId,
};
use crate::types::permissions::{
    AccessControlList,
    PermissionTy,
    Role,
};
use crate::types::generated::id_generated::{
    B2cId as FbsB2cId,
    ColumnGroupId as FbsColumnGroupId,
    ContentId as FbsContentId,
    DataStateId as FbsDataStateId,
    GenericId as FbsGenericId,
    GraphNodeId as FbsGraphNodeId,
    ObjectId as FbsObjectId,
    StreamId as FbsStreamId,
    ObjectNamespace as FbsObjectNamespace,
};
use crate::types::generated::notification_generated::{
    AccessRequest as FbsAccessRequest,
    DriveChange as FbsDriveChange,
    Inbox as FbsInbox,
    InboxItem as FbsInboxItem,
    JobComplete as FbsJobComplete,
    Notification as FbsNotification,
    Response as FbsResponse,
    Share as FbsShare,
    ShareDetails as FbsShareDetails,
    DriveAction as FbsDriveAction,
    NotificationUnion as FbsNotificationUnion,
    ReadStatus as FbsReadStatus,
    RequestStatus as FbsRequestStatus,
};
use crate::types::generated::permissions_generated::{
    AccessControlList as FbsAccessControlList,
    Role as FbsRole,
    PermissionTy as FbsPermissionTy,
};

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum DriveAction {
    #[default]
    Add = 0,
    Remove = 1,
    Overwrite = 2,
}

impl From<DriveAction> for FbsDriveAction {
    fn from(val: DriveAction) -> Self {
        match val {
            DriveAction::Add => FbsDriveAction::Add,
            DriveAction::Remove => FbsDriveAction::Remove,
            DriveAction::Overwrite => FbsDriveAction::Overwrite,
        }
    }
}

impl From<FbsDriveAction> for DriveAction {
    fn from(fbs: FbsDriveAction) -> Self {
        match fbs.0 {
            0 => Self::Add,
            1 => Self::Remove,
            2 => Self::Overwrite,
            _ => panic!("Invalid value {} when constructing DriveAction", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum ReadStatus {
    #[default]
    Unread = 0,
    Read = 1,
}

impl From<ReadStatus> for FbsReadStatus {
    fn from(val: ReadStatus) -> Self {
        match val {
            ReadStatus::Unread => FbsReadStatus::Unread,
            ReadStatus::Read => FbsReadStatus::Read,
        }
    }
}

impl From<FbsReadStatus> for ReadStatus {
    fn from(fbs: FbsReadStatus) -> Self {
        match fbs.0 {
            0 => Self::Unread,
            1 => Self::Read,
            _ => panic!("Invalid value {} when constructing ReadStatus", fbs.0)
        }
    }
}

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum RequestStatus {
    #[default]
    Pending = 0,
    Approved = 1,
    Rejected = 2,
}

impl From<RequestStatus> for FbsRequestStatus {
    fn from(val: RequestStatus) -> Self {
        match val {
            RequestStatus::Pending => FbsRequestStatus::Pending,
            RequestStatus::Approved => FbsRequestStatus::Approved,
            RequestStatus::Rejected => FbsRequestStatus::Rejected,
        }
    }
}

impl From<FbsRequestStatus> for RequestStatus {
    fn from(fbs: FbsRequestStatus) -> Self {
        match fbs.0 {
            0 => Self::Pending,
            1 => Self::Approved,
            2 => Self::Rejected,
            _ => panic!("Invalid value {} when constructing RequestStatus", fbs.0)
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Share {
    pub dest: Option<String>,
    pub msg: Option<String>,
    pub new_perms: PermissionTy,
    pub object: ObjectId,
    pub old_perms: PermissionTy,
}

impl Share {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsShare<'a>> {
        use crate::types::generated::notification_generated::ShareBuilder as FbsShareBuilder;

        let dest_offset = self.dest.as_ref().map(|s| builder.create_string(s));
        let msg_offset = self.msg.as_ref().map(|s| builder.create_string(s));
        let object_offset = self.object.serialize_to(builder);

        let mut bldr = FbsShareBuilder::new(builder);
        if let Some(offset) = dest_offset {
            bldr.add_dest(offset);
        }
        if let Some(offset) = msg_offset {
            bldr.add_msg(offset);
        }
        bldr.add_new_perms(FbsPermissionTy::from(self.new_perms));
        bldr.add_object(object_offset);
        bldr.add_old_perms(FbsPermissionTy::from(self.old_perms));
        bldr.finish()
    }
}

impl From<FbsShare<'_>> for Share {
    fn from(fbs: FbsShare<'_>) -> Self {
        let dest = fbs.dest().map(ToOwned::to_owned);
        let msg = fbs.msg().map(ToOwned::to_owned);
        let new_perms = PermissionTy::from(fbs.new_perms());
        let object = ObjectId::from(fbs.object());
        let old_perms = PermissionTy::from(fbs.old_perms());
        Self {
            dest,
            msg,
            new_perms,
            object,
            old_perms,
        }
    }
}

impl TryFrom<&[u8]> for Share {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsShare>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<Share> for Vec<u8> {
    fn from(obj: Share) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct JobComplete {
    pub job: ObjectId,
}

impl JobComplete {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsJobComplete<'a>> {
        use crate::types::generated::notification_generated::JobCompleteBuilder as FbsJobCompleteBuilder;

        let job_offset = self.job.serialize_to(builder);

        let mut bldr = FbsJobCompleteBuilder::new(builder);
        bldr.add_job(job_offset);
        bldr.finish()
    }
}

impl From<FbsJobComplete<'_>> for JobComplete {
    fn from(fbs: FbsJobComplete<'_>) -> Self {
        let job = ObjectId::from(fbs.job());
        Self {
            job,
        }
    }
}

impl TryFrom<&[u8]> for JobComplete {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsJobComplete>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<JobComplete> for Vec<u8> {
    fn from(obj: JobComplete) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct AccessRequest {
    pub msg: Option<String>,
    pub object: ObjectId,
    pub perms: u32,
    pub requested_ownership: u32,
    pub status: RequestStatus,
}

impl AccessRequest {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsAccessRequest<'a>> {
        use crate::types::generated::notification_generated::AccessRequestBuilder as FbsAccessRequestBuilder;

        let msg_offset = self.msg.as_ref().map(|s| builder.create_string(s));
        let object_offset = self.object.serialize_to(builder);

        let mut bldr = FbsAccessRequestBuilder::new(builder);
        if let Some(offset) = msg_offset {
            bldr.add_msg(offset);
        }
        bldr.add_object(object_offset);
        bldr.add_perms(self.perms);
        bldr.add_requested_ownership(self.requested_ownership);
        bldr.add_status(FbsRequestStatus::from(self.status));
        bldr.finish()
    }
}

impl From<FbsAccessRequest<'_>> for AccessRequest {
    fn from(fbs: FbsAccessRequest<'_>) -> Self {
        let msg = fbs.msg().map(ToOwned::to_owned);
        let object = ObjectId::from(fbs.object());
        let perms = fbs.perms();
        let requested_ownership = fbs.requested_ownership();
        let status = RequestStatus::from(fbs.status());
        Self {
            msg,
            object,
            perms,
            requested_ownership,
            status,
        }
    }
}

impl TryFrom<&[u8]> for AccessRequest {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsAccessRequest>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<AccessRequest> for Vec<u8> {
    fn from(obj: AccessRequest) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct DriveChange {
    pub action: DriveAction,
    pub object: ObjectId,
    pub root: ObjectId,
}

impl DriveChange {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsDriveChange<'a>> {
        use crate::types::generated::notification_generated::DriveChangeBuilder as FbsDriveChangeBuilder;

        let object_offset = self.object.serialize_to(builder);
        let root_offset = self.root.serialize_to(builder);

        let mut bldr = FbsDriveChangeBuilder::new(builder);
        bldr.add_action(FbsDriveAction::from(self.action));
        bldr.add_object(object_offset);
        bldr.add_root(root_offset);
        bldr.finish()
    }
}

impl From<FbsDriveChange<'_>> for DriveChange {
    fn from(fbs: FbsDriveChange<'_>) -> Self {
        let action = DriveAction::from(fbs.action());
        let object = ObjectId::from(fbs.object());
        let root = ObjectId::from(fbs.root());
        Self {
            action,
            object,
            root,
        }
    }
}

impl TryFrom<&[u8]> for DriveChange {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsDriveChange>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<DriveChange> for Vec<u8> {
    fn from(obj: DriveChange) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Clone, Debug, PartialEq)]
pub enum NotificationUnion {
    Share(Share),
    JobComplete(JobComplete),
    AccessRequest(AccessRequest),
    DriveChange(DriveChange),
}

impl Default for NotificationUnion {
    fn default() -> Self {
        Self::Share(Share::default())
    }
}

impl NotificationUnion {
    pub fn serialize_to(&self, builder: &mut flatbuffers::FlatBufferBuilder) -> (WIPOffset<UnionWIPOffset>, FbsNotificationUnion) {
        match self {
            Self::Share(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsNotificationUnion::Share;
                (offset, ty)
            }
            Self::JobComplete(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsNotificationUnion::JobComplete;
                (offset, ty)
            }
            Self::AccessRequest(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsNotificationUnion::AccessRequest;
                (offset, ty)
            }
            Self::DriveChange(val) => {
                let offset = val.serialize_to(builder).as_union_value();
                let ty = FbsNotificationUnion::DriveChange;
                (offset, ty)
            }
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Inbox {
    pub items: Vec<InboxItem>,
}

impl Inbox {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsInbox<'a>> {
        use crate::types::generated::notification_generated::InboxBuilder as FbsInboxBuilder;

        let mut items_offsets = Vec::with_capacity(self.items.len());
        for val in self.items.iter() {
            let offset = val.serialize_to(builder);
            items_offsets.push(offset);
        }
        let items_offset = builder.create_vector(&items_offsets);

        let mut bldr = FbsInboxBuilder::new(builder);
        bldr.add_items(items_offset);
        bldr.finish()
    }
}

impl From<FbsInbox<'_>> for Inbox {
    fn from(fbs: FbsInbox<'_>) -> Self {
        let mut items = Vec::new();
        for elem in fbs.items() {
            items.push(elem.into());
        }

        Self {
            items,
        }
    }
}

impl TryFrom<&[u8]> for Inbox {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsInbox>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<Inbox> for Vec<u8> {
    fn from(obj: Inbox) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct InboxItem {
    pub notification: ObjectId,
    pub status: ReadStatus,
    pub time: u64,
}

impl InboxItem {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsInboxItem<'a>> {
        use crate::types::generated::notification_generated::InboxItemBuilder as FbsInboxItemBuilder;

        let notification_offset = self.notification.serialize_to(builder);

        let mut bldr = FbsInboxItemBuilder::new(builder);
        bldr.add_notification(notification_offset);
        bldr.add_status(FbsReadStatus::from(self.status));
        bldr.add_time(self.time);
        bldr.finish()
    }
}

impl From<FbsInboxItem<'_>> for InboxItem {
    fn from(fbs: FbsInboxItem<'_>) -> Self {
        let notification = ObjectId::from(fbs.notification());
        let status = ReadStatus::from(fbs.status());
        let time = fbs.time();
        Self {
            notification,
            status,
            time,
        }
    }
}

impl TryFrom<&[u8]> for InboxItem {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsInboxItem>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<InboxItem> for Vec<u8> {
    fn from(obj: InboxItem) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Notification {
    pub notification: Option<NotificationUnion>,
    pub sender: Option<B2cId>,
}

impl Notification {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsNotification<'a>> {
        use crate::types::generated::notification_generated::NotificationBuilder as FbsNotificationBuilder;

        let notification_offset = self.notification.as_ref().map(|u| u.serialize_to(builder));
        let sender_offset = self.sender.as_ref().map(|o| o.serialize_to(builder));

        let mut bldr = FbsNotificationBuilder::new(builder);
        if let Some((offset, ty)) = notification_offset {
            bldr.add_notification(offset);
            bldr.add_notification_type(ty);
        }
        if let Some(offset) = sender_offset {
            bldr.add_sender(offset);
        }
        bldr.finish()
    }
}

impl From<FbsNotification<'_>> for Notification {
    fn from(fbs: FbsNotification<'_>) -> Self {
        let notification = if let Some(val) = fbs.notification() {
            let notification = match fbs.notification_type() {
                FbsNotificationUnion::Share => NotificationUnion::Share(Share::from(fbs.notification_as_share().unwrap())),
                FbsNotificationUnion::JobComplete => NotificationUnion::JobComplete(JobComplete::from(fbs.notification_as_job_complete().unwrap())),
                FbsNotificationUnion::AccessRequest => NotificationUnion::AccessRequest(AccessRequest::from(fbs.notification_as_access_request().unwrap())),
                FbsNotificationUnion::DriveChange => NotificationUnion::DriveChange(DriveChange::from(fbs.notification_as_drive_change().unwrap())),
                _ => unreachable!(),
            };

            Some(notification)
        } else {
            None
        };

        let sender = fbs.sender().map(B2cId::from);
        Self {
            notification,
            sender,
        }
    }
}

impl TryFrom<&[u8]> for Notification {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsNotification>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<Notification> for Vec<u8> {
    fn from(obj: Notification) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct Response {
    pub msg: Option<String>,
}

impl Response {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsResponse<'a>> {
        use crate::types::generated::notification_generated::ResponseBuilder as FbsResponseBuilder;

        let msg_offset = self.msg.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsResponseBuilder::new(builder);
        if let Some(offset) = msg_offset {
            bldr.add_msg(offset);
        }
        bldr.finish()
    }
}

impl From<FbsResponse<'_>> for Response {
    fn from(fbs: FbsResponse<'_>) -> Self {
        let msg = fbs.msg().map(ToOwned::to_owned);
        Self {
            msg,
        }
    }
}

impl TryFrom<&[u8]> for Response {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsResponse>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<Response> for Vec<u8> {
    fn from(obj: Response) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ShareDetails {
    pub msg: Option<String>,
    pub notify: bool,
}

impl ShareDetails {
    pub fn serialize_to<'a>(&self, builder: &mut flatbuffers::FlatBufferBuilder<'a>) -> flatbuffers::WIPOffset<FbsShareDetails<'a>> {
        use crate::types::generated::notification_generated::ShareDetailsBuilder as FbsShareDetailsBuilder;

        let msg_offset = self.msg.as_ref().map(|s| builder.create_string(s));

        let mut bldr = FbsShareDetailsBuilder::new(builder);
        if let Some(offset) = msg_offset {
            bldr.add_msg(offset);
        }
        bldr.add_notify(self.notify);
        bldr.finish()
    }
}

impl From<FbsShareDetails<'_>> for ShareDetails {
    fn from(fbs: FbsShareDetails<'_>) -> Self {
        let msg = fbs.msg().map(ToOwned::to_owned);
        let notify = fbs.notify();
        Self {
            msg,
            notify,
        }
    }
}

impl TryFrom<&[u8]> for ShareDetails {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsShareDetails>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ShareDetails> for Vec<u8> {
    fn from(obj: ShareDetails) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_access_request() {
        let t0 = AccessRequest::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = AccessRequest::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_drive_change() {
        let t0 = DriveChange::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = DriveChange::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_inbox() {
        let t0 = Inbox::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = Inbox::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_inbox_item() {
        let t0 = InboxItem::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = InboxItem::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_job_complete() {
        let t0 = JobComplete::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = JobComplete::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_notification() {
        let t0 = Notification::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = Notification::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_response() {
        let t0 = Response::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = Response::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_share() {
        let t0 = Share::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = Share::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_share_details() {
        let t0 = ShareDetails::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ShareDetails::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

}