// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(unused)]
#![allow(clippy::doc_lazy_continuation)]
#![allow(clippy::useless_conversion)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::enum_clike_unportable_variant)]

use bitflags::bitflags;
use core::ops::Deref;
use flatbuffers::{UnionWIPOffset, WIPOffset};

use crate::types::generated::id_generated::{
    B2cId as FbsB2cId, ColumnGroupId as FbsColumnGroupId, ContentId as FbsContentId,
    DataStateId as FbsDataStateId, GenericId as FbsGenericId, GraphNodeId as FbsGraphNodeId,
    ObjectId as FbsObjectId, ObjectNamespace as FbsObjectNamespace, StreamId as FbsStreamId,
};

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub enum ObjectNamespace {
    #[default]
    Global = 0,
    User = 1,
    Generated = 2,
    Acl = 3,
    Evaluator = 4,
    Drive = 5,
    Notifications = 6,
    Unknown = 65535,
}

impl From<ObjectNamespace> for FbsObjectNamespace {
    fn from(val: ObjectNamespace) -> Self {
        match val {
            ObjectNamespace::Global => FbsObjectNamespace::Global,
            ObjectNamespace::User => FbsObjectNamespace::User,
            ObjectNamespace::Generated => FbsObjectNamespace::Generated,
            ObjectNamespace::Acl => FbsObjectNamespace::Acl,
            ObjectNamespace::Evaluator => FbsObjectNamespace::Evaluator,
            ObjectNamespace::Drive => FbsObjectNamespace::Drive,
            ObjectNamespace::Notifications => FbsObjectNamespace::Notifications,
            ObjectNamespace::Unknown => FbsObjectNamespace::Unknown,
        }
    }
}

impl From<FbsObjectNamespace> for ObjectNamespace {
    fn from(fbs: FbsObjectNamespace) -> Self {
        match fbs.0 {
            0 => Self::Global,
            1 => Self::User,
            2 => Self::Generated,
            3 => Self::Acl,
            4 => Self::Evaluator,
            5 => Self::Drive,
            6 => Self::Notifications,
            65535 => Self::Unknown,
            _ => panic!("Invalid value {} when constructing ObjectNamespace", fbs.0),
        }
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct B2cId {
    pub b: Vec<u8>,
}

impl B2cId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsB2cId<'a>> {
        use crate::types::generated::id_generated::B2cIdBuilder as FbsB2cIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsB2cIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsB2cId<'_>> for B2cId {
    fn from(fbs: FbsB2cId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for B2cId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsB2cId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<B2cId> for Vec<u8> {
    fn from(obj: B2cId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ColumnGroupId {
    pub b: Vec<u8>,
}

impl ColumnGroupId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsColumnGroupId<'a>> {
        use crate::types::generated::id_generated::ColumnGroupIdBuilder as FbsColumnGroupIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsColumnGroupIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsColumnGroupId<'_>> for ColumnGroupId {
    fn from(fbs: FbsColumnGroupId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for ColumnGroupId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsColumnGroupId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ColumnGroupId> for Vec<u8> {
    fn from(obj: ColumnGroupId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ContentId {
    pub b: Vec<u8>,
}

impl ContentId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsContentId<'a>> {
        use crate::types::generated::id_generated::ContentIdBuilder as FbsContentIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsContentIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsContentId<'_>> for ContentId {
    fn from(fbs: FbsContentId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for ContentId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsContentId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ContentId> for Vec<u8> {
    fn from(obj: ContentId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct DataStateId {
    pub b: Vec<u8>,
}

impl DataStateId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsDataStateId<'a>> {
        use crate::types::generated::id_generated::DataStateIdBuilder as FbsDataStateIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsDataStateIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsDataStateId<'_>> for DataStateId {
    fn from(fbs: FbsDataStateId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for DataStateId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsDataStateId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<DataStateId> for Vec<u8> {
    fn from(obj: DataStateId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct GenericId {
    pub b: Vec<u8>,
}

impl GenericId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsGenericId<'a>> {
        use crate::types::generated::id_generated::GenericIdBuilder as FbsGenericIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsGenericIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsGenericId<'_>> for GenericId {
    fn from(fbs: FbsGenericId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for GenericId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsGenericId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<GenericId> for Vec<u8> {
    fn from(obj: GenericId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct GraphNodeId {
    pub b: Vec<u8>,
}

impl GraphNodeId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsGraphNodeId<'a>> {
        use crate::types::generated::id_generated::GraphNodeIdBuilder as FbsGraphNodeIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsGraphNodeIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsGraphNodeId<'_>> for GraphNodeId {
    fn from(fbs: FbsGraphNodeId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for GraphNodeId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsGraphNodeId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<GraphNodeId> for Vec<u8> {
    fn from(obj: GraphNodeId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct ObjectId {
    pub b: Vec<u8>,
}

impl ObjectId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsObjectId<'a>> {
        use crate::types::generated::id_generated::ObjectIdBuilder as FbsObjectIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsObjectIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsObjectId<'_>> for ObjectId {
    fn from(fbs: FbsObjectId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for ObjectId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsObjectId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<ObjectId> for Vec<u8> {
    fn from(obj: ObjectId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[derive(Default, PartialEq, Debug, Clone)]
pub struct StreamId {
    pub b: Vec<u8>,
}

impl StreamId {
    pub fn serialize_to<'a>(
        &self,
        builder: &mut flatbuffers::FlatBufferBuilder<'a>,
    ) -> flatbuffers::WIPOffset<FbsStreamId<'a>> {
        use crate::types::generated::id_generated::StreamIdBuilder as FbsStreamIdBuilder;

        let b_offset = builder.create_vector(&self.b);

        let mut bldr = FbsStreamIdBuilder::new(builder);
        bldr.add_b(b_offset);
        bldr.finish()
    }
}

impl From<FbsStreamId<'_>> for StreamId {
    fn from(fbs: FbsStreamId<'_>) -> Self {
        let mut b = Vec::new();
        for elem in fbs.b() {
            b.push(elem.into());
        }

        Self { b }
    }
}

impl TryFrom<&[u8]> for StreamId {
    type Error = flatbuffers::InvalidFlatbuffer;
    fn try_from(bytes: &[u8]) -> Result<Self, Self::Error> {
        let fbs = flatbuffers::size_prefixed_root::<FbsStreamId>(bytes)?;
        Ok(Self::from(fbs))
    }
}

impl From<StreamId> for Vec<u8> {
    fn from(obj: StreamId) -> Self {
        let mut bldr = flatbuffers::FlatBufferBuilder::new();
        let offset = obj.serialize_to(&mut bldr);
        bldr.finish_size_prefixed(offset, None);
        bldr.finished_data().to_vec()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_b_2c_id() {
        let t0 = B2cId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = B2cId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_column_group_id() {
        let t0 = ColumnGroupId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ColumnGroupId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_content_id() {
        let t0 = ContentId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ContentId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_data_state_id() {
        let t0 = DataStateId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = DataStateId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_generic_id() {
        let t0 = GenericId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = GenericId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_graph_node_id() {
        let t0 = GraphNodeId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = GraphNodeId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_object_id() {
        let t0 = ObjectId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = ObjectId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }

    #[test]
    fn test_stream_id() {
        let t0 = StreamId::default();
        let buf: Vec<u8> = t0.clone().into();
        let t1 = StreamId::try_from(buf.as_slice()).unwrap();
        assert_eq!(t0, t1);
    }
}
