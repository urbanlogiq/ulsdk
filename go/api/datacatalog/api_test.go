// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package datacatalog

import (
	"encoding/json"
	"fmt"
	"github.com/apache/arrow-go/v18/arrow"
	"github.com/apache/arrow-go/v18/arrow/array"
	"github.com/apache/arrow-go/v18/arrow/memory"
	"github.com/urbanlogiq/ulsdk"
	"github.com/urbanlogiq/ulsdk/api"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/metadata"
	"github.com/urbanlogiq/ulsdk/types/object"
	"github.com/urbanlogiq/ulsdk/types/query"
	"github.com/urbanlogiq/ulsdk/types/table"
	"testing"
	"time"
)

var _ = fmt.Sprintf
var _ = json.Marshal
var _ = arrow.BinaryTypes.String
var _ = array.NewRecordBuilder
var _ = memory.NewGoAllocator

func makeTestBatches(t *testing.T) []arrow.Record {
	t.Helper()
	alloc := memory.NewGoAllocator()
	schema := arrow.NewSchema([]arrow.Field{
		{Name: "v", Type: arrow.BinaryTypes.String},
		{Name: "i", Type: arrow.PrimitiveTypes.Int32},
	}, nil)
	b := array.NewRecordBuilder(alloc, schema)
	defer b.Release()
	for i := 0; i < 20; i++ {
		b.Field(0).(*array.StringBuilder).Append(fmt.Sprintf("test%d", i))
		b.Field(1).(*array.Int32Builder).Append(int32(i))
	}
	rec := b.NewRecord()
	return []arrow.Record{rec}
}

func makeTestBatchesAndBytes(t *testing.T) ([]arrow.Record, []byte) {
	t.Helper()
	batches := makeTestBatches(t)
	data, err := api.WriteArrowIPC(batches)
	if err != nil {
		t.Fatalf("failed to write Arrow IPC: %v", err)
	}
	return batches, data
}

func TestGetObjectAtRevision(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.ContentId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.DataCatalogObject{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetObjectAtRevision(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetAcl(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ObjectId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetAcl(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetHeadRevision(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummary{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetHeadRevision(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetObject(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.DataCatalogObject{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetObject(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUpdateObject(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	body := &object.DataCatalogObject{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ContentId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := UpdateObject(
			ctx,
			p0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUpdateAttributes(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	q0 := true
	body := map[string]interface{}{}

	success := false
	for i := 0; i < 5; i++ {
		err := UpdateAttributes(
			ctx,
			p0,
			q0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestDeleteAttribute(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."

	success := false
	for i := 0; i < 5; i++ {
		err := DeleteAttribute(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetObjectSummaries(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &object.ObjectIdList{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummaryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetObjectSummaries(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestBulkFetchMetadata(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &object.ObjectIdList{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectIdPairList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := BulkFetchMetadata(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestBulkFetchObjects(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &object.ObjectIdList{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectIdPairList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := BulkFetchObjects(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateObject(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummaryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := CreateObject(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryAggregateNumeric(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := QueryAggregateNumeric(
			ctx,
			q0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryAggregateString(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := QueryAggregateString(
			ctx,
			q0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryAggregateHisto(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0 := int64(42)
	q1 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := QueryAggregateHisto(
			ctx,
			q0,
			q1,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryAggregateRelativeHisto(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0 := int64(42)
	q1 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	q2 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := QueryAggregateRelativeHisto(
			ctx,
			q0,
			q1,
			q2,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetArrow(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetArrow(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetParquet(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetParquet(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetCsv(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetCsv(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetXlsx(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetXlsx(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetJson(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetJson(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetText(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetText(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamGetHtml(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.PinnedObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := StreamGetHtml(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamPutArrow(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	body := makeTestBatches(t)

	success := false
	for i := 0; i < 5; i++ {
		err := StreamPutArrow(
			ctx,
			p0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamPutDiffstream(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	body := &table.DiffStream{}

	success := false
	for i := 0; i < 5; i++ {
		err := StreamPutDiffstream(
			ctx,
			p0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamPutJson(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	body := []map[string]interface{}{}

	success := false
	for i := 0; i < 5; i++ {
		err := StreamPutJson(
			ctx,
			p0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGenerateMetadata(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &metadata.Metadata{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GenerateMetadata(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUpdateMetadata(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	q0_val := true
	q0 := &q0_val
	q1_val := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	q1 := &q1_val
	q2_val := id.ContentId{}
	q2 := &q2_val
	q3_val := id.ContentId{}
	q3 := &q3_val
	body := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")

	success := false
	for i := 0; i < 5; i++ {
		err := UpdateMetadata(
			ctx,
			p0,
			q0,
			q1,
			q2,
			q3,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestStreamCompact(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		err := StreamCompact(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestTableRowHistory(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.GenericId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &table.History{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := TableRowHistory(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestTableHistory(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &table.History{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := TableHistory(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetTableAttachmentsDirectory(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.GenericId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ObjectId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetTableAttachmentsDirectory(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetOrCreateTableAttachmentsDirectory(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.GenericId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ObjectId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetOrCreateTableAttachmentsDirectory(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateTable(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &table.NewTable{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ObjectId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := CreateTable(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateTables(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &table.NewTableList{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &table.NewTableListResult{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := CreateTables(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSchemaArrow(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := SchemaArrow(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSchemaRaw(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := SchemaRaw(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSchemaOnly(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := SchemaOnly(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryArrow(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expectedBatches, expectedBytes := makeTestBatchesAndBytes(t)
		_ = expectedBatches
		ctx.SetResponse(expectedBytes)
		result, err := QueryArrow(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryParquet(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryParquet(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryCsv(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryCsv(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryXlsx(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryXlsx(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryJson(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryJson(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryText(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryText(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryHtml(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &query.Query{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := QueryHtml(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}
