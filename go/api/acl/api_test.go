// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package acl

import (
	"fmt"
	"github.com/urbanlogiq/ulsdk"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/notification"
	"github.com/urbanlogiq/ulsdk/types/object"
	"testing"
	"time"
)

var _ = fmt.Sprintf

func TestNewAcl(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummaryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := NewAcl(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestNewFrom(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0_val := id.ObjectId{}
	q0 := &q0_val

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummaryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := NewFrom(
			ctx,
			q0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestRequest(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &notification.AccessRequest{}

	success := false
	for i := 0; i < 5; i++ {
		err := Request(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestShare(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	p2 := int64(42)

	success := false
	for i := 0; i < 5; i++ {
		err := Share(
			ctx,
			p0,
			p1,
			p2,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestShareWithDetails(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	p2 := int64(42)
	body := &notification.ShareDetails{}

	success := false
	for i := 0; i < 5; i++ {
		err := ShareWithDetails(
			ctx,
			p0,
			p1,
			p2,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestShareAll(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := ShareAll(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestShareAllWithDetails(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	body := &notification.ShareDetails{}

	success := false
	for i := 0; i < 5; i++ {
		err := ShareAllWithDetails(
			ctx,
			p0,
			p1,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGrant(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	p2 := int64(42)

	success := false
	for i := 0; i < 5; i++ {
		err := Grant(
			ctx,
			p0,
			p1,
			p2,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGrantWithDetails(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	p2 := int64(42)
	body := &notification.ShareDetails{}

	success := false
	for i := 0; i < 5; i++ {
		err := GrantWithDetails(
			ctx,
			p0,
			p1,
			p2,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGrantAll(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		err := GrantAll(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGrantAllWithDetails(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}
	body := &notification.ShareDetails{}

	success := false
	for i := 0; i < 5; i++ {
		err := GrantAllWithDetails(
			ctx,
			p0,
			p1,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestRevoke(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := Revoke(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetPermissions(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := GetPermissions(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSetDefaultMode(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := int64(42)

	success := false
	for i := 0; i < 5; i++ {
		err := SetDefaultMode(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSet(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		err := Set(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}
