// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package acl

import (
	"fmt"
	"github.com/urbanlogiq/ulsdk/api"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/notification"
	"github.com/urbanlogiq/ulsdk/types/object"
	"strings"
)

var _ = fmt.Errorf

// NewAcl -
// Create a new access control list. This ACL will be created with the current user as the owner.
func NewAcl(ctx api.RequestContext) (*object.ObjectSummaryList, error) {
	path := "/v1/api/ulv2/datacatalog/acl/"

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	if err != nil {
		return nil, err
	}
	result, err := object.ObjectSummaryListFromBytes(res)
	if err != nil {
		return nil, fmt.Errorf("failed to deserialize response: %w", err)
	}
	return result, nil
}

// NewFrom -
// Create a new access control list that inherits from an existing ACL
func NewFrom(ctx api.RequestContext, extends *id.ObjectId) (*object.ObjectSummaryList, error) {
	path := "/v1/api/ulv2/datacatalog/acl/"

	params := [][2]string{}
	if extends != nil {
	params = append(params, [2]string{"extends", fmt.Sprintf("%s", *extends)})
	}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	if err != nil {
		return nil, err
	}
	result, err := object.ObjectSummaryListFromBytes(res)
	if err != nil {
		return nil, fmt.Errorf("failed to deserialize response: %w", err)
	}
	return result, nil
}

// Request -
// Request access to an object
func Request(ctx api.RequestContext, request *notification.AccessRequest) error {
	path := "/v1/api/ulv2/datacatalog/acl/request"

	params := [][2]string{}

	headers := map[string]string{}

	bodyBytes := request.ToBytes()
	res, err := ctx.Post(path, bodyBytes, "application/octet-stream", params, headers)
	_ = res
	return err
}

// Share -
// Share an object to a specific access control list principal with specified permissions.
func Share(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, permissionBits int64) error {
	path := "/v1/api/ulv2/datacatalog/acl/share/:id/:to/:permission"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)
	path = strings.Replace(path, ":permission", fmt.Sprintf("%v", permissionBits), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// ShareWithDetails -
// Share an object to a specific access control list principal with specified permissions.
func ShareWithDetails(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, permissionBits int64, shareDetails *notification.ShareDetails) error {
	path := "/v1/api/ulv2/datacatalog/acl/share/:id/:to/:permission"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)
	path = strings.Replace(path, ":permission", fmt.Sprintf("%v", permissionBits), 1)

	params := [][2]string{}

	headers := map[string]string{}

	bodyBytes := shareDetails.ToBytes()
	res, err := ctx.Post(path, bodyBytes, "application/octet-stream", params, headers)
	_ = res
	return err
}

// ShareAll -
// Share an object to a specific access control list principal with all permissions.
func ShareAll(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid) error {
	path := "/v1/api/ulv2/datacatalog/acl/share/:id/:to"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// ShareAllWithDetails -
// Share an object to a specific access control list principal with all permissions.
func ShareAllWithDetails(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, shareDetails *notification.ShareDetails) error {
	path := "/v1/api/ulv2/datacatalog/acl/share/:id/:to"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)

	params := [][2]string{}

	headers := map[string]string{}

	bodyBytes := shareDetails.ToBytes()
	res, err := ctx.Post(path, bodyBytes, "application/octet-stream", params, headers)
	_ = res
	return err
}

// Grant -
// Grant an object to a specific access control list principal with specified permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
func Grant(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, permissionBits int64) error {
	path := "/v1/api/ulv2/datacatalog/acl/grant/:id/:to/:permission"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)
	path = strings.Replace(path, ":permission", fmt.Sprintf("%v", permissionBits), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// GrantWithDetails -
// Grant an object to a specific access control list principal with specified permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
func GrantWithDetails(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, permissionBits int64, grantDetails *notification.ShareDetails) error {
	path := "/v1/api/ulv2/datacatalog/acl/grant/:id/:to/:permission"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)
	path = strings.Replace(path, ":permission", fmt.Sprintf("%v", permissionBits), 1)

	params := [][2]string{}

	headers := map[string]string{}

	bodyBytes := grantDetails.ToBytes()
	res, err := ctx.Post(path, bodyBytes, "application/octet-stream", params, headers)
	_ = res
	return err
}

// GrantAll -
// Grant an object to a specific access control list principal with all permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
func GrantAll(ctx api.RequestContext, idParam id.ObjectId, to id.ObjectId) error {
	path := "/v1/api/ulv2/datacatalog/acl/grant/:id/:to"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// GrantAllWithDetails -
// Grant an object to a specific access control list principal with all permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
func GrantAllWithDetails(ctx api.RequestContext, idParam id.ObjectId, to id.B2cid, grantDetails *notification.ShareDetails) error {
	path := "/v1/api/ulv2/datacatalog/acl/grant/:id/:to"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":to", fmt.Sprintf("%v", to), 1)

	params := [][2]string{}

	headers := map[string]string{}

	bodyBytes := grantDetails.ToBytes()
	res, err := ctx.Post(path, bodyBytes, "application/octet-stream", params, headers)
	_ = res
	return err
}

// Revoke -
// Revoke all access from a specified ACL to an object.
func Revoke(ctx api.RequestContext, idParam id.ObjectId, from id.B2cid) error {
	path := "/v1/api/ulv2/datacatalog/acl/revoke/:id/:from"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":from", fmt.Sprintf("%v", from), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// GetPermissions -
// Get the permissions the caller has on the object with the given ID.
func GetPermissions(ctx api.RequestContext, idParam id.ObjectId) ([]byte, error) {
	path := "/v1/api/ulv2/datacatalog/acl/perms/:id"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Get(path, params, headers)
	if err != nil {
		return nil, err
	}
	return res, nil
}

// SetDefaultMode -
// Set the default mode of an object. This can only be done by an object's owner.
func SetDefaultMode(ctx api.RequestContext, idParam id.ObjectId, permission int64) error {
	path := "/v1/api/ulv2/datacatalog/acl/default/:id/:permission"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":permission", fmt.Sprintf("%v", permission), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// Set -
// Forcibly set an object's ACL to another ACL object. Note that the target ACL needs to contain the exact same permissions as the current ACL otherwise this method will return 400 Bad Request. This is a safeguard to ensure the user cannot lock themselves out of an object.
func Set(ctx api.RequestContext, idParam id.ObjectId, aclId id.ObjectId) error {
	path := "/v1/api/ulv2/datacatalog/acl/set/:id/:acl_id"
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":acl_id", fmt.Sprintf("%v", aclId), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}
