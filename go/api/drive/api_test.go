// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package drive

import (
	"fmt"
	"github.com/urbanlogiq/ulsdk"
	"github.com/urbanlogiq/ulsdk/types/fs"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/object"
	"testing"
	"time"
)

var _ = fmt.Sprintf

func TestLs(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	p1 := "tail"

	success := false
	for i := 0; i < 5; i++ {
		expected := &fs.DirectoryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := Ls(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestLsMe(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "tail"

	success := false
	for i := 0; i < 5; i++ {
		expected := &fs.DirectoryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := LsMe(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestLsUnion(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "tail"
	q0 := true
	q1 := true

	success := false
	for i := 0; i < 5; i++ {
		expected := &fs.DirectoryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := LsUnion(
			ctx,
			p0,
			q0,
			q1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateEntry(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := "tail"
	q0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	q1 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	q2 := int64(42)

	success := false
	for i := 0; i < 5; i++ {
		expected := &object.ObjectSummary{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := CreateEntry(
			ctx,
			p0,
			p1,
			q0,
			q1,
			q2,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetRoots(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := &fs.DirectoryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetRoots(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestPostFile(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	q0 := true
	body := []string{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &fs.DirectoryList{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := PostFile(
			ctx,
			p0,
			q0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUnlinkList(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &object.ObjectIdList{}

	success := false
	for i := 0; i < 5; i++ {
		err := UnlinkList(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUnlink(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		err := Unlink(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestMove(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &fs.MoveRequest{}

	success := false
	for i := 0; i < 5; i++ {
		err := Move(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCopy(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &fs.MoveRequest{}

	success := false
	for i := 0; i < 5; i++ {
		err := Copy(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetFile(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")
		expectedBytes := expected
		ctx.SetResponse(expectedBytes)
		result, err := GetFile(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestPutFileChunk(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.ObjectId{}
	p1 := int64(42)
	q0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	body := []byte("Lorem ipsum dolor sit amet, consectetur adipiscing elit")

	success := false
	for i := 0; i < 5; i++ {
		err := PutFileChunk(
			ctx,
			p0,
			p1,
			q0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetRootId(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &id.ObjectId{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := GetRootId(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}
