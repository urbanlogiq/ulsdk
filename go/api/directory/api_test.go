// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package directory

import (
	"encoding/json"
	"fmt"
	"github.com/urbanlogiq/ulsdk"
	"github.com/urbanlogiq/ulsdk/types/id"
	"testing"
	"time"
)

var _ = fmt.Sprintf
var _ = json.Marshal

func TestGetPrincipal(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &Principal{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetPrincipal(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetPrincipals(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."

	success := false
	for i := 0; i < 5; i++ {
		expected := []Principal{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetPrincipals(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestQueryPrincipals(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."

	success := false
	for i := 0; i < 5; i++ {
		expected := []Principal{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := QueryPrincipals(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetUsers(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := []AdUser{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetUsers(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetUsersDisplayNames(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := []DisplayNames{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetUsersDisplayNames(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetCurrentUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	q0_val := true
	q0 := &q0_val

	success := false
	for i := 0; i < 5; i++ {
		expected := &AdUserWithAuditLog{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetCurrentUser(
			ctx,
			q0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &CreateUserRequest{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &CreateUserPayload{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := CreateUser(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUpdateCurrentUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &UpdateCurrentUserPayload{}

	success := false
	for i := 0; i < 5; i++ {
		err := UpdateCurrentUser(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}
	q0_val := true
	q0 := &q0_val

	success := false
	for i := 0; i < 5; i++ {
		expected := &AdUserWithAuditLog{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetUser(
			ctx,
			p0,
			q0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestUpdateUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}
	body := &UpdateUserPayload{}

	success := false
	for i := 0; i < 5; i++ {
		err := UpdateUser(
			ctx,
			p0,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestDeleteUser(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := DeleteUser(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetGroups(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}


	success := false
	for i := 0; i < 5; i++ {
		expected := []AdGroup{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetGroups(
			ctx,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestCreateGroup(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	body := &CreateGroupPayload{}

	success := false
	for i := 0; i < 5; i++ {
		expected := &AdGroup{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := CreateGroup(
			ctx,
			body,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestGetGroupMembers(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		expected := []GroupMembership{}
		expectedBytes, err := json.Marshal(expected)
		if err != nil {
			t.Fatalf("failed to marshal expected: %v", err)
		}
		ctx.SetResponse(expectedBytes)
		result, err := GetGroupMembers(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestDeleteGroup(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := DeleteGroup(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestAddGroupMember(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}
	p1 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := AddGroupMember(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestRemoveGroupMember(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := id.B2cid{}
	p1 := id.B2cid{}

	success := false
	for i := 0; i < 5; i++ {
		err := RemoveGroupMember(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}
