// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package inbox

import (
	"fmt"
	"github.com/urbanlogiq/ulsdk"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/notification"
	"testing"
	"time"
)

var _ = fmt.Sprintf

func TestFetch(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."

	success := false
	for i := 0; i < 5; i++ {
		expected := &notification.Inbox{}
		expectedBytes := expected.ToBytes()
		ctx.SetResponse(expectedBytes)
		result, err := Fetch(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		_ = result
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestClearAllStatus(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."

	success := false
	for i := 0; i < 5; i++ {
		err := ClearAllStatus(
			ctx,
			p0,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestSetStatus(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	p1 := id.ObjectId{}
	p2 := int64(42)

	success := false
	for i := 0; i < 5; i++ {
		err := SetStatus(
			ctx,
			p0,
			p1,
			p2,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}

func TestClearStatus(t *testing.T) {
	ctx := ulsdk.NewTestContextFromEnv()
	if ctx == nil {
		t.Skip("credentials not set, skipping API test")
	}

	p0 := "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
	p1 := id.ObjectId{}

	success := false
	for i := 0; i < 5; i++ {
		err := ClearStatus(
			ctx,
			p0,
			p1,
		)
		if err != nil {
			time.Sleep(time.Duration(i+1) * time.Second)
			continue
		}
		success = true
		break
	}
	if !success {
		t.Fatal("test was unable to complete with retries")
	}
}
