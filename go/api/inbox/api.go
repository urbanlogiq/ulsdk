// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package inbox

import (
	"fmt"
	"github.com/urbanlogiq/ulsdk/api"
	"github.com/urbanlogiq/ulsdk/types/id"
	"github.com/urbanlogiq/ulsdk/types/notification"
	"strings"
)

var _ = fmt.Errorf

// Fetch -
// Fetch the contents of the specified inbox folder
func Fetch(ctx api.RequestContext, folder string) (*notification.Inbox, error) {
	path := "/v1/api/ulv2/inbox/:folder"
	path = strings.Replace(path, ":folder", fmt.Sprintf("%v", folder), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Get(path, params, headers)
	if err != nil {
		return nil, err
	}
	result, err := notification.InboxFromBytes(res)
	if err != nil {
		return nil, fmt.Errorf("failed to deserialize response: %w", err)
	}
	return result, nil
}

// ClearAllStatus -
// Clear the status of all notifications in the specified folder
func ClearAllStatus(ctx api.RequestContext, folder string) error {
	path := "/v1/api/ulv2/inbox/:folder"
	path = strings.Replace(path, ":folder", fmt.Sprintf("%v", folder), 1)

	params := [][2]string{}

	headers := map[string]string{}

	_, err := ctx.Delete(path, params, headers)
	return err
}

// SetStatus -
// Set the status of a notification in the specified folder
func SetStatus(ctx api.RequestContext, folder string, idParam id.ObjectId, status int64) error {
	path := "/v1/api/ulv2/inbox/:folder/:id/status/:status"
	path = strings.Replace(path, ":folder", fmt.Sprintf("%v", folder), 1)
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)
	path = strings.Replace(path, ":status", fmt.Sprintf("%v", status), 1)

	params := [][2]string{}

	headers := map[string]string{}

	res, err := ctx.Post(path, nil, "", params, headers)
	_ = res
	return err
}

// ClearStatus -
// Clear the status of a notification in the specified folder
func ClearStatus(ctx api.RequestContext, folder string, idParam id.ObjectId) error {
	path := "/v1/api/ulv2/inbox/:folder/:id"
	path = strings.Replace(path, ":folder", fmt.Sprintf("%v", folder), 1)
	path = strings.Replace(path, ":id", fmt.Sprintf("%v", idParam), 1)

	params := [][2]string{}

	headers := map[string]string{}

	_, err := ctx.Delete(path, params, headers)
	return err
}
