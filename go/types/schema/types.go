// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package schema

import (
	flatbuffers "github.com/google/flatbuffers/go"
	"github.com/urbanlogiq/ulsdk/types/generated"
)

var _ flatbuffers.UOffsetT

type DateUnit int16

const (
	DateUnitDAY DateUnit = 0
	DateUnitMILLISECOND DateUnit = 1
)

// DictionaryKind -
//  ----------------------------------------------------------------------
//  Dictionary encoding metadata
//  Maintained for forwards compatibility, in the future
//  Dictionaries might be explicit maps between integers and values
//  allowing for non-contiguous index values
type DictionaryKind int16

const (
	DictionaryKindDenseArray DictionaryKind = 0
)

// Endianness -
//  ----------------------------------------------------------------------
//  Endianness of the platform producing the data
type Endianness int16

const (
	EndiannessLittle Endianness = 0
	EndiannessBig Endianness = 1
)

// Feature -
//  Represents Arrow Features that might not have full support
//  within implementations. This is intended to be used in
//  two scenarios:
//   1.  A mechanism for readers of Arrow Streams
//       and files to understand that the stream or file makes
//       use of a feature that isn't supported or unknown to
//       the implementation (and therefore can meet the Arrow
//       forward compatibility guarantees).
//   2.  A means of negotiating between a client and server
//       what features a stream is allowed to use. The enums
//       values here are intented to represent higher level
//       features, additional details maybe negotiated
//       with key-value pairs specific to the protocol.
//
//  Enums added to this list should be assigned power-of-two values
//  to facilitate exchanging and comparing bitmaps for supported
//  features.
type Feature int64

const (
	FeatureUNUSED Feature = 0
	FeatureDICTIONARY_REPLACEMENT Feature = 1
	FeatureCOMPRESSED_BODY Feature = 2
)

type IntervalUnit int16

const (
	IntervalUnitYEAR_MONTH IntervalUnit = 0
	IntervalUnitDAY_TIME IntervalUnit = 1
	IntervalUnitMONTH_DAY_NANO IntervalUnit = 2
)

// MetadataVersion -
//  Logical types, vector layouts, and schemas
//  Format Version History.
//  Version 1.0 - Forward and backwards compatibility guaranteed.
//  Version 1.1 - Add Decimal256.
//  Version 1.2 - Add Interval MONTH_DAY_NANO.
//  Version 1.3 - Add Run-End Encoded.
//  Version 1.4 - Add BinaryView, Utf8View, variadicBufferCounts, ListView, and
//  LargeListView.
type MetadataVersion int16

const (
	MetadataVersionV1 MetadataVersion = 0
	MetadataVersionV2 MetadataVersion = 1
	MetadataVersionV3 MetadataVersion = 2
	MetadataVersionV4 MetadataVersion = 3
	MetadataVersionV5 MetadataVersion = 4
)

type Precision int16

const (
	PrecisionHALF Precision = 0
	PrecisionSINGLE Precision = 1
	PrecisionDOUBLE Precision = 2
)

type TimeUnit int16

const (
	TimeUnitSECOND TimeUnit = 0
	TimeUnitMILLISECOND TimeUnit = 1
	TimeUnitMICROSECOND TimeUnit = 2
	TimeUnitNANOSECOND TimeUnit = 3
)

// Null -
//  These are stored in the flatbuffer in the Type union below
type Null struct {
}

func NullFromFbs(fbs *generated.Null) *Null {
	o := &Null{}
	return o
}

// NullFromBytes deserializes a Null from size-prefixed FlatBuffer bytes.
func NullFromBytes(data []byte) (*Null, error) {
	fbs := generated.GetSizePrefixedRootAsNull(data, 0)
	return NullFromFbs(fbs), nil
}

// ToBytes serializes the Null to size-prefixed FlatBuffer bytes.
func (o *Null) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Null into a FlatBuffer builder and returns the offset.
func (o *Null) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.NullStart(builder)
	return generated.NullEnd(builder)
}

type Int struct {
	BitWidth int32
	IsSigned bool
}

func IntFromFbs(fbs *generated.Int) *Int {
	o := &Int{}
	o.BitWidth = fbs.BitWidth()
	o.IsSigned = fbs.IsSigned()
	return o
}

// IntFromBytes deserializes a Int from size-prefixed FlatBuffer bytes.
func IntFromBytes(data []byte) (*Int, error) {
	fbs := generated.GetSizePrefixedRootAsInt(data, 0)
	return IntFromFbs(fbs), nil
}

// ToBytes serializes the Int to size-prefixed FlatBuffer bytes.
func (o *Int) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Int into a FlatBuffer builder and returns the offset.
func (o *Int) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.IntStart(builder)
	generated.IntAddBitWidth(builder, o.BitWidth)
	generated.IntAddIsSigned(builder, o.IsSigned)
	return generated.IntEnd(builder)
}

type FloatingPoint struct {
	Precision int16
}

func FloatingPointFromFbs(fbs *generated.FloatingPoint) *FloatingPoint {
	o := &FloatingPoint{}
	o.Precision = int16(fbs.Precision())
	return o
}

// FloatingPointFromBytes deserializes a FloatingPoint from size-prefixed FlatBuffer bytes.
func FloatingPointFromBytes(data []byte) (*FloatingPoint, error) {
	fbs := generated.GetSizePrefixedRootAsFloatingPoint(data, 0)
	return FloatingPointFromFbs(fbs), nil
}

// ToBytes serializes the FloatingPoint to size-prefixed FlatBuffer bytes.
func (o *FloatingPoint) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the FloatingPoint into a FlatBuffer builder and returns the offset.
func (o *FloatingPoint) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.FloatingPointStart(builder)
	generated.FloatingPointAddPrecision(builder, generated.Precision(o.Precision))
	return generated.FloatingPointEnd(builder)
}

// Binary -
//  Opaque binary data
type Binary struct {
}

func BinaryFromFbs(fbs *generated.Binary) *Binary {
	o := &Binary{}
	return o
}

// BinaryFromBytes deserializes a Binary from size-prefixed FlatBuffer bytes.
func BinaryFromBytes(data []byte) (*Binary, error) {
	fbs := generated.GetSizePrefixedRootAsBinary(data, 0)
	return BinaryFromFbs(fbs), nil
}

// ToBytes serializes the Binary to size-prefixed FlatBuffer bytes.
func (o *Binary) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Binary into a FlatBuffer builder and returns the offset.
func (o *Binary) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.BinaryStart(builder)
	return generated.BinaryEnd(builder)
}

// Utf8 -
//  Unicode with UTF-8 encoding
type Utf8 struct {
}

func Utf8FromFbs(fbs *generated.Utf8) *Utf8 {
	o := &Utf8{}
	return o
}

// Utf8FromBytes deserializes a Utf8 from size-prefixed FlatBuffer bytes.
func Utf8FromBytes(data []byte) (*Utf8, error) {
	fbs := generated.GetSizePrefixedRootAsUtf8(data, 0)
	return Utf8FromFbs(fbs), nil
}

// ToBytes serializes the Utf8 to size-prefixed FlatBuffer bytes.
func (o *Utf8) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Utf8 into a FlatBuffer builder and returns the offset.
func (o *Utf8) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.Utf8Start(builder)
	return generated.Utf8End(builder)
}

type Bool struct {
}

func BoolFromFbs(fbs *generated.Bool) *Bool {
	o := &Bool{}
	return o
}

// BoolFromBytes deserializes a Bool from size-prefixed FlatBuffer bytes.
func BoolFromBytes(data []byte) (*Bool, error) {
	fbs := generated.GetSizePrefixedRootAsBool(data, 0)
	return BoolFromFbs(fbs), nil
}

// ToBytes serializes the Bool to size-prefixed FlatBuffer bytes.
func (o *Bool) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Bool into a FlatBuffer builder and returns the offset.
func (o *Bool) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.BoolStart(builder)
	return generated.BoolEnd(builder)
}

// Decimal -
//  Exact decimal value represented as an integer value in two's
//  complement. Currently only 128-bit (16-byte) and 256-bit (32-byte) integers
//  are used. The representation uses the endianness indicated
//  in the Schema.
type Decimal struct {
	BitWidth int32
	Precision int32
	Scale int32
}

func DecimalFromFbs(fbs *generated.Decimal) *Decimal {
	o := &Decimal{}
	o.BitWidth = fbs.BitWidth()
	o.Precision = fbs.Precision()
	o.Scale = fbs.Scale()
	return o
}

// DecimalFromBytes deserializes a Decimal from size-prefixed FlatBuffer bytes.
func DecimalFromBytes(data []byte) (*Decimal, error) {
	fbs := generated.GetSizePrefixedRootAsDecimal(data, 0)
	return DecimalFromFbs(fbs), nil
}

// ToBytes serializes the Decimal to size-prefixed FlatBuffer bytes.
func (o *Decimal) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Decimal into a FlatBuffer builder and returns the offset.
func (o *Decimal) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.DecimalStart(builder)
	generated.DecimalAddBitWidth(builder, o.BitWidth)
	generated.DecimalAddPrecision(builder, o.Precision)
	generated.DecimalAddScale(builder, o.Scale)
	return generated.DecimalEnd(builder)
}

// Date -
//  Date is either a 32-bit or 64-bit signed integer type representing an
//  elapsed time since UNIX epoch (1970-01-01), stored in either of two units:
//
//  * Milliseconds (64 bits) indicating UNIX time elapsed since the epoch (no
//    leap seconds), where the values are evenly divisible by 86400000
//  * Days (32 bits) since the UNIX epoch
type Date struct {
	Unit int16
}

func DateFromFbs(fbs *generated.Date) *Date {
	o := &Date{}
	o.Unit = int16(fbs.Unit())
	return o
}

// DateFromBytes deserializes a Date from size-prefixed FlatBuffer bytes.
func DateFromBytes(data []byte) (*Date, error) {
	fbs := generated.GetSizePrefixedRootAsDate(data, 0)
	return DateFromFbs(fbs), nil
}

// ToBytes serializes the Date to size-prefixed FlatBuffer bytes.
func (o *Date) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Date into a FlatBuffer builder and returns the offset.
func (o *Date) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.DateStart(builder)
	generated.DateAddUnit(builder, generated.DateUnit(o.Unit))
	return generated.DateEnd(builder)
}

// Time -
//  Time is either a 32-bit or 64-bit signed integer type representing an
//  elapsed time since midnight, stored in either of four units: seconds,
//  milliseconds, microseconds or nanoseconds.
//
//  The integer `bitWidth` depends on the `unit` and must be one of the following:
//  * SECOND and MILLISECOND: 32 bits
//  * MICROSECOND and NANOSECOND: 64 bits
//
//  The allowed values are between 0 (inclusive) and 86400 (=24*60*60) seconds
//  (exclusive), adjusted for the time unit (for example, up to 86400000
//  exclusive for the MILLISECOND unit).
//  This definition doesn't allow for leap seconds. Time values from
//  measurements with leap seconds will need to be corrected when ingesting
//  into Arrow (for example by replacing the value 86400 with 86399).
type Time struct {
	BitWidth int32
	Unit int16
}

func TimeFromFbs(fbs *generated.Time) *Time {
	o := &Time{}
	o.BitWidth = fbs.BitWidth()
	o.Unit = int16(fbs.Unit())
	return o
}

// TimeFromBytes deserializes a Time from size-prefixed FlatBuffer bytes.
func TimeFromBytes(data []byte) (*Time, error) {
	fbs := generated.GetSizePrefixedRootAsTime(data, 0)
	return TimeFromFbs(fbs), nil
}

// ToBytes serializes the Time to size-prefixed FlatBuffer bytes.
func (o *Time) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Time into a FlatBuffer builder and returns the offset.
func (o *Time) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.TimeStart(builder)
	generated.TimeAddBitWidth(builder, o.BitWidth)
	generated.TimeAddUnit(builder, generated.TimeUnit(o.Unit))
	return generated.TimeEnd(builder)
}

// Timestamp -
//  Timestamp is a 64-bit signed integer representing an elapsed time since a
//  fixed epoch, stored in either of four units: seconds, milliseconds,
//  microseconds or nanoseconds, and is optionally annotated with a timezone.
//
//  Timestamp values do not include any leap seconds (in other words, all
//  days are considered 86400 seconds long).
//
//  Timestamps with a non-empty timezone
//  ------------------------------------
//
//  If a Timestamp column has a non-empty timezone value, its epoch is
//  1970-01-01 00:00:00 (January 1st 1970, midnight) in the *UTC* timezone
//  (the Unix epoch), regardless of the Timestamp's own timezone.
//
//  Therefore, timestamp values with a non-empty timezone correspond to
//  physical points in time together with some additional information about
//  how the data was obtained and/or how to display it (the timezone).
//
//    For example, the timestamp value 0 with the timezone string "Europe/Paris"
//    corresponds to "January 1st 1970, 00h00" in the UTC timezone, but the
//    application may prefer to display it as "January 1st 1970, 01h00" in
//    the Europe/Paris timezone (which is the same physical point in time).
//
//  One consequence is that timestamp values with a non-empty timezone
//  can be compared and ordered directly, since they all share the same
//  well-known point of reference (the Unix epoch).
//
//  Timestamps with an unset / empty timezone
//  -----------------------------------------
//
//  If a Timestamp column has no timezone value, its epoch is
//  1970-01-01 00:00:00 (January 1st 1970, midnight) in an *unknown* timezone.
//
//  Therefore, timestamp values without a timezone cannot be meaningfully
//  interpreted as physical points in time, but only as calendar / clock
//  indications ("wall clock time") in an unspecified timezone.
//
//    For example, the timestamp value 0 with an empty timezone string
//    corresponds to "January 1st 1970, 00h00" in an unknown timezone: there
//    is not enough information to interpret it as a well-defined physical
//    point in time.
//
//  One consequence is that timestamp values without a timezone cannot
//  be reliably compared or ordered, since they may have different points of
//  reference.  In particular, it is *not* possible to interpret an unset
//  or empty timezone as the same as "UTC".
//
//  Conversion between timezones
//  ----------------------------
//
//  If a Timestamp column has a non-empty timezone, changing the timezone
//  to a different non-empty value is a metadata-only operation:
//  the timestamp values need not change as their point of reference remains
//  the same (the Unix epoch).
//
//  However, if a Timestamp column has no timezone value, changing it to a
//  non-empty value requires to think about the desired semantics.
//  One possibility is to assume that the original timestamp values are
//  relative to the epoch of the timezone being set; timestamp values should
//  then adjusted to the Unix epoch (for example, changing the timezone from
//  empty to "Europe/Paris" would require converting the timestamp values
//  from "Europe/Paris" to "UTC", which seems counter-intuitive but is
//  nevertheless correct).
//
//  Guidelines for encoding data from external libraries
//  ----------------------------------------------------
//
//  Date & time libraries often have multiple different data types for temporal
//  data. In order to ease interoperability between different implementations the
//  Arrow project has some recommendations for encoding these types into a Timestamp
//  column.
//
//  An "instant" represents a physical point in time that has no relevant timezone
//  (for example, astronomical data). To encode an instant, use a Timestamp with
//  the timezone string set to "UTC", and make sure the Timestamp values
//  are relative to the UTC epoch (January 1st 1970, midnight).
//
//  A "zoned date-time" represents a physical point in time annotated with an
//  informative timezone (for example, the timezone in which the data was
//  recorded).  To encode a zoned date-time, use a Timestamp with the timezone
//  string set to the name of the timezone, and make sure the Timestamp values
//  are relative to the UTC epoch (January 1st 1970, midnight).
//
//   (There is some ambiguity between an instant and a zoned date-time with the
//    UTC timezone.  Both of these are stored the same in Arrow.  Typically,
//    this distinction does not matter.  If it does, then an application should
//    use custom metadata or an extension type to distinguish between the two cases.)
//
//  An "offset date-time" represents a physical point in time combined with an
//  explicit offset from UTC.  To encode an offset date-time, use a Timestamp
//  with the timezone string set to the numeric timezone offset string
//  (e.g. "+03:00"), and make sure the Timestamp values are relative to
//  the UTC epoch (January 1st 1970, midnight).
//
//  A "naive date-time" (also called "local date-time" in some libraries)
//  represents a wall clock time combined with a calendar date, but with
//  no indication of how to map this information to a physical point in time.
//  Naive date-times must be handled with care because of this missing
//  information, and also because daylight saving time (DST) may make
//  some values ambiguous or nonexistent. A naive date-time may be
//  stored as a struct with Date and Time fields. However, it may also be
//  encoded into a Timestamp column with an empty timezone. The timestamp
//  values should be computed "as if" the timezone of the date-time values
//  was UTC; for example, the naive date-time "January 1st 1970, 00h00" would
//  be encoded as timestamp value 0.
type Timestamp struct {
	Timezone *string
	Unit int16
}

func TimestampFromFbs(fbs *generated.Timestamp) *Timestamp {
	o := &Timestamp{}
	if s := fbs.Timezone(); s != nil {
		str := string(s)
		o.Timezone = &str
	}
	o.Unit = int16(fbs.Unit())
	return o
}

// TimestampFromBytes deserializes a Timestamp from size-prefixed FlatBuffer bytes.
func TimestampFromBytes(data []byte) (*Timestamp, error) {
	fbs := generated.GetSizePrefixedRootAsTimestamp(data, 0)
	return TimestampFromFbs(fbs), nil
}

// ToBytes serializes the Timestamp to size-prefixed FlatBuffer bytes.
func (o *Timestamp) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Timestamp into a FlatBuffer builder and returns the offset.
func (o *Timestamp) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	var timezoneOffset flatbuffers.UOffsetT
	if o.Timezone != nil {
		timezoneOffset = builder.CreateString(*o.Timezone)
	}
	generated.TimestampStart(builder)
	generated.TimestampAddTimezone(builder, timezoneOffset)
	generated.TimestampAddUnit(builder, generated.TimeUnit(o.Unit))
	return generated.TimestampEnd(builder)
}

type Interval struct {
	Unit int16
}

func IntervalFromFbs(fbs *generated.Interval) *Interval {
	o := &Interval{}
	o.Unit = int16(fbs.Unit())
	return o
}

// IntervalFromBytes deserializes a Interval from size-prefixed FlatBuffer bytes.
func IntervalFromBytes(data []byte) (*Interval, error) {
	fbs := generated.GetSizePrefixedRootAsInterval(data, 0)
	return IntervalFromFbs(fbs), nil
}

// ToBytes serializes the Interval to size-prefixed FlatBuffer bytes.
func (o *Interval) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Interval into a FlatBuffer builder and returns the offset.
func (o *Interval) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.IntervalStart(builder)
	generated.IntervalAddUnit(builder, generated.IntervalUnit(o.Unit))
	return generated.IntervalEnd(builder)
}

type List struct {
}

func ListFromFbs(fbs *generated.List) *List {
	o := &List{}
	return o
}

// ListFromBytes deserializes a List from size-prefixed FlatBuffer bytes.
func ListFromBytes(data []byte) (*List, error) {
	fbs := generated.GetSizePrefixedRootAsList(data, 0)
	return ListFromFbs(fbs), nil
}

// ToBytes serializes the List to size-prefixed FlatBuffer bytes.
func (o *List) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the List into a FlatBuffer builder and returns the offset.
func (o *List) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.ListStart(builder)
	return generated.ListEnd(builder)
}

// Struct -
//  A Struct_ in the flatbuffer metadata is the same as an Arrow Struct
//  (according to the physical memory layout). We used Struct_ here as
//  Struct is a reserved word in Flatbuffers
type Struct struct {
}

func StructFromFbs(fbs *generated.Struct_) *Struct {
	o := &Struct{}
	return o
}

// StructFromBytes deserializes a Struct from size-prefixed FlatBuffer bytes.
func StructFromBytes(data []byte) (*Struct, error) {
	fbs := generated.GetSizePrefixedRootAsStruct_(data, 0)
	return StructFromFbs(fbs), nil
}

// ToBytes serializes the Struct to size-prefixed FlatBuffer bytes.
func (o *Struct) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Struct into a FlatBuffer builder and returns the offset.
func (o *Struct) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.Struct_Start(builder)
	return generated.Struct_End(builder)
}

// Union -
//  A union is a complex type with children in Field
//  By default ids in the type vector refer to the offsets in the children
//  optionally typeIds provides an indirection between the child offset and the type id
//  for each child `typeIds[offset]` is the id used in the type vector
type Union struct {
	Mode int16
	TypeIds []int32
}

func UnionFromFbs(fbs *generated.Union) *Union {
	o := &Union{}
	o.Mode = int16(fbs.Mode())
	for i := 0; i < fbs.TypeIdsLength(); i++ {
		o.TypeIds = append(o.TypeIds, fbs.TypeIds(i))
	}
	return o
}

// UnionFromBytes deserializes a Union from size-prefixed FlatBuffer bytes.
func UnionFromBytes(data []byte) (*Union, error) {
	fbs := generated.GetSizePrefixedRootAsUnion(data, 0)
	return UnionFromFbs(fbs), nil
}

// ToBytes serializes the Union to size-prefixed FlatBuffer bytes.
func (o *Union) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Union into a FlatBuffer builder and returns the offset.
func (o *Union) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.UnionStartTypeIdsVector(builder, len(o.TypeIds))
	for i := len(o.TypeIds) - 1; i >= 0; i-- {
		builder.PrependInt32(o.TypeIds[i])
	}
	typeIdsVecOffset := builder.EndVector(len(o.TypeIds))
	generated.UnionStart(builder)
	generated.UnionAddMode(builder, generated.UnionMode(o.Mode))
	generated.UnionAddTypeIds(builder, typeIdsVecOffset)
	return generated.UnionEnd(builder)
}

type FixedSizeBinary struct {
	ByteWidth int32
}

func FixedSizeBinaryFromFbs(fbs *generated.FixedSizeBinary) *FixedSizeBinary {
	o := &FixedSizeBinary{}
	o.ByteWidth = fbs.ByteWidth()
	return o
}

// FixedSizeBinaryFromBytes deserializes a FixedSizeBinary from size-prefixed FlatBuffer bytes.
func FixedSizeBinaryFromBytes(data []byte) (*FixedSizeBinary, error) {
	fbs := generated.GetSizePrefixedRootAsFixedSizeBinary(data, 0)
	return FixedSizeBinaryFromFbs(fbs), nil
}

// ToBytes serializes the FixedSizeBinary to size-prefixed FlatBuffer bytes.
func (o *FixedSizeBinary) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the FixedSizeBinary into a FlatBuffer builder and returns the offset.
func (o *FixedSizeBinary) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.FixedSizeBinaryStart(builder)
	generated.FixedSizeBinaryAddByteWidth(builder, o.ByteWidth)
	return generated.FixedSizeBinaryEnd(builder)
}

type FixedSizeList struct {
	ListSize int32
}

func FixedSizeListFromFbs(fbs *generated.FixedSizeList) *FixedSizeList {
	o := &FixedSizeList{}
	o.ListSize = fbs.ListSize()
	return o
}

// FixedSizeListFromBytes deserializes a FixedSizeList from size-prefixed FlatBuffer bytes.
func FixedSizeListFromBytes(data []byte) (*FixedSizeList, error) {
	fbs := generated.GetSizePrefixedRootAsFixedSizeList(data, 0)
	return FixedSizeListFromFbs(fbs), nil
}

// ToBytes serializes the FixedSizeList to size-prefixed FlatBuffer bytes.
func (o *FixedSizeList) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the FixedSizeList into a FlatBuffer builder and returns the offset.
func (o *FixedSizeList) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.FixedSizeListStart(builder)
	generated.FixedSizeListAddListSize(builder, o.ListSize)
	return generated.FixedSizeListEnd(builder)
}

// Map -
//  A Map is a logical nested type that is represented as
//
//  List<entries: Struct<key: K, value: V>>
//
//  In this layout, the keys and values are each respectively contiguous. We do
//  not constrain the key and value types, so the application is responsible
//  for ensuring that the keys are hashable and unique. Whether the keys are sorted
//  may be set in the metadata for this field.
//
//  In a field with Map type, the field has a child Struct field, which then
//  has two children: key type and the second the value type. The names of the
//  child fields may be respectively "entries", "key", and "value", but this is
//  not enforced.
//
//  Map
//  ```text
//    - child[0] entries: Struct
//      - child[0] key: K
//      - child[1] value: V
//  ```
//  Neither the "entries" field nor the "key" field may be nullable.
//
//  The metadata is structured so that Arrow systems without special handling
//  for Map can make Map an alias for List. The "layout" attribute for the Map
//  field must have the same contents as a List.
type Map struct {
	KeysSorted bool
}

func MapFromFbs(fbs *generated.Map) *Map {
	o := &Map{}
	o.KeysSorted = fbs.KeysSorted()
	return o
}

// MapFromBytes deserializes a Map from size-prefixed FlatBuffer bytes.
func MapFromBytes(data []byte) (*Map, error) {
	fbs := generated.GetSizePrefixedRootAsMap(data, 0)
	return MapFromFbs(fbs), nil
}

// ToBytes serializes the Map to size-prefixed FlatBuffer bytes.
func (o *Map) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Map into a FlatBuffer builder and returns the offset.
func (o *Map) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.MapStart(builder)
	generated.MapAddKeysSorted(builder, o.KeysSorted)
	return generated.MapEnd(builder)
}

type Duration struct {
	Unit int16
}

func DurationFromFbs(fbs *generated.Duration) *Duration {
	o := &Duration{}
	o.Unit = int16(fbs.Unit())
	return o
}

// DurationFromBytes deserializes a Duration from size-prefixed FlatBuffer bytes.
func DurationFromBytes(data []byte) (*Duration, error) {
	fbs := generated.GetSizePrefixedRootAsDuration(data, 0)
	return DurationFromFbs(fbs), nil
}

// ToBytes serializes the Duration to size-prefixed FlatBuffer bytes.
func (o *Duration) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Duration into a FlatBuffer builder and returns the offset.
func (o *Duration) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.DurationStart(builder)
	generated.DurationAddUnit(builder, generated.TimeUnit(o.Unit))
	return generated.DurationEnd(builder)
}

// LargeBinary -
//  Same as Binary, but with 64-bit offsets, allowing to represent
//  extremely large data values.
type LargeBinary struct {
}

func LargeBinaryFromFbs(fbs *generated.LargeBinary) *LargeBinary {
	o := &LargeBinary{}
	return o
}

// LargeBinaryFromBytes deserializes a LargeBinary from size-prefixed FlatBuffer bytes.
func LargeBinaryFromBytes(data []byte) (*LargeBinary, error) {
	fbs := generated.GetSizePrefixedRootAsLargeBinary(data, 0)
	return LargeBinaryFromFbs(fbs), nil
}

// ToBytes serializes the LargeBinary to size-prefixed FlatBuffer bytes.
func (o *LargeBinary) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the LargeBinary into a FlatBuffer builder and returns the offset.
func (o *LargeBinary) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.LargeBinaryStart(builder)
	return generated.LargeBinaryEnd(builder)
}

// LargeUtf8 -
//  Same as Utf8, but with 64-bit offsets, allowing to represent
//  extremely large data values.
type LargeUtf8 struct {
}

func LargeUtf8FromFbs(fbs *generated.LargeUtf8) *LargeUtf8 {
	o := &LargeUtf8{}
	return o
}

// LargeUtf8FromBytes deserializes a LargeUtf8 from size-prefixed FlatBuffer bytes.
func LargeUtf8FromBytes(data []byte) (*LargeUtf8, error) {
	fbs := generated.GetSizePrefixedRootAsLargeUtf8(data, 0)
	return LargeUtf8FromFbs(fbs), nil
}

// ToBytes serializes the LargeUtf8 to size-prefixed FlatBuffer bytes.
func (o *LargeUtf8) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the LargeUtf8 into a FlatBuffer builder and returns the offset.
func (o *LargeUtf8) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.LargeUtf8Start(builder)
	return generated.LargeUtf8End(builder)
}

// LargeList -
//  Same as List, but with 64-bit offsets, allowing to represent
//  extremely large data values.
type LargeList struct {
}

func LargeListFromFbs(fbs *generated.LargeList) *LargeList {
	o := &LargeList{}
	return o
}

// LargeListFromBytes deserializes a LargeList from size-prefixed FlatBuffer bytes.
func LargeListFromBytes(data []byte) (*LargeList, error) {
	fbs := generated.GetSizePrefixedRootAsLargeList(data, 0)
	return LargeListFromFbs(fbs), nil
}

// ToBytes serializes the LargeList to size-prefixed FlatBuffer bytes.
func (o *LargeList) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the LargeList into a FlatBuffer builder and returns the offset.
func (o *LargeList) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.LargeListStart(builder)
	return generated.LargeListEnd(builder)
}

// RunEndEncoded -
//  Contains two child arrays, run_ends and values.
//  The run_ends child array must be a 16/32/64-bit integer array
//  which encodes the indices at which the run with the value in
//  each corresponding index in the values child array ends.
//  Like list/struct types, the value array can be of any type.
type RunEndEncoded struct {
}

func RunEndEncodedFromFbs(fbs *generated.RunEndEncoded) *RunEndEncoded {
	o := &RunEndEncoded{}
	return o
}

// RunEndEncodedFromBytes deserializes a RunEndEncoded from size-prefixed FlatBuffer bytes.
func RunEndEncodedFromBytes(data []byte) (*RunEndEncoded, error) {
	fbs := generated.GetSizePrefixedRootAsRunEndEncoded(data, 0)
	return RunEndEncodedFromFbs(fbs), nil
}

// ToBytes serializes the RunEndEncoded to size-prefixed FlatBuffer bytes.
func (o *RunEndEncoded) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the RunEndEncoded into a FlatBuffer builder and returns the offset.
func (o *RunEndEncoded) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.RunEndEncodedStart(builder)
	return generated.RunEndEncodedEnd(builder)
}

// BinaryView -
//  Logically the same as Binary, but the internal representation uses a view
//  struct that contains the string length and either the string's entire data
//  inline (for small strings) or an inlined prefix, an index of another buffer,
//  and an offset pointing to a slice in that buffer (for non-small strings).
//
//  Since it uses a variable number of data buffers, each Field with this type
//  must have a corresponding entry in `variadicBufferCounts`.
type BinaryView struct {
}

func BinaryViewFromFbs(fbs *generated.BinaryView) *BinaryView {
	o := &BinaryView{}
	return o
}

// BinaryViewFromBytes deserializes a BinaryView from size-prefixed FlatBuffer bytes.
func BinaryViewFromBytes(data []byte) (*BinaryView, error) {
	fbs := generated.GetSizePrefixedRootAsBinaryView(data, 0)
	return BinaryViewFromFbs(fbs), nil
}

// ToBytes serializes the BinaryView to size-prefixed FlatBuffer bytes.
func (o *BinaryView) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the BinaryView into a FlatBuffer builder and returns the offset.
func (o *BinaryView) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.BinaryViewStart(builder)
	return generated.BinaryViewEnd(builder)
}

// Utf8view -
//  Logically the same as Utf8, but the internal representation uses a view
//  struct that contains the string length and either the string's entire data
//  inline (for small strings) or an inlined prefix, an index of another buffer,
//  and an offset pointing to a slice in that buffer (for non-small strings).
//
//  Since it uses a variable number of data buffers, each Field with this type
//  must have a corresponding entry in `variadicBufferCounts`.
type Utf8view struct {
}

func Utf8viewFromFbs(fbs *generated.Utf8View) *Utf8view {
	o := &Utf8view{}
	return o
}

// Utf8viewFromBytes deserializes a Utf8view from size-prefixed FlatBuffer bytes.
func Utf8viewFromBytes(data []byte) (*Utf8view, error) {
	fbs := generated.GetSizePrefixedRootAsUtf8View(data, 0)
	return Utf8viewFromFbs(fbs), nil
}

// ToBytes serializes the Utf8view to size-prefixed FlatBuffer bytes.
func (o *Utf8view) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Utf8view into a FlatBuffer builder and returns the offset.
func (o *Utf8view) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.Utf8ViewStart(builder)
	return generated.Utf8ViewEnd(builder)
}

// ListView -
//  Represents the same logical types that List can, but contains offsets and
//  sizes allowing for writes in any order and sharing of child values among
//  list values.
type ListView struct {
}

func ListViewFromFbs(fbs *generated.ListView) *ListView {
	o := &ListView{}
	return o
}

// ListViewFromBytes deserializes a ListView from size-prefixed FlatBuffer bytes.
func ListViewFromBytes(data []byte) (*ListView, error) {
	fbs := generated.GetSizePrefixedRootAsListView(data, 0)
	return ListViewFromFbs(fbs), nil
}

// ToBytes serializes the ListView to size-prefixed FlatBuffer bytes.
func (o *ListView) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the ListView into a FlatBuffer builder and returns the offset.
func (o *ListView) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.ListViewStart(builder)
	return generated.ListViewEnd(builder)
}

// LargeListView -
//  Same as ListView, but with 64-bit offsets and sizes, allowing to represent
//  extremely large data values.
type LargeListView struct {
}

func LargeListViewFromFbs(fbs *generated.LargeListView) *LargeListView {
	o := &LargeListView{}
	return o
}

// LargeListViewFromBytes deserializes a LargeListView from size-prefixed FlatBuffer bytes.
func LargeListViewFromBytes(data []byte) (*LargeListView, error) {
	fbs := generated.GetSizePrefixedRootAsLargeListView(data, 0)
	return LargeListViewFromFbs(fbs), nil
}

// ToBytes serializes the LargeListView to size-prefixed FlatBuffer bytes.
func (o *LargeListView) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the LargeListView into a FlatBuffer builder and returns the offset.
func (o *LargeListView) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	generated.LargeListViewStart(builder)
	return generated.LargeListViewEnd(builder)
}

// Type -
//  ----------------------------------------------------------------------
//  Top-level Type value, enabling extensible type-specific metadata. We can
//  add new logical types to Type without breaking backwards compatibility
// Type is a union type. Possible concrete types: Null, Int, FloatingPoint, Binary, Utf8, Bool, Decimal, Date, Time, Timestamp, Interval, List, Struct, Union, FixedSizeBinary, FixedSizeList, Map, Duration, LargeBinary, LargeUtf8, LargeList, RunEndEncoded, BinaryView, Utf8view, ListView, LargeListView
type Type interface {
	isType()
}

type UnionMode int16

const (
	UnionModeSparse UnionMode = 0
	UnionModeDense UnionMode = 1
)

// Buffer -
//  ----------------------------------------------------------------------
//  A Buffer represents a single contiguous memory segment
type Buffer struct {
	Length int64
	Offset int64
}

func BufferFromFbs(fbs *generated.Buffer) *Buffer {
	o := &Buffer{}
	o.Length = fbs.Length()
	o.Offset = fbs.Offset()
	return o
}


type DictionaryEncoding struct {
	DictionaryKind int16
	Id int64
	IndexType *Int
	IsOrdered bool
}

func DictionaryEncodingFromFbs(fbs *generated.DictionaryEncoding) *DictionaryEncoding {
	o := &DictionaryEncoding{}
	o.DictionaryKind = int16(fbs.DictionaryKind())
	o.Id = fbs.Id()
	if fbsVal := fbs.IndexType(nil); fbsVal != nil {
		o.IndexType = IntFromFbs(fbsVal)
	}
	o.IsOrdered = fbs.IsOrdered()
	return o
}

// DictionaryEncodingFromBytes deserializes a DictionaryEncoding from size-prefixed FlatBuffer bytes.
func DictionaryEncodingFromBytes(data []byte) (*DictionaryEncoding, error) {
	fbs := generated.GetSizePrefixedRootAsDictionaryEncoding(data, 0)
	return DictionaryEncodingFromFbs(fbs), nil
}

// ToBytes serializes the DictionaryEncoding to size-prefixed FlatBuffer bytes.
func (o *DictionaryEncoding) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the DictionaryEncoding into a FlatBuffer builder and returns the offset.
func (o *DictionaryEncoding) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	var indexTypeOffset flatbuffers.UOffsetT
	if o.IndexType != nil {
		indexTypeOffset = o.IndexType.SerializeTo(builder)
	}
	generated.DictionaryEncodingStart(builder)
	generated.DictionaryEncodingAddDictionaryKind(builder, generated.DictionaryKind(o.DictionaryKind))
	generated.DictionaryEncodingAddId(builder, o.Id)
	generated.DictionaryEncodingAddIndexType(builder, indexTypeOffset)
	generated.DictionaryEncodingAddIsOrdered(builder, o.IsOrdered)
	return generated.DictionaryEncodingEnd(builder)
}

// Field -
//  ----------------------------------------------------------------------
//  A field represents a named column in a record / row batch or child of a
//  nested type.
type Field struct {
	Children []Field
	CustomMetadata []KeyValue
	Dictionary *DictionaryEncoding
	Name *string
	Nullable bool
	Type interface{}
}

func FieldFromFbs(fbs *generated.Field) *Field {
	o := &Field{}
	for i := 0; i < fbs.ChildrenLength(); i++ {
		var item generated.Field
		if fbs.Children(&item, i) {
			o.Children = append(o.Children, *FieldFromFbs(&item))
		}
	}
	for i := 0; i < fbs.CustomMetadataLength(); i++ {
		var item generated.KeyValue
		if fbs.CustomMetadata(&item, i) {
			o.CustomMetadata = append(o.CustomMetadata, *KeyValueFromFbs(&item))
		}
	}
	if fbsVal := fbs.Dictionary(nil); fbsVal != nil {
		o.Dictionary = DictionaryEncodingFromFbs(fbsVal)
	}
	if s := fbs.Name(); s != nil {
		str := string(s)
		o.Name = &str
	}
	o.Nullable = fbs.Nullable()
	return o
}

// FieldFromBytes deserializes a Field from size-prefixed FlatBuffer bytes.
func FieldFromBytes(data []byte) (*Field, error) {
	fbs := generated.GetSizePrefixedRootAsField(data, 0)
	return FieldFromFbs(fbs), nil
}

// ToBytes serializes the Field to size-prefixed FlatBuffer bytes.
func (o *Field) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Field into a FlatBuffer builder and returns the offset.
func (o *Field) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	childrenOffsets := make([]flatbuffers.UOffsetT, len(o.Children))
	for i := range o.Children {
		childrenOffsets[i] = o.Children[i].SerializeTo(builder)
	}
	generated.FieldStartChildrenVector(builder, len(o.Children))
	for i := len(childrenOffsets) - 1; i >= 0; i-- {
		builder.PrependUOffsetT(childrenOffsets[i])
	}
	childrenVecOffset := builder.EndVector(len(o.Children))
	customMetadataOffsets := make([]flatbuffers.UOffsetT, len(o.CustomMetadata))
	for i := range o.CustomMetadata {
		customMetadataOffsets[i] = o.CustomMetadata[i].SerializeTo(builder)
	}
	generated.FieldStartCustomMetadataVector(builder, len(o.CustomMetadata))
	for i := len(customMetadataOffsets) - 1; i >= 0; i-- {
		builder.PrependUOffsetT(customMetadataOffsets[i])
	}
	customMetadataVecOffset := builder.EndVector(len(o.CustomMetadata))
	var dictionaryOffset flatbuffers.UOffsetT
	if o.Dictionary != nil {
		dictionaryOffset = o.Dictionary.SerializeTo(builder)
	}
	var nameOffset flatbuffers.UOffsetT
	if o.Name != nil {
		nameOffset = builder.CreateString(*o.Name)
	}
	generated.FieldStart(builder)
	generated.FieldAddChildren(builder, childrenVecOffset)
	generated.FieldAddCustomMetadata(builder, customMetadataVecOffset)
	generated.FieldAddDictionary(builder, dictionaryOffset)
	generated.FieldAddName(builder, nameOffset)
	generated.FieldAddNullable(builder, o.Nullable)
	return generated.FieldEnd(builder)
}

// KeyValue -
//  ----------------------------------------------------------------------
//  user defined key value pairs to add custom metadata to arrow
//  key namespacing is the responsibility of the user
type KeyValue struct {
	Key *string
	Value *string
}

func KeyValueFromFbs(fbs *generated.KeyValue) *KeyValue {
	o := &KeyValue{}
	if s := fbs.Key(); s != nil {
		str := string(s)
		o.Key = &str
	}
	if s := fbs.Value(); s != nil {
		str := string(s)
		o.Value = &str
	}
	return o
}

// KeyValueFromBytes deserializes a KeyValue from size-prefixed FlatBuffer bytes.
func KeyValueFromBytes(data []byte) (*KeyValue, error) {
	fbs := generated.GetSizePrefixedRootAsKeyValue(data, 0)
	return KeyValueFromFbs(fbs), nil
}

// ToBytes serializes the KeyValue to size-prefixed FlatBuffer bytes.
func (o *KeyValue) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the KeyValue into a FlatBuffer builder and returns the offset.
func (o *KeyValue) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	var keyOffset flatbuffers.UOffsetT
	if o.Key != nil {
		keyOffset = builder.CreateString(*o.Key)
	}
	var valueOffset flatbuffers.UOffsetT
	if o.Value != nil {
		valueOffset = builder.CreateString(*o.Value)
	}
	generated.KeyValueStart(builder)
	generated.KeyValueAddKey(builder, keyOffset)
	generated.KeyValueAddValue(builder, valueOffset)
	return generated.KeyValueEnd(builder)
}

// Schema -
//  ----------------------------------------------------------------------
//  A Schema describes the columns in a row batch
type Schema struct {
	CustomMetadata []KeyValue
	Endianness int16
	Features []int64
	Fields []Field
}

func SchemaFromFbs(fbs *generated.Schema) *Schema {
	o := &Schema{}
	for i := 0; i < fbs.CustomMetadataLength(); i++ {
		var item generated.KeyValue
		if fbs.CustomMetadata(&item, i) {
			o.CustomMetadata = append(o.CustomMetadata, *KeyValueFromFbs(&item))
		}
	}
	o.Endianness = int16(fbs.Endianness())
	for i := 0; i < fbs.FeaturesLength(); i++ {
		o.Features = append(o.Features, int64(fbs.Features(i)))
	}
	for i := 0; i < fbs.FieldsLength(); i++ {
		var item generated.Field
		if fbs.Fields(&item, i) {
			o.Fields = append(o.Fields, *FieldFromFbs(&item))
		}
	}
	return o
}

// SchemaFromBytes deserializes a Schema from size-prefixed FlatBuffer bytes.
func SchemaFromBytes(data []byte) (*Schema, error) {
	fbs := generated.GetSizePrefixedRootAsSchema(data, 0)
	return SchemaFromFbs(fbs), nil
}

// ToBytes serializes the Schema to size-prefixed FlatBuffer bytes.
func (o *Schema) ToBytes() []byte {
	builder := flatbuffers.NewBuilder(256)
	offset := o.SerializeTo(builder)
	builder.FinishSizePrefixed(offset)
	return builder.FinishedBytes()
}

// SerializeTo writes the Schema into a FlatBuffer builder and returns the offset.
func (o *Schema) SerializeTo(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	customMetadataOffsets := make([]flatbuffers.UOffsetT, len(o.CustomMetadata))
	for i := range o.CustomMetadata {
		customMetadataOffsets[i] = o.CustomMetadata[i].SerializeTo(builder)
	}
	generated.SchemaStartCustomMetadataVector(builder, len(o.CustomMetadata))
	for i := len(customMetadataOffsets) - 1; i >= 0; i-- {
		builder.PrependUOffsetT(customMetadataOffsets[i])
	}
	customMetadataVecOffset := builder.EndVector(len(o.CustomMetadata))
	generated.SchemaStartFeaturesVector(builder, len(o.Features))
	for i := len(o.Features) - 1; i >= 0; i-- {
		builder.PrependInt64(o.Features[i])
	}
	featuresVecOffset := builder.EndVector(len(o.Features))
	fieldsOffsets := make([]flatbuffers.UOffsetT, len(o.Fields))
	for i := range o.Fields {
		fieldsOffsets[i] = o.Fields[i].SerializeTo(builder)
	}
	generated.SchemaStartFieldsVector(builder, len(o.Fields))
	for i := len(fieldsOffsets) - 1; i >= 0; i-- {
		builder.PrependUOffsetT(fieldsOffsets[i])
	}
	fieldsVecOffset := builder.EndVector(len(o.Fields))
	generated.SchemaStart(builder)
	generated.SchemaAddCustomMetadata(builder, customMetadataVecOffset)
	generated.SchemaAddEndianness(builder, generated.Endianness(o.Endianness))
	generated.SchemaAddFeatures(builder, featuresVecOffset)
	generated.SchemaAddFields(builder, fieldsVecOffset)
	return generated.SchemaEnd(builder)
}
