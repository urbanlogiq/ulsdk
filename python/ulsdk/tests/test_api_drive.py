# Copyright (c), CommunityLogiq Software
# 
# THIS FILE IS AUTOGENERATED, DO NOT EDIT

import time
import os
import pyarrow as pa
from ..api_key_context import ApiKeyContext
from ..test_context import TestContext
from ..keys import Region, Key as SigningKey, Environment
from ..api.drive import *

def test_ls():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
    p1 = "tail";
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = ls(
                ctx,
                p0,
                p1
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_ls_me():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = "tail";
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = ls_me(
                ctx,
                p0
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_ls_union():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = "tail";
    q0 = True
    q1 = True
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = ls_union(
                ctx,
                p0,
                q0,
                q1
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_create_entry():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = ObjectId.from_uuid("00000000-0000-0000-0000-000000000000");
    p1 = "tail";
    q0 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
    q1 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
    q2 = 42
    success = False
    for i in range(5):
        expected = ObjectSummary.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = create_entry(
                ctx,
                p0,
                p1,
                q0,
                q1,
                q2
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_get_roots():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = get_roots(
                ctx
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_post_file():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = ObjectId.from_uuid("00000000-0000-0000-0000-000000000000");
    q0 = True
    body = []
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = post_file(
                ctx,
                p0,
                q0,
                body
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_unlink():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = ObjectId.from_uuid("00000000-0000-0000-0000-000000000000");
    success = False
    for i in range(5):
        expected = DirectoryList.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = unlink(
                ctx,
                p0
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_move():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    body = MoveRequest.make_default()
    success = False
    for i in range(5):
        try:
            move(
                ctx,
                body
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_copy():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    body = MoveRequest.make_default()
    success = False
    for i in range(5):
        try:
            copy(
                ctx,
                body
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_get_file():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = ObjectId.from_uuid("00000000-0000-0000-0000-000000000000");
    success = False
    for i in range(5):
        expected = b"Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
        expected_bytes = expected
        ctx.set_response(expected_bytes);
        try:
            result = get_file(
                ctx,
                p0
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_put_file_chunk():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = ObjectId.from_uuid("00000000-0000-0000-0000-000000000000");
    p1 = 42
    q0 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
    body = b"Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
    success = False
    for i in range(5):
        try:
            put_file_chunk(
                ctx,
                p0,
                p1,
                q0,
                body
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        if success:
            break
    assert success, "test was unable to complete with retries"

def test_get_root_id():
    user = os.environ["CA_USER"] if "CA_USER" in os.environ else None
    access_key = os.environ["CA_ACCESS_KEY"] if "CA_ACCESS_KEY" in os.environ else None
    secret_key = os.environ["CA_SECRET_KEY"] if "CA_SECRET_KEY" in os.environ else None

    if user == None or access_key is None or secret_key is None:
        raise Exception("cannot run test as no credentials are specified")
    key = SigningKey(UUID(user), Region.CA, access_key, secret_key)
    key_ctx = ApiKeyContext(key, Environment.Stage)
    ctx = TestContext(key_ctx)
    p0 = B2cId.from_uuid("00000000-0000-0000-0000-000000000000");
    success = False
    for i in range(5):
        expected = ObjectId.make_default();
        expected_bytes = expected.to_bytes();
        ctx.set_response(expected_bytes);
        try:
            result = get_root_id(
                ctx,
                p0
            )
            success = True
        except Exception as e:
            time.sleep(i + 1)
            continue
        assert result == expected
        if success:
            break
    assert success, "test was unable to complete with retries"
