// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { Attr } from './attr';
import { B2cId } from './b2c-id';
import { ChangeOpEntry } from './change-op-entry';
import { ContentId } from './content-id';
export class ChangeSet {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsChangeSet(bb, obj) {
        return (obj || new ChangeSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsChangeSet(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ChangeSet()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    revision(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new ContentId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    who(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new B2cId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    when() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    ops(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new ChangeOpEntry()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    opsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    attributes(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? (obj || new Attr()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    attributesLength() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startChangeSet(builder) {
        builder.startObject(5);
    }
    static addRevision(builder, revisionOffset) {
        builder.addFieldOffset(0, revisionOffset, 0);
    }
    static addWho(builder, whoOffset) {
        builder.addFieldOffset(1, whoOffset, 0);
    }
    static addWhen(builder, when) {
        builder.addFieldInt64(2, when, BigInt('0'));
    }
    static addOps(builder, opsOffset) {
        builder.addFieldOffset(3, opsOffset, 0);
    }
    static createOpsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startOpsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addAttributes(builder, attributesOffset) {
        builder.addFieldOffset(4, attributesOffset, 0);
    }
    static createAttributesVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startAttributesVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endChangeSet(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // revision
        builder.requiredField(offset, 6); // who
        builder.requiredField(offset, 10); // ops
        return offset;
    }
    unpack() {
        return new ChangeSetT((this.revision() !== null ? this.revision().unpack() : null), (this.who() !== null ? this.who().unpack() : null), this.when(), this.bb.createObjList(this.ops.bind(this), this.opsLength()), this.bb.createObjList(this.attributes.bind(this), this.attributesLength()));
    }
    unpackTo(_o) {
        _o.revision = (this.revision() !== null ? this.revision().unpack() : null);
        _o.who = (this.who() !== null ? this.who().unpack() : null);
        _o.when = this.when();
        _o.ops = this.bb.createObjList(this.ops.bind(this), this.opsLength());
        _o.attributes = this.bb.createObjList(this.attributes.bind(this), this.attributesLength());
    }
}
export class ChangeSetT {
    revision;
    who;
    when;
    ops;
    attributes;
    constructor(revision = null, who = null, when = BigInt('0'), ops = [], attributes = []) {
        this.revision = revision;
        this.who = who;
        this.when = when;
        this.ops = ops;
        this.attributes = attributes;
    }
    pack(builder) {
        const revision = (this.revision !== null ? this.revision.pack(builder) : 0);
        const who = (this.who !== null ? this.who.pack(builder) : 0);
        const ops = ChangeSet.createOpsVector(builder, builder.createObjectOffsetList(this.ops));
        const attributes = ChangeSet.createAttributesVector(builder, builder.createObjectOffsetList(this.attributes));
        ChangeSet.startChangeSet(builder);
        ChangeSet.addRevision(builder, revision);
        ChangeSet.addWho(builder, who);
        ChangeSet.addWhen(builder, this.when);
        ChangeSet.addOps(builder, ops);
        ChangeSet.addAttributes(builder, attributes);
        return ChangeSet.endChangeSet(builder);
    }
}
//# sourceMappingURL=change-set.js.map