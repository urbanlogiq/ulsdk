// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { ObjectId } from './object-id';
import { PermissionTy } from './permission-ty';
export class Share {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsShare(bb, obj) {
        return (obj || new Share()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsShare(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new Share()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    object(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new ObjectId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    dest(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    msg(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    oldPerms() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : PermissionTy.PERM_BROWSE;
    }
    newPerms() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : PermissionTy.PERM_BROWSE;
    }
    static startShare(builder) {
        builder.startObject(5);
    }
    static addObject(builder, objectOffset) {
        builder.addFieldOffset(0, objectOffset, 0);
    }
    static addDest(builder, destOffset) {
        builder.addFieldOffset(1, destOffset, 0);
    }
    static addMsg(builder, msgOffset) {
        builder.addFieldOffset(2, msgOffset, 0);
    }
    static addOldPerms(builder, oldPerms) {
        builder.addFieldInt32(3, oldPerms, PermissionTy.PERM_BROWSE);
    }
    static addNewPerms(builder, newPerms) {
        builder.addFieldInt32(4, newPerms, PermissionTy.PERM_BROWSE);
    }
    static endShare(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // object
        return offset;
    }
    static createShare(builder, objectOffset, destOffset, msgOffset, oldPerms, newPerms) {
        Share.startShare(builder);
        Share.addObject(builder, objectOffset);
        Share.addDest(builder, destOffset);
        Share.addMsg(builder, msgOffset);
        Share.addOldPerms(builder, oldPerms);
        Share.addNewPerms(builder, newPerms);
        return Share.endShare(builder);
    }
    unpack() {
        return new ShareT((this.object() !== null ? this.object().unpack() : null), this.dest(), this.msg(), this.oldPerms(), this.newPerms());
    }
    unpackTo(_o) {
        _o.object = (this.object() !== null ? this.object().unpack() : null);
        _o.dest = this.dest();
        _o.msg = this.msg();
        _o.oldPerms = this.oldPerms();
        _o.newPerms = this.newPerms();
    }
}
export class ShareT {
    object;
    dest;
    msg;
    oldPerms;
    newPerms;
    constructor(object = null, dest = null, msg = null, oldPerms = PermissionTy.PERM_BROWSE, newPerms = PermissionTy.PERM_BROWSE) {
        this.object = object;
        this.dest = dest;
        this.msg = msg;
        this.oldPerms = oldPerms;
        this.newPerms = newPerms;
    }
    pack(builder) {
        const object = (this.object !== null ? this.object.pack(builder) : 0);
        const dest = (this.dest !== null ? builder.createString(this.dest) : 0);
        const msg = (this.msg !== null ? builder.createString(this.msg) : 0);
        return Share.createShare(builder, object, dest, msg, this.oldPerms, this.newPerms);
    }
}
//# sourceMappingURL=share.js.map