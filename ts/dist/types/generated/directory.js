// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { B2cId } from './b2c-id';
import { Slot } from './slot';
/**
 * This Directory table holds the entries in the actual directory
 */
export class Directory {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsDirectory(bb, obj) {
        return (obj || new Directory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsDirectory(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new Directory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    slots(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new Slot()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    slotsLength() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    notifications(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new B2cId()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    notificationsLength() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startDirectory(builder) {
        builder.startObject(2);
    }
    static addSlots(builder, slotsOffset) {
        builder.addFieldOffset(0, slotsOffset, 0);
    }
    static createSlotsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSlotsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addNotifications(builder, notificationsOffset) {
        builder.addFieldOffset(1, notificationsOffset, 0);
    }
    static createNotificationsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startNotificationsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static endDirectory(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // slots
        return offset;
    }
    static createDirectory(builder, slotsOffset, notificationsOffset) {
        Directory.startDirectory(builder);
        Directory.addSlots(builder, slotsOffset);
        Directory.addNotifications(builder, notificationsOffset);
        return Directory.endDirectory(builder);
    }
    unpack() {
        return new DirectoryT(this.bb.createObjList(this.slots.bind(this), this.slotsLength()), this.bb.createObjList(this.notifications.bind(this), this.notificationsLength()));
    }
    unpackTo(_o) {
        _o.slots = this.bb.createObjList(this.slots.bind(this), this.slotsLength());
        _o.notifications = this.bb.createObjList(this.notifications.bind(this), this.notificationsLength());
    }
}
export class DirectoryT {
    slots;
    notifications;
    constructor(slots = [], notifications = []) {
        this.slots = slots;
        this.notifications = notifications;
    }
    pack(builder) {
        const slots = Directory.createSlotsVector(builder, builder.createObjectOffsetList(this.slots));
        const notifications = Directory.createNotificationsVector(builder, builder.createObjectOffsetList(this.notifications));
        return Directory.createDirectory(builder, slots, notifications);
    }
}
//# sourceMappingURL=directory.js.map