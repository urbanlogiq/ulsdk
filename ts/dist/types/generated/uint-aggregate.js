// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { UIntBucket } from './uint-bucket';
export class UIntAggregate {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsUIntAggregate(bb, obj) {
        return (obj || new UIntAggregate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsUIntAggregate(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new UIntAggregate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    min() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    max() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    mean() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    count() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    sum() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    variance() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    histo(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? (obj || new UIntBucket()).__init(this.bb.__vector(this.bb_pos + offset) + index * 16, this.bb) : null;
    }
    histoLength() {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    static startUIntAggregate(builder) {
        builder.startObject(7);
    }
    static addMin(builder, min) {
        builder.addFieldInt64(0, min, BigInt('0'));
    }
    static addMax(builder, max) {
        builder.addFieldInt64(1, max, BigInt('0'));
    }
    static addMean(builder, mean) {
        builder.addFieldInt64(2, mean, BigInt('0'));
    }
    static addCount(builder, count) {
        builder.addFieldInt64(3, count, BigInt('0'));
    }
    static addSum(builder, sum) {
        builder.addFieldInt64(4, sum, BigInt('0'));
    }
    static addVariance(builder, variance) {
        builder.addFieldInt64(5, variance, BigInt('0'));
    }
    static addHisto(builder, histoOffset) {
        builder.addFieldOffset(6, histoOffset, 0);
    }
    static startHistoVector(builder, numElems) {
        builder.startVector(16, numElems, 8);
    }
    static endUIntAggregate(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createUIntAggregate(builder, min, max, mean, count, sum, variance, histoOffset) {
        UIntAggregate.startUIntAggregate(builder);
        UIntAggregate.addMin(builder, min);
        UIntAggregate.addMax(builder, max);
        UIntAggregate.addMean(builder, mean);
        UIntAggregate.addCount(builder, count);
        UIntAggregate.addSum(builder, sum);
        UIntAggregate.addVariance(builder, variance);
        UIntAggregate.addHisto(builder, histoOffset);
        return UIntAggregate.endUIntAggregate(builder);
    }
    unpack() {
        return new UIntAggregateT(this.min(), this.max(), this.mean(), this.count(), this.sum(), this.variance(), this.bb.createObjList(this.histo.bind(this), this.histoLength()));
    }
    unpackTo(_o) {
        _o.min = this.min();
        _o.max = this.max();
        _o.mean = this.mean();
        _o.count = this.count();
        _o.sum = this.sum();
        _o.variance = this.variance();
        _o.histo = this.bb.createObjList(this.histo.bind(this), this.histoLength());
    }
}
export class UIntAggregateT {
    min;
    max;
    mean;
    count;
    sum;
    variance;
    histo;
    constructor(min = BigInt('0'), max = BigInt('0'), mean = BigInt('0'), count = BigInt('0'), sum = BigInt('0'), variance = BigInt('0'), histo = []) {
        this.min = min;
        this.max = max;
        this.mean = mean;
        this.count = count;
        this.sum = sum;
        this.variance = variance;
        this.histo = histo;
    }
    pack(builder) {
        const histo = builder.createStructOffsetList(this.histo, UIntAggregate.startHistoVector);
        return UIntAggregate.createUIntAggregate(builder, this.min, this.max, this.mean, this.count, this.sum, this.variance, histo);
    }
}
//# sourceMappingURL=uint-aggregate.js.map