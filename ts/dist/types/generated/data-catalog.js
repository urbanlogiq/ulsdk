// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { ContentId } from './content-id';
import { ObjectId } from './object-id';
import { TablePartition, unionToTablePartition } from './table-partition';
export class DataCatalog {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsDataCatalog(bb, obj) {
        return (obj || new DataCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsDataCatalog(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new DataCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    id(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new ObjectId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    partitionType() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : TablePartition.NONE;
    }
    /**
     * The partition of the table to query; can be null.
     */
    partition(obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__union(obj, this.bb_pos + offset) : null;
    }
    revision(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new ContentId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startDataCatalog(builder) {
        builder.startObject(4);
    }
    static addId(builder, idOffset) {
        builder.addFieldOffset(0, idOffset, 0);
    }
    static addPartitionType(builder, partitionType) {
        builder.addFieldInt8(1, partitionType, TablePartition.NONE);
    }
    static addPartition(builder, partitionOffset) {
        builder.addFieldOffset(2, partitionOffset, 0);
    }
    static addRevision(builder, revisionOffset) {
        builder.addFieldOffset(3, revisionOffset, 0);
    }
    static endDataCatalog(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // id
        return offset;
    }
    unpack() {
        return new DataCatalogT((this.id() !== null ? this.id().unpack() : null), this.partitionType(), (() => {
            const temp = unionToTablePartition(this.partitionType(), this.partition.bind(this));
            if (temp === null) {
                return null;
            }
            return temp.unpack();
        })(), (this.revision() !== null ? this.revision().unpack() : null));
    }
    unpackTo(_o) {
        _o.id = (this.id() !== null ? this.id().unpack() : null);
        _o.partitionType = this.partitionType();
        _o.partition = (() => {
            const temp = unionToTablePartition(this.partitionType(), this.partition.bind(this));
            if (temp === null) {
                return null;
            }
            return temp.unpack();
        })();
        _o.revision = (this.revision() !== null ? this.revision().unpack() : null);
    }
}
export class DataCatalogT {
    id;
    partitionType;
    partition;
    revision;
    constructor(id = null, partitionType = TablePartition.NONE, partition = null, revision = null) {
        this.id = id;
        this.partitionType = partitionType;
        this.partition = partition;
        this.revision = revision;
    }
    pack(builder) {
        const id = (this.id !== null ? this.id.pack(builder) : 0);
        const partition = builder.createObjectOffset(this.partition);
        const revision = (this.revision !== null ? this.revision.pack(builder) : 0);
        DataCatalog.startDataCatalog(builder);
        DataCatalog.addId(builder, id);
        DataCatalog.addPartitionType(builder, this.partitionType);
        DataCatalog.addPartition(builder, partition);
        DataCatalog.addRevision(builder, revision);
        return DataCatalog.endDataCatalog(builder);
    }
}
//# sourceMappingURL=data-catalog.js.map