// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { Function } from './function';
import { SetExpr } from './set-expr';
import { TableSourceUnion, unionToTableSourceUnion } from './table-source-union';
export class UpdateQueryElement {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsUpdateQueryElement(bb, obj) {
        return (obj || new UpdateQueryElement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsUpdateQueryElement(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new UpdateQueryElement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    sourceType() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : TableSourceUnion.NONE;
    }
    source(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.__union(obj, this.bb_pos + offset) : null;
    }
    sets(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new SetExpr()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    setsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    filter(obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? (obj || new Function()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    static startUpdateQueryElement(builder) {
        builder.startObject(4);
    }
    static addSourceType(builder, sourceType) {
        builder.addFieldInt8(0, sourceType, TableSourceUnion.NONE);
    }
    static addSource(builder, sourceOffset) {
        builder.addFieldOffset(1, sourceOffset, 0);
    }
    static addSets(builder, setsOffset) {
        builder.addFieldOffset(2, setsOffset, 0);
    }
    static createSetsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startSetsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addFilter(builder, filterOffset) {
        builder.addFieldOffset(3, filterOffset, 0);
    }
    static endUpdateQueryElement(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 6); // source
        builder.requiredField(offset, 8); // sets
        return offset;
    }
    unpack() {
        return new UpdateQueryElementT(this.sourceType(), (() => {
            const temp = unionToTableSourceUnion(this.sourceType(), this.source.bind(this));
            if (temp === null) {
                return null;
            }
            return temp.unpack();
        })(), this.bb.createObjList(this.sets.bind(this), this.setsLength()), (this.filter() !== null ? this.filter().unpack() : null));
    }
    unpackTo(_o) {
        _o.sourceType = this.sourceType();
        _o.source = (() => {
            const temp = unionToTableSourceUnion(this.sourceType(), this.source.bind(this));
            if (temp === null) {
                return null;
            }
            return temp.unpack();
        })();
        _o.sets = this.bb.createObjList(this.sets.bind(this), this.setsLength());
        _o.filter = (this.filter() !== null ? this.filter().unpack() : null);
    }
}
export class UpdateQueryElementT {
    sourceType;
    source;
    sets;
    filter;
    constructor(sourceType = TableSourceUnion.NONE, source = null, sets = [], filter = null) {
        this.sourceType = sourceType;
        this.source = source;
        this.sets = sets;
        this.filter = filter;
    }
    pack(builder) {
        const source = builder.createObjectOffset(this.source);
        const sets = UpdateQueryElement.createSetsVector(builder, builder.createObjectOffsetList(this.sets));
        const filter = (this.filter !== null ? this.filter.pack(builder) : 0);
        UpdateQueryElement.startUpdateQueryElement(builder);
        UpdateQueryElement.addSourceType(builder, this.sourceType);
        UpdateQueryElement.addSource(builder, source);
        UpdateQueryElement.addSets(builder, sets);
        UpdateQueryElement.addFilter(builder, filter);
        return UpdateQueryElement.endUpdateQueryElement(builder);
    }
}
//# sourceMappingURL=update-query-element.js.map