// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { ObjectId } from './object-id';
import { RequestStatus } from './request-status';
export class AccessRequest {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsAccessRequest(bb, obj) {
        return (obj || new AccessRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsAccessRequest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new AccessRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    object(obj) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? (obj || new ObjectId()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    perms() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    status() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.readUint8(this.bb_pos + offset) : RequestStatus.Pending;
    }
    msg(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    requestedOwnership() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
    }
    static startAccessRequest(builder) {
        builder.startObject(5);
    }
    static addObject(builder, objectOffset) {
        builder.addFieldOffset(0, objectOffset, 0);
    }
    static addPerms(builder, perms) {
        builder.addFieldInt32(1, perms, 0);
    }
    static addStatus(builder, status) {
        builder.addFieldInt8(2, status, RequestStatus.Pending);
    }
    static addMsg(builder, msgOffset) {
        builder.addFieldOffset(3, msgOffset, 0);
    }
    static addRequestedOwnership(builder, requestedOwnership) {
        builder.addFieldInt32(4, requestedOwnership, 0);
    }
    static endAccessRequest(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // object
        return offset;
    }
    static createAccessRequest(builder, objectOffset, perms, status, msgOffset, requestedOwnership) {
        AccessRequest.startAccessRequest(builder);
        AccessRequest.addObject(builder, objectOffset);
        AccessRequest.addPerms(builder, perms);
        AccessRequest.addStatus(builder, status);
        AccessRequest.addMsg(builder, msgOffset);
        AccessRequest.addRequestedOwnership(builder, requestedOwnership);
        return AccessRequest.endAccessRequest(builder);
    }
    unpack() {
        return new AccessRequestT((this.object() !== null ? this.object().unpack() : null), this.perms(), this.status(), this.msg(), this.requestedOwnership());
    }
    unpackTo(_o) {
        _o.object = (this.object() !== null ? this.object().unpack() : null);
        _o.perms = this.perms();
        _o.status = this.status();
        _o.msg = this.msg();
        _o.requestedOwnership = this.requestedOwnership();
    }
}
export class AccessRequestT {
    object;
    perms;
    status;
    msg;
    requestedOwnership;
    constructor(object = null, perms = 0, status = RequestStatus.Pending, msg = null, requestedOwnership = 0) {
        this.object = object;
        this.perms = perms;
        this.status = status;
        this.msg = msg;
        this.requestedOwnership = requestedOwnership;
    }
    pack(builder) {
        const object = (this.object !== null ? this.object.pack(builder) : 0);
        const msg = (this.msg !== null ? builder.createString(this.msg) : 0);
        return AccessRequest.createAccessRequest(builder, object, this.perms, this.status, msg, this.requestedOwnership);
    }
}
//# sourceMappingURL=access-request.js.map