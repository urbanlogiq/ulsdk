// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT
// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { NullableUint } from './nullable-uint';
import { ObjectId } from './object-id';
export class Vector {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsVector(bb, obj) {
        return (obj || new Vector()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsVector(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new Vector()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    query(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    limit(obj) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? (obj || new NullableUint()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
    }
    /**
     * List of vectordbs to query. If this is empty, query all available vectordbs.
     */
    ids(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? (obj || new ObjectId()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
    }
    idsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    /**
     * Optionally limit the results to those with a distance value less than
     * max_distance. We treat max_distance=0 as no limit.
     */
    maxDistance() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readFloat32(this.bb_pos + offset) : 0.0;
    }
    static startVector(builder) {
        builder.startObject(4);
    }
    static addQuery(builder, queryOffset) {
        builder.addFieldOffset(0, queryOffset, 0);
    }
    static addLimit(builder, limitOffset) {
        builder.addFieldOffset(1, limitOffset, 0);
    }
    static addIds(builder, idsOffset) {
        builder.addFieldOffset(2, idsOffset, 0);
    }
    static createIdsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startIdsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMaxDistance(builder, maxDistance) {
        builder.addFieldFloat32(3, maxDistance, 0.0);
    }
    static endVector(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // query
        builder.requiredField(offset, 8); // ids
        return offset;
    }
    unpack() {
        return new VectorT(this.query(), (this.limit() !== null ? this.limit().unpack() : null), this.bb.createObjList(this.ids.bind(this), this.idsLength()), this.maxDistance());
    }
    unpackTo(_o) {
        _o.query = this.query();
        _o.limit = (this.limit() !== null ? this.limit().unpack() : null);
        _o.ids = this.bb.createObjList(this.ids.bind(this), this.idsLength());
        _o.maxDistance = this.maxDistance();
    }
}
export class VectorT {
    query;
    limit;
    ids;
    maxDistance;
    constructor(query = null, limit = null, ids = [], maxDistance = 0.0) {
        this.query = query;
        this.limit = limit;
        this.ids = ids;
        this.maxDistance = maxDistance;
    }
    pack(builder) {
        const query = (this.query !== null ? builder.createString(this.query) : 0);
        const limit = (this.limit !== null ? this.limit.pack(builder) : 0);
        const ids = Vector.createIdsVector(builder, builder.createObjectOffsetList(this.ids));
        Vector.startVector(builder);
        Vector.addQuery(builder, query);
        Vector.addLimit(builder, limit);
        Vector.addIds(builder, ids);
        Vector.addMaxDistance(builder, this.maxDistance);
        return Vector.endVector(builder);
    }
}
//# sourceMappingURL=vector.js.map