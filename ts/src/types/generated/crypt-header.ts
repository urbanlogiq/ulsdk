// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';



export class CryptHeader implements flatbuffers.IUnpackableObject<CryptHeaderT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CryptHeader {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCryptHeader(bb:flatbuffers.ByteBuffer, obj?:CryptHeader):CryptHeader {
  return (obj || new CryptHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCryptHeader(bb:flatbuffers.ByteBuffer, obj?:CryptHeader):CryptHeader {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CryptHeader()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * An ID for the key used to encrypt this particular encrypted object.
 */
kid():string|null
kid(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
kid(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

nonce(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

nonceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

nonceArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

plaintextLen():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startCryptHeader(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addKid(builder:flatbuffers.Builder, kidOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, kidOffset, 0);
}

static addNonce(builder:flatbuffers.Builder, nonceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nonceOffset, 0);
}

static createNonceVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startNonceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPlaintextLen(builder:flatbuffers.Builder, plaintextLen:number) {
  builder.addFieldInt32(2, plaintextLen, 0);
}

static endCryptHeader(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // kid
  builder.requiredField(offset, 6) // nonce
  return offset;
}

static createCryptHeader(builder:flatbuffers.Builder, kidOffset:flatbuffers.Offset, nonceOffset:flatbuffers.Offset, plaintextLen:number):flatbuffers.Offset {
  CryptHeader.startCryptHeader(builder);
  CryptHeader.addKid(builder, kidOffset);
  CryptHeader.addNonce(builder, nonceOffset);
  CryptHeader.addPlaintextLen(builder, plaintextLen);
  return CryptHeader.endCryptHeader(builder);
}

unpack(): CryptHeaderT {
  return new CryptHeaderT(
    this.kid(),
    this.bb!.createScalarList<number>(this.nonce.bind(this), this.nonceLength()),
    this.plaintextLen()
  );
}


unpackTo(_o: CryptHeaderT): void {
  _o.kid = this.kid();
  _o.nonce = this.bb!.createScalarList<number>(this.nonce.bind(this), this.nonceLength());
  _o.plaintextLen = this.plaintextLen();
}
}

export class CryptHeaderT implements flatbuffers.IGeneratedObject {
constructor(
  public kid: string|Uint8Array|null = null,
  public nonce: (number)[] = [],
  public plaintextLen: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const kid = (this.kid !== null ? builder.createString(this.kid!) : 0);
  const nonce = CryptHeader.createNonceVector(builder, this.nonce);

  return CryptHeader.createCryptHeader(builder,
    kid,
    nonce,
    this.plaintextLen
  );
}
}