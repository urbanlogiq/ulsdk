// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { ObjectId, ObjectIdT } from './object-id';
import { PermissionTy } from './permission-ty';


export class Share implements flatbuffers.IUnpackableObject<ShareT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Share {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsShare(bb:flatbuffers.ByteBuffer, obj?:Share):Share {
  return (obj || new Share()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsShare(bb:flatbuffers.ByteBuffer, obj?:Share):Share {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Share()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

object(obj?:ObjectId):ObjectId|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ObjectId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

dest():string|null
dest(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
dest(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

msg():string|null
msg(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
msg(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

oldPerms():PermissionTy {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : PermissionTy.PERM_BROWSE;
}

newPerms():PermissionTy {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : PermissionTy.PERM_BROWSE;
}

static startShare(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addObject(builder:flatbuffers.Builder, objectOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, objectOffset, 0);
}

static addDest(builder:flatbuffers.Builder, destOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, destOffset, 0);
}

static addMsg(builder:flatbuffers.Builder, msgOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, msgOffset, 0);
}

static addOldPerms(builder:flatbuffers.Builder, oldPerms:PermissionTy) {
  builder.addFieldInt32(3, oldPerms, PermissionTy.PERM_BROWSE);
}

static addNewPerms(builder:flatbuffers.Builder, newPerms:PermissionTy) {
  builder.addFieldInt32(4, newPerms, PermissionTy.PERM_BROWSE);
}

static endShare(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // object
  return offset;
}

static createShare(builder:flatbuffers.Builder, objectOffset:flatbuffers.Offset, destOffset:flatbuffers.Offset, msgOffset:flatbuffers.Offset, oldPerms:PermissionTy, newPerms:PermissionTy):flatbuffers.Offset {
  Share.startShare(builder);
  Share.addObject(builder, objectOffset);
  Share.addDest(builder, destOffset);
  Share.addMsg(builder, msgOffset);
  Share.addOldPerms(builder, oldPerms);
  Share.addNewPerms(builder, newPerms);
  return Share.endShare(builder);
}

unpack(): ShareT {
  return new ShareT(
    (this.object() !== null ? this.object()!.unpack() : null),
    this.dest(),
    this.msg(),
    this.oldPerms(),
    this.newPerms()
  );
}


unpackTo(_o: ShareT): void {
  _o.object = (this.object() !== null ? this.object()!.unpack() : null);
  _o.dest = this.dest();
  _o.msg = this.msg();
  _o.oldPerms = this.oldPerms();
  _o.newPerms = this.newPerms();
}
}

export class ShareT implements flatbuffers.IGeneratedObject {
constructor(
  public object: ObjectIdT|null = null,
  public dest: string|Uint8Array|null = null,
  public msg: string|Uint8Array|null = null,
  public oldPerms: PermissionTy = PermissionTy.PERM_BROWSE,
  public newPerms: PermissionTy = PermissionTy.PERM_BROWSE
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const object = (this.object !== null ? this.object!.pack(builder) : 0);
  const dest = (this.dest !== null ? builder.createString(this.dest!) : 0);
  const msg = (this.msg !== null ? builder.createString(this.msg!) : 0);

  return Share.createShare(builder,
    object,
    dest,
    msg,
    this.oldPerms,
    this.newPerms
  );
}
}