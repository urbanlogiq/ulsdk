// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Arrow, ArrowT } from './arrow';
import { DataCatalog, DataCatalogT } from './data-catalog';
import { Drive, DriveT } from './drive';
import { Function, FunctionT } from './function';
import { GraphQuery, GraphQueryT } from './graph-query';
import { Join, JoinT } from './join';
import { Placeholder, PlaceholderT } from './placeholder';
import { QueryTableSource, QueryTableSourceT } from './query-table-source';
import { SetExpr, SetExprT } from './set-expr';
import { TableSource, TableSourceT } from './table-source';
import { TableSourceUnion, unionToTableSourceUnion, unionListToTableSourceUnion } from './table-source-union';
import { Values, ValuesT } from './values';
import { Vector, VectorT } from './vector';


export class UpdateQueryElement implements flatbuffers.IUnpackableObject<UpdateQueryElementT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UpdateQueryElement {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUpdateQueryElement(bb:flatbuffers.ByteBuffer, obj?:UpdateQueryElement):UpdateQueryElement {
  return (obj || new UpdateQueryElement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUpdateQueryElement(bb:flatbuffers.ByteBuffer, obj?:UpdateQueryElement):UpdateQueryElement {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UpdateQueryElement()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

sourceType():TableSourceUnion {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : TableSourceUnion.NONE;
}

source<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

sets(index: number, obj?:SetExpr):SetExpr|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SetExpr()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

setsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

filter(obj?:Function):Function|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Function()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Additional table sources from a FROM clause (UPDATE ... FROM ... syntax).
 * Source indexes: target table is implicitly at index 0, from_sources start at index 1.
 */
fromSources(index: number, obj?:TableSource):TableSource|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new TableSource()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

fromSourcesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Joins within the FROM clause. Indexes reference the combined source list
 * (0 = target, 1+ = from_sources).
 */
joins(index: number, obj?:Join):Join|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new Join()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

joinsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUpdateQueryElement(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addSourceType(builder:flatbuffers.Builder, sourceType:TableSourceUnion) {
  builder.addFieldInt8(0, sourceType, TableSourceUnion.NONE);
}

static addSource(builder:flatbuffers.Builder, sourceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, sourceOffset, 0);
}

static addSets(builder:flatbuffers.Builder, setsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, setsOffset, 0);
}

static createSetsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSetsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addFilter(builder:flatbuffers.Builder, filterOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, filterOffset, 0);
}

static addFromSources(builder:flatbuffers.Builder, fromSourcesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, fromSourcesOffset, 0);
}

static createFromSourcesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFromSourcesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addJoins(builder:flatbuffers.Builder, joinsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, joinsOffset, 0);
}

static createJoinsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startJoinsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUpdateQueryElement(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // source
  builder.requiredField(offset, 8) // sets
  return offset;
}


unpack(): UpdateQueryElementT {
  return new UpdateQueryElementT(
    this.sourceType(),
    (() => {
      const temp = unionToTableSourceUnion(this.sourceType(), this.source.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })(),
    this.bb!.createObjList<SetExpr, SetExprT>(this.sets.bind(this), this.setsLength()),
    (this.filter() !== null ? this.filter()!.unpack() : null),
    this.bb!.createObjList<TableSource, TableSourceT>(this.fromSources.bind(this), this.fromSourcesLength()),
    this.bb!.createObjList<Join, JoinT>(this.joins.bind(this), this.joinsLength())
  );
}


unpackTo(_o: UpdateQueryElementT): void {
  _o.sourceType = this.sourceType();
  _o.source = (() => {
      const temp = unionToTableSourceUnion(this.sourceType(), this.source.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
  _o.sets = this.bb!.createObjList<SetExpr, SetExprT>(this.sets.bind(this), this.setsLength());
  _o.filter = (this.filter() !== null ? this.filter()!.unpack() : null);
  _o.fromSources = this.bb!.createObjList<TableSource, TableSourceT>(this.fromSources.bind(this), this.fromSourcesLength());
  _o.joins = this.bb!.createObjList<Join, JoinT>(this.joins.bind(this), this.joinsLength());
}
}

export class UpdateQueryElementT implements flatbuffers.IGeneratedObject {
constructor(
  public sourceType: TableSourceUnion = TableSourceUnion.NONE,
  public source: ArrowT|DataCatalogT|DriveT|GraphQueryT|PlaceholderT|QueryTableSourceT|ValuesT|VectorT|null = null,
  public sets: (SetExprT)[] = [],
  public filter: FunctionT|null = null,
  public fromSources: (TableSourceT)[] = [],
  public joins: (JoinT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const source = builder.createObjectOffset(this.source);
  const sets = UpdateQueryElement.createSetsVector(builder, builder.createObjectOffsetList(this.sets));
  const filter = (this.filter !== null ? this.filter!.pack(builder) : 0);
  const fromSources = UpdateQueryElement.createFromSourcesVector(builder, builder.createObjectOffsetList(this.fromSources));
  const joins = UpdateQueryElement.createJoinsVector(builder, builder.createObjectOffsetList(this.joins));

  UpdateQueryElement.startUpdateQueryElement(builder);
  UpdateQueryElement.addSourceType(builder, this.sourceType);
  UpdateQueryElement.addSource(builder, source);
  UpdateQueryElement.addSets(builder, sets);
  UpdateQueryElement.addFilter(builder, filter);
  UpdateQueryElement.addFromSources(builder, fromSources);
  UpdateQueryElement.addJoins(builder, joins);

  return UpdateQueryElement.endUpdateQueryElement(builder);
}
}