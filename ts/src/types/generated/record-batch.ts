// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { BodyCompression, BodyCompressionT } from './body-compression';
import { Buffer, BufferT } from './buffer';
import { FieldNode, FieldNodeT } from './field-node';


/**
 * A data header describing the shared memory layout of a "record" or "row"
 * batch. Some systems call this a "row batch" internally and others a "record
 * batch".
 */
export class RecordBatch implements flatbuffers.IUnpackableObject<RecordBatchT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RecordBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRecordBatch(bb:flatbuffers.ByteBuffer, obj?:RecordBatch):RecordBatch {
  return (obj || new RecordBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRecordBatch(bb:flatbuffers.ByteBuffer, obj?:RecordBatch):RecordBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RecordBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * number of records / rows. The arrays in the batch should all have this
 * length
 */
length():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

/**
 * Nodes correspond to the pre-ordered flattened logical schema
 */
nodes(index: number, obj?:FieldNode):FieldNode|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new FieldNode()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 16, this.bb!) : null;
}

nodesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Buffers correspond to the pre-ordered flattened buffer tree
 *
 * The number of buffers appended to this list depends on the schema. For
 * example, most primitive arrays will have 2 buffers, 1 for the validity
 * bitmap and 1 for the values. For struct arrays, there will only be a
 * single buffer for the validity (nulls) bitmap
 */
buffers(index: number, obj?:Buffer):Buffer|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Buffer()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 16, this.bb!) : null;
}

buffersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Optional compression of the message body
 */
compression(obj?:BodyCompression):BodyCompression|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new BodyCompression()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

/**
 * Some types such as Utf8View are represented using a variable number of buffers.
 * For each such Field in the pre-ordered flattened logical schema, there will be
 * an entry in variadicBufferCounts to indicate the number of number of variadic
 * buffers which belong to that Field in the current RecordBatch.
 *
 * For example, the schema
 *     col1: Struct<alpha: Int32, beta: BinaryView, gamma: Float64>
 *     col2: Utf8View
 * contains two Fields with variadic buffers so variadicBufferCounts will have
 * two entries, the first counting the variadic buffers of `col1.beta` and the
 * second counting `col2`'s.
 *
 * This field may be omitted if and only if the schema contains no Fields with
 * a variable number of buffers, such as BinaryView and Utf8View.
 */
variadicBufferCounts(index: number):bigint|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : BigInt(0);
}

variadicBufferCountsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startRecordBatch(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addLength(builder:flatbuffers.Builder, length:bigint) {
  builder.addFieldInt64(0, length, BigInt('0'));
}

static addNodes(builder:flatbuffers.Builder, nodesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nodesOffset, 0);
}

static startNodesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(16, numElems, 8);
}

static addBuffers(builder:flatbuffers.Builder, buffersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, buffersOffset, 0);
}

static startBuffersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(16, numElems, 8);
}

static addCompression(builder:flatbuffers.Builder, compressionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, compressionOffset, 0);
}

static addVariadicBufferCounts(builder:flatbuffers.Builder, variadicBufferCountsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, variadicBufferCountsOffset, 0);
}

static createVariadicBufferCountsVector(builder:flatbuffers.Builder, data:bigint[]):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt64(data[i]!);
  }
  return builder.endVector();
}

static startVariadicBufferCountsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endRecordBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): RecordBatchT {
  return new RecordBatchT(
    this.length(),
    this.bb!.createObjList<FieldNode, FieldNodeT>(this.nodes.bind(this), this.nodesLength()),
    this.bb!.createObjList<Buffer, BufferT>(this.buffers.bind(this), this.buffersLength()),
    (this.compression() !== null ? this.compression()!.unpack() : null),
    this.bb!.createScalarList<bigint>(this.variadicBufferCounts.bind(this), this.variadicBufferCountsLength())
  );
}


unpackTo(_o: RecordBatchT): void {
  _o.length = this.length();
  _o.nodes = this.bb!.createObjList<FieldNode, FieldNodeT>(this.nodes.bind(this), this.nodesLength());
  _o.buffers = this.bb!.createObjList<Buffer, BufferT>(this.buffers.bind(this), this.buffersLength());
  _o.compression = (this.compression() !== null ? this.compression()!.unpack() : null);
  _o.variadicBufferCounts = this.bb!.createScalarList<bigint>(this.variadicBufferCounts.bind(this), this.variadicBufferCountsLength());
}
}

export class RecordBatchT implements flatbuffers.IGeneratedObject {
constructor(
  public length: bigint = BigInt('0'),
  public nodes: (FieldNodeT)[] = [],
  public buffers: (BufferT)[] = [],
  public compression: BodyCompressionT|null = null,
  public variadicBufferCounts: (bigint)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const nodes = builder.createStructOffsetList(this.nodes, RecordBatch.startNodesVector);
  const buffers = builder.createStructOffsetList(this.buffers, RecordBatch.startBuffersVector);
  const compression = (this.compression !== null ? this.compression!.pack(builder) : 0);
  const variadicBufferCounts = RecordBatch.createVariadicBufferCountsVector(builder, this.variadicBufferCounts);

  RecordBatch.startRecordBatch(builder);
  RecordBatch.addLength(builder, this.length);
  RecordBatch.addNodes(builder, nodes);
  RecordBatch.addBuffers(builder, buffers);
  RecordBatch.addCompression(builder, compression);
  RecordBatch.addVariadicBufferCounts(builder, variadicBufferCounts);

  return RecordBatch.endRecordBatch(builder);
}
}