// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { B2cId, B2cIdT } from './b2c-id';
import { Slot, SlotT } from './slot';


/**
 * This Directory table holds the entries in the actual directory
 */
export class Directory implements flatbuffers.IUnpackableObject<DirectoryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Directory {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDirectory(bb:flatbuffers.ByteBuffer, obj?:Directory):Directory {
  return (obj || new Directory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDirectory(bb:flatbuffers.ByteBuffer, obj?:Directory):Directory {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Directory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

slots(index: number, obj?:Slot):Slot|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Slot()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

slotsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

notifications(index: number, obj?:B2cId):B2cId|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new B2cId()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

notificationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDirectory(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addSlots(builder:flatbuffers.Builder, slotsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, slotsOffset, 0);
}

static createSlotsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSlotsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addNotifications(builder:flatbuffers.Builder, notificationsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, notificationsOffset, 0);
}

static createNotificationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startNotificationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDirectory(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // slots
  return offset;
}

static createDirectory(builder:flatbuffers.Builder, slotsOffset:flatbuffers.Offset, notificationsOffset:flatbuffers.Offset):flatbuffers.Offset {
  Directory.startDirectory(builder);
  Directory.addSlots(builder, slotsOffset);
  Directory.addNotifications(builder, notificationsOffset);
  return Directory.endDirectory(builder);
}

unpack(): DirectoryT {
  return new DirectoryT(
    this.bb!.createObjList<Slot, SlotT>(this.slots.bind(this), this.slotsLength()),
    this.bb!.createObjList<B2cId, B2cIdT>(this.notifications.bind(this), this.notificationsLength())
  );
}


unpackTo(_o: DirectoryT): void {
  _o.slots = this.bb!.createObjList<Slot, SlotT>(this.slots.bind(this), this.slotsLength());
  _o.notifications = this.bb!.createObjList<B2cId, B2cIdT>(this.notifications.bind(this), this.notificationsLength());
}
}

export class DirectoryT implements flatbuffers.IGeneratedObject {
constructor(
  public slots: (SlotT)[] = [],
  public notifications: (B2cIdT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const slots = Directory.createSlotsVector(builder, builder.createObjectOffsetList(this.slots));
  const notifications = Directory.createNotificationsVector(builder, builder.createObjectOffsetList(this.notifications));

  return Directory.createDirectory(builder,
    slots,
    notifications
  );
}
}