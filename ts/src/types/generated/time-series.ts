// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';



/**
 * Synthetic timestamp series used to fill empty time buckets in metric queries.
 * Emits one row per bucket between `start_ms` and `end_ms`
 * (inclusive) at `interval_ms` spacing, in a single column named `output_column`.
 * All bounds are UTC unix milliseconds - to align with the
 * time_bucket UDF, callers should pass values that are already snapped to a
 * bucket boundary in the desired zone.
 * Timezone-aware generation is not yet supported.
 *
 * If `infer_bounds` is true, `start_ms` and `end_ms` are ignored and the
 * catalog planner resolves them from the WHERE filter of whichever source
 * this series is LEFT JOINed against. The joined source must produce the
 * join column via `time_bucket(width, ts)` or
 * `date_trunc(unit, [at_timezone(]ts[, tz)])` and have a closed range
 * filter on `ts`; otherwise planning errors.
 *
 * Exactly one of `interval_ms` / `interval_months` is non-zero (or both
 * zero when `infer_bounds=true` — the planner fills in whichever flavour
 * the joined source's bucket function dictates). `interval_months` is the
 * calendar-month count used to step the synthetic series; years are
 * represented as 12N months.
 *
 * `tz` is the IANA timezone name (e.g. `"America/Los_Angeles"`) used to
 * floor bounds and step the series in local calendar time. Empty means
 * UTC. This is only set when the joined source's bucket expression wraps
 * the timestamp in `at_timezone(ts, '<tz>')` — bare `date_trunc(unit, ts)`
 * stays UTC. When `tz` is non-empty, the catalog emits the series as a
 * precomputed VALUES list rather than a DataFusion `generate_series` call
 * so the local-calendar boundaries (including DST transitions) are
 * authoritative at plan time.
 */
export class TimeSeries implements flatbuffers.IUnpackableObject<TimeSeriesT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TimeSeries {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTimeSeries(bb:flatbuffers.ByteBuffer, obj?:TimeSeries):TimeSeries {
  return (obj || new TimeSeries()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTimeSeries(bb:flatbuffers.ByteBuffer, obj?:TimeSeries):TimeSeries {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TimeSeries()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

startMs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

endMs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

intervalMs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

outputColumn():string|null
outputColumn(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
outputColumn(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

inferBounds():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

intervalMonths():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

tz():string|null
tz(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
tz(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startTimeSeries(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addStartMs(builder:flatbuffers.Builder, startMs:bigint) {
  builder.addFieldInt64(0, startMs, BigInt('0'));
}

static addEndMs(builder:flatbuffers.Builder, endMs:bigint) {
  builder.addFieldInt64(1, endMs, BigInt('0'));
}

static addIntervalMs(builder:flatbuffers.Builder, intervalMs:bigint) {
  builder.addFieldInt64(2, intervalMs, BigInt('0'));
}

static addOutputColumn(builder:flatbuffers.Builder, outputColumnOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, outputColumnOffset, 0);
}

static addInferBounds(builder:flatbuffers.Builder, inferBounds:boolean) {
  builder.addFieldInt8(4, +inferBounds, +false);
}

static addIntervalMonths(builder:flatbuffers.Builder, intervalMonths:number) {
  builder.addFieldInt32(5, intervalMonths, 0);
}

static addTz(builder:flatbuffers.Builder, tzOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, tzOffset, 0);
}

static endTimeSeries(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 10) // output_column
  return offset;
}

static createTimeSeries(builder:flatbuffers.Builder, startMs:bigint, endMs:bigint, intervalMs:bigint, outputColumnOffset:flatbuffers.Offset, inferBounds:boolean, intervalMonths:number, tzOffset:flatbuffers.Offset):flatbuffers.Offset {
  TimeSeries.startTimeSeries(builder);
  TimeSeries.addStartMs(builder, startMs);
  TimeSeries.addEndMs(builder, endMs);
  TimeSeries.addIntervalMs(builder, intervalMs);
  TimeSeries.addOutputColumn(builder, outputColumnOffset);
  TimeSeries.addInferBounds(builder, inferBounds);
  TimeSeries.addIntervalMonths(builder, intervalMonths);
  TimeSeries.addTz(builder, tzOffset);
  return TimeSeries.endTimeSeries(builder);
}

unpack(): TimeSeriesT {
  return new TimeSeriesT(
    this.startMs(),
    this.endMs(),
    this.intervalMs(),
    this.outputColumn(),
    this.inferBounds(),
    this.intervalMonths(),
    this.tz()
  );
}


unpackTo(_o: TimeSeriesT): void {
  _o.startMs = this.startMs();
  _o.endMs = this.endMs();
  _o.intervalMs = this.intervalMs();
  _o.outputColumn = this.outputColumn();
  _o.inferBounds = this.inferBounds();
  _o.intervalMonths = this.intervalMonths();
  _o.tz = this.tz();
}
}

export class TimeSeriesT implements flatbuffers.IGeneratedObject {
constructor(
  public startMs: bigint = BigInt('0'),
  public endMs: bigint = BigInt('0'),
  public intervalMs: bigint = BigInt('0'),
  public outputColumn: string|Uint8Array|null = null,
  public inferBounds: boolean = false,
  public intervalMonths: number = 0,
  public tz: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const outputColumn = (this.outputColumn !== null ? builder.createString(this.outputColumn!) : 0);
  const tz = (this.tz !== null ? builder.createString(this.tz!) : 0);

  return TimeSeries.createTimeSeries(builder,
    this.startMs,
    this.endMs,
    this.intervalMs,
    outputColumn,
    this.inferBounds,
    this.intervalMonths,
    tz
  );
}
}