// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Attr, AttrT } from './attr';
import { ContentId, ContentIdT } from './content-id';
import { OpEntry, OpEntryT } from './op-entry';


/**
 * A DiffStream encodes a sequence of operations that should be performed on a table.
 * The operations are applied in order to the table, i.e. the ordering of the `seq` field is significant.
 */
export class DiffStream implements flatbuffers.IUnpackableObject<DiffStreamT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DiffStream {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDiffStream(bb:flatbuffers.ByteBuffer, obj?:DiffStream):DiffStream {
  return (obj || new DiffStream()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDiffStream(bb:flatbuffers.ByteBuffer, obj?:DiffStream):DiffStream {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DiffStream()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * This is the head revision of the directory object that contains the table.
 */
base(obj?:ContentId):ContentId|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ContentId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

seq(index: number, obj?:OpEntry):OpEntry|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new OpEntry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

seqLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * We can optionally associate attributes with the diffstream.
 * When the change history of the table is retrieved, the attributes from the diffstream
 * will be accessible as the `attributes` field on the ChangeSet associated with this diffstream.
 */
attributes(index: number, obj?:Attr):Attr|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Attr()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

attributesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDiffStream(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addBase(builder:flatbuffers.Builder, baseOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, baseOffset, 0);
}

static addSeq(builder:flatbuffers.Builder, seqOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, seqOffset, 0);
}

static createSeqVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSeqVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addAttributes(builder:flatbuffers.Builder, attributesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, attributesOffset, 0);
}

static createAttributesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAttributesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDiffStream(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // base
  builder.requiredField(offset, 6) // seq
  return offset;
}

static createDiffStream(builder:flatbuffers.Builder, baseOffset:flatbuffers.Offset, seqOffset:flatbuffers.Offset, attributesOffset:flatbuffers.Offset):flatbuffers.Offset {
  DiffStream.startDiffStream(builder);
  DiffStream.addBase(builder, baseOffset);
  DiffStream.addSeq(builder, seqOffset);
  DiffStream.addAttributes(builder, attributesOffset);
  return DiffStream.endDiffStream(builder);
}

unpack(): DiffStreamT {
  return new DiffStreamT(
    (this.base() !== null ? this.base()!.unpack() : null),
    this.bb!.createObjList<OpEntry, OpEntryT>(this.seq.bind(this), this.seqLength()),
    this.bb!.createObjList<Attr, AttrT>(this.attributes.bind(this), this.attributesLength())
  );
}


unpackTo(_o: DiffStreamT): void {
  _o.base = (this.base() !== null ? this.base()!.unpack() : null);
  _o.seq = this.bb!.createObjList<OpEntry, OpEntryT>(this.seq.bind(this), this.seqLength());
  _o.attributes = this.bb!.createObjList<Attr, AttrT>(this.attributes.bind(this), this.attributesLength());
}
}

export class DiffStreamT implements flatbuffers.IGeneratedObject {
constructor(
  public base: ContentIdT|null = null,
  public seq: (OpEntryT)[] = [],
  public attributes: (AttrT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const base = (this.base !== null ? this.base!.pack(builder) : 0);
  const seq = DiffStream.createSeqVector(builder, builder.createObjectOffsetList(this.seq));
  const attributes = DiffStream.createAttributesVector(builder, builder.createObjectOffsetList(this.attributes));

  return DiffStream.createDiffStream(builder,
    base,
    seq,
    attributes
  );
}
}