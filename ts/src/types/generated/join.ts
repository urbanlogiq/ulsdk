// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { JoinTy } from './join-ty';


export class Join implements flatbuffers.IUnpackableObject<JoinT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Join {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsJoin(bb:flatbuffers.ByteBuffer, obj?:Join):Join {
  return (obj || new Join()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsJoin(bb:flatbuffers.ByteBuffer, obj?:Join):Join {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Join()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

srcIdx():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

destIdx():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

srcCol():string|null
srcCol(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
srcCol(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

destCol():string|null
destCol(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
destCol(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

ty():JoinTy {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : JoinTy.Inner;
}

static startJoin(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addSrcIdx(builder:flatbuffers.Builder, srcIdx:number) {
  builder.addFieldInt32(0, srcIdx, 0);
}

static addDestIdx(builder:flatbuffers.Builder, destIdx:number) {
  builder.addFieldInt32(1, destIdx, 0);
}

static addSrcCol(builder:flatbuffers.Builder, srcColOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, srcColOffset, 0);
}

static addDestCol(builder:flatbuffers.Builder, destColOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, destColOffset, 0);
}

static addTy(builder:flatbuffers.Builder, ty:JoinTy) {
  builder.addFieldInt8(4, ty, JoinTy.Inner);
}

static endJoin(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createJoin(builder:flatbuffers.Builder, srcIdx:number, destIdx:number, srcColOffset:flatbuffers.Offset, destColOffset:flatbuffers.Offset, ty:JoinTy):flatbuffers.Offset {
  Join.startJoin(builder);
  Join.addSrcIdx(builder, srcIdx);
  Join.addDestIdx(builder, destIdx);
  Join.addSrcCol(builder, srcColOffset);
  Join.addDestCol(builder, destColOffset);
  Join.addTy(builder, ty);
  return Join.endJoin(builder);
}

unpack(): JoinT {
  return new JoinT(
    this.srcIdx(),
    this.destIdx(),
    this.srcCol(),
    this.destCol(),
    this.ty()
  );
}


unpackTo(_o: JoinT): void {
  _o.srcIdx = this.srcIdx();
  _o.destIdx = this.destIdx();
  _o.srcCol = this.srcCol();
  _o.destCol = this.destCol();
  _o.ty = this.ty();
}
}

export class JoinT implements flatbuffers.IGeneratedObject {
constructor(
  public srcIdx: number = 0,
  public destIdx: number = 0,
  public srcCol: string|Uint8Array|null = null,
  public destCol: string|Uint8Array|null = null,
  public ty: JoinTy = JoinTy.Inner
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const srcCol = (this.srcCol !== null ? builder.createString(this.srcCol!) : 0);
  const destCol = (this.destCol !== null ? builder.createString(this.destCol!) : 0);

  return Join.createJoin(builder,
    this.srcIdx,
    this.destIdx,
    srcCol,
    destCol,
    this.ty
  );
}
}