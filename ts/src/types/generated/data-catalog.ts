// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { ContentId, ContentIdT } from './content-id';
import { MvdbPartition, MvdbPartitionT } from './mvdb-partition';
import { ObjectId, ObjectIdT } from './object-id';
import { TablePartition, unionToTablePartition, unionListToTablePartition } from './table-partition';
import { WorklogPartition, WorklogPartitionT } from './worklog-partition';


export class DataCatalog implements flatbuffers.IUnpackableObject<DataCatalogT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DataCatalog {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDataCatalog(bb:flatbuffers.ByteBuffer, obj?:DataCatalog):DataCatalog {
  return (obj || new DataCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDataCatalog(bb:flatbuffers.ByteBuffer, obj?:DataCatalog):DataCatalog {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DataCatalog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

id(obj?:ObjectId):ObjectId|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ObjectId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

partitionType():TablePartition {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : TablePartition.NONE;
}

/**
 * The partition of the table to query; can be null.
 */
partition<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

revision(obj?:ContentId):ContentId|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new ContentId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startDataCatalog(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addId(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, idOffset, 0);
}

static addPartitionType(builder:flatbuffers.Builder, partitionType:TablePartition) {
  builder.addFieldInt8(1, partitionType, TablePartition.NONE);
}

static addPartition(builder:flatbuffers.Builder, partitionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, partitionOffset, 0);
}

static addRevision(builder:flatbuffers.Builder, revisionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, revisionOffset, 0);
}

static endDataCatalog(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // id
  return offset;
}


unpack(): DataCatalogT {
  return new DataCatalogT(
    (this.id() !== null ? this.id()!.unpack() : null),
    this.partitionType(),
    (() => {
      const temp = unionToTablePartition(this.partitionType(), this.partition.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })(),
    (this.revision() !== null ? this.revision()!.unpack() : null)
  );
}


unpackTo(_o: DataCatalogT): void {
  _o.id = (this.id() !== null ? this.id()!.unpack() : null);
  _o.partitionType = this.partitionType();
  _o.partition = (() => {
      const temp = unionToTablePartition(this.partitionType(), this.partition.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
  _o.revision = (this.revision() !== null ? this.revision()!.unpack() : null);
}
}

export class DataCatalogT implements flatbuffers.IGeneratedObject {
constructor(
  public id: ObjectIdT|null = null,
  public partitionType: TablePartition = TablePartition.NONE,
  public partition: MvdbPartitionT|WorklogPartitionT|null = null,
  public revision: ContentIdT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const id = (this.id !== null ? this.id!.pack(builder) : 0);
  const partition = builder.createObjectOffset(this.partition);
  const revision = (this.revision !== null ? this.revision!.pack(builder) : 0);

  DataCatalog.startDataCatalog(builder);
  DataCatalog.addId(builder, id);
  DataCatalog.addPartitionType(builder, this.partitionType);
  DataCatalog.addPartition(builder, partition);
  DataCatalog.addRevision(builder, revision);

  return DataCatalog.endDataCatalog(builder);
}
}