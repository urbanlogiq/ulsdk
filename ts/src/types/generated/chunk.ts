// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Digest, unionToDigest, unionListToDigest } from './digest';
import { GenericId, GenericIdT } from './generic-id';
import { Sha256, Sha256T } from './sha256';


export class Chunk implements flatbuffers.IUnpackableObject<ChunkT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Chunk {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsChunk(bb:flatbuffers.ByteBuffer, obj?:Chunk):Chunk {
  return (obj || new Chunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsChunk(bb:flatbuffers.ByteBuffer, obj?:Chunk):Chunk {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Chunk()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

blob(obj?:GenericId):GenericId|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GenericId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

digestType():Digest {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : Digest.NONE;
}

digest<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

size():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startChunk(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addBlob(builder:flatbuffers.Builder, blobOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, blobOffset, 0);
}

static addDigestType(builder:flatbuffers.Builder, digestType:Digest) {
  builder.addFieldInt8(1, digestType, Digest.NONE);
}

static addDigest(builder:flatbuffers.Builder, digestOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, digestOffset, 0);
}

static addSize(builder:flatbuffers.Builder, size:bigint) {
  builder.addFieldInt64(3, size, BigInt('0'));
}

static endChunk(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // blob
  builder.requiredField(offset, 8) // digest
  return offset;
}

static createChunk(builder:flatbuffers.Builder, blobOffset:flatbuffers.Offset, digestType:Digest, digestOffset:flatbuffers.Offset, size:bigint):flatbuffers.Offset {
  Chunk.startChunk(builder);
  Chunk.addBlob(builder, blobOffset);
  Chunk.addDigestType(builder, digestType);
  Chunk.addDigest(builder, digestOffset);
  Chunk.addSize(builder, size);
  return Chunk.endChunk(builder);
}

unpack(): ChunkT {
  return new ChunkT(
    (this.blob() !== null ? this.blob()!.unpack() : null),
    this.digestType(),
    (() => {
      const temp = unionToDigest(this.digestType(), this.digest.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })(),
    this.size()
  );
}


unpackTo(_o: ChunkT): void {
  _o.blob = (this.blob() !== null ? this.blob()!.unpack() : null);
  _o.digestType = this.digestType();
  _o.digest = (() => {
      const temp = unionToDigest(this.digestType(), this.digest.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
  _o.size = this.size();
}
}

export class ChunkT implements flatbuffers.IGeneratedObject {
constructor(
  public blob: GenericIdT|null = null,
  public digestType: Digest = Digest.NONE,
  public digest: Sha256T|null = null,
  public size: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const blob = (this.blob !== null ? this.blob!.pack(builder) : 0);
  const digest = builder.createObjectOffset(this.digest);

  return Chunk.createChunk(builder,
    blob,
    this.digestType,
    digest,
    this.size
  );
}
}