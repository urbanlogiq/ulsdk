// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { EdgeTy } from './edge-ty';
import { ObjectId, ObjectIdT } from './object-id';


/**
 * For most streams with GeometrySourceType==WorldGraphGeometry, edge_path will
 * be empty and start_stream_id will be unset.
 *
 * If edge_path is empty and start_stream_id is unset we just query for nodes
 * whose stream predicate matches this stream's streamId in order to fetch the stream's geometry.
 *
 * If edge_path is non-empty and start_stream_id is set: Start at nodes with
 * streamId=start_stream_id and follow the edge_path. Retrieve geometry from
 * the last node on that path.
 *
 * If edge_path is non-empty and start_stream_id is unset: Start at nodes with
 * streamId=id of this stream and follow the edge_path to retrieve the geeometry.
 *
 * If edge_path is empty and start_stream_id is set: Just query for nodes with
 * streamId == start_stream_id.
 */
export class WorldGraphGeometry implements flatbuffers.IUnpackableObject<WorldGraphGeometryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WorldGraphGeometry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWorldGraphGeometry(bb:flatbuffers.ByteBuffer, obj?:WorldGraphGeometry):WorldGraphGeometry {
  return (obj || new WorldGraphGeometry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWorldGraphGeometry(bb:flatbuffers.ByteBuffer, obj?:WorldGraphGeometry):WorldGraphGeometry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WorldGraphGeometry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Edges to follow to reach the nodes with geometry.
 */
edgePath(index: number):EdgeTy|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

edgePathLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

edgePathArray():Int32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Int32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Stream id of the starting node in the query path for the geometry
 */
startStreamId(obj?:ObjectId):ObjectId|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new ObjectId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

static startWorldGraphGeometry(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addEdgePath(builder:flatbuffers.Builder, edgePathOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, edgePathOffset, 0);
}

static createEdgePathVector(builder:flatbuffers.Builder, data:EdgeTy[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startEdgePathVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addStartStreamId(builder:flatbuffers.Builder, startStreamIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, startStreamIdOffset, 0);
}

static endWorldGraphGeometry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // edge_path
  return offset;
}


unpack(): WorldGraphGeometryT {
  return new WorldGraphGeometryT(
    this.bb!.createScalarList<EdgeTy>(this.edgePath.bind(this), this.edgePathLength()),
    (this.startStreamId() !== null ? this.startStreamId()!.unpack() : null)
  );
}


unpackTo(_o: WorldGraphGeometryT): void {
  _o.edgePath = this.bb!.createScalarList<EdgeTy>(this.edgePath.bind(this), this.edgePathLength());
  _o.startStreamId = (this.startStreamId() !== null ? this.startStreamId()!.unpack() : null);
}
}

export class WorldGraphGeometryT implements flatbuffers.IGeneratedObject {
constructor(
  public edgePath: (EdgeTy)[] = [],
  public startStreamId: ObjectIdT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const edgePath = WorldGraphGeometry.createEdgePathVector(builder, this.edgePath);
  const startStreamId = (this.startStreamId !== null ? this.startStreamId!.pack(builder) : 0);

  WorldGraphGeometry.startWorldGraphGeometry(builder);
  WorldGraphGeometry.addEdgePath(builder, edgePath);
  WorldGraphGeometry.addStartStreamId(builder, startStreamId);

  return WorldGraphGeometry.endWorldGraphGeometry(builder);
}
}