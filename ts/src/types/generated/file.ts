// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Chunk, ChunkT } from './chunk';
import { Digest, unionToDigest, unionListToDigest } from './digest';
import { GenericId, GenericIdT } from './generic-id';
import { Sha256, Sha256T } from './sha256';
import { StorageTier } from './storage-tier';


export class File implements flatbuffers.IUnpackableObject<FileT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):File {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFile(bb:flatbuffers.ByteBuffer, obj?:File):File {
  return (obj || new File()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFile(bb:flatbuffers.ByteBuffer, obj?:File):File {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new File()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

mime():string|null
mime(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
mime(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

size():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

blob(obj?:GenericId):GenericId|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new GenericId()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

virus():string|null
virus(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
virus(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

digestType():Digest {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : Digest.NONE;
}

digest<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

account():string|null
account(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
account(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

container():string|null
container(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
container(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

chunks(index: number, obj?:Chunk):Chunk|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new Chunk()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

chunksLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

tier():StorageTier {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : StorageTier.Hot;
}

static startFile(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addMime(builder:flatbuffers.Builder, mimeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, mimeOffset, 0);
}

static addSize(builder:flatbuffers.Builder, size:bigint) {
  builder.addFieldInt64(1, size, BigInt('0'));
}

static addBlob(builder:flatbuffers.Builder, blobOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, blobOffset, 0);
}

static addVirus(builder:flatbuffers.Builder, virusOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, virusOffset, 0);
}

static addDigestType(builder:flatbuffers.Builder, digestType:Digest) {
  builder.addFieldInt8(4, digestType, Digest.NONE);
}

static addDigest(builder:flatbuffers.Builder, digestOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, digestOffset, 0);
}

static addAccount(builder:flatbuffers.Builder, accountOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, accountOffset, 0);
}

static addContainer(builder:flatbuffers.Builder, containerOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, containerOffset, 0);
}

static addChunks(builder:flatbuffers.Builder, chunksOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, chunksOffset, 0);
}

static createChunksVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startChunksVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTier(builder:flatbuffers.Builder, tier:StorageTier) {
  builder.addFieldInt8(9, tier, StorageTier.Hot);
}

static endFile(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // mime
  builder.requiredField(offset, 16) // account
  return offset;
}


unpack(): FileT {
  return new FileT(
    this.mime(),
    this.size(),
    (this.blob() !== null ? this.blob()!.unpack() : null),
    this.virus(),
    this.digestType(),
    (() => {
      const temp = unionToDigest(this.digestType(), this.digest.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })(),
    this.account(),
    this.container(),
    this.bb!.createObjList<Chunk, ChunkT>(this.chunks.bind(this), this.chunksLength()),
    this.tier()
  );
}


unpackTo(_o: FileT): void {
  _o.mime = this.mime();
  _o.size = this.size();
  _o.blob = (this.blob() !== null ? this.blob()!.unpack() : null);
  _o.virus = this.virus();
  _o.digestType = this.digestType();
  _o.digest = (() => {
      const temp = unionToDigest(this.digestType(), this.digest.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
  _o.account = this.account();
  _o.container = this.container();
  _o.chunks = this.bb!.createObjList<Chunk, ChunkT>(this.chunks.bind(this), this.chunksLength());
  _o.tier = this.tier();
}
}

export class FileT implements flatbuffers.IGeneratedObject {
constructor(
  public mime: string|Uint8Array|null = null,
  public size: bigint = BigInt('0'),
  public blob: GenericIdT|null = null,
  public virus: string|Uint8Array|null = null,
  public digestType: Digest = Digest.NONE,
  public digest: Sha256T|null = null,
  public account: string|Uint8Array|null = null,
  public container: string|Uint8Array|null = null,
  public chunks: (ChunkT)[] = [],
  public tier: StorageTier = StorageTier.Hot
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const mime = (this.mime !== null ? builder.createString(this.mime!) : 0);
  const blob = (this.blob !== null ? this.blob!.pack(builder) : 0);
  const virus = (this.virus !== null ? builder.createString(this.virus!) : 0);
  const digest = builder.createObjectOffset(this.digest);
  const account = (this.account !== null ? builder.createString(this.account!) : 0);
  const container = (this.container !== null ? builder.createString(this.container!) : 0);
  const chunks = File.createChunksVector(builder, builder.createObjectOffsetList(this.chunks));

  File.startFile(builder);
  File.addMime(builder, mime);
  File.addSize(builder, this.size);
  File.addBlob(builder, blob);
  File.addVirus(builder, virus);
  File.addDigestType(builder, this.digestType);
  File.addDigest(builder, digest);
  File.addAccount(builder, account);
  File.addContainer(builder, container);
  File.addChunks(builder, chunks);
  File.addTier(builder, this.tier);

  return File.endFile(builder);
}
}