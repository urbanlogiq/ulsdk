// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { ByteArray as FbsByteArray, ByteArrayT as FbsByteArrayT } from './generated/byte-array';
import { ContainerRef as FbsContainerRef, ContainerRefT as FbsContainerRefT } from './generated/container-ref';
import { GitRef as FbsGitRef, GitRefT as FbsGitRefT } from './generated/git-ref';
import { Layout as FbsLayout, LayoutT as FbsLayoutT } from './generated/layout';
import { ParameterFlags as FbsParameterFlags, ParameterFlagsT as FbsParameterFlagsT } from './generated/parameter-flags';
import { ParameterValue as FbsParameterValue } from './generated/parameter-value';
import { Producer as FbsProducer, ProducerT as FbsProducerT } from './generated/producer';
import { ProducerRef as FbsProducerRef } from './generated/producer-ref';
import { TileData as FbsTileData, TileDataT as FbsTileDataT } from './generated/tile-data';
import { TileSettings as FbsTileSettings, TileSettingsT as FbsTileSettingsT } from './generated/tile-settings';
import { UserSettings as FbsUserSettings, UserSettingsT as FbsUserSettingsT } from './generated/user-settings';
import { WorkLog as FbsWorkLog, WorkLogT as FbsWorkLogT } from './generated/work-log';
import { WorklogParameter as FbsWorklogParameter, WorklogParameterT as FbsWorklogParameterT } from './generated/worklog-parameter';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { Attr } from './attr';
import { Attr as FbsAttr } from './generated/attr';
import { AttributePair, BinaryYesNo, DayOfWeek, DirectionAndRoadName, DirectionAndRoadNames, DirectionTy, NamedParameter, NamedParameterFlags, OutputSchema, RoadUserTy, Source, StatisticTy, TimeGranularity, TurnTy } from './data';
import { AttributePair as FbsAttributePair } from './generated/attribute-pair';
import { BinaryYesNo as FbsBinaryYesNo } from './generated/binary-yes-no';
import { DayOfWeek as FbsDayOfWeek } from './generated/day-of-week';
import { DirectionAndRoadName as FbsDirectionAndRoadName } from './generated/direction-and-road-name';
import { DirectionAndRoadNames as FbsDirectionAndRoadNames } from './generated/direction-and-road-names';
import { DirectionTy as FbsDirectionTy } from './generated/direction-ty';
import { NamedParameter as FbsNamedParameter } from './generated/named-parameter';
import { NamedParameterFlags as FbsNamedParameterFlags } from './generated/named-parameter-flags';
import { OutputSchema as FbsOutputSchema } from './generated/output-schema';
import { RoadUserTy as FbsRoadUserTy } from './generated/road-user-ty';
import { Source as FbsSource } from './generated/source';
import { StatisticTy as FbsStatisticTy } from './generated/statistic-ty';
import { TimeGranularity as FbsTimeGranularity } from './generated/time-granularity';
import { TurnTy as FbsTurnTy } from './generated/turn-ty';
import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, PinnedObjectId, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { PinnedObjectId as FbsPinnedObjectId } from './generated/pinned-object-id';
import { StreamId as FbsStreamId } from './generated/stream-id';
import { DeprecatedRunSpec, DeprecatedTaskParameter, Edge, EmbeddedTable, Job, Node, ParamIndices, RunSpec, Schematic, Status, Task, TaskErrorTy, TaskList, TaskParameter, TaskParameterValue, TaskPriority, TaskRunFlags } from './job';
import { DeprecatedRunSpec as FbsDeprecatedRunSpec } from './generated/deprecated-run-spec';
import { DeprecatedTaskParameter as FbsDeprecatedTaskParameter } from './generated/deprecated-task-parameter';
import { Edge as FbsEdge } from './generated/edge';
import { EmbeddedTable as FbsEmbeddedTable } from './generated/embedded-table';
import { Job as FbsJob } from './generated/job';
import { Node as FbsNode } from './generated/node';
import { ParamIndices as FbsParamIndices } from './generated/param-indices';
import { RunSpec as FbsRunSpec } from './generated/run-spec';
import { Schematic as FbsSchematic } from './generated/schematic';
import { Status as FbsStatus } from './generated/status';
import { Task as FbsTask } from './generated/task';
import { TaskErrorTy as FbsTaskErrorTy } from './generated/task-error-ty';
import { TaskList as FbsTaskList } from './generated/task-list';
import { TaskParameter as FbsTaskParameter } from './generated/task-parameter';
import { TaskParameterValue as FbsTaskParameterValue } from './generated/task-parameter-value';
import { TaskPriority as FbsTaskPriority } from './generated/task-priority';
import { TaskRunFlags as FbsTaskRunFlags } from './generated/task-run-flags';
import { Point2D, Tri2D, VArray, VBool, VBytes, VChar, VF32, VF64, VFixedSizeBytes, VI16, VI32, VI64, VI8, VIsize, VNull, VPlaceholder, VStr, VTimestampMs, VTimestampMsUtc, VTimestampNs, VTimestampNsUtc, VTri2D, VU16, VU32, VU64, VU8, VUnit, VUsize, Value, ValueInstance, ValueTy } from './value';
import { Point2D as FbsPoint2D } from './generated/point2-d';
import { Tri2D as FbsTri2D } from './generated/tri2-d';
import { VArray as FbsVArray } from './generated/varray';
import { VBool as FbsVBool } from './generated/vbool';
import { VBytes as FbsVBytes } from './generated/vbytes';
import { VChar as FbsVChar } from './generated/vchar';
import { VF32 as FbsVF32 } from './generated/vf32';
import { VF64 as FbsVF64 } from './generated/vf64';
import { VFixedSizeBytes as FbsVFixedSizeBytes } from './generated/vfixed-size-bytes';
import { VI16 as FbsVI16 } from './generated/vi16';
import { VI32 as FbsVI32 } from './generated/vi32';
import { VI64 as FbsVI64 } from './generated/vi64';
import { VI8 as FbsVI8 } from './generated/vi8';
import { VIsize as FbsVIsize } from './generated/visize';
import { VNull as FbsVNull } from './generated/vnull';
import { VPlaceholder as FbsVPlaceholder } from './generated/vplaceholder';
import { VStr as FbsVStr } from './generated/vstr';
import { VTimestampMs as FbsVTimestampMs } from './generated/vtimestamp-ms';
import { VTimestampMsUtc as FbsVTimestampMsUtc } from './generated/vtimestamp-ms-utc';
import { VTimestampNs as FbsVTimestampNs } from './generated/vtimestamp-ns';
import { VTimestampNsUtc as FbsVTimestampNsUtc } from './generated/vtimestamp-ns-utc';
import { VTri2D as FbsVTri2D } from './generated/vtri2-d';
import { VU16 as FbsVU16 } from './generated/vu16';
import { VU32 as FbsVU32 } from './generated/vu32';
import { VU64 as FbsVU64 } from './generated/vu64';
import { VU8 as FbsVU8 } from './generated/vu8';
import { VUnit as FbsVUnit } from './generated/vunit';
import { VUsize as FbsVUsize } from './generated/vusize';
import { Value as FbsValue } from './generated/value';
import { ValueInstance as FbsValueInstance } from './generated/value-instance';
import { ValueTy as FbsValueTy } from './generated/value-ty';

/**
 *  In the aggregate data, there are two relevant keys relevant to the user,
 *  sum and average. This stores which to show in the chart
 */
export { AggregationTy } from './generated/aggregation-ty';

/**
 *  The type of chart to use to display the data
 */
export { ChartTypeTy } from './generated/chart-type-ty';

export class ByteArray {
  private _b!: number[] | null;

  constructor(arg?: FbsByteArray | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsByteArray.getSizePrefixedRootAsByteArray(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsByteArray) {
      this._initFromFbs(arg);
    } else {
      this._b = null;
    }
  }

  private _initFromFbs(fbs: FbsByteArray): void {
    if (fbs.bLength() > 0) {
      const arr = fbs.bArray();
      this._b = arr ? Array.from(arr) : [];
    } else {
      this._b = null;
    }
  }

  get b(): number[] | null {
    return this._b;
  }

  set b(value: number[] | null) {
    this._b = value;
  }

  toFbsT(): FbsByteArrayT {
    const t = new FbsByteArrayT();
    t.b = this._b ?? [];
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class ParameterFlags {
  private _flags!: bigint;

  constructor(arg?: FbsParameterFlags | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsParameterFlags.getSizePrefixedRootAsParameterFlags(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsParameterFlags) {
      this._initFromFbs(arg);
    } else {
      this._flags = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsParameterFlags): void {
    this._flags = fbs.flags();
  }

  get flags(): bigint {
    return this._flags;
  }

  set flags(value: bigint) {
    this._flags = value;
  }

  toFbsT(): FbsParameterFlagsT {
    const t = new FbsParameterFlagsT();
    t.flags = this._flags;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type ParameterValue = ByteArray | ObjectId | ParameterFlags | ValueInstance;

export class ContainerRef {
  private _image!: string;

  constructor(arg?: FbsContainerRef | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsContainerRef.getSizePrefixedRootAsContainerRef(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsContainerRef) {
      this._initFromFbs(arg);
    } else {
      this._image = '';
    }
  }

  private _initFromFbs(fbs: FbsContainerRef): void {
    this._image = fbs.image() ?? '';
  }

  get image(): string {
    return this._image;
  }

  set image(value: string) {
    this._image = value;
  }

  toFbsT(): FbsContainerRefT {
    const t = new FbsContainerRefT();
    t.image = this._image;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type ProducerRef = ObjectId | ContainerRef;

/**
 *  Whether do display raw numbers or percentages
 */
export { ValuesFormatTy } from './generated/values-format-ty';

export class GitRef {
  private _commitish!: string;

  private _repo!: string;

  constructor(arg?: FbsGitRef | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGitRef.getSizePrefixedRootAsGitRef(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGitRef) {
      this._initFromFbs(arg);
    } else {
      this._commitish = '';
      this._repo = '';
    }
  }

  private _initFromFbs(fbs: FbsGitRef): void {
    this._commitish = fbs.commitish() ?? '';
    this._repo = fbs.repo() ?? '';
  }

  get commitish(): string {
    return this._commitish;
  }

  set commitish(value: string) {
    this._commitish = value;
  }

  get repo(): string {
    return this._repo;
  }

  set repo(value: string) {
    this._repo = value;
  }

  toFbsT(): FbsGitRefT {
    const t = new FbsGitRefT();
    t.commitish = this._commitish;
    t.repo = this._repo;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Layout {
/**
 *  The height of the chart tile in react-grid-layout grid units
 */
  private _height!: number;

/**
 *  The width in react-grid-layout grid units
 */
  private _width!: number;

/**
 *  The x position in react-grid-layout grid units
 */
  private _x!: number;

/**
 *  The y position in react-grid-layout grid units
 */
  private _y!: number;

  constructor(arg?: FbsLayout | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsLayout.getSizePrefixedRootAsLayout(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsLayout) {
      this._initFromFbs(arg);
    } else {
      this._height = 0;
      this._width = 0;
      this._x = 0;
      this._y = 0;
    }
  }

  private _initFromFbs(fbs: FbsLayout): void {
    this._height = fbs.height();
    this._width = fbs.width();
    this._x = fbs.x();
    this._y = fbs.y();
  }

  get height(): number {
    return this._height;
  }

  set height(value: number) {
    this._height = value;
  }

  get width(): number {
    return this._width;
  }

  set width(value: number) {
    this._width = value;
  }

  get x(): number {
    return this._x;
  }

  set x(value: number) {
    this._x = value;
  }

  get y(): number {
    return this._y;
  }

  set y(value: number) {
    this._y = value;
  }

  toFbsT(): FbsLayoutT {
    const t = new FbsLayoutT();
    t.height = this._height;
    t.width = this._width;
    t.x = this._x;
    t.y = this._y;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Producer {
  private _builderCodeRef!: GitRef | null;

  private _executor!: GitRef;

  private _model!: ProducerRef | null;

  constructor(arg?: FbsProducer | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsProducer.getSizePrefixedRootAsProducer(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsProducer) {
      this._initFromFbs(arg);
    } else {
      this._builderCodeRef = null;
      this._executor = new GitRef();
      this._model = null;
    }
  }

  private _initFromFbs(fbs: FbsProducer): void {
    const builderCodeRefVal = fbs.builderCodeRef();
    this._builderCodeRef = builderCodeRefVal ? new GitRef(builderCodeRefVal) : null;
    const executorVal = fbs.executor();
    this._executor = executorVal ? new GitRef(executorVal) : new GitRef();
    const modelTy = fbs.modelType();
    if (modelTy === FbsProducerRef.ObjectId) {
      const modelFbsVal = fbs.model(new FbsObjectId());
      this._model = modelFbsVal ? new ObjectId(modelFbsVal) : null;
    } else if (modelTy === FbsProducerRef.ContainerRef) {
      const modelFbsVal = fbs.model(new FbsContainerRef());
      this._model = modelFbsVal ? new ContainerRef(modelFbsVal) : null;
    } else {
      this._model = null;
    }
  }

  get builderCodeRef(): GitRef | null {
    return this._builderCodeRef;
  }

  set builderCodeRef(value: GitRef | null) {
    this._builderCodeRef = value;
  }

  get executor(): GitRef {
    return this._executor;
  }

  set executor(value: GitRef) {
    this._executor = value;
  }

  get model(): ProducerRef | null {
    return this._model;
  }

  set model(value: ProducerRef | null) {
    this._model = value;
  }

  toFbsT(): FbsProducerT {
    const t = new FbsProducerT();
    t.builderCodeRef = this._builderCodeRef ? this._builderCodeRef.toFbsT() : null;
    t.executor = this._executor.toFbsT();
    if (this._model instanceof ObjectId) {
      t.modelType = FbsProducerRef.ObjectId;
      t.model = this._model.toFbsT();
    } else if (this._model instanceof ContainerRef) {
      t.modelType = FbsProducerRef.ContainerRef;
      t.model = this._model.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class TileSettings {
/**
 *  The column of tbe aggregation dataset to use
 */
  private _aggregation!: number;

/**
 *  The category
 */
  private _category!: number;

/**
 *  What chart type to display the data as
 */
  private _chartType!: number;

/**
 *  The field name from the metadata and dataset. Note that if it is a
 *  relationshipField, it will use the displayName instead
 */
  private _fieldName!: string;

/**
 *  Whether to group the other fields under "Other" if not showing all columns
 */
  private _groupOthers!: boolean;

/**
 *  Record-count tiles report on the total number of graph nodes for the stream in the area, rather than
 *  on any specific field in that stream.
 */
  private _isRecordCountTile!: boolean;

/**
 *  Whether it is a relationship field (or a non-associated field)
 */
  private _isRelationshipField!: boolean;

/**
 *  The metadata id for the field shown
 */
  private _metadataId!: ObjectId;

/**
 *  Which output stream the report belongs to
 */
  private _outputStreamIndex!: number;

  private _recordCountStreamId!: ObjectId | null;

/**
 *  Which columns the user has selected to show. If this is a relationship field, the user
 *  can select which of the relationship fields to show. If it is nonassociated field, it's
 *  possible that they only want to show certain ranges, which would be stored here, but
 *  that isn't currently supported
 */
  private _selectedColumns!: string[];

/**
 *  Font size for text tiles
 */
  private _textTileFontSize!: number;

/**
 *  The title of the tile
 */
  private _title!: string;

/**
 *  Percentage or RawNumber
 */
  private _valuesFormat!: number;

  constructor(arg?: FbsTileSettings | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsTileSettings.getSizePrefixedRootAsTileSettings(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsTileSettings) {
      this._initFromFbs(arg);
    } else {
      this._aggregation = 0;
      this._category = 0;
      this._chartType = 0;
      this._fieldName = '';
      this._groupOthers = false;
      this._isRecordCountTile = false;
      this._isRelationshipField = false;
      this._metadataId = new ObjectId();
      this._outputStreamIndex = 0;
      this._recordCountStreamId = null;
      this._selectedColumns = [];
      this._textTileFontSize = 0;
      this._title = '';
      this._valuesFormat = 0;
    }
  }

  private _initFromFbs(fbs: FbsTileSettings): void {
    this._aggregation = fbs.aggregation();
    this._category = fbs.category();
    this._chartType = fbs.chartType();
    this._fieldName = fbs.fieldName() ?? '';
    this._groupOthers = fbs.groupOthers();
    this._isRecordCountTile = fbs.isRecordCountTile();
    this._isRelationshipField = fbs.isRelationshipField();
    const metadataIdVal = fbs.metadataId();
    this._metadataId = metadataIdVal ? new ObjectId(metadataIdVal) : new ObjectId();
    this._outputStreamIndex = fbs.outputStreamIndex();
    const recordCountStreamIdVal = fbs.recordCountStreamId();
    this._recordCountStreamId = recordCountStreamIdVal ? new ObjectId(recordCountStreamIdVal) : null;
    this._selectedColumns = Array.from({ length: fbs.selectedColumnsLength() }, (_, i) => fbs.selectedColumns(i) ?? '');
    this._textTileFontSize = fbs.textTileFontSize();
    this._title = fbs.title() ?? '';
    this._valuesFormat = fbs.valuesFormat();
  }

  get aggregation(): number {
    return this._aggregation;
  }

  set aggregation(value: number) {
    this._aggregation = value;
  }

  get category(): number {
    return this._category;
  }

  set category(value: number) {
    this._category = value;
  }

  get chartType(): number {
    return this._chartType;
  }

  set chartType(value: number) {
    this._chartType = value;
  }

  get fieldName(): string {
    return this._fieldName;
  }

  set fieldName(value: string) {
    this._fieldName = value;
  }

  get groupOthers(): boolean {
    return this._groupOthers;
  }

  set groupOthers(value: boolean) {
    this._groupOthers = value;
  }

  get isRecordCountTile(): boolean {
    return this._isRecordCountTile;
  }

  set isRecordCountTile(value: boolean) {
    this._isRecordCountTile = value;
  }

  get isRelationshipField(): boolean {
    return this._isRelationshipField;
  }

  set isRelationshipField(value: boolean) {
    this._isRelationshipField = value;
  }

  get metadataId(): ObjectId {
    return this._metadataId;
  }

  set metadataId(value: ObjectId) {
    this._metadataId = value;
  }

  get outputStreamIndex(): number {
    return this._outputStreamIndex;
  }

  set outputStreamIndex(value: number) {
    this._outputStreamIndex = value;
  }

  get recordCountStreamId(): ObjectId | null {
    return this._recordCountStreamId;
  }

  set recordCountStreamId(value: ObjectId | null) {
    this._recordCountStreamId = value;
  }

  get selectedColumns(): string[] {
    return this._selectedColumns;
  }

  set selectedColumns(value: string[]) {
    this._selectedColumns = value;
  }

  get textTileFontSize(): number {
    return this._textTileFontSize;
  }

  set textTileFontSize(value: number) {
    this._textTileFontSize = value;
  }

  get title(): string {
    return this._title;
  }

  set title(value: string) {
    this._title = value;
  }

  get valuesFormat(): number {
    return this._valuesFormat;
  }

  set valuesFormat(value: number) {
    this._valuesFormat = value;
  }

  toFbsT(): FbsTileSettingsT {
    const t = new FbsTileSettingsT();
    t.aggregation = this._aggregation;
    t.category = this._category;
    t.chartType = this._chartType;
    t.fieldName = this._fieldName;
    t.groupOthers = this._groupOthers;
    t.isRecordCountTile = this._isRecordCountTile;
    t.isRelationshipField = this._isRelationshipField;
    t.metadataId = this._metadataId.toFbsT();
    t.outputStreamIndex = this._outputStreamIndex;
    t.recordCountStreamId = this._recordCountStreamId ? this._recordCountStreamId.toFbsT() : null;
    t.selectedColumns = this._selectedColumns;
    t.textTileFontSize = this._textTileFontSize;
    t.title = this._title;
    t.valuesFormat = this._valuesFormat;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class TileData {
  private _layout!: Layout;

  private _tileSettings!: TileSettings;

  constructor(arg?: FbsTileData | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsTileData.getSizePrefixedRootAsTileData(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsTileData) {
      this._initFromFbs(arg);
    } else {
      this._layout = new Layout();
      this._tileSettings = new TileSettings();
    }
  }

  private _initFromFbs(fbs: FbsTileData): void {
    const layoutVal = fbs.layout();
    this._layout = layoutVal ? new Layout(layoutVal) : new Layout();
    const tileSettingsVal = fbs.tileSettings();
    this._tileSettings = tileSettingsVal ? new TileSettings(tileSettingsVal) : new TileSettings();
  }

  get layout(): Layout {
    return this._layout;
  }

  set layout(value: Layout) {
    this._layout = value;
  }

  get tileSettings(): TileSettings {
    return this._tileSettings;
  }

  set tileSettings(value: TileSettings) {
    this._tileSettings = value;
  }

  toFbsT(): FbsTileDataT {
    const t = new FbsTileDataT();
    t.layout = this._layout.toFbsT();
    t.tileSettings = this._tileSettings.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class UserSettings {
  private _isTemplate!: boolean;

  private _tileData!: TileData[];

  constructor(arg?: FbsUserSettings | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsUserSettings.getSizePrefixedRootAsUserSettings(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsUserSettings) {
      this._initFromFbs(arg);
    } else {
      this._isTemplate = false;
      this._tileData = [];
    }
  }

  private _initFromFbs(fbs: FbsUserSettings): void {
    this._isTemplate = fbs.isTemplate();
    this._tileData = Array.from({ length: fbs.tileDataLength() }, (_, i) => {
      const item = fbs.tileData(i);
      return item ? new TileData(item) : new TileData();
    });
  }

  get isTemplate(): boolean {
    return this._isTemplate;
  }

  set isTemplate(value: boolean) {
    this._isTemplate = value;
  }

  get tileData(): TileData[] {
    return this._tileData;
  }

  set tileData(value: TileData[]) {
    this._tileData = value;
  }

  toFbsT(): FbsUserSettingsT {
    const t = new FbsUserSettingsT();
    t.isTemplate = this._isTemplate;
    t.tileData = this._tileData.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class WorkLog {
/**
 *  Input streams and/or worklogs. These may be either work logs or streams.
 */
  private _inputStreams!: PinnedObjectId[] | null;

  private _jobId!: ObjectId | null;

/**
 *  A human-readable tag.
 */
  private _name!: string | null;

/**
 *  The output_streams contain a list of Parquet documents that consist of
 *  the results. These documents may expire (ie: if this is a temporary
 *  step) so there should be enough information in the worklog necessary
 *  to reconstruct these output streams.
 */
  private _outputStreams!: PinnedObjectId[];

/**
 *  These are the serialized parameters passed into the task which created
 *  this worklog.
 */
  private _params!: WorklogParameter[];

/**
 *  Worklogs can contain multiple "levels". Consider the case where the user
 *  submits a request for multiple ADT reports. We will create separate ADT
 *  reports as required but also one that ties them all together. There are a
 *  couple reasons for this; the primary is that the output of a schematic
 *  node is allocated before the job is run and before it knows how many
 *  worklogs will be generated. Another is that it makes it it easy (or
 *  easier) to organize because we can sort based on "stuff the user requested",
 *  instead of just "stuff the system generated".
 */
  private _parent!: ObjectId | null;

  private _producer!: Producer | null;

/**
 *  The schematic used behind creating the worklog. This may be empty/null
 *  if we are just layering data, for example.
 */
  private _schematic!: PinnedObjectId;

  private _userSettings!: UserSettings | null;

  constructor(arg?: FbsWorkLog | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsWorkLog.getSizePrefixedRootAsWorkLog(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsWorkLog) {
      this._initFromFbs(arg);
    } else {
      this._inputStreams = null;
      this._jobId = null;
      this._name = null;
      this._outputStreams = [];
      this._params = [];
      this._parent = null;
      this._producer = null;
      this._schematic = new PinnedObjectId();
      this._userSettings = null;
    }
  }

  private _initFromFbs(fbs: FbsWorkLog): void {
    if (fbs.inputStreamsLength() > 0) {
      this._inputStreams = Array.from({ length: fbs.inputStreamsLength() }, (_, i) => {
        const item = fbs.inputStreams(i);
        return item ? new PinnedObjectId(item) : new PinnedObjectId();
      });
    } else {
      this._inputStreams = null;
    }
    const jobIdVal = fbs.jobId();
    this._jobId = jobIdVal ? new ObjectId(jobIdVal) : null;
    this._name = fbs.name();
    this._outputStreams = Array.from({ length: fbs.outputStreamsLength() }, (_, i) => {
      const item = fbs.outputStreams(i);
      return item ? new PinnedObjectId(item) : new PinnedObjectId();
    });
    this._params = Array.from({ length: fbs.paramsLength() }, (_, i) => {
      const item = fbs.params(i);
      return item ? new WorklogParameter(item) : new WorklogParameter();
    });
    const parentVal = fbs.parent();
    this._parent = parentVal ? new ObjectId(parentVal) : null;
    const producerVal = fbs.producer();
    this._producer = producerVal ? new Producer(producerVal) : null;
    const schematicVal = fbs.schematic();
    this._schematic = schematicVal ? new PinnedObjectId(schematicVal) : new PinnedObjectId();
    const userSettingsVal = fbs.userSettings();
    this._userSettings = userSettingsVal ? new UserSettings(userSettingsVal) : null;
  }

  get inputStreams(): PinnedObjectId[] | null {
    return this._inputStreams;
  }

  set inputStreams(value: PinnedObjectId[] | null) {
    this._inputStreams = value;
  }

  get jobId(): ObjectId | null {
    return this._jobId;
  }

  set jobId(value: ObjectId | null) {
    this._jobId = value;
  }

  get name(): string | null {
    return this._name;
  }

  set name(value: string | null) {
    this._name = value;
  }

  get outputStreams(): PinnedObjectId[] {
    return this._outputStreams;
  }

  set outputStreams(value: PinnedObjectId[]) {
    this._outputStreams = value;
  }

  get params(): WorklogParameter[] {
    return this._params;
  }

  set params(value: WorklogParameter[]) {
    this._params = value;
  }

  get parent(): ObjectId | null {
    return this._parent;
  }

  set parent(value: ObjectId | null) {
    this._parent = value;
  }

  get producer(): Producer | null {
    return this._producer;
  }

  set producer(value: Producer | null) {
    this._producer = value;
  }

  get schematic(): PinnedObjectId {
    return this._schematic;
  }

  set schematic(value: PinnedObjectId) {
    this._schematic = value;
  }

  get userSettings(): UserSettings | null {
    return this._userSettings;
  }

  set userSettings(value: UserSettings | null) {
    this._userSettings = value;
  }

  toFbsT(): FbsWorkLogT {
    const t = new FbsWorkLogT();
    t.inputStreams = this._inputStreams ? this._inputStreams.map(item => item.toFbsT()) : [];
    t.jobId = this._jobId ? this._jobId.toFbsT() : null;
    t.name = this._name;
    t.outputStreams = this._outputStreams.map(item => item.toFbsT());
    t.params = this._params.map(item => item.toFbsT());
    t.parent = this._parent ? this._parent.toFbsT() : null;
    t.producer = this._producer ? this._producer.toFbsT() : null;
    t.schematic = this._schematic.toFbsT();
    t.userSettings = this._userSettings ? this._userSettings.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class WorklogParameter {
  private _key!: string;

  private _value!: ParameterValue | null;

  constructor(arg?: FbsWorklogParameter | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsWorklogParameter.getSizePrefixedRootAsWorklogParameter(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsWorklogParameter) {
      this._initFromFbs(arg);
    } else {
      this._key = '';
      this._value = null;
    }
  }

  private _initFromFbs(fbs: FbsWorklogParameter): void {
    this._key = fbs.key() ?? '';
    const valueTy = fbs.valueType();
    if (valueTy === FbsParameterValue.ByteArray) {
      const valueFbsVal = fbs.value(new FbsByteArray());
      this._value = valueFbsVal ? new ByteArray(valueFbsVal) : null;
    } else if (valueTy === FbsParameterValue.ObjectId) {
      const valueFbsVal = fbs.value(new FbsObjectId());
      this._value = valueFbsVal ? new ObjectId(valueFbsVal) : null;
    } else if (valueTy === FbsParameterValue.ParameterFlags) {
      const valueFbsVal = fbs.value(new FbsParameterFlags());
      this._value = valueFbsVal ? new ParameterFlags(valueFbsVal) : null;
    } else if (valueTy === FbsParameterValue.ValueInstance) {
      const valueFbsVal = fbs.value(new FbsValueInstance());
      this._value = valueFbsVal ? new ValueInstance(valueFbsVal) : null;
    } else {
      this._value = null;
    }
  }

  get key(): string {
    return this._key;
  }

  set key(value: string) {
    this._key = value;
  }

  get value(): ParameterValue | null {
    return this._value;
  }

  set value(value: ParameterValue | null) {
    this._value = value;
  }

  toFbsT(): FbsWorklogParameterT {
    const t = new FbsWorklogParameterT();
    t.key = this._key;
    if (this._value instanceof ByteArray) {
      t.valueType = FbsParameterValue.ByteArray;
      t.value = this._value.toFbsT();
    } else if (this._value instanceof ObjectId) {
      t.valueType = FbsParameterValue.ObjectId;
      t.value = this._value.toFbsT();
    } else if (this._value instanceof ParameterFlags) {
      t.valueType = FbsParameterValue.ParameterFlags;
      t.value = this._value.toFbsT();
    } else if (this._value instanceof ValueInstance) {
      t.valueType = FbsParameterValue.ValueInstance;
      t.value = this._value.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
