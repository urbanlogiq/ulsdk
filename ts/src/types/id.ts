// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { B2cId as FbsB2cId, B2cIdT as FbsB2cIdT } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId, ColumnGroupIdT as FbsColumnGroupIdT } from './generated/column-group-id';
import { ContentId as FbsContentId, ContentIdT as FbsContentIdT } from './generated/content-id';
import { DataStateId as FbsDataStateId, DataStateIdT as FbsDataStateIdT } from './generated/data-state-id';
import { GenericId as FbsGenericId, GenericIdT as FbsGenericIdT } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId, GraphNodeIdT as FbsGraphNodeIdT } from './generated/graph-node-id';
import { ObjectId as FbsObjectId, ObjectIdT as FbsObjectIdT } from './generated/object-id';
import { StreamId as FbsStreamId, StreamIdT as FbsStreamIdT } from './generated/stream-id';

export { ObjectNamespace } from './generated/object-namespace';

export class B2cId {
  private _b!: number[];

  constructor(arg?: FbsB2cId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsB2cId.getSizePrefixedRootAsB2cId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsB2cId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsB2cId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsB2cIdT {
    const t = new FbsB2cIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class ColumnGroupId {
  private _b!: number[];

  constructor(arg?: FbsColumnGroupId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsColumnGroupId.getSizePrefixedRootAsColumnGroupId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsColumnGroupId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsColumnGroupId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsColumnGroupIdT {
    const t = new FbsColumnGroupIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class ContentId {
  private _b!: number[];

  constructor(arg?: FbsContentId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsContentId.getSizePrefixedRootAsContentId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsContentId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsContentId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsContentIdT {
    const t = new FbsContentIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class DataStateId {
  private _b!: number[];

  constructor(arg?: FbsDataStateId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDataStateId.getSizePrefixedRootAsDataStateId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDataStateId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsDataStateId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsDataStateIdT {
    const t = new FbsDataStateIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class GenericId {
  private _b!: number[];

  constructor(arg?: FbsGenericId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGenericId.getSizePrefixedRootAsGenericId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGenericId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsGenericId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsGenericIdT {
    const t = new FbsGenericIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class GraphNodeId {
  private _b!: number[];

  constructor(arg?: FbsGraphNodeId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGraphNodeId.getSizePrefixedRootAsGraphNodeId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGraphNodeId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsGraphNodeId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsGraphNodeIdT {
    const t = new FbsGraphNodeIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class ObjectId {
  private _b!: number[];

  constructor(arg?: FbsObjectId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsObjectId.getSizePrefixedRootAsObjectId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsObjectId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsObjectId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsObjectIdT {
    const t = new FbsObjectIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}

export class StreamId {
  private _b!: number[];

  constructor(arg?: FbsStreamId | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsStreamId.getSizePrefixedRootAsStreamId(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsStreamId) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsStreamId): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsStreamIdT {
    const t = new FbsStreamIdT();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }

  static fromUuid(uuid: string): ReturnType<typeof Object.create> {
    const hex = uuid.replace(/-/g, '');
    const b: number[] = [];
    for (let i = 0; i < hex.length; i += 2) {
      b.push(parseInt(hex.substring(i, i + 2), 16));
    }
    const instance = new (this as any)();
    instance._b = b;
    return instance;
  }

  toUuid(): string {
    const hex = (this as any)._b.map((byte: number) => byte.toString(16).padStart(2, '0')).join('');
    return [
      hex.substring(0, 8),
      hex.substring(8, 12),
      hex.substring(12, 16),
      hex.substring(16, 20),
      hex.substring(20, 32),
    ].join('-');
  }

  toString(): string {
    return this.toUuid();
  }
}
