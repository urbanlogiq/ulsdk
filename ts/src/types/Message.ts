// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { BodyCompression as FbsBodyCompression, BodyCompressionT as FbsBodyCompressionT } from './generated/body-compression';
import { DictionaryBatch as FbsDictionaryBatch, DictionaryBatchT as FbsDictionaryBatchT } from './generated/dictionary-batch';
import { FieldNode as FbsFieldNode, FieldNodeT as FbsFieldNodeT } from './generated/field-node';
import { Message as FbsMessage, MessageT as FbsMessageT } from './generated/message';
import { MessageHeader as FbsMessageHeader } from './generated/message-header';
import { RecordBatch as FbsRecordBatch, RecordBatchT as FbsRecordBatchT } from './generated/record-batch';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { SparseMatrixCompressedAxis, SparseMatrixIndexCSX, SparseTensor, SparseTensorIndex, SparseTensorIndexCOO, SparseTensorIndexCSF } from './SparseTensor';
import { SparseMatrixCompressedAxis as FbsSparseMatrixCompressedAxis } from './generated/sparse-matrix-compressed-axis';
import { SparseMatrixIndexCSX as FbsSparseMatrixIndexCSX } from './generated/sparse-matrix-index-csx';
import { SparseTensor as FbsSparseTensor } from './generated/sparse-tensor';
import { SparseTensorIndex as FbsSparseTensorIndex } from './generated/sparse-tensor-index';
import { SparseTensorIndexCOO as FbsSparseTensorIndexCOO } from './generated/sparse-tensor-index-coo';
import { SparseTensorIndexCSF as FbsSparseTensorIndexCSF } from './generated/sparse-tensor-index-csf';
import { Tensor, TensorDim } from './Tensor';
import { Tensor as FbsTensor } from './generated/tensor';
import { TensorDim as FbsTensorDim } from './generated/tensor-dim';

/**
 *  Provided for forward compatibility in case we need to support different
 *  strategies for compressing the IPC message body (like whole-body
 *  compression rather than buffer-level) in the future
 */
export { BodyCompressionMethod } from './generated/body-compression-method';

export { CompressionType } from './generated/compression-type';

/**
 *  Optional compression for the memory buffers constituting IPC message
 *  bodies. Intended for use with RecordBatch but could be used for other
 *  message types
 */
export class BodyCompression {
/**
 *  Compressor library.
 *  For LZ4_FRAME, each compressed buffer must consist of a single frame.
 */
  private _codec!: number;

/**
 *  Indicates the way the record batch body was compressed
 */
  private _method!: number;

  constructor(arg?: FbsBodyCompression | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsBodyCompression.getSizePrefixedRootAsBodyCompression(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsBodyCompression) {
      this._initFromFbs(arg);
    } else {
      this._codec = 0;
      this._method = 0;
    }
  }

  private _initFromFbs(fbs: FbsBodyCompression): void {
    this._codec = fbs.codec();
    this._method = fbs.method();
  }

  get codec(): number {
    return this._codec;
  }

  set codec(value: number) {
    this._codec = value;
  }

  get method(): number {
    return this._method;
  }

  set method(value: number) {
    this._method = value;
  }

  toFbsT(): FbsBodyCompressionT {
    const t = new FbsBodyCompressionT();
    t.codec = this._codec;
    t.method = this._method;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  A data header describing the shared memory layout of a "record" or "row"
 *  batch. Some systems call this a "row batch" internally and others a "record
 *  batch".
 */
export class RecordBatch {
/**
 *  Buffers correspond to the pre-ordered flattened buffer tree
 *
 *  The number of buffers appended to this list depends on the schema. For
 *  example, most primitive arrays will have 2 buffers, 1 for the validity
 *  bitmap and 1 for the values. For struct arrays, there will only be a
 *  single buffer for the validity (nulls) bitmap
 */
  private _buffers!: Buffer[] | null;

/**
 *  Optional compression of the message body
 */
  private _compression!: BodyCompression | null;

/**
 *  number of records / rows. The arrays in the batch should all have this
 *  length
 */
  private _length!: bigint;

/**
 *  Nodes correspond to the pre-ordered flattened logical schema
 */
  private _nodes!: FieldNode[] | null;

/**
 *  Some types such as Utf8View are represented using a variable number of buffers.
 *  For each such Field in the pre-ordered flattened logical schema, there will be
 *  an entry in variadicBufferCounts to indicate the number of number of variadic
 *  buffers which belong to that Field in the current RecordBatch.
 *
 *  For example, the schema
 *      col1: Struct<alpha: Int32, beta: BinaryView, gamma: Float64>
 *      col2: Utf8View
 *  contains two Fields with variadic buffers so variadicBufferCounts will have
 *  two entries, the first counting the variadic buffers of `col1.beta` and the
 *  second counting `col2`'s.
 *
 *  This field may be omitted if and only if the schema contains no Fields with
 *  a variable number of buffers, such as BinaryView and Utf8View.
 */
  private _variadicBufferCounts!: bigint[] | null;

  constructor(arg?: FbsRecordBatch | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRecordBatch.getSizePrefixedRootAsRecordBatch(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRecordBatch) {
      this._initFromFbs(arg);
    } else {
      this._buffers = null;
      this._compression = null;
      this._length = BigInt(0);
      this._nodes = null;
      this._variadicBufferCounts = null;
    }
  }

  private _initFromFbs(fbs: FbsRecordBatch): void {
    if (fbs.buffersLength() > 0) {
      this._buffers = Array.from({ length: fbs.buffersLength() }, (_, i) => {
        const item = fbs.buffers(i);
        return item ? new Buffer(item) : new Buffer();
      });
    } else {
      this._buffers = null;
    }
    const compressionVal = fbs.compression();
    this._compression = compressionVal ? new BodyCompression(compressionVal) : null;
    this._length = fbs.length();
    if (fbs.nodesLength() > 0) {
      this._nodes = Array.from({ length: fbs.nodesLength() }, (_, i) => {
        const item = fbs.nodes(i);
        return item ? new FieldNode(item) : new FieldNode();
      });
    } else {
      this._nodes = null;
    }
    if (fbs.variadicBufferCountsLength() > 0) {
      this._variadicBufferCounts = Array.from({ length: fbs.variadicBufferCountsLength() }, (_, i) => fbs.variadicBufferCounts(i)!);
    } else {
      this._variadicBufferCounts = null;
    }
  }

  get buffers(): Buffer[] | null {
    return this._buffers;
  }

  set buffers(value: Buffer[] | null) {
    this._buffers = value;
  }

  get compression(): BodyCompression | null {
    return this._compression;
  }

  set compression(value: BodyCompression | null) {
    this._compression = value;
  }

  get length(): bigint {
    return this._length;
  }

  set length(value: bigint) {
    this._length = value;
  }

  get nodes(): FieldNode[] | null {
    return this._nodes;
  }

  set nodes(value: FieldNode[] | null) {
    this._nodes = value;
  }

  get variadicBufferCounts(): bigint[] | null {
    return this._variadicBufferCounts;
  }

  set variadicBufferCounts(value: bigint[] | null) {
    this._variadicBufferCounts = value;
  }

  toFbsT(): FbsRecordBatchT {
    const t = new FbsRecordBatchT();
    t.buffers = this._buffers ? this._buffers.map(item => item.toFbsT()) : [];
    t.compression = this._compression ? this._compression.toFbsT() : null;
    t.length = this._length;
    t.nodes = this._nodes ? this._nodes.map(item => item.toFbsT()) : [];
    t.variadicBufferCounts = this._variadicBufferCounts ?? [];
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  For sending dictionary encoding information. Any Field can be
 *  dictionary-encoded, but in this case none of its children may be
 *  dictionary-encoded.
 *  There is one vector / column per dictionary, but that vector / column
 *  may be spread across multiple dictionary batches by using the isDelta
 *  flag
 */
export class DictionaryBatch {
  private _data!: RecordBatch | null;

  private _id!: bigint;

/**
 *  If isDelta is true the values in the dictionary are to be appended to a
 *  dictionary with the indicated id. If isDelta is false this dictionary
 *  should replace the existing dictionary.
 */
  private _isDelta!: boolean;

  constructor(arg?: FbsDictionaryBatch | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDictionaryBatch.getSizePrefixedRootAsDictionaryBatch(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDictionaryBatch) {
      this._initFromFbs(arg);
    } else {
      this._data = null;
      this._id = BigInt(0);
      this._isDelta = false;
    }
  }

  private _initFromFbs(fbs: FbsDictionaryBatch): void {
    const dataVal = fbs.data();
    this._data = dataVal ? new RecordBatch(dataVal) : null;
    this._id = fbs.id();
    this._isDelta = fbs.isDelta();
  }

  get data(): RecordBatch | null {
    return this._data;
  }

  set data(value: RecordBatch | null) {
    this._data = value;
  }

  get id(): bigint {
    return this._id;
  }

  set id(value: bigint) {
    this._id = value;
  }

  get isDelta(): boolean {
    return this._isDelta;
  }

  set isDelta(value: boolean) {
    this._isDelta = value;
  }

  toFbsT(): FbsDictionaryBatchT {
    const t = new FbsDictionaryBatchT();
    t.data = this._data ? this._data.toFbsT() : null;
    t.id = this._id;
    t.isDelta = this._isDelta;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  ----------------------------------------------------------------------
 *  The root Message type
 *  This union enables us to easily send different message types without
 *  redundant storage, and in the future we can easily add new message types.
 *
 *  Arrow implementations do not need to implement all of the message types,
 *  which may include experimental metadata types. For maximum compatibility,
 *  it is best to send data using RecordBatch
 */
export type MessageHeader = Schema | DictionaryBatch | RecordBatch | Tensor | SparseTensor;

/**
 *  ----------------------------------------------------------------------
 *  Data structures for describing a table row batch (a collection of
 *  equal-length Arrow arrays)
 *  Metadata about a field at some level of a nested type tree (but not
 *  its children).
 *
 *  For example, a List<Int16> with values `[[1, 2, 3], null, [4], [5, 6], null]`
 *  would have {length: 5, null_count: 2} for its List node, and {length: 6,
 *  null_count: 0} for its Int16 node, as separate FieldNode structs
 */
export class FieldNode {
/**
 *  The number of value slots in the Arrow array at this level of a nested
 *  tree
 */
  private _length!: bigint;

/**
 *  The number of observed nulls. Fields with null_count == 0 may choose not
 *  to write their physical validity bitmap out as a materialized buffer,
 *  instead setting the length of the bitmap buffer to 0.
 */
  private _nullCount!: bigint;

  constructor(arg?: FbsFieldNode) {
    if (arg instanceof FbsFieldNode) {
      this._initFromFbs(arg);
    } else {
      this._length = BigInt(0);
      this._nullCount = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsFieldNode): void {
    this._length = fbs.length();
    this._nullCount = fbs.nullCount();
  }

  get length(): bigint {
    return this._length;
  }

  set length(value: bigint) {
    this._length = value;
  }

  get nullCount(): bigint {
    return this._nullCount;
  }

  set nullCount(value: bigint) {
    this._nullCount = value;
  }

  toFbsT(): FbsFieldNodeT {
    const t = new FbsFieldNodeT();
    t.length = this._length;
    t.nullCount = this._nullCount;
    return t;
  }

}

export class Message {
  private _bodyLength!: bigint;

  private _customMetadata!: KeyValue[] | null;

  private _header!: MessageHeader | null;

  private _version!: number;

  constructor(arg?: FbsMessage | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsMessage.getSizePrefixedRootAsMessage(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsMessage) {
      this._initFromFbs(arg);
    } else {
      this._bodyLength = BigInt(0);
      this._customMetadata = null;
      this._header = null;
      this._version = 0;
    }
  }

  private _initFromFbs(fbs: FbsMessage): void {
    this._bodyLength = fbs.bodyLength();
    if (fbs.customMetadataLength() > 0) {
      this._customMetadata = Array.from({ length: fbs.customMetadataLength() }, (_, i) => {
        const item = fbs.customMetadata(i);
        return item ? new KeyValue(item) : new KeyValue();
      });
    } else {
      this._customMetadata = null;
    }
    const headerTy = fbs.headerType();
    if (headerTy === FbsMessageHeader.Schema) {
      const headerFbsVal = fbs.header(new FbsSchema());
      this._header = headerFbsVal ? new Schema(headerFbsVal) : null;
    } else if (headerTy === FbsMessageHeader.DictionaryBatch) {
      const headerFbsVal = fbs.header(new FbsDictionaryBatch());
      this._header = headerFbsVal ? new DictionaryBatch(headerFbsVal) : null;
    } else if (headerTy === FbsMessageHeader.RecordBatch) {
      const headerFbsVal = fbs.header(new FbsRecordBatch());
      this._header = headerFbsVal ? new RecordBatch(headerFbsVal) : null;
    } else if (headerTy === FbsMessageHeader.Tensor) {
      const headerFbsVal = fbs.header(new FbsTensor());
      this._header = headerFbsVal ? new Tensor(headerFbsVal) : null;
    } else if (headerTy === FbsMessageHeader.SparseTensor) {
      const headerFbsVal = fbs.header(new FbsSparseTensor());
      this._header = headerFbsVal ? new SparseTensor(headerFbsVal) : null;
    } else {
      this._header = null;
    }
    this._version = fbs.version();
  }

  get bodyLength(): bigint {
    return this._bodyLength;
  }

  set bodyLength(value: bigint) {
    this._bodyLength = value;
  }

  get customMetadata(): KeyValue[] | null {
    return this._customMetadata;
  }

  set customMetadata(value: KeyValue[] | null) {
    this._customMetadata = value;
  }

  get header(): MessageHeader | null {
    return this._header;
  }

  set header(value: MessageHeader | null) {
    this._header = value;
  }

  get version(): number {
    return this._version;
  }

  set version(value: number) {
    this._version = value;
  }

  toFbsT(): FbsMessageT {
    const t = new FbsMessageT();
    t.bodyLength = this._bodyLength;
    t.customMetadata = this._customMetadata ? this._customMetadata.map(item => item.toFbsT()) : [];
    if (this._header instanceof Schema) {
      t.headerType = FbsMessageHeader.Schema;
      t.header = this._header.toFbsT();
    } else if (this._header instanceof DictionaryBatch) {
      t.headerType = FbsMessageHeader.DictionaryBatch;
      t.header = this._header.toFbsT();
    } else if (this._header instanceof RecordBatch) {
      t.headerType = FbsMessageHeader.RecordBatch;
      t.header = this._header.toFbsT();
    } else if (this._header instanceof Tensor) {
      t.headerType = FbsMessageHeader.Tensor;
      t.header = this._header.toFbsT();
    } else if (this._header instanceof SparseTensor) {
      t.headerType = FbsMessageHeader.SparseTensor;
      t.header = this._header.toFbsT();
    }
    t.version = this._version;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
