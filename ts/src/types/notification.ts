// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { AccessRequest as FbsAccessRequest, AccessRequestT as FbsAccessRequestT } from './generated/access-request';
import { DriveChange as FbsDriveChange, DriveChangeT as FbsDriveChangeT } from './generated/drive-change';
import { Inbox as FbsInbox, InboxT as FbsInboxT } from './generated/inbox';
import { InboxItem as FbsInboxItem, InboxItemT as FbsInboxItemT } from './generated/inbox-item';
import { JobComplete as FbsJobComplete, JobCompleteT as FbsJobCompleteT } from './generated/job-complete';
import { Notification as FbsNotification, NotificationT as FbsNotificationT } from './generated/notification';
import { NotificationUnion as FbsNotificationUnion } from './generated/notification-union';
import { Response as FbsResponse, ResponseT as FbsResponseT } from './generated/response';
import { Share as FbsShare, ShareT as FbsShareT } from './generated/share';
import { ShareDetails as FbsShareDetails, ShareDetailsT as FbsShareDetailsT } from './generated/share-details';

import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, PinnedObjectId, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { PinnedObjectId as FbsPinnedObjectId } from './generated/pinned-object-id';
import { StreamId as FbsStreamId } from './generated/stream-id';
import { AccessControlList, PermissionTy, Role } from './permissions';
import { AccessControlList as FbsAccessControlList } from './generated/access-control-list';
import { PermissionTy as FbsPermissionTy } from './generated/permission-ty';
import { Role as FbsRole } from './generated/role';

export { DriveAction } from './generated/drive-action';

export class Share {
  private _dest!: string | null;

  private _msg!: string | null;

  private _newPerms!: number;

  private _object!: ObjectId;

  private _oldPerms!: number;

  constructor(arg?: FbsShare | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsShare.getSizePrefixedRootAsShare(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsShare) {
      this._initFromFbs(arg);
    } else {
      this._dest = null;
      this._msg = null;
      this._newPerms = 0;
      this._object = new ObjectId();
      this._oldPerms = 0;
    }
  }

  private _initFromFbs(fbs: FbsShare): void {
    this._dest = fbs.dest();
    this._msg = fbs.msg();
    this._newPerms = fbs.newPerms();
    const objectVal = fbs.object();
    this._object = objectVal ? new ObjectId(objectVal) : new ObjectId();
    this._oldPerms = fbs.oldPerms();
  }

  get dest(): string | null {
    return this._dest;
  }

  set dest(value: string | null) {
    this._dest = value;
  }

  get msg(): string | null {
    return this._msg;
  }

  set msg(value: string | null) {
    this._msg = value;
  }

  get newPerms(): number {
    return this._newPerms;
  }

  set newPerms(value: number) {
    this._newPerms = value;
  }

  get object(): ObjectId {
    return this._object;
  }

  set object(value: ObjectId) {
    this._object = value;
  }

  get oldPerms(): number {
    return this._oldPerms;
  }

  set oldPerms(value: number) {
    this._oldPerms = value;
  }

  toFbsT(): FbsShareT {
    const t = new FbsShareT();
    t.dest = this._dest;
    t.msg = this._msg;
    t.newPerms = this._newPerms;
    t.object = this._object.toFbsT();
    t.oldPerms = this._oldPerms;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class JobComplete {
  private _job!: ObjectId;

  constructor(arg?: FbsJobComplete | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsJobComplete.getSizePrefixedRootAsJobComplete(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsJobComplete) {
      this._initFromFbs(arg);
    } else {
      this._job = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsJobComplete): void {
    const jobVal = fbs.job();
    this._job = jobVal ? new ObjectId(jobVal) : new ObjectId();
  }

  get job(): ObjectId {
    return this._job;
  }

  set job(value: ObjectId) {
    this._job = value;
  }

  toFbsT(): FbsJobCompleteT {
    const t = new FbsJobCompleteT();
    t.job = this._job.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class AccessRequest {
  private _msg!: string | null;

  private _object!: ObjectId;

  private _perms!: number;

  private _requestedOwnership!: number;

  private _status!: number;

  constructor(arg?: FbsAccessRequest | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsAccessRequest.getSizePrefixedRootAsAccessRequest(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsAccessRequest) {
      this._initFromFbs(arg);
    } else {
      this._msg = null;
      this._object = new ObjectId();
      this._perms = 0;
      this._requestedOwnership = 0;
      this._status = 0;
    }
  }

  private _initFromFbs(fbs: FbsAccessRequest): void {
    this._msg = fbs.msg();
    const objectVal = fbs.object();
    this._object = objectVal ? new ObjectId(objectVal) : new ObjectId();
    this._perms = fbs.perms();
    this._requestedOwnership = fbs.requestedOwnership();
    this._status = fbs.status();
  }

  get msg(): string | null {
    return this._msg;
  }

  set msg(value: string | null) {
    this._msg = value;
  }

  get object(): ObjectId {
    return this._object;
  }

  set object(value: ObjectId) {
    this._object = value;
  }

  get perms(): number {
    return this._perms;
  }

  set perms(value: number) {
    this._perms = value;
  }

  get requestedOwnership(): number {
    return this._requestedOwnership;
  }

  set requestedOwnership(value: number) {
    this._requestedOwnership = value;
  }

  get status(): number {
    return this._status;
  }

  set status(value: number) {
    this._status = value;
  }

  toFbsT(): FbsAccessRequestT {
    const t = new FbsAccessRequestT();
    t.msg = this._msg;
    t.object = this._object.toFbsT();
    t.perms = this._perms;
    t.requestedOwnership = this._requestedOwnership;
    t.status = this._status;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class DriveChange {
  private _action!: number;

  private _object!: ObjectId;

  private _root!: ObjectId;

  constructor(arg?: FbsDriveChange | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDriveChange.getSizePrefixedRootAsDriveChange(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDriveChange) {
      this._initFromFbs(arg);
    } else {
      this._action = 0;
      this._object = new ObjectId();
      this._root = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsDriveChange): void {
    this._action = fbs.action();
    const objectVal = fbs.object();
    this._object = objectVal ? new ObjectId(objectVal) : new ObjectId();
    const rootVal = fbs.root();
    this._root = rootVal ? new ObjectId(rootVal) : new ObjectId();
  }

  get action(): number {
    return this._action;
  }

  set action(value: number) {
    this._action = value;
  }

  get object(): ObjectId {
    return this._object;
  }

  set object(value: ObjectId) {
    this._object = value;
  }

  get root(): ObjectId {
    return this._root;
  }

  set root(value: ObjectId) {
    this._root = value;
  }

  toFbsT(): FbsDriveChangeT {
    const t = new FbsDriveChangeT();
    t.action = this._action;
    t.object = this._object.toFbsT();
    t.root = this._root.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type NotificationUnion = Share | JobComplete | AccessRequest | DriveChange;

export { ReadStatus } from './generated/read-status';

export { RequestStatus } from './generated/request-status';

export class Inbox {
  private _items!: InboxItem[];

  constructor(arg?: FbsInbox | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsInbox.getSizePrefixedRootAsInbox(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsInbox) {
      this._initFromFbs(arg);
    } else {
      this._items = [];
    }
  }

  private _initFromFbs(fbs: FbsInbox): void {
    this._items = Array.from({ length: fbs.itemsLength() }, (_, i) => {
      const item = fbs.items(i);
      return item ? new InboxItem(item) : new InboxItem();
    });
  }

  get items(): InboxItem[] {
    return this._items;
  }

  set items(value: InboxItem[]) {
    this._items = value;
  }

  toFbsT(): FbsInboxT {
    const t = new FbsInboxT();
    t.items = this._items.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class InboxItem {
  private _notification!: ObjectId;

  private _status!: number;

  private _time!: bigint;

  constructor(arg?: FbsInboxItem | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsInboxItem.getSizePrefixedRootAsInboxItem(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsInboxItem) {
      this._initFromFbs(arg);
    } else {
      this._notification = new ObjectId();
      this._status = 0;
      this._time = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsInboxItem): void {
    const notificationVal = fbs.notification();
    this._notification = notificationVal ? new ObjectId(notificationVal) : new ObjectId();
    this._status = fbs.status();
    this._time = fbs.time();
  }

  get notification(): ObjectId {
    return this._notification;
  }

  set notification(value: ObjectId) {
    this._notification = value;
  }

  get status(): number {
    return this._status;
  }

  set status(value: number) {
    this._status = value;
  }

  get time(): bigint {
    return this._time;
  }

  set time(value: bigint) {
    this._time = value;
  }

  toFbsT(): FbsInboxItemT {
    const t = new FbsInboxItemT();
    t.notification = this._notification.toFbsT();
    t.status = this._status;
    t.time = this._time;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Notification {
  private _notification!: NotificationUnion | null;

  private _sender!: B2cId | null;

  constructor(arg?: FbsNotification | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNotification.getSizePrefixedRootAsNotification(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNotification) {
      this._initFromFbs(arg);
    } else {
      this._notification = null;
      this._sender = null;
    }
  }

  private _initFromFbs(fbs: FbsNotification): void {
    const notificationTy = fbs.notificationType();
    if (notificationTy === FbsNotificationUnion.Share) {
      const notificationFbsVal = fbs.notification(new FbsShare());
      this._notification = notificationFbsVal ? new Share(notificationFbsVal) : null;
    } else if (notificationTy === FbsNotificationUnion.JobComplete) {
      const notificationFbsVal = fbs.notification(new FbsJobComplete());
      this._notification = notificationFbsVal ? new JobComplete(notificationFbsVal) : null;
    } else if (notificationTy === FbsNotificationUnion.AccessRequest) {
      const notificationFbsVal = fbs.notification(new FbsAccessRequest());
      this._notification = notificationFbsVal ? new AccessRequest(notificationFbsVal) : null;
    } else if (notificationTy === FbsNotificationUnion.DriveChange) {
      const notificationFbsVal = fbs.notification(new FbsDriveChange());
      this._notification = notificationFbsVal ? new DriveChange(notificationFbsVal) : null;
    } else {
      this._notification = null;
    }
    const senderVal = fbs.sender();
    this._sender = senderVal ? new B2cId(senderVal) : null;
  }

  get notification(): NotificationUnion | null {
    return this._notification;
  }

  set notification(value: NotificationUnion | null) {
    this._notification = value;
  }

  get sender(): B2cId | null {
    return this._sender;
  }

  set sender(value: B2cId | null) {
    this._sender = value;
  }

  toFbsT(): FbsNotificationT {
    const t = new FbsNotificationT();
    if (this._notification instanceof Share) {
      t.notificationType = FbsNotificationUnion.Share;
      t.notification = this._notification.toFbsT();
    } else if (this._notification instanceof JobComplete) {
      t.notificationType = FbsNotificationUnion.JobComplete;
      t.notification = this._notification.toFbsT();
    } else if (this._notification instanceof AccessRequest) {
      t.notificationType = FbsNotificationUnion.AccessRequest;
      t.notification = this._notification.toFbsT();
    } else if (this._notification instanceof DriveChange) {
      t.notificationType = FbsNotificationUnion.DriveChange;
      t.notification = this._notification.toFbsT();
    }
    t.sender = this._sender ? this._sender.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Response {
  private _msg!: string | null;

  constructor(arg?: FbsResponse | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsResponse.getSizePrefixedRootAsResponse(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsResponse) {
      this._initFromFbs(arg);
    } else {
      this._msg = null;
    }
  }

  private _initFromFbs(fbs: FbsResponse): void {
    this._msg = fbs.msg();
  }

  get msg(): string | null {
    return this._msg;
  }

  set msg(value: string | null) {
    this._msg = value;
  }

  toFbsT(): FbsResponseT {
    const t = new FbsResponseT();
    t.msg = this._msg;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class ShareDetails {
  private _msg!: string | null;

  private _notify!: boolean;

  constructor(arg?: FbsShareDetails | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsShareDetails.getSizePrefixedRootAsShareDetails(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsShareDetails) {
      this._initFromFbs(arg);
    } else {
      this._msg = null;
      this._notify = false;
    }
  }

  private _initFromFbs(fbs: FbsShareDetails): void {
    this._msg = fbs.msg();
    this._notify = fbs.notify();
  }

  get msg(): string | null {
    return this._msg;
  }

  set msg(value: string | null) {
    this._msg = value;
  }

  get notify(): boolean {
    return this._notify;
  }

  set notify(value: boolean) {
    this._notify = value;
  }

  toFbsT(): FbsShareDetailsT {
    const t = new FbsShareDetailsT();
    t.msg = this._msg;
    t.notify = this._notify;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
