// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { BodyCompression, DictionaryBatch, Message, RecordBatch } from '../Message';

describe('BodyCompression', () => {
  it('should default-construct', () => {
    const instance = new BodyCompression();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new BodyCompression();
    const bytes = instance.toBytes();
    const reconstituted = new BodyCompression(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('DictionaryBatch', () => {
  it('should default-construct', () => {
    const instance = new DictionaryBatch();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new DictionaryBatch();
    const bytes = instance.toBytes();
    const reconstituted = new DictionaryBatch(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Message', () => {
  it('should default-construct', () => {
    const instance = new Message();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Message();
    const bytes = instance.toBytes();
    const reconstituted = new Message(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('RecordBatch', () => {
  it('should default-construct', () => {
    const instance = new RecordBatch();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new RecordBatch();
    const bytes = instance.toBytes();
    const reconstituted = new RecordBatch(bytes);
    expect(reconstituted).toBeDefined();
  });
});
