// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { Chunk, Directory, DirectoryEntry, DirectoryList, File, ListDirectory, ListFile, ListObject, ListSlot, MoveRequest, NewLink, ObjectRef, Slot, TopLevelDirectory } from '../fs';

describe('Chunk', () => {
  it('should default-construct', () => {
    const instance = new Chunk();
    expect(instance).toBeDefined();
  });

});

describe('Directory', () => {
  it('should default-construct', () => {
    const instance = new Directory();
    expect(instance).toBeDefined();
  });

});

describe('DirectoryEntry', () => {
  it('should default-construct', () => {
    const instance = new DirectoryEntry();
    expect(instance).toBeDefined();
  });

});

describe('DirectoryList', () => {
  it('should default-construct', () => {
    const instance = new DirectoryList();
    expect(instance).toBeDefined();
  });

});

describe('File', () => {
  it('should default-construct', () => {
    const instance = new File();
    expect(instance).toBeDefined();
  });

});

describe('ListDirectory', () => {
  it('should default-construct', () => {
    const instance = new ListDirectory();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ListDirectory();
    const bytes = instance.toBytes();
    const reconstituted = new ListDirectory(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ListFile', () => {
  it('should default-construct', () => {
    const instance = new ListFile();
    expect(instance).toBeDefined();
  });

});

describe('ListObject', () => {
  it('should default-construct', () => {
    const instance = new ListObject();
    expect(instance).toBeDefined();
  });

});

describe('ListSlot', () => {
  it('should default-construct', () => {
    const instance = new ListSlot();
    expect(instance).toBeDefined();
  });

});

describe('MoveRequest', () => {
  it('should default-construct', () => {
    const instance = new MoveRequest();
    expect(instance).toBeDefined();
  });

});

describe('NewLink', () => {
  it('should default-construct', () => {
    const instance = new NewLink();
    expect(instance).toBeDefined();
  });

});

describe('ObjectRef', () => {
  it('should default-construct', () => {
    const instance = new ObjectRef();
    expect(instance).toBeDefined();
  });

});

describe('Slot', () => {
  it('should default-construct', () => {
    const instance = new Slot();
    expect(instance).toBeDefined();
  });

});

describe('TopLevelDirectory', () => {
  it('should default-construct', () => {
    const instance = new TopLevelDirectory();
    expect(instance).toBeDefined();
  });

});
