// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { EdgeList, EdgeQuery, Geom, GeomOp, GraphQuery, NodeIdPair, NodeList, NodeQuery, OrderBy, Projection, QueryPathElement } from '../graph';

describe('EdgeList', () => {
  it('should default-construct', () => {
    const instance = new EdgeList();
    expect(instance).toBeDefined();
  });

});

describe('EdgeQuery', () => {
  it('should default-construct', () => {
    const instance = new EdgeQuery();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new EdgeQuery();
    const bytes = instance.toBytes();
    const reconstituted = new EdgeQuery(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Geom', () => {
  it('should default-construct', () => {
    const instance = new Geom();
    expect(instance).toBeDefined();
  });

});

describe('GeomOp', () => {
  it('should default-construct', () => {
    const instance = new GeomOp();
    expect(instance).toBeDefined();
  });

});

describe('GraphQuery', () => {
  it('should default-construct', () => {
    const instance = new GraphQuery();
    expect(instance).toBeDefined();
  });

});

describe('NodeIdPair', () => {
  it('should default-construct', () => {
    const instance = new NodeIdPair();
    expect(instance).toBeDefined();
  });

});

describe('NodeList', () => {
  it('should default-construct', () => {
    const instance = new NodeList();
    expect(instance).toBeDefined();
  });

});

describe('NodeQuery', () => {
  it('should default-construct', () => {
    const instance = new NodeQuery();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NodeQuery();
    const bytes = instance.toBytes();
    const reconstituted = new NodeQuery(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('OrderBy', () => {
  it('should default-construct', () => {
    const instance = new OrderBy();
    expect(instance).toBeDefined();
  });

});

describe('Projection', () => {
  it('should default-construct', () => {
    const instance = new Projection();
    expect(instance).toBeDefined();
  });

});

describe('QueryPathElement', () => {
  it('should default-construct', () => {
    const instance = new QueryPathElement();
    expect(instance).toBeDefined();
  });

});
