// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { AttributePair, DirectionAndRoadName, DirectionAndRoadNames, NamedParameter, OutputSchema, Source } from '../data';

describe('AttributePair', () => {
  it('should default-construct', () => {
    const instance = new AttributePair();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new AttributePair();
    const bytes = instance.toBytes();
    const reconstituted = new AttributePair(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('DirectionAndRoadName', () => {
  it('should default-construct', () => {
    const instance = new DirectionAndRoadName();
    expect(instance).toBeDefined();
  });

});

describe('DirectionAndRoadNames', () => {
  it('should default-construct', () => {
    const instance = new DirectionAndRoadNames();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new DirectionAndRoadNames();
    const bytes = instance.toBytes();
    const reconstituted = new DirectionAndRoadNames(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NamedParameter', () => {
  it('should default-construct', () => {
    const instance = new NamedParameter();
    expect(instance).toBeDefined();
  });

});

describe('OutputSchema', () => {
  it('should default-construct', () => {
    const instance = new OutputSchema();
    expect(instance).toBeDefined();
  });

});

describe('Source', () => {
  it('should default-construct', () => {
    const instance = new Source();
    expect(instance).toBeDefined();
  });

});
