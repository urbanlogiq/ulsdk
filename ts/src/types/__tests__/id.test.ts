// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, StreamId } from '../id';

describe('B2cId', () => {
  it('should default-construct', () => {
    const instance = new B2cId();
    expect(instance).toBeDefined();
  });

});

describe('ColumnGroupId', () => {
  it('should default-construct', () => {
    const instance = new ColumnGroupId();
    expect(instance).toBeDefined();
  });

});

describe('ContentId', () => {
  it('should default-construct', () => {
    const instance = new ContentId();
    expect(instance).toBeDefined();
  });

});

describe('DataStateId', () => {
  it('should default-construct', () => {
    const instance = new DataStateId();
    expect(instance).toBeDefined();
  });

});

describe('GenericId', () => {
  it('should default-construct', () => {
    const instance = new GenericId();
    expect(instance).toBeDefined();
  });

});

describe('GraphNodeId', () => {
  it('should default-construct', () => {
    const instance = new GraphNodeId();
    expect(instance).toBeDefined();
  });

});

describe('ObjectId', () => {
  it('should default-construct', () => {
    const instance = new ObjectId();
    expect(instance).toBeDefined();
  });

});

describe('StreamId', () => {
  it('should default-construct', () => {
    const instance = new StreamId();
    expect(instance).toBeDefined();
  });

});
