// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { VArray, VBool, VBytes, VChar, VF32, VF64, VFixedSizeBytes, VI16, VI32, VI64, VI8, VIsize, VNull, VPlaceholder, VStr, VTimestampMs, VTimestampMsUtc, VTimestampNs, VTimestampNsUtc, VTri2D, VU16, VU32, VU64, VU8, VUnit, VUsize, ValueInstance } from '../value';

describe('VArray', () => {
  it('should default-construct', () => {
    const instance = new VArray();
    expect(instance).toBeDefined();
  });

});

describe('VBool', () => {
  it('should default-construct', () => {
    const instance = new VBool();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VBool();
    const bytes = instance.toBytes();
    const reconstituted = new VBool(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VBytes', () => {
  it('should default-construct', () => {
    const instance = new VBytes();
    expect(instance).toBeDefined();
  });

});

describe('VChar', () => {
  it('should default-construct', () => {
    const instance = new VChar();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VChar();
    const bytes = instance.toBytes();
    const reconstituted = new VChar(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VF32', () => {
  it('should default-construct', () => {
    const instance = new VF32();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VF32();
    const bytes = instance.toBytes();
    const reconstituted = new VF32(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VF64', () => {
  it('should default-construct', () => {
    const instance = new VF64();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VF64();
    const bytes = instance.toBytes();
    const reconstituted = new VF64(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VFixedSizeBytes', () => {
  it('should default-construct', () => {
    const instance = new VFixedSizeBytes();
    expect(instance).toBeDefined();
  });

});

describe('VI16', () => {
  it('should default-construct', () => {
    const instance = new VI16();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VI16();
    const bytes = instance.toBytes();
    const reconstituted = new VI16(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VI32', () => {
  it('should default-construct', () => {
    const instance = new VI32();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VI32();
    const bytes = instance.toBytes();
    const reconstituted = new VI32(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VI64', () => {
  it('should default-construct', () => {
    const instance = new VI64();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VI64();
    const bytes = instance.toBytes();
    const reconstituted = new VI64(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VI8', () => {
  it('should default-construct', () => {
    const instance = new VI8();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VI8();
    const bytes = instance.toBytes();
    const reconstituted = new VI8(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VIsize', () => {
  it('should default-construct', () => {
    const instance = new VIsize();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VIsize();
    const bytes = instance.toBytes();
    const reconstituted = new VIsize(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VNull', () => {
  it('should default-construct', () => {
    const instance = new VNull();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VNull();
    const bytes = instance.toBytes();
    const reconstituted = new VNull(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VPlaceholder', () => {
  it('should default-construct', () => {
    const instance = new VPlaceholder();
    expect(instance).toBeDefined();
  });

});

describe('VStr', () => {
  it('should default-construct', () => {
    const instance = new VStr();
    expect(instance).toBeDefined();
  });

});

describe('VTimestampMs', () => {
  it('should default-construct', () => {
    const instance = new VTimestampMs();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VTimestampMs();
    const bytes = instance.toBytes();
    const reconstituted = new VTimestampMs(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VTimestampMsUtc', () => {
  it('should default-construct', () => {
    const instance = new VTimestampMsUtc();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VTimestampMsUtc();
    const bytes = instance.toBytes();
    const reconstituted = new VTimestampMsUtc(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VTimestampNs', () => {
  it('should default-construct', () => {
    const instance = new VTimestampNs();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VTimestampNs();
    const bytes = instance.toBytes();
    const reconstituted = new VTimestampNs(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VTimestampNsUtc', () => {
  it('should default-construct', () => {
    const instance = new VTimestampNsUtc();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VTimestampNsUtc();
    const bytes = instance.toBytes();
    const reconstituted = new VTimestampNsUtc(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VTri2D', () => {
  it('should default-construct', () => {
    const instance = new VTri2D();
    expect(instance).toBeDefined();
  });

});

describe('VU16', () => {
  it('should default-construct', () => {
    const instance = new VU16();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VU16();
    const bytes = instance.toBytes();
    const reconstituted = new VU16(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VU32', () => {
  it('should default-construct', () => {
    const instance = new VU32();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VU32();
    const bytes = instance.toBytes();
    const reconstituted = new VU32(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VU64', () => {
  it('should default-construct', () => {
    const instance = new VU64();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VU64();
    const bytes = instance.toBytes();
    const reconstituted = new VU64(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VU8', () => {
  it('should default-construct', () => {
    const instance = new VU8();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VU8();
    const bytes = instance.toBytes();
    const reconstituted = new VU8(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VUnit', () => {
  it('should default-construct', () => {
    const instance = new VUnit();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VUnit();
    const bytes = instance.toBytes();
    const reconstituted = new VUnit(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('VUsize', () => {
  it('should default-construct', () => {
    const instance = new VUsize();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new VUsize();
    const bytes = instance.toBytes();
    const reconstituted = new VUsize(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ValueInstance', () => {
  it('should default-construct', () => {
    const instance = new ValueInstance();
    expect(instance).toBeDefined();
  });

});
