// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { GraphEdge, GraphNode, Line, MultiLine, MultiPoint, MultiPolygon, Point, Polygon } from '../entity';

describe('GraphEdge', () => {
  it('should default-construct', () => {
    const instance = new GraphEdge();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new GraphEdge();
    const bytes = instance.toBytes();
    const reconstituted = new GraphEdge(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('GraphNode', () => {
  it('should default-construct', () => {
    const instance = new GraphNode();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new GraphNode();
    const bytes = instance.toBytes();
    const reconstituted = new GraphNode(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Line', () => {
  it('should default-construct', () => {
    const instance = new Line();
    expect(instance).toBeDefined();
  });

});

describe('MultiLine', () => {
  it('should default-construct', () => {
    const instance = new MultiLine();
    expect(instance).toBeDefined();
  });

});

describe('MultiPoint', () => {
  it('should default-construct', () => {
    const instance = new MultiPoint();
    expect(instance).toBeDefined();
  });

});

describe('MultiPolygon', () => {
  it('should default-construct', () => {
    const instance = new MultiPolygon();
    expect(instance).toBeDefined();
  });

});

describe('Point', () => {
  it('should default-construct', () => {
    const instance = new Point();
    expect(instance).toBeDefined();
  });

});

describe('Polygon', () => {
  it('should default-construct', () => {
    const instance = new Polygon();
    expect(instance).toBeDefined();
  });

});
