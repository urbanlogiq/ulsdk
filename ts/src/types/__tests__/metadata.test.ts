// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { CategoryRelationshipData, ContactInfo, DatacatalogGeometry, DatasetSource, Dates, DatetimeRange, Document, Documents, FloatAggregate, FloatRange, GeometryData, HierarchicalRelationship, HierarchyRelationshipData, IntAggregate, IntRange, IntegerDisplayString, Metadata, NestedCategoryRelationshipData, NestedCategoryRelationshipNode, NestedHierarchyRelationshipData, NestedHierarchyRelationshipNode, NestedStringCategories, NestedStringCategoryNode, NoGeometry, NumericalFieldFormat, RawGeom, StringAggregate, StringCategories, TimeInterval, UIntAggregate, UlField, UlFieldRelationship, WorldGraphGeometry } from '../metadata';

describe('CategoryRelationshipData', () => {
  it('should default-construct', () => {
    const instance = new CategoryRelationshipData();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new CategoryRelationshipData();
    const bytes = instance.toBytes();
    const reconstituted = new CategoryRelationshipData(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ContactInfo', () => {
  it('should default-construct', () => {
    const instance = new ContactInfo();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ContactInfo();
    const bytes = instance.toBytes();
    const reconstituted = new ContactInfo(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('DatacatalogGeometry', () => {
  it('should default-construct', () => {
    const instance = new DatacatalogGeometry();
    expect(instance).toBeDefined();
  });

});

describe('DatasetSource', () => {
  it('should default-construct', () => {
    const instance = new DatasetSource();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new DatasetSource();
    const bytes = instance.toBytes();
    const reconstituted = new DatasetSource(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Dates', () => {
  it('should default-construct', () => {
    const instance = new Dates();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Dates();
    const bytes = instance.toBytes();
    const reconstituted = new Dates(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('DatetimeRange', () => {
  it('should default-construct', () => {
    const instance = new DatetimeRange();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new DatetimeRange();
    const bytes = instance.toBytes();
    const reconstituted = new DatetimeRange(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Document', () => {
  it('should default-construct', () => {
    const instance = new Document();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Document();
    const bytes = instance.toBytes();
    const reconstituted = new Document(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Documents', () => {
  it('should default-construct', () => {
    const instance = new Documents();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Documents();
    const bytes = instance.toBytes();
    const reconstituted = new Documents(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('FloatAggregate', () => {
  it('should default-construct', () => {
    const instance = new FloatAggregate();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new FloatAggregate();
    const bytes = instance.toBytes();
    const reconstituted = new FloatAggregate(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('FloatRange', () => {
  it('should default-construct', () => {
    const instance = new FloatRange();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new FloatRange();
    const bytes = instance.toBytes();
    const reconstituted = new FloatRange(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('GeometryData', () => {
  it('should default-construct', () => {
    const instance = new GeometryData();
    expect(instance).toBeDefined();
  });

});

describe('HierarchicalRelationship', () => {
  it('should default-construct', () => {
    const instance = new HierarchicalRelationship();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new HierarchicalRelationship();
    const bytes = instance.toBytes();
    const reconstituted = new HierarchicalRelationship(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('HierarchyRelationshipData', () => {
  it('should default-construct', () => {
    const instance = new HierarchyRelationshipData();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new HierarchyRelationshipData();
    const bytes = instance.toBytes();
    const reconstituted = new HierarchyRelationshipData(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('IntAggregate', () => {
  it('should default-construct', () => {
    const instance = new IntAggregate();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new IntAggregate();
    const bytes = instance.toBytes();
    const reconstituted = new IntAggregate(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('IntRange', () => {
  it('should default-construct', () => {
    const instance = new IntRange();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new IntRange();
    const bytes = instance.toBytes();
    const reconstituted = new IntRange(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('IntegerDisplayString', () => {
  it('should default-construct', () => {
    const instance = new IntegerDisplayString();
    expect(instance).toBeDefined();
  });

});

describe('Metadata', () => {
  it('should default-construct', () => {
    const instance = new Metadata();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Metadata();
    const bytes = instance.toBytes();
    const reconstituted = new Metadata(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NestedCategoryRelationshipData', () => {
  it('should default-construct', () => {
    const instance = new NestedCategoryRelationshipData();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NestedCategoryRelationshipData();
    const bytes = instance.toBytes();
    const reconstituted = new NestedCategoryRelationshipData(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NestedCategoryRelationshipNode', () => {
  it('should default-construct', () => {
    const instance = new NestedCategoryRelationshipNode();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NestedCategoryRelationshipNode();
    const bytes = instance.toBytes();
    const reconstituted = new NestedCategoryRelationshipNode(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NestedHierarchyRelationshipData', () => {
  it('should default-construct', () => {
    const instance = new NestedHierarchyRelationshipData();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NestedHierarchyRelationshipData();
    const bytes = instance.toBytes();
    const reconstituted = new NestedHierarchyRelationshipData(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NestedHierarchyRelationshipNode', () => {
  it('should default-construct', () => {
    const instance = new NestedHierarchyRelationshipNode();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NestedHierarchyRelationshipNode();
    const bytes = instance.toBytes();
    const reconstituted = new NestedHierarchyRelationshipNode(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NestedStringCategories', () => {
  it('should default-construct', () => {
    const instance = new NestedStringCategories();
    expect(instance).toBeDefined();
  });

});

describe('NestedStringCategoryNode', () => {
  it('should default-construct', () => {
    const instance = new NestedStringCategoryNode();
    expect(instance).toBeDefined();
  });

});

describe('NoGeometry', () => {
  it('should default-construct', () => {
    const instance = new NoGeometry();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NoGeometry();
    const bytes = instance.toBytes();
    const reconstituted = new NoGeometry(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('NumericalFieldFormat', () => {
  it('should default-construct', () => {
    const instance = new NumericalFieldFormat();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new NumericalFieldFormat();
    const bytes = instance.toBytes();
    const reconstituted = new NumericalFieldFormat(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('RawGeom', () => {
  it('should default-construct', () => {
    const instance = new RawGeom();
    expect(instance).toBeDefined();
  });

});

describe('StringAggregate', () => {
  it('should default-construct', () => {
    const instance = new StringAggregate();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new StringAggregate();
    const bytes = instance.toBytes();
    const reconstituted = new StringAggregate(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('StringCategories', () => {
  it('should default-construct', () => {
    const instance = new StringCategories();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new StringCategories();
    const bytes = instance.toBytes();
    const reconstituted = new StringCategories(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('TimeInterval', () => {
  it('should default-construct', () => {
    const instance = new TimeInterval();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new TimeInterval();
    const bytes = instance.toBytes();
    const reconstituted = new TimeInterval(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('UIntAggregate', () => {
  it('should default-construct', () => {
    const instance = new UIntAggregate();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new UIntAggregate();
    const bytes = instance.toBytes();
    const reconstituted = new UIntAggregate(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('UlField', () => {
  it('should default-construct', () => {
    const instance = new UlField();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new UlField();
    const bytes = instance.toBytes();
    const reconstituted = new UlField(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('UlFieldRelationship', () => {
  it('should default-construct', () => {
    const instance = new UlFieldRelationship();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new UlFieldRelationship();
    const bytes = instance.toBytes();
    const reconstituted = new UlFieldRelationship(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('WorldGraphGeometry', () => {
  it('should default-construct', () => {
    const instance = new WorldGraphGeometry();
    expect(instance).toBeDefined();
  });

});
