// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { CryptHeader, EncryptedObject, Sha256, Signature } from '../crypto';

describe('CryptHeader', () => {
  it('should default-construct', () => {
    const instance = new CryptHeader();
    expect(instance).toBeDefined();
  });

});

describe('EncryptedObject', () => {
  it('should default-construct', () => {
    const instance = new EncryptedObject();
    expect(instance).toBeDefined();
  });

});

describe('Sha256', () => {
  it('should default-construct', () => {
    const instance = new Sha256();
    expect(instance).toBeDefined();
  });

});

describe('Signature', () => {
  it('should default-construct', () => {
    const instance = new Signature();
    expect(instance).toBeDefined();
  });

});
