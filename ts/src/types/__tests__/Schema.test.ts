// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { Binary, BinaryView, Bool, Date, Decimal, DictionaryEncoding, Duration, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, Null, RunEndEncoded, Schema, Struct_, Time, Timestamp, Union, Utf8, Utf8View } from '../Schema';

describe('Binary', () => {
  it('should default-construct', () => {
    const instance = new Binary();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Binary();
    const bytes = instance.toBytes();
    const reconstituted = new Binary(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('BinaryView', () => {
  it('should default-construct', () => {
    const instance = new BinaryView();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new BinaryView();
    const bytes = instance.toBytes();
    const reconstituted = new BinaryView(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Bool', () => {
  it('should default-construct', () => {
    const instance = new Bool();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Bool();
    const bytes = instance.toBytes();
    const reconstituted = new Bool(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Date', () => {
  it('should default-construct', () => {
    const instance = new Date();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Date();
    const bytes = instance.toBytes();
    const reconstituted = new Date(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Decimal', () => {
  it('should default-construct', () => {
    const instance = new Decimal();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Decimal();
    const bytes = instance.toBytes();
    const reconstituted = new Decimal(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('DictionaryEncoding', () => {
  it('should default-construct', () => {
    const instance = new DictionaryEncoding();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new DictionaryEncoding();
    const bytes = instance.toBytes();
    const reconstituted = new DictionaryEncoding(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Duration', () => {
  it('should default-construct', () => {
    const instance = new Duration();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Duration();
    const bytes = instance.toBytes();
    const reconstituted = new Duration(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Field', () => {
  it('should default-construct', () => {
    const instance = new Field();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Field();
    const bytes = instance.toBytes();
    const reconstituted = new Field(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('FixedSizeBinary', () => {
  it('should default-construct', () => {
    const instance = new FixedSizeBinary();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new FixedSizeBinary();
    const bytes = instance.toBytes();
    const reconstituted = new FixedSizeBinary(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('FixedSizeList', () => {
  it('should default-construct', () => {
    const instance = new FixedSizeList();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new FixedSizeList();
    const bytes = instance.toBytes();
    const reconstituted = new FixedSizeList(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('FloatingPoint', () => {
  it('should default-construct', () => {
    const instance = new FloatingPoint();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new FloatingPoint();
    const bytes = instance.toBytes();
    const reconstituted = new FloatingPoint(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Int', () => {
  it('should default-construct', () => {
    const instance = new Int();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Int();
    const bytes = instance.toBytes();
    const reconstituted = new Int(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Interval', () => {
  it('should default-construct', () => {
    const instance = new Interval();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Interval();
    const bytes = instance.toBytes();
    const reconstituted = new Interval(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('KeyValue', () => {
  it('should default-construct', () => {
    const instance = new KeyValue();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new KeyValue();
    const bytes = instance.toBytes();
    const reconstituted = new KeyValue(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('LargeBinary', () => {
  it('should default-construct', () => {
    const instance = new LargeBinary();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new LargeBinary();
    const bytes = instance.toBytes();
    const reconstituted = new LargeBinary(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('LargeList', () => {
  it('should default-construct', () => {
    const instance = new LargeList();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new LargeList();
    const bytes = instance.toBytes();
    const reconstituted = new LargeList(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('LargeListView', () => {
  it('should default-construct', () => {
    const instance = new LargeListView();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new LargeListView();
    const bytes = instance.toBytes();
    const reconstituted = new LargeListView(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('LargeUtf8', () => {
  it('should default-construct', () => {
    const instance = new LargeUtf8();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new LargeUtf8();
    const bytes = instance.toBytes();
    const reconstituted = new LargeUtf8(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('List', () => {
  it('should default-construct', () => {
    const instance = new List();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new List();
    const bytes = instance.toBytes();
    const reconstituted = new List(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ListView', () => {
  it('should default-construct', () => {
    const instance = new ListView();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ListView();
    const bytes = instance.toBytes();
    const reconstituted = new ListView(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Map', () => {
  it('should default-construct', () => {
    const instance = new Map();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Map();
    const bytes = instance.toBytes();
    const reconstituted = new Map(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Null', () => {
  it('should default-construct', () => {
    const instance = new Null();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Null();
    const bytes = instance.toBytes();
    const reconstituted = new Null(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('RunEndEncoded', () => {
  it('should default-construct', () => {
    const instance = new RunEndEncoded();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new RunEndEncoded();
    const bytes = instance.toBytes();
    const reconstituted = new RunEndEncoded(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Schema', () => {
  it('should default-construct', () => {
    const instance = new Schema();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Schema();
    const bytes = instance.toBytes();
    const reconstituted = new Schema(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Struct_', () => {
  it('should default-construct', () => {
    const instance = new Struct_();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Struct_();
    const bytes = instance.toBytes();
    const reconstituted = new Struct_(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Time', () => {
  it('should default-construct', () => {
    const instance = new Time();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Time();
    const bytes = instance.toBytes();
    const reconstituted = new Time(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Timestamp', () => {
  it('should default-construct', () => {
    const instance = new Timestamp();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Timestamp();
    const bytes = instance.toBytes();
    const reconstituted = new Timestamp(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Union', () => {
  it('should default-construct', () => {
    const instance = new Union();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Union();
    const bytes = instance.toBytes();
    const reconstituted = new Union(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Utf8', () => {
  it('should default-construct', () => {
    const instance = new Utf8();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Utf8();
    const bytes = instance.toBytes();
    const reconstituted = new Utf8(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Utf8View', () => {
  it('should default-construct', () => {
    const instance = new Utf8View();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Utf8View();
    const bytes = instance.toBytes();
    const reconstituted = new Utf8View(bytes);
    expect(reconstituted).toBeDefined();
  });
});
