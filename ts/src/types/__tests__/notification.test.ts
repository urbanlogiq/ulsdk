// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { AccessRequest, DriveChange, Inbox, InboxItem, JobComplete, Notification, Response, Share, ShareDetails } from '../notification';

describe('AccessRequest', () => {
  it('should default-construct', () => {
    const instance = new AccessRequest();
    expect(instance).toBeDefined();
  });

});

describe('DriveChange', () => {
  it('should default-construct', () => {
    const instance = new DriveChange();
    expect(instance).toBeDefined();
  });

});

describe('Inbox', () => {
  it('should default-construct', () => {
    const instance = new Inbox();
    expect(instance).toBeDefined();
  });

});

describe('InboxItem', () => {
  it('should default-construct', () => {
    const instance = new InboxItem();
    expect(instance).toBeDefined();
  });

});

describe('JobComplete', () => {
  it('should default-construct', () => {
    const instance = new JobComplete();
    expect(instance).toBeDefined();
  });

});

describe('Notification', () => {
  it('should default-construct', () => {
    const instance = new Notification();
    expect(instance).toBeDefined();
  });

});

describe('Response', () => {
  it('should default-construct', () => {
    const instance = new Response();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Response();
    const bytes = instance.toBytes();
    const reconstituted = new Response(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Share', () => {
  it('should default-construct', () => {
    const instance = new Share();
    expect(instance).toBeDefined();
  });

});

describe('ShareDetails', () => {
  it('should default-construct', () => {
    const instance = new ShareDetails();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ShareDetails();
    const bytes = instance.toBytes();
    const reconstituted = new ShareDetails(bytes);
    expect(reconstituted).toBeDefined();
  });
});
