// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import { ByteArray, ContainerRef, GitRef, Layout, ParameterFlags, Producer, TileData, TileSettings, UserSettings, WorkLog, WorklogParameter } from '../worklog';

describe('ByteArray', () => {
  it('should default-construct', () => {
    const instance = new ByteArray();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ByteArray();
    const bytes = instance.toBytes();
    const reconstituted = new ByteArray(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ContainerRef', () => {
  it('should default-construct', () => {
    const instance = new ContainerRef();
    expect(instance).toBeDefined();
  });

});

describe('GitRef', () => {
  it('should default-construct', () => {
    const instance = new GitRef();
    expect(instance).toBeDefined();
  });

});

describe('Layout', () => {
  it('should default-construct', () => {
    const instance = new Layout();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new Layout();
    const bytes = instance.toBytes();
    const reconstituted = new Layout(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('ParameterFlags', () => {
  it('should default-construct', () => {
    const instance = new ParameterFlags();
    expect(instance).toBeDefined();
  });

  it('should serialize and deserialize', () => {
    const instance = new ParameterFlags();
    const bytes = instance.toBytes();
    const reconstituted = new ParameterFlags(bytes);
    expect(reconstituted).toBeDefined();
  });
});

describe('Producer', () => {
  it('should default-construct', () => {
    const instance = new Producer();
    expect(instance).toBeDefined();
  });

});

describe('TileData', () => {
  it('should default-construct', () => {
    const instance = new TileData();
    expect(instance).toBeDefined();
  });

});

describe('TileSettings', () => {
  it('should default-construct', () => {
    const instance = new TileSettings();
    expect(instance).toBeDefined();
  });

});

describe('UserSettings', () => {
  it('should default-construct', () => {
    const instance = new UserSettings();
    expect(instance).toBeDefined();
  });

});

describe('WorkLog', () => {
  it('should default-construct', () => {
    const instance = new WorkLog();
    expect(instance).toBeDefined();
  });

});

describe('WorklogParameter', () => {
  it('should default-construct', () => {
    const instance = new WorklogParameter();
    expect(instance).toBeDefined();
  });

});
