// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { CryptHeader as FbsCryptHeader, CryptHeaderT as FbsCryptHeaderT } from './generated/crypt-header';
import { Digest as FbsDigest } from './generated/digest';
import { EncryptedObject as FbsEncryptedObject, EncryptedObjectT as FbsEncryptedObjectT } from './generated/encrypted-object';
import { Sha256 as FbsSha256, Sha256T as FbsSha256T } from './generated/sha256';
import { Signature as FbsSignature, SignatureT as FbsSignatureT } from './generated/signature';

export class Sha256 {
  private _b!: number[];

  constructor(arg?: FbsSha256 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSha256.getSizePrefixedRootAsSha256(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSha256) {
      this._initFromFbs(arg);
    } else {
      this._b = [];
    }
  }

  private _initFromFbs(fbs: FbsSha256): void {
    const bArr = fbs.bArray();
    this._b = bArr ? Array.from(bArr) : [];
  }

  get b(): number[] {
    return this._b;
  }

  set b(value: number[]) {
    this._b = value;
  }

  toFbsT(): FbsSha256T {
    const t = new FbsSha256T();
    t.b = this._b;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type Digest = Sha256;

export class CryptHeader {
/**
 *  An ID for the key used to encrypt this particular encrypted object.
 */
  private _kid!: string;

  private _nonce!: number[];

  private _plaintextLen!: number;

  constructor(arg?: FbsCryptHeader | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsCryptHeader.getSizePrefixedRootAsCryptHeader(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsCryptHeader) {
      this._initFromFbs(arg);
    } else {
      this._kid = '';
      this._nonce = [];
      this._plaintextLen = 0;
    }
  }

  private _initFromFbs(fbs: FbsCryptHeader): void {
    this._kid = fbs.kid() ?? '';
    const nonceArr = fbs.nonceArray();
    this._nonce = nonceArr ? Array.from(nonceArr) : [];
    this._plaintextLen = fbs.plaintextLen();
  }

  get kid(): string {
    return this._kid;
  }

  set kid(value: string) {
    this._kid = value;
  }

  get nonce(): number[] {
    return this._nonce;
  }

  set nonce(value: number[]) {
    this._nonce = value;
  }

  get plaintextLen(): number {
    return this._plaintextLen;
  }

  set plaintextLen(value: number) {
    this._plaintextLen = value;
  }

  toFbsT(): FbsCryptHeaderT {
    const t = new FbsCryptHeaderT();
    t.kid = this._kid;
    t.nonce = this._nonce;
    t.plaintextLen = this._plaintextLen;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class EncryptedObject {
  private _header!: CryptHeader;

  private _obj!: number[];

  constructor(arg?: FbsEncryptedObject | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsEncryptedObject.getSizePrefixedRootAsEncryptedObject(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsEncryptedObject) {
      this._initFromFbs(arg);
    } else {
      this._header = new CryptHeader();
      this._obj = [];
    }
  }

  private _initFromFbs(fbs: FbsEncryptedObject): void {
    const headerVal = fbs.header();
    this._header = headerVal ? new CryptHeader(headerVal) : new CryptHeader();
    const objArr = fbs.objArray();
    this._obj = objArr ? Array.from(objArr) : [];
  }

  get header(): CryptHeader {
    return this._header;
  }

  set header(value: CryptHeader) {
    this._header = value;
  }

  get obj(): number[] {
    return this._obj;
  }

  set obj(value: number[]) {
    this._obj = value;
  }

  toFbsT(): FbsEncryptedObjectT {
    const t = new FbsEncryptedObjectT();
    t.header = this._header.toFbsT();
    t.obj = this._obj;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Signature {
  private _kid!: string;

  private _sig!: number[];

  constructor(arg?: FbsSignature | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSignature.getSizePrefixedRootAsSignature(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSignature) {
      this._initFromFbs(arg);
    } else {
      this._kid = '';
      this._sig = [];
    }
  }

  private _initFromFbs(fbs: FbsSignature): void {
    this._kid = fbs.kid() ?? '';
    const sigArr = fbs.sigArray();
    this._sig = sigArr ? Array.from(sigArr) : [];
  }

  get kid(): string {
    return this._kid;
  }

  set kid(value: string) {
    this._kid = value;
  }

  get sig(): number[] {
    return this._sig;
  }

  set sig(value: number[]) {
    this._sig = value;
  }

  toFbsT(): FbsSignatureT {
    const t = new FbsSignatureT();
    t.kid = this._kid;
    t.sig = this._sig;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
