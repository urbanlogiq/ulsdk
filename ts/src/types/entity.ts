// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Geometry as FbsGeometry } from './generated/geometry';
import { GraphEdge as FbsGraphEdge, GraphEdgeT as FbsGraphEdgeT } from './generated/graph-edge';
import { GraphNode as FbsGraphNode, GraphNodeT as FbsGraphNodeT } from './generated/graph-node';
import { Line as FbsLine, LineT as FbsLineT } from './generated/line';
import { MultiLine as FbsMultiLine, MultiLineT as FbsMultiLineT } from './generated/multi-line';
import { MultiPoint as FbsMultiPoint, MultiPointT as FbsMultiPointT } from './generated/multi-point';
import { MultiPolygon as FbsMultiPolygon, MultiPolygonT as FbsMultiPolygonT } from './generated/multi-polygon';
import { Point as FbsPoint, PointT as FbsPointT } from './generated/point';
import { Polygon as FbsPolygon, PolygonT as FbsPolygonT } from './generated/polygon';

import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, PinnedObjectId, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { PinnedObjectId as FbsPinnedObjectId } from './generated/pinned-object-id';
import { StreamId as FbsStreamId } from './generated/stream-id';

export { EdgeTy } from './generated/edge-ty';

export { EntityTy } from './generated/entity-ty';

export class Point {
  private _pointGeo!: number[];

  constructor(arg?: FbsPoint | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsPoint.getSizePrefixedRootAsPoint(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsPoint) {
      this._initFromFbs(arg);
    } else {
      this._pointGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsPoint): void {
    this._pointGeo = Array.from({ length: fbs.pointGeoLength() }, (_, i) => fbs.pointGeo(i)!);
  }

  get pointGeo(): number[] {
    return this._pointGeo;
  }

  set pointGeo(value: number[]) {
    this._pointGeo = value;
  }

  toFbsT(): FbsPointT {
    const t = new FbsPointT();
    t.pointGeo = this._pointGeo;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Line {
  private _lineGeo!: Point[];

  constructor(arg?: FbsLine | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsLine.getSizePrefixedRootAsLine(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsLine) {
      this._initFromFbs(arg);
    } else {
      this._lineGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsLine): void {
    this._lineGeo = Array.from({ length: fbs.lineGeoLength() }, (_, i) => {
      const item = fbs.lineGeo(i);
      return item ? new Point(item) : new Point();
    });
  }

  get lineGeo(): Point[] {
    return this._lineGeo;
  }

  set lineGeo(value: Point[]) {
    this._lineGeo = value;
  }

  toFbsT(): FbsLineT {
    const t = new FbsLineT();
    t.lineGeo = this._lineGeo.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class MultiLine {
  private _multilineGeo!: Line[];

  constructor(arg?: FbsMultiLine | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsMultiLine.getSizePrefixedRootAsMultiLine(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsMultiLine) {
      this._initFromFbs(arg);
    } else {
      this._multilineGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsMultiLine): void {
    this._multilineGeo = Array.from({ length: fbs.multilineGeoLength() }, (_, i) => {
      const item = fbs.multilineGeo(i);
      return item ? new Line(item) : new Line();
    });
  }

  get multilineGeo(): Line[] {
    return this._multilineGeo;
  }

  set multilineGeo(value: Line[]) {
    this._multilineGeo = value;
  }

  toFbsT(): FbsMultiLineT {
    const t = new FbsMultiLineT();
    t.multilineGeo = this._multilineGeo.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Polygon is an array of arrays of points.
 *  The first array is exterior coords, following are any interior holes
 */
export class Polygon {
  private _polygonGeo!: Line[];

  constructor(arg?: FbsPolygon | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsPolygon.getSizePrefixedRootAsPolygon(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsPolygon) {
      this._initFromFbs(arg);
    } else {
      this._polygonGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsPolygon): void {
    this._polygonGeo = Array.from({ length: fbs.polygonGeoLength() }, (_, i) => {
      const item = fbs.polygonGeo(i);
      return item ? new Line(item) : new Line();
    });
  }

  get polygonGeo(): Line[] {
    return this._polygonGeo;
  }

  set polygonGeo(value: Line[]) {
    this._polygonGeo = value;
  }

  toFbsT(): FbsPolygonT {
    const t = new FbsPolygonT();
    t.polygonGeo = this._polygonGeo.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class MultiPolygon {
  private _multipolygonGeo!: Polygon[];

  constructor(arg?: FbsMultiPolygon | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsMultiPolygon.getSizePrefixedRootAsMultiPolygon(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsMultiPolygon) {
      this._initFromFbs(arg);
    } else {
      this._multipolygonGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsMultiPolygon): void {
    this._multipolygonGeo = Array.from({ length: fbs.multipolygonGeoLength() }, (_, i) => {
      const item = fbs.multipolygonGeo(i);
      return item ? new Polygon(item) : new Polygon();
    });
  }

  get multipolygonGeo(): Polygon[] {
    return this._multipolygonGeo;
  }

  set multipolygonGeo(value: Polygon[]) {
    this._multipolygonGeo = value;
  }

  toFbsT(): FbsMultiPolygonT {
    const t = new FbsMultiPolygonT();
    t.multipolygonGeo = this._multipolygonGeo.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class MultiPoint {
  private _pointGeo!: Point[];

  constructor(arg?: FbsMultiPoint | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsMultiPoint.getSizePrefixedRootAsMultiPoint(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsMultiPoint) {
      this._initFromFbs(arg);
    } else {
      this._pointGeo = [];
    }
  }

  private _initFromFbs(fbs: FbsMultiPoint): void {
    this._pointGeo = Array.from({ length: fbs.pointGeoLength() }, (_, i) => {
      const item = fbs.pointGeo(i);
      return item ? new Point(item) : new Point();
    });
  }

  get pointGeo(): Point[] {
    return this._pointGeo;
  }

  set pointGeo(value: Point[]) {
    this._pointGeo = value;
  }

  toFbsT(): FbsMultiPointT {
    const t = new FbsMultiPointT();
    t.pointGeo = this._pointGeo.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type Geometry = Point | Line | MultiLine | Polygon | MultiPolygon | MultiPoint;

export { NodeTy } from './generated/node-ty';

export class GraphEdge {
  private __from!: bigint;

  private __kind!: number;

  private __to!: bigint;

  constructor(arg?: FbsGraphEdge | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGraphEdge.getSizePrefixedRootAsGraphEdge(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGraphEdge) {
      this._initFromFbs(arg);
    } else {
      this.__from = BigInt(0);
      this.__kind = 0;
      this.__to = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsGraphEdge): void {
    this.__from = fbs._From();
    this.__kind = fbs._Kind();
    this.__to = fbs._To();
  }

  get _from(): bigint {
    return this.__from;
  }

  set _from(value: bigint) {
    this.__from = value;
  }

  get _kind(): number {
    return this.__kind;
  }

  set _kind(value: number) {
    this.__kind = value;
  }

  get _to(): bigint {
    return this.__to;
  }

  set _to(value: bigint) {
    this.__to = value;
  }

  toFbsT(): FbsGraphEdgeT {
    const t = new FbsGraphEdgeT();
    t._From = this.__from;
    t._Kind = this.__kind;
    t._To = this.__to;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class GraphNode {
/**
 *  A human-centric description of this graph node.
 */
  private __description!: string | null;

/**
 *  Entity type (ie: traffic loop, road, power line, building, business, demographic data, collision,  ...)
 */
  private __entityType!: number;

/**
 *  polygon / line / point / null
 */
  private __geom!: Geometry | null;

/**
 *  lat/lng point in space, or centroid if not a point
 */
  private __location!: Point | null;

/**
 *  Record id in the data source.
 */
  private __nodeId!: GenericId | null;

/**
 *  Node type, such as emitter vs. entity
 */
  private __nodeType!: number;

/**
 *  ID of the associated data source
 */
  private __stream!: ObjectId | null;

/**
 *  Unique database-specific identifier
 */
  private __uid!: bigint;

  constructor(arg?: FbsGraphNode | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGraphNode.getSizePrefixedRootAsGraphNode(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGraphNode) {
      this._initFromFbs(arg);
    } else {
      this.__description = null;
      this.__entityType = 0;
      this.__geom = null;
      this.__location = null;
      this.__nodeId = null;
      this.__nodeType = 0;
      this.__stream = null;
      this.__uid = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsGraphNode): void {
    this.__description = fbs._Description();
    this.__entityType = fbs._EntityType();
    const _geomTy = fbs._GeomType();
    if (_geomTy === FbsGeometry.Point) {
      const _geomFbsVal = fbs._Geom(new FbsPoint());
      this.__geom = _geomFbsVal ? new Point(_geomFbsVal) : null;
    } else if (_geomTy === FbsGeometry.Line) {
      const _geomFbsVal = fbs._Geom(new FbsLine());
      this.__geom = _geomFbsVal ? new Line(_geomFbsVal) : null;
    } else if (_geomTy === FbsGeometry.MultiLine) {
      const _geomFbsVal = fbs._Geom(new FbsMultiLine());
      this.__geom = _geomFbsVal ? new MultiLine(_geomFbsVal) : null;
    } else if (_geomTy === FbsGeometry.Polygon) {
      const _geomFbsVal = fbs._Geom(new FbsPolygon());
      this.__geom = _geomFbsVal ? new Polygon(_geomFbsVal) : null;
    } else if (_geomTy === FbsGeometry.MultiPolygon) {
      const _geomFbsVal = fbs._Geom(new FbsMultiPolygon());
      this.__geom = _geomFbsVal ? new MultiPolygon(_geomFbsVal) : null;
    } else if (_geomTy === FbsGeometry.MultiPoint) {
      const _geomFbsVal = fbs._Geom(new FbsMultiPoint());
      this.__geom = _geomFbsVal ? new MultiPoint(_geomFbsVal) : null;
    } else {
      this.__geom = null;
    }
    const _locationVal = fbs._Location();
    this.__location = _locationVal ? new Point(_locationVal) : null;
    const _nodeIdVal = fbs._NodeId();
    this.__nodeId = _nodeIdVal ? new GenericId(_nodeIdVal) : null;
    this.__nodeType = fbs._NodeType();
    const _streamVal = fbs._Stream();
    this.__stream = _streamVal ? new ObjectId(_streamVal) : null;
    this.__uid = fbs._Uid();
  }

  get _description(): string | null {
    return this.__description;
  }

  set _description(value: string | null) {
    this.__description = value;
  }

  get _entityType(): number {
    return this.__entityType;
  }

  set _entityType(value: number) {
    this.__entityType = value;
  }

  get _geom(): Geometry | null {
    return this.__geom;
  }

  set _geom(value: Geometry | null) {
    this.__geom = value;
  }

  get _location(): Point | null {
    return this.__location;
  }

  set _location(value: Point | null) {
    this.__location = value;
  }

  get _nodeId(): GenericId | null {
    return this.__nodeId;
  }

  set _nodeId(value: GenericId | null) {
    this.__nodeId = value;
  }

  get _nodeType(): number {
    return this.__nodeType;
  }

  set _nodeType(value: number) {
    this.__nodeType = value;
  }

  get _stream(): ObjectId | null {
    return this.__stream;
  }

  set _stream(value: ObjectId | null) {
    this.__stream = value;
  }

  get _uid(): bigint {
    return this.__uid;
  }

  set _uid(value: bigint) {
    this.__uid = value;
  }

  toFbsT(): FbsGraphNodeT {
    const t = new FbsGraphNodeT();
    t._Description = this.__description;
    t._EntityType = this.__entityType;
    if (this.__geom instanceof Point) {
      t._GeomType = FbsGeometry.Point;
      t._Geom = this.__geom.toFbsT();
    } else if (this.__geom instanceof Line) {
      t._GeomType = FbsGeometry.Line;
      t._Geom = this.__geom.toFbsT();
    } else if (this.__geom instanceof MultiLine) {
      t._GeomType = FbsGeometry.MultiLine;
      t._Geom = this.__geom.toFbsT();
    } else if (this.__geom instanceof Polygon) {
      t._GeomType = FbsGeometry.Polygon;
      t._Geom = this.__geom.toFbsT();
    } else if (this.__geom instanceof MultiPolygon) {
      t._GeomType = FbsGeometry.MultiPolygon;
      t._Geom = this.__geom.toFbsT();
    } else if (this.__geom instanceof MultiPoint) {
      t._GeomType = FbsGeometry.MultiPoint;
      t._Geom = this.__geom.toFbsT();
    }
    t._Location = this.__location ? this.__location.toFbsT() : null;
    t._NodeId = this.__nodeId ? this.__nodeId.toFbsT() : null;
    t._NodeType = this.__nodeType;
    t._Stream = this.__stream ? this.__stream.toFbsT() : null;
    t._Uid = this.__uid;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
