// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Append as FbsAppend, AppendT as FbsAppendT } from './generated/append';
import { ChangeOp as FbsChangeOp } from './generated/change-op';
import { ChangeOpEntry as FbsChangeOpEntry, ChangeOpEntryT as FbsChangeOpEntryT } from './generated/change-op-entry';
import { ChangeSet as FbsChangeSet, ChangeSetT as FbsChangeSetT } from './generated/change-set';
import { Delete as FbsDelete, DeleteT as FbsDeleteT } from './generated/delete';
import { DiffStream as FbsDiffStream, DiffStreamT as FbsDiffStreamT } from './generated/diff-stream';
import { History as FbsHistory, HistoryT as FbsHistoryT } from './generated/history';
import { Modify as FbsModify, ModifyT as FbsModifyT } from './generated/modify';
import { NewTable as FbsNewTable, NewTableT as FbsNewTableT } from './generated/new-table';
import { Op as FbsOp } from './generated/op';
import { OpEntry as FbsOpEntry, OpEntryT as FbsOpEntryT } from './generated/op-entry';
import { Restore as FbsRestore, RestoreT as FbsRestoreT } from './generated/restore';
import { RestoreRow as FbsRestoreRow, RestoreRowT as FbsRestoreRowT } from './generated/restore-row';
import { RmRow as FbsRmRow, RmRowT as FbsRmRowT } from './generated/rm-row';
import { Set as FbsSet, SetT as FbsSetT } from './generated/set';
import { TableFrom as FbsTableFrom } from './generated/table-from';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { SortOrder } from './api';
import { SortOrder as FbsSortOrder } from './generated/sort-order';
import { Attr } from './attr';
import { Attr as FbsAttr } from './generated/attr';
import { EdgeTy, EntityTy, Geometry, GraphEdge, GraphNode, Line, MultiLine, MultiPoint, MultiPolygon, NodeTy, Point, Polygon } from './entity';
import { EdgeTy as FbsEdgeTy } from './generated/edge-ty';
import { EntityTy as FbsEntityTy } from './generated/entity-ty';
import { Geometry as FbsGeometry } from './generated/geometry';
import { GraphEdge as FbsGraphEdge } from './generated/graph-edge';
import { GraphNode as FbsGraphNode } from './generated/graph-node';
import { Line as FbsLine } from './generated/line';
import { MultiLine as FbsMultiLine } from './generated/multi-line';
import { MultiPoint as FbsMultiPoint } from './generated/multi-point';
import { MultiPolygon as FbsMultiPolygon } from './generated/multi-polygon';
import { NodeTy as FbsNodeTy } from './generated/node-ty';
import { Point as FbsPoint } from './generated/point';
import { Polygon as FbsPolygon } from './generated/polygon';
import { Fn } from './fun';
import { Fn as FbsFn } from './generated/fn';
import { EdgeList, EdgeQuery, Geom, GeomOp, GraphQuery, NodeIdPair, NodeList, NodeQuery, OrderBy, Predicate, Projection, QueryPathElement, QueryPathElementUnion, ValueTransform } from './graph';
import { EdgeList as FbsEdgeList } from './generated/edge-list';
import { EdgeQuery as FbsEdgeQuery } from './generated/edge-query';
import { Geom as FbsGeom } from './generated/geom';
import { GeomOp as FbsGeomOp } from './generated/geom-op';
import { GraphQuery as FbsGraphQuery } from './generated/graph-query';
import { NodeIdPair as FbsNodeIdPair } from './generated/node-id-pair';
import { NodeList as FbsNodeList } from './generated/node-list';
import { NodeQuery as FbsNodeQuery } from './generated/node-query';
import { OrderBy as FbsOrderBy } from './generated/order-by';
import { Predicate as FbsPredicate } from './generated/predicate';
import { Projection as FbsProjection } from './generated/projection';
import { QueryPathElement as FbsQueryPathElement } from './generated/query-path-element';
import { QueryPathElementUnion as FbsQueryPathElementUnion } from './generated/query-path-element-union';
import { ValueTransform as FbsValueTransform } from './generated/value-transform';
import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, PinnedObjectId, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { PinnedObjectId as FbsPinnedObjectId } from './generated/pinned-object-id';
import { StreamId as FbsStreamId } from './generated/stream-id';
import { AddCol, AggregateFilter, AllColumns, AlterTableElement, AlterTableOperation, AlterTableOperationUnion, Arrow, BinaryQueryElement, Case, Column, ConflictAction, DataCatalog, DeleteQueryElement, Distinct, DoNothing, DoUpdate, Drive, DropCol, Explain, ExplainFormat, Expr, ExprUnion, Function, InsertConflicting, InsertQueryElement, Join, JoinTy, MvdbPartition, NullableUint, OnConflict, OrderByExpr, Partition, Placeholder, Query, QueryElement, QueryElementOp, QueryElementUnion, QueryTableSource, SetExpr, TableOrderBy, TablePartition, TableSource, TableSourceInstance, TableSourceUnion, TimeSeries, TypeHint, UnaryQueryElement, UnsetArgument, UpdateQueryElement, ValueIndex, ValueName, ValueRow, Values, Vector, When, Window, WorklogPartition } from './query';
import { AddCol as FbsAddCol } from './generated/add-col';
import { AggregateFilter as FbsAggregateFilter } from './generated/aggregate-filter';
import { AllColumns as FbsAllColumns } from './generated/all-columns';
import { AlterTableElement as FbsAlterTableElement } from './generated/alter-table-element';
import { AlterTableOperation as FbsAlterTableOperation } from './generated/alter-table-operation';
import { AlterTableOperationUnion as FbsAlterTableOperationUnion } from './generated/alter-table-operation-union';
import { Arrow as FbsArrow } from './generated/arrow';
import { BinaryQueryElement as FbsBinaryQueryElement } from './generated/binary-query-element';
import { Case as FbsCase } from './generated/case';
import { Column as FbsColumn } from './generated/column';
import { ConflictAction as FbsConflictAction } from './generated/conflict-action';
import { DataCatalog as FbsDataCatalog } from './generated/data-catalog';
import { DeleteQueryElement as FbsDeleteQueryElement } from './generated/delete-query-element';
import { Distinct as FbsDistinct } from './generated/distinct';
import { DoNothing as FbsDoNothing } from './generated/do-nothing';
import { DoUpdate as FbsDoUpdate } from './generated/do-update';
import { Drive as FbsDrive } from './generated/drive';
import { DropCol as FbsDropCol } from './generated/drop-col';
import { Explain as FbsExplain } from './generated/explain';
import { ExplainFormat as FbsExplainFormat } from './generated/explain-format';
import { Expr as FbsExpr } from './generated/expr';
import { ExprUnion as FbsExprUnion } from './generated/expr-union';
import { Function as FbsFunction } from './generated/function';
import { InsertConflicting as FbsInsertConflicting } from './generated/insert-conflicting';
import { InsertQueryElement as FbsInsertQueryElement } from './generated/insert-query-element';
import { Join as FbsJoin } from './generated/join';
import { JoinTy as FbsJoinTy } from './generated/join-ty';
import { MvdbPartition as FbsMvdbPartition } from './generated/mvdb-partition';
import { NullableUint as FbsNullableUint } from './generated/nullable-uint';
import { OnConflict as FbsOnConflict } from './generated/on-conflict';
import { OrderByExpr as FbsOrderByExpr } from './generated/order-by-expr';
import { Partition as FbsPartition } from './generated/partition';
import { Placeholder as FbsPlaceholder } from './generated/placeholder';
import { Query as FbsQuery } from './generated/query';
import { QueryElement as FbsQueryElement } from './generated/query-element';
import { QueryElementOp as FbsQueryElementOp } from './generated/query-element-op';
import { QueryElementUnion as FbsQueryElementUnion } from './generated/query-element-union';
import { QueryTableSource as FbsQueryTableSource } from './generated/query-table-source';
import { SetExpr as FbsSetExpr } from './generated/set-expr';
import { TableOrderBy as FbsTableOrderBy } from './generated/table-order-by';
import { TablePartition as FbsTablePartition } from './generated/table-partition';
import { TableSource as FbsTableSource } from './generated/table-source';
import { TableSourceInstance as FbsTableSourceInstance } from './generated/table-source-instance';
import { TableSourceUnion as FbsTableSourceUnion } from './generated/table-source-union';
import { TimeSeries as FbsTimeSeries } from './generated/time-series';
import { TypeHint as FbsTypeHint } from './generated/type-hint';
import { UnaryQueryElement as FbsUnaryQueryElement } from './generated/unary-query-element';
import { UnsetArgument as FbsUnsetArgument } from './generated/unset-argument';
import { UpdateQueryElement as FbsUpdateQueryElement } from './generated/update-query-element';
import { ValueIndex as FbsValueIndex } from './generated/value-index';
import { ValueName as FbsValueName } from './generated/value-name';
import { ValueRow as FbsValueRow } from './generated/value-row';
import { Values as FbsValues } from './generated/values';
import { Vector as FbsVector } from './generated/vector';
import { When as FbsWhen } from './generated/when';
import { Window as FbsWindow } from './generated/window';
import { WorklogPartition as FbsWorklogPartition } from './generated/worklog-partition';
import { Point2D, Tri2D, VArray, VBool, VBytes, VChar, VF32, VF64, VFixedSizeBytes, VI16, VI32, VI64, VI8, VIsize, VNull, VPlaceholder, VStr, VTimestampMs, VTimestampMsUtc, VTimestampNs, VTimestampNsUtc, VTri2D, VU16, VU32, VU64, VU8, VUnit, VUsize, Value, ValueInstance, ValueTy } from './value';
import { Point2D as FbsPoint2D } from './generated/point2-d';
import { Tri2D as FbsTri2D } from './generated/tri2-d';
import { VArray as FbsVArray } from './generated/varray';
import { VBool as FbsVBool } from './generated/vbool';
import { VBytes as FbsVBytes } from './generated/vbytes';
import { VChar as FbsVChar } from './generated/vchar';
import { VF32 as FbsVF32 } from './generated/vf32';
import { VF64 as FbsVF64 } from './generated/vf64';
import { VFixedSizeBytes as FbsVFixedSizeBytes } from './generated/vfixed-size-bytes';
import { VI16 as FbsVI16 } from './generated/vi16';
import { VI32 as FbsVI32 } from './generated/vi32';
import { VI64 as FbsVI64 } from './generated/vi64';
import { VI8 as FbsVI8 } from './generated/vi8';
import { VIsize as FbsVIsize } from './generated/visize';
import { VNull as FbsVNull } from './generated/vnull';
import { VPlaceholder as FbsVPlaceholder } from './generated/vplaceholder';
import { VStr as FbsVStr } from './generated/vstr';
import { VTimestampMs as FbsVTimestampMs } from './generated/vtimestamp-ms';
import { VTimestampMsUtc as FbsVTimestampMsUtc } from './generated/vtimestamp-ms-utc';
import { VTimestampNs as FbsVTimestampNs } from './generated/vtimestamp-ns';
import { VTimestampNsUtc as FbsVTimestampNsUtc } from './generated/vtimestamp-ns-utc';
import { VTri2D as FbsVTri2D } from './generated/vtri2-d';
import { VU16 as FbsVU16 } from './generated/vu16';
import { VU32 as FbsVU32 } from './generated/vu32';
import { VU64 as FbsVU64 } from './generated/vu64';
import { VU8 as FbsVU8 } from './generated/vu8';
import { VUnit as FbsVUnit } from './generated/vunit';
import { VUsize as FbsVUsize } from './generated/vusize';
import { Value as FbsValue } from './generated/value';
import { ValueInstance as FbsValueInstance } from './generated/value-instance';
import { ValueTy as FbsValueTy } from './generated/value-ty';

export class Modify {
  private _col!: string;

  private _previous!: ValueInstance | null;

  private _row!: GenericId;

  private _value!: ValueInstance;

  constructor(arg?: FbsModify | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsModify.getSizePrefixedRootAsModify(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsModify) {
      this._initFromFbs(arg);
    } else {
      this._col = '';
      this._previous = null;
      this._row = new GenericId();
      this._value = new ValueInstance();
    }
  }

  private _initFromFbs(fbs: FbsModify): void {
    this._col = fbs.col() ?? '';
    const previousVal = fbs.previous();
    this._previous = previousVal ? new ValueInstance(previousVal) : null;
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
    const valueVal = fbs.value();
    this._value = valueVal ? new ValueInstance(valueVal) : new ValueInstance();
  }

  get col(): string {
    return this._col;
  }

  set col(value: string) {
    this._col = value;
  }

  get previous(): ValueInstance | null {
    return this._previous;
  }

  set previous(value: ValueInstance | null) {
    this._previous = value;
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  get value(): ValueInstance {
    return this._value;
  }

  set value(value: ValueInstance) {
    this._value = value;
  }

  toFbsT(): FbsModifyT {
    const t = new FbsModifyT();
    t.col = this._col;
    t.previous = this._previous ? this._previous.toFbsT() : null;
    t.row = this._row.toFbsT();
    t.value = this._value.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Delete {
  private _row!: GenericId;

  constructor(arg?: FbsDelete | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDelete.getSizePrefixedRootAsDelete(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDelete) {
      this._initFromFbs(arg);
    } else {
      this._row = new GenericId();
    }
  }

  private _initFromFbs(fbs: FbsDelete): void {
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  toFbsT(): FbsDeleteT {
    const t = new FbsDeleteT();
    t.row = this._row.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Restore {
  private _row!: GenericId;

  constructor(arg?: FbsRestore | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRestore.getSizePrefixedRootAsRestore(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRestore) {
      this._initFromFbs(arg);
    } else {
      this._row = new GenericId();
    }
  }

  private _initFromFbs(fbs: FbsRestore): void {
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  toFbsT(): FbsRestoreT {
    const t = new FbsRestoreT();
    t.row = this._row.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type ChangeOp = Modify | Delete | Restore;

/**
 *  The Set operation is used to set the value of a cell in a table.
 */
export class Set {
/**
 *  Name of the column to set.
 */
  private _col!: string;

/**
 *  The value of the ul_node_id column, which uniquely identifies the row.
 */
  private _row!: GenericId;

/**
 *  The value to set.
 */
  private _value!: ValueInstance;

  constructor(arg?: FbsSet | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSet.getSizePrefixedRootAsSet(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSet) {
      this._initFromFbs(arg);
    } else {
      this._col = '';
      this._row = new GenericId();
      this._value = new ValueInstance();
    }
  }

  private _initFromFbs(fbs: FbsSet): void {
    this._col = fbs.col() ?? '';
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
    const valueVal = fbs.value();
    this._value = valueVal ? new ValueInstance(valueVal) : new ValueInstance();
  }

  get col(): string {
    return this._col;
  }

  set col(value: string) {
    this._col = value;
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  get value(): ValueInstance {
    return this._value;
  }

  set value(value: ValueInstance) {
    this._value = value;
  }

  toFbsT(): FbsSetT {
    const t = new FbsSetT();
    t.col = this._col;
    t.row = this._row.toFbsT();
    t.value = this._value.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  The RmRow operation is used to remove a row from a table.
 */
export class RmRow {
/**
 *  The value of the ul_node_id column, which uniquely identifies the row.
 */
  private _row!: GenericId;

  constructor(arg?: FbsRmRow | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRmRow.getSizePrefixedRootAsRmRow(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRmRow) {
      this._initFromFbs(arg);
    } else {
      this._row = new GenericId();
    }
  }

  private _initFromFbs(fbs: FbsRmRow): void {
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  toFbsT(): FbsRmRowT {
    const t = new FbsRmRowT();
    t.row = this._row.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  The RestoreRow operation restore a deleted row in the table
 *  "Restore" is implemented by setting the value of the `ul_keep` system column to true.
 *  This means that formerly "removed" rows are no longer treated as "removed" and will then be returned by queries.
 */
export class RestoreRow {
/**
 *  The value of the ul_node_id column, which uniquely identifies the row.
 */
  private _row!: GenericId;

  constructor(arg?: FbsRestoreRow | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRestoreRow.getSizePrefixedRootAsRestoreRow(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRestoreRow) {
      this._initFromFbs(arg);
    } else {
      this._row = new GenericId();
    }
  }

  private _initFromFbs(fbs: FbsRestoreRow): void {
    const rowVal = fbs.row();
    this._row = rowVal ? new GenericId(rowVal) : new GenericId();
  }

  get row(): GenericId {
    return this._row;
  }

  set row(value: GenericId) {
    this._row = value;
  }

  toFbsT(): FbsRestoreRowT {
    const t = new FbsRestoreRowT();
    t.row = this._row.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Append rows to a table. The `content` field is Arrow IPC Stream formatted.
 */
export class Append {
/**
 *  The row content to append in Arrow IPC Stream format.
 */
  private _content!: number[];

  constructor(arg?: FbsAppend | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsAppend.getSizePrefixedRootAsAppend(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsAppend) {
      this._initFromFbs(arg);
    } else {
      this._content = [];
    }
  }

  private _initFromFbs(fbs: FbsAppend): void {
    const contentArr = fbs.contentArray();
    this._content = contentArr ? Array.from(contentArr) : [];
  }

  get content(): number[] {
    return this._content;
  }

  set content(value: number[]) {
    this._content = value;
  }

  toFbsT(): FbsAppendT {
    const t = new FbsAppendT();
    t.content = this._content;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Table Ops are used to modify the contents of a table.
 */
export type Op = Set | RmRow | RestoreRow | Append;

export type TableFrom = ObjectId | Schema;

export class ChangeOpEntry {
  private _op!: ChangeOp | null;

  constructor(arg?: FbsChangeOpEntry | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsChangeOpEntry.getSizePrefixedRootAsChangeOpEntry(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsChangeOpEntry) {
      this._initFromFbs(arg);
    } else {
      this._op = null;
    }
  }

  private _initFromFbs(fbs: FbsChangeOpEntry): void {
    const opTy = fbs.opType();
    if (opTy === FbsChangeOp.Modify) {
      const opFbsVal = fbs.op(new FbsModify());
      this._op = opFbsVal ? new Modify(opFbsVal) : null;
    } else if (opTy === FbsChangeOp.Delete) {
      const opFbsVal = fbs.op(new FbsDelete());
      this._op = opFbsVal ? new Delete(opFbsVal) : null;
    } else if (opTy === FbsChangeOp.Restore) {
      const opFbsVal = fbs.op(new FbsRestore());
      this._op = opFbsVal ? new Restore(opFbsVal) : null;
    } else {
      this._op = null;
    }
  }

  get op(): ChangeOp | null {
    return this._op;
  }

  set op(value: ChangeOp | null) {
    this._op = value;
  }

  toFbsT(): FbsChangeOpEntryT {
    const t = new FbsChangeOpEntryT();
    if (this._op instanceof Modify) {
      t.opType = FbsChangeOp.Modify;
      t.op = this._op.toFbsT();
    } else if (this._op instanceof Delete) {
      t.opType = FbsChangeOp.Delete;
      t.op = this._op.toFbsT();
    } else if (this._op instanceof Restore) {
      t.opType = FbsChangeOp.Restore;
      t.op = this._op.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class ChangeSet {
  private _attributes!: Attr[] | null;

  private _ops!: ChangeOpEntry[];

  private _revision!: ContentId;

  private _when!: bigint;

  private _who!: B2cId;

  constructor(arg?: FbsChangeSet | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsChangeSet.getSizePrefixedRootAsChangeSet(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsChangeSet) {
      this._initFromFbs(arg);
    } else {
      this._attributes = null;
      this._ops = [];
      this._revision = new ContentId();
      this._when = BigInt(0);
      this._who = new B2cId();
    }
  }

  private _initFromFbs(fbs: FbsChangeSet): void {
    if (fbs.attributesLength() > 0) {
      this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
        const item = fbs.attributes(i);
        return item ? new Attr(item) : new Attr();
      });
    } else {
      this._attributes = null;
    }
    this._ops = Array.from({ length: fbs.opsLength() }, (_, i) => {
      const item = fbs.ops(i);
      return item ? new ChangeOpEntry(item) : new ChangeOpEntry();
    });
    const revisionVal = fbs.revision();
    this._revision = revisionVal ? new ContentId(revisionVal) : new ContentId();
    this._when = fbs.when();
    const whoVal = fbs.who();
    this._who = whoVal ? new B2cId(whoVal) : new B2cId();
  }

  get attributes(): Attr[] | null {
    return this._attributes;
  }

  set attributes(value: Attr[] | null) {
    this._attributes = value;
  }

  get ops(): ChangeOpEntry[] {
    return this._ops;
  }

  set ops(value: ChangeOpEntry[]) {
    this._ops = value;
  }

  get revision(): ContentId {
    return this._revision;
  }

  set revision(value: ContentId) {
    this._revision = value;
  }

  get when(): bigint {
    return this._when;
  }

  set when(value: bigint) {
    this._when = value;
  }

  get who(): B2cId {
    return this._who;
  }

  set who(value: B2cId) {
    this._who = value;
  }

  toFbsT(): FbsChangeSetT {
    const t = new FbsChangeSetT();
    t.attributes = this._attributes ? this._attributes.map(item => item.toFbsT()) : [];
    t.ops = this._ops.map(item => item.toFbsT());
    t.revision = this._revision.toFbsT();
    t.when = this._when;
    t.who = this._who.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  A DiffStream encodes a sequence of operations that should be performed on a table.
 *  The operations are applied in order to the table, i.e. the ordering of the `seq` field is significant.
 */
export class DiffStream {
/**
 *  We can optionally associate attributes with the diffstream.
 *  When the change history of the table is retrieved, the attributes from the diffstream
 *  will be accessible as the `attributes` field on the ChangeSet associated with this diffstream.
 */
  private _attributes!: Attr[] | null;

/**
 *  This is the head revision of the directory object that contains the table.
 */
  private _base!: ContentId;

  private _seq!: OpEntry[];

  constructor(arg?: FbsDiffStream | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDiffStream.getSizePrefixedRootAsDiffStream(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDiffStream) {
      this._initFromFbs(arg);
    } else {
      this._attributes = null;
      this._base = new ContentId();
      this._seq = [];
    }
  }

  private _initFromFbs(fbs: FbsDiffStream): void {
    if (fbs.attributesLength() > 0) {
      this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
        const item = fbs.attributes(i);
        return item ? new Attr(item) : new Attr();
      });
    } else {
      this._attributes = null;
    }
    const baseVal = fbs.base();
    this._base = baseVal ? new ContentId(baseVal) : new ContentId();
    this._seq = Array.from({ length: fbs.seqLength() }, (_, i) => {
      const item = fbs.seq(i);
      return item ? new OpEntry(item) : new OpEntry();
    });
  }

  get attributes(): Attr[] | null {
    return this._attributes;
  }

  set attributes(value: Attr[] | null) {
    this._attributes = value;
  }

  get base(): ContentId {
    return this._base;
  }

  set base(value: ContentId) {
    this._base = value;
  }

  get seq(): OpEntry[] {
    return this._seq;
  }

  set seq(value: OpEntry[]) {
    this._seq = value;
  }

  toFbsT(): FbsDiffStreamT {
    const t = new FbsDiffStreamT();
    t.attributes = this._attributes ? this._attributes.map(item => item.toFbsT()) : [];
    t.base = this._base.toFbsT();
    t.seq = this._seq.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class History {
  private _changes!: ChangeSet[];

  private _continuationId!: ContentId | null;

  constructor(arg?: FbsHistory | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsHistory.getSizePrefixedRootAsHistory(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsHistory) {
      this._initFromFbs(arg);
    } else {
      this._changes = [];
      this._continuationId = null;
    }
  }

  private _initFromFbs(fbs: FbsHistory): void {
    this._changes = Array.from({ length: fbs.changesLength() }, (_, i) => {
      const item = fbs.changes(i);
      return item ? new ChangeSet(item) : new ChangeSet();
    });
    const continuationIdVal = fbs.continuationId();
    this._continuationId = continuationIdVal ? new ContentId(continuationIdVal) : null;
  }

  get changes(): ChangeSet[] {
    return this._changes;
  }

  set changes(value: ChangeSet[]) {
    this._changes = value;
  }

  get continuationId(): ContentId | null {
    return this._continuationId;
  }

  set continuationId(value: ContentId | null) {
    this._continuationId = value;
  }

  toFbsT(): FbsHistoryT {
    const t = new FbsHistoryT();
    t.changes = this._changes.map(item => item.toFbsT());
    t.continuationId = this._continuationId ? this._continuationId.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Body parameter for POST datacatalog/table
 */
export class NewTable {
/**
 *  The base to use for the table. If an object ID is provided, this will
 *  take the schema from the provided stream or metadata object. If a
 *  schema is provided, the table will be created, empty, from that.           
 */
  private _from!: TableFrom | null;

/**
 *  If true, data will be copied into the new table from the source ID
 *  provided.
 */
  private _migrate!: boolean;

  private _name!: string;

/**
 *  Parent drive directory in which the table is to be created.
 */
  private _parent!: ObjectId | null;

/**
 *  If specified, creates a new table using this as the object ID.
 */
  private _target!: ObjectId | null;

  constructor(arg?: FbsNewTable | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNewTable.getSizePrefixedRootAsNewTable(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNewTable) {
      this._initFromFbs(arg);
    } else {
      this._from = null;
      this._migrate = false;
      this._name = '';
      this._parent = null;
      this._target = null;
    }
  }

  private _initFromFbs(fbs: FbsNewTable): void {
    const fromTy = fbs.fromType();
    if (fromTy === FbsTableFrom.ObjectId) {
      const fromFbsVal = fbs.from(new FbsObjectId());
      this._from = fromFbsVal ? new ObjectId(fromFbsVal) : null;
    } else if (fromTy === FbsTableFrom.Schema) {
      const fromFbsVal = fbs.from(new FbsSchema());
      this._from = fromFbsVal ? new Schema(fromFbsVal) : null;
    } else {
      this._from = null;
    }
    this._migrate = fbs.migrate();
    this._name = fbs.name() ?? '';
    const parentVal = fbs.parent();
    this._parent = parentVal ? new ObjectId(parentVal) : null;
    const targetVal = fbs.target();
    this._target = targetVal ? new ObjectId(targetVal) : null;
  }

  get from(): TableFrom | null {
    return this._from;
  }

  set from(value: TableFrom | null) {
    this._from = value;
  }

  get migrate(): boolean {
    return this._migrate;
  }

  set migrate(value: boolean) {
    this._migrate = value;
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get parent(): ObjectId | null {
    return this._parent;
  }

  set parent(value: ObjectId | null) {
    this._parent = value;
  }

  get target(): ObjectId | null {
    return this._target;
  }

  set target(value: ObjectId | null) {
    this._target = value;
  }

  toFbsT(): FbsNewTableT {
    const t = new FbsNewTableT();
    if (this._from instanceof ObjectId) {
      t.fromType = FbsTableFrom.ObjectId;
      t.from = this._from.toFbsT();
    } else if (this._from instanceof Schema) {
      t.fromType = FbsTableFrom.Schema;
      t.from = this._from.toFbsT();
    }
    t.migrate = this._migrate;
    t.name = this._name;
    t.parent = this._parent ? this._parent.toFbsT() : null;
    t.target = this._target ? this._target.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class OpEntry {
  private _op!: Op | null;

  constructor(arg?: FbsOpEntry | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsOpEntry.getSizePrefixedRootAsOpEntry(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsOpEntry) {
      this._initFromFbs(arg);
    } else {
      this._op = null;
    }
  }

  private _initFromFbs(fbs: FbsOpEntry): void {
    const opTy = fbs.opType();
    if (opTy === FbsOp.Set) {
      const opFbsVal = fbs.op(new FbsSet());
      this._op = opFbsVal ? new Set(opFbsVal) : null;
    } else if (opTy === FbsOp.RmRow) {
      const opFbsVal = fbs.op(new FbsRmRow());
      this._op = opFbsVal ? new RmRow(opFbsVal) : null;
    } else if (opTy === FbsOp.RestoreRow) {
      const opFbsVal = fbs.op(new FbsRestoreRow());
      this._op = opFbsVal ? new RestoreRow(opFbsVal) : null;
    } else if (opTy === FbsOp.Append) {
      const opFbsVal = fbs.op(new FbsAppend());
      this._op = opFbsVal ? new Append(opFbsVal) : null;
    } else {
      this._op = null;
    }
  }

  get op(): Op | null {
    return this._op;
  }

  set op(value: Op | null) {
    this._op = value;
  }

  toFbsT(): FbsOpEntryT {
    const t = new FbsOpEntryT();
    if (this._op instanceof Set) {
      t.opType = FbsOp.Set;
      t.op = this._op.toFbsT();
    } else if (this._op instanceof RmRow) {
      t.opType = FbsOp.RmRow;
      t.op = this._op.toFbsT();
    } else if (this._op instanceof RestoreRow) {
      t.opType = FbsOp.RestoreRow;
      t.op = this._op.toFbsT();
    } else if (this._op instanceof Append) {
      t.opType = FbsOp.Append;
      t.op = this._op.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
