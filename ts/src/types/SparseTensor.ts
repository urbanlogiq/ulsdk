// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { SparseMatrixIndexCSX as FbsSparseMatrixIndexCSX, SparseMatrixIndexCSXT as FbsSparseMatrixIndexCSXT } from './generated/sparse-matrix-index-csx';
import { SparseTensor as FbsSparseTensor, SparseTensorT as FbsSparseTensorT } from './generated/sparse-tensor';
import { SparseTensorIndex as FbsSparseTensorIndex } from './generated/sparse-tensor-index';
import { SparseTensorIndexCOO as FbsSparseTensorIndexCOO, SparseTensorIndexCOOT as FbsSparseTensorIndexCOOT } from './generated/sparse-tensor-index-coo';
import { SparseTensorIndexCSF as FbsSparseTensorIndexCSF, SparseTensorIndexCSFT as FbsSparseTensorIndexCSFT } from './generated/sparse-tensor-index-csf';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { Tensor, TensorDim } from './Tensor';
import { Tensor as FbsTensor } from './generated/tensor';
import { TensorDim as FbsTensorDim } from './generated/tensor-dim';

export { SparseMatrixCompressedAxis } from './generated/sparse-matrix-compressed-axis';

/**
 *  ----------------------------------------------------------------------
 *  EXPERIMENTAL: Data structures for sparse tensors
 *  Coordinate (COO) format of sparse tensor index.
 *
 *  COO's index list are represented as a NxM matrix,
 *  where N is the number of non-zero values,
 *  and M is the number of dimensions of a sparse tensor.
 *
 *  indicesBuffer stores the location and size of the data of this indices
 *  matrix.  The value type and the stride of the indices matrix is
 *  specified in indicesType and indicesStrides fields.
 *
 *  For example, let X be a 2x3x4x5 tensor, and it has the following
 *  6 non-zero values:
 *  ```text
 *    X[0, 1, 2, 0] := 1
 *    X[1, 1, 2, 3] := 2
 *    X[0, 2, 1, 0] := 3
 *    X[0, 1, 3, 0] := 4
 *    X[0, 1, 2, 1] := 5
 *    X[1, 2, 0, 4] := 6
 *  ```
 *  In COO format, the index matrix of X is the following 4x6 matrix:
 *  ```text
 *    [[0, 0, 0, 0, 1, 1],
 *     [1, 1, 1, 2, 1, 2],
 *     [2, 2, 3, 1, 2, 0],
 *     [0, 1, 0, 0, 3, 4]]
 *  ```
 *  When isCanonical is true, the indices is sorted in lexicographical order
 *  (row-major order), and it does not have duplicated entries.  Otherwise,
 *  the indices may not be sorted, or may have duplicated entries.
 */
export class SparseTensorIndexCOO {
/**
 *  The location and size of the indices matrix's data
 */
  private _indicesBuffer!: Buffer;

/**
 *  Non-negative byte offsets to advance one value cell along each dimension
 *  If omitted, default to row-major order (C-like).
 */
  private _indicesStrides!: bigint[] | null;

/**
 *  The type of values in indicesBuffer
 */
  private _indicesType!: Int;

/**
 *  This flag is true if and only if the indices matrix is sorted in
 *  row-major order, and does not have duplicated entries.
 *  This sort order is the same as of Tensorflow's SparseTensor,
 *  but it is inverse order of SciPy's canonical coo_matrix
 *  (SciPy employs column-major order for its coo_matrix).
 */
  private _isCanonical!: boolean;

  constructor(arg?: FbsSparseTensorIndexCOO | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSparseTensorIndexCOO.getSizePrefixedRootAsSparseTensorIndexCOO(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSparseTensorIndexCOO) {
      this._initFromFbs(arg);
    } else {
      this._indicesBuffer = new Buffer();
      this._indicesStrides = null;
      this._indicesType = new Int();
      this._isCanonical = false;
    }
  }

  private _initFromFbs(fbs: FbsSparseTensorIndexCOO): void {
    const indicesBufferVal = fbs.indicesBuffer();
    this._indicesBuffer = indicesBufferVal ? new Buffer(indicesBufferVal) : new Buffer();
    if (fbs.indicesStridesLength() > 0) {
      this._indicesStrides = Array.from({ length: fbs.indicesStridesLength() }, (_, i) => fbs.indicesStrides(i)!);
    } else {
      this._indicesStrides = null;
    }
    const indicesTypeVal = fbs.indicesType();
    this._indicesType = indicesTypeVal ? new Int(indicesTypeVal) : new Int();
    this._isCanonical = fbs.isCanonical();
  }

  get indicesBuffer(): Buffer {
    return this._indicesBuffer;
  }

  set indicesBuffer(value: Buffer) {
    this._indicesBuffer = value;
  }

  get indicesStrides(): bigint[] | null {
    return this._indicesStrides;
  }

  set indicesStrides(value: bigint[] | null) {
    this._indicesStrides = value;
  }

  get indicesType(): Int {
    return this._indicesType;
  }

  set indicesType(value: Int) {
    this._indicesType = value;
  }

  get isCanonical(): boolean {
    return this._isCanonical;
  }

  set isCanonical(value: boolean) {
    this._isCanonical = value;
  }

  toFbsT(): FbsSparseTensorIndexCOOT {
    const t = new FbsSparseTensorIndexCOOT();
    t.indicesBuffer = this._indicesBuffer.toFbsT();
    t.indicesStrides = this._indicesStrides ?? [];
    t.indicesType = this._indicesType.toFbsT();
    t.isCanonical = this._isCanonical;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Compressed Sparse format, that is matrix-specific.
 */
export class SparseMatrixIndexCSX {
/**
 *  Which axis, row or column, is compressed
 */
  private _compressedAxis!: number;

/**
 *  indicesBuffer stores the location and size of the array that
 *  contains the column indices of the corresponding non-zero values.
 *  The type of index value is long.
 *
 *  For example, the indices of the above X is:
 *  ```text
 *    indices(X) = [1, 2, 2, 1, 3, 0, 2, 3, 1].
 *  ```
 *  Note that the indices are sorted in lexicographical order for each row.
 */
  private _indicesBuffer!: Buffer;

/**
 *  The type of values in indicesBuffer
 */
  private _indicesType!: Int;

/**
 *  indptrBuffer stores the location and size of indptr array that
 *  represents the range of the rows.
 *  The i-th row spans from `indptr[i]` to `indptr[i+1]` in the data.
 *  The length of this array is 1 + (the number of rows), and the type
 *  of index value is long.
 *
 *  For example, let X be the following 6x4 matrix:
 *  ```text
 *    X := [[0, 1, 2, 0],
 *          [0, 0, 3, 0],
 *          [0, 4, 0, 5],
 *          [0, 0, 0, 0],
 *          [6, 0, 7, 8],
 *          [0, 9, 0, 0]].
 *  ```
 *  The array of non-zero values in X is:
 *  ```text
 *    values(X) = [1, 2, 3, 4, 5, 6, 7, 8, 9].
 *  ```
 *  And the indptr of X is:
 *  ```text
 *    indptr(X) = [0, 2, 3, 5, 5, 8, 10].
 *  ```
 */
  private _indptrBuffer!: Buffer;

/**
 *  The type of values in indptrBuffer
 */
  private _indptrType!: Int;

  constructor(arg?: FbsSparseMatrixIndexCSX | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSparseMatrixIndexCSX.getSizePrefixedRootAsSparseMatrixIndexCSX(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSparseMatrixIndexCSX) {
      this._initFromFbs(arg);
    } else {
      this._compressedAxis = 0;
      this._indicesBuffer = new Buffer();
      this._indicesType = new Int();
      this._indptrBuffer = new Buffer();
      this._indptrType = new Int();
    }
  }

  private _initFromFbs(fbs: FbsSparseMatrixIndexCSX): void {
    this._compressedAxis = fbs.compressedAxis();
    const indicesBufferVal = fbs.indicesBuffer();
    this._indicesBuffer = indicesBufferVal ? new Buffer(indicesBufferVal) : new Buffer();
    const indicesTypeVal = fbs.indicesType();
    this._indicesType = indicesTypeVal ? new Int(indicesTypeVal) : new Int();
    const indptrBufferVal = fbs.indptrBuffer();
    this._indptrBuffer = indptrBufferVal ? new Buffer(indptrBufferVal) : new Buffer();
    const indptrTypeVal = fbs.indptrType();
    this._indptrType = indptrTypeVal ? new Int(indptrTypeVal) : new Int();
  }

  get compressedAxis(): number {
    return this._compressedAxis;
  }

  set compressedAxis(value: number) {
    this._compressedAxis = value;
  }

  get indicesBuffer(): Buffer {
    return this._indicesBuffer;
  }

  set indicesBuffer(value: Buffer) {
    this._indicesBuffer = value;
  }

  get indicesType(): Int {
    return this._indicesType;
  }

  set indicesType(value: Int) {
    this._indicesType = value;
  }

  get indptrBuffer(): Buffer {
    return this._indptrBuffer;
  }

  set indptrBuffer(value: Buffer) {
    this._indptrBuffer = value;
  }

  get indptrType(): Int {
    return this._indptrType;
  }

  set indptrType(value: Int) {
    this._indptrType = value;
  }

  toFbsT(): FbsSparseMatrixIndexCSXT {
    const t = new FbsSparseMatrixIndexCSXT();
    t.compressedAxis = this._compressedAxis;
    t.indicesBuffer = this._indicesBuffer.toFbsT();
    t.indicesType = this._indicesType.toFbsT();
    t.indptrBuffer = this._indptrBuffer.toFbsT();
    t.indptrType = this._indptrType.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  Compressed Sparse Fiber (CSF) sparse tensor index.
 */
export class SparseTensorIndexCSF {
/**
 *  axisOrder stores the sequence in which dimensions were traversed to
 *  produce the prefix tree.
 *  For example, the axisOrder for the above X is:
 *  ```text
 *    axisOrder(X) = [0, 1, 2, 3].
 *  ```
 */
  private _axisOrder!: number[];

/**
 *  indicesBuffers stores values of nodes.
 *  Each tensor dimension corresponds to a buffer in indicesBuffers.
 *  For example, the indicesBuffers for the above X is:
 *  ```text
 *    indicesBuffer(X) = [
 *                         [0, 1],
 *                         [0, 1, 1],
 *                         [0, 0, 1, 1],
 *                         [1, 2, 0, 2, 0, 0, 1, 2]
 *                       ].
 *  ```
 */
  private _indicesBuffers!: Buffer[];

/**
 *  The type of values in indicesBuffers
 */
  private _indicesType!: Int;

/**
 *  indptrBuffers stores the sparsity structure.
 *  Each two consecutive dimensions in a tensor correspond to a buffer in
 *  indptrBuffers. A pair of consecutive values at `indptrBuffers[dim][i]`
 *  and `indptrBuffers[dim][i + 1]` signify a range of nodes in
 *  `indicesBuffers[dim + 1]` who are children of `indicesBuffers[dim][i]` node.
 *
 *  For example, the indptrBuffers for the above X is:
 *  ```text
 *    indptrBuffer(X) = [
 *                        [0, 2, 3],
 *                        [0, 1, 3, 4],
 *                        [0, 2, 4, 5, 8]
 *                      ].
 *  ```
 */
  private _indptrBuffers!: Buffer[];

/**
 *  CSF is a generalization of compressed sparse row (CSR) index.
 *  See [smith2017knl](http://shaden.io/pub-files/smith2017knl.pdf)
 *
 *  CSF index recursively compresses each dimension of a tensor into a set
 *  of prefix trees. Each path from a root to leaf forms one tensor
 *  non-zero index. CSF is implemented with two arrays of buffers and one
 *  arrays of integers.
 *
 *  For example, let X be a 2x3x4x5 tensor and let it have the following
 *  8 non-zero values:
 *  ```text
 *    X[0, 0, 0, 1] := 1
 *    X[0, 0, 0, 2] := 2
 *    X[0, 1, 0, 0] := 3
 *    X[0, 1, 0, 2] := 4
 *    X[0, 1, 1, 0] := 5
 *    X[1, 1, 1, 0] := 6
 *    X[1, 1, 1, 1] := 7
 *    X[1, 1, 1, 2] := 8
 *  ```
 *  As a prefix tree this would be represented as:
 *  ```text
 *          0          1
 *         / \         |
 *        0   1        1
 *       /   / \       |
 *      0   0   1      1
 *     /|  /|   |    /| |
 *    1 2 0 2   0   0 1 2
 *  ```
 *  The type of values in indptrBuffers
 */
  private _indptrType!: Int;

  constructor(arg?: FbsSparseTensorIndexCSF | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSparseTensorIndexCSF.getSizePrefixedRootAsSparseTensorIndexCSF(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSparseTensorIndexCSF) {
      this._initFromFbs(arg);
    } else {
      this._axisOrder = [];
      this._indicesBuffers = [];
      this._indicesType = new Int();
      this._indptrBuffers = [];
      this._indptrType = new Int();
    }
  }

  private _initFromFbs(fbs: FbsSparseTensorIndexCSF): void {
    this._axisOrder = Array.from({ length: fbs.axisOrderLength() }, (_, i) => fbs.axisOrder(i)!);
    this._indicesBuffers = Array.from({ length: fbs.indicesBuffersLength() }, (_, i) => {
      const item = fbs.indicesBuffers(i);
      return item ? new Buffer(item) : new Buffer();
    });
    const indicesTypeVal = fbs.indicesType();
    this._indicesType = indicesTypeVal ? new Int(indicesTypeVal) : new Int();
    this._indptrBuffers = Array.from({ length: fbs.indptrBuffersLength() }, (_, i) => {
      const item = fbs.indptrBuffers(i);
      return item ? new Buffer(item) : new Buffer();
    });
    const indptrTypeVal = fbs.indptrType();
    this._indptrType = indptrTypeVal ? new Int(indptrTypeVal) : new Int();
  }

  get axisOrder(): number[] {
    return this._axisOrder;
  }

  set axisOrder(value: number[]) {
    this._axisOrder = value;
  }

  get indicesBuffers(): Buffer[] {
    return this._indicesBuffers;
  }

  set indicesBuffers(value: Buffer[]) {
    this._indicesBuffers = value;
  }

  get indicesType(): Int {
    return this._indicesType;
  }

  set indicesType(value: Int) {
    this._indicesType = value;
  }

  get indptrBuffers(): Buffer[] {
    return this._indptrBuffers;
  }

  set indptrBuffers(value: Buffer[]) {
    this._indptrBuffers = value;
  }

  get indptrType(): Int {
    return this._indptrType;
  }

  set indptrType(value: Int) {
    this._indptrType = value;
  }

  toFbsT(): FbsSparseTensorIndexCSFT {
    const t = new FbsSparseTensorIndexCSFT();
    t.axisOrder = this._axisOrder;
    t.indicesBuffers = this._indicesBuffers.map(item => item.toFbsT());
    t.indicesType = this._indicesType.toFbsT();
    t.indptrBuffers = this._indptrBuffers.map(item => item.toFbsT());
    t.indptrType = this._indptrType.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type SparseTensorIndex = SparseTensorIndexCOO | SparseMatrixIndexCSX | SparseTensorIndexCSF;

export class SparseTensor {
/**
 *  The location and size of the tensor's data
 */
  private _data!: Buffer;

/**
 *  The number of non-zero values in a sparse tensor.
 */
  private _nonZeroLength!: bigint;

/**
 *  The dimensions of the tensor, optionally named.
 */
  private _shape!: TensorDim[];

/**
 *  Sparse tensor index
 */
  private _sparseIndex!: SparseTensorIndex | null;

/**
 *  The type of data contained in a value cell.
 *  Currently only fixed-width value types are supported,
 *  no strings or nested types.
 */
  private _type!: Type | null;

  constructor(arg?: FbsSparseTensor | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSparseTensor.getSizePrefixedRootAsSparseTensor(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSparseTensor) {
      this._initFromFbs(arg);
    } else {
      this._data = new Buffer();
      this._nonZeroLength = BigInt(0);
      this._shape = [];
      this._sparseIndex = null;
      this._type = null;
    }
  }

  private _initFromFbs(fbs: FbsSparseTensor): void {
    const dataVal = fbs.data();
    this._data = dataVal ? new Buffer(dataVal) : new Buffer();
    this._nonZeroLength = fbs.nonZeroLength();
    this._shape = Array.from({ length: fbs.shapeLength() }, (_, i) => {
      const item = fbs.shape(i);
      return item ? new TensorDim(item) : new TensorDim();
    });
    const sparseIndexTy = fbs.sparseIndexType();
    if (sparseIndexTy === FbsSparseTensorIndex.SparseTensorIndexCOO) {
      const sparseIndexFbsVal = fbs.sparseIndex(new FbsSparseTensorIndexCOO());
      this._sparseIndex = sparseIndexFbsVal ? new SparseTensorIndexCOO(sparseIndexFbsVal) : null;
    } else if (sparseIndexTy === FbsSparseTensorIndex.SparseMatrixIndexCSX) {
      const sparseIndexFbsVal = fbs.sparseIndex(new FbsSparseMatrixIndexCSX());
      this._sparseIndex = sparseIndexFbsVal ? new SparseMatrixIndexCSX(sparseIndexFbsVal) : null;
    } else if (sparseIndexTy === FbsSparseTensorIndex.SparseTensorIndexCSF) {
      const sparseIndexFbsVal = fbs.sparseIndex(new FbsSparseTensorIndexCSF());
      this._sparseIndex = sparseIndexFbsVal ? new SparseTensorIndexCSF(sparseIndexFbsVal) : null;
    } else {
      this._sparseIndex = null;
    }
    const typeTy = fbs.typeType();
    if (typeTy === FbsType.Null) {
      const typeFbsVal = fbs.type(new FbsNull());
      this._type = typeFbsVal ? new Null(typeFbsVal) : null;
    } else if (typeTy === FbsType.Int) {
      const typeFbsVal = fbs.type(new FbsInt());
      this._type = typeFbsVal ? new Int(typeFbsVal) : null;
    } else if (typeTy === FbsType.FloatingPoint) {
      const typeFbsVal = fbs.type(new FbsFloatingPoint());
      this._type = typeFbsVal ? new FloatingPoint(typeFbsVal) : null;
    } else if (typeTy === FbsType.Binary) {
      const typeFbsVal = fbs.type(new FbsBinary());
      this._type = typeFbsVal ? new Binary(typeFbsVal) : null;
    } else if (typeTy === FbsType.Utf8) {
      const typeFbsVal = fbs.type(new FbsUtf8());
      this._type = typeFbsVal ? new Utf8(typeFbsVal) : null;
    } else if (typeTy === FbsType.Bool) {
      const typeFbsVal = fbs.type(new FbsBool());
      this._type = typeFbsVal ? new Bool(typeFbsVal) : null;
    } else if (typeTy === FbsType.Decimal) {
      const typeFbsVal = fbs.type(new FbsDecimal());
      this._type = typeFbsVal ? new Decimal(typeFbsVal) : null;
    } else if (typeTy === FbsType.Date) {
      const typeFbsVal = fbs.type(new FbsDate());
      this._type = typeFbsVal ? new Date(typeFbsVal) : null;
    } else if (typeTy === FbsType.Time) {
      const typeFbsVal = fbs.type(new FbsTime());
      this._type = typeFbsVal ? new Time(typeFbsVal) : null;
    } else if (typeTy === FbsType.Timestamp) {
      const typeFbsVal = fbs.type(new FbsTimestamp());
      this._type = typeFbsVal ? new Timestamp(typeFbsVal) : null;
    } else if (typeTy === FbsType.Interval) {
      const typeFbsVal = fbs.type(new FbsInterval());
      this._type = typeFbsVal ? new Interval(typeFbsVal) : null;
    } else if (typeTy === FbsType.List) {
      const typeFbsVal = fbs.type(new FbsList());
      this._type = typeFbsVal ? new List(typeFbsVal) : null;
    } else if (typeTy === FbsType.Struct_) {
      const typeFbsVal = fbs.type(new FbsStruct_());
      this._type = typeFbsVal ? new Struct_(typeFbsVal) : null;
    } else if (typeTy === FbsType.Union) {
      const typeFbsVal = fbs.type(new FbsUnion());
      this._type = typeFbsVal ? new Union(typeFbsVal) : null;
    } else if (typeTy === FbsType.FixedSizeBinary) {
      const typeFbsVal = fbs.type(new FbsFixedSizeBinary());
      this._type = typeFbsVal ? new FixedSizeBinary(typeFbsVal) : null;
    } else if (typeTy === FbsType.FixedSizeList) {
      const typeFbsVal = fbs.type(new FbsFixedSizeList());
      this._type = typeFbsVal ? new FixedSizeList(typeFbsVal) : null;
    } else if (typeTy === FbsType.Map) {
      const typeFbsVal = fbs.type(new FbsMap());
      this._type = typeFbsVal ? new Map(typeFbsVal) : null;
    } else if (typeTy === FbsType.Duration) {
      const typeFbsVal = fbs.type(new FbsDuration());
      this._type = typeFbsVal ? new Duration(typeFbsVal) : null;
    } else if (typeTy === FbsType.LargeBinary) {
      const typeFbsVal = fbs.type(new FbsLargeBinary());
      this._type = typeFbsVal ? new LargeBinary(typeFbsVal) : null;
    } else if (typeTy === FbsType.LargeUtf8) {
      const typeFbsVal = fbs.type(new FbsLargeUtf8());
      this._type = typeFbsVal ? new LargeUtf8(typeFbsVal) : null;
    } else if (typeTy === FbsType.LargeList) {
      const typeFbsVal = fbs.type(new FbsLargeList());
      this._type = typeFbsVal ? new LargeList(typeFbsVal) : null;
    } else if (typeTy === FbsType.RunEndEncoded) {
      const typeFbsVal = fbs.type(new FbsRunEndEncoded());
      this._type = typeFbsVal ? new RunEndEncoded(typeFbsVal) : null;
    } else if (typeTy === FbsType.BinaryView) {
      const typeFbsVal = fbs.type(new FbsBinaryView());
      this._type = typeFbsVal ? new BinaryView(typeFbsVal) : null;
    } else if (typeTy === FbsType.Utf8View) {
      const typeFbsVal = fbs.type(new FbsUtf8View());
      this._type = typeFbsVal ? new Utf8View(typeFbsVal) : null;
    } else if (typeTy === FbsType.ListView) {
      const typeFbsVal = fbs.type(new FbsListView());
      this._type = typeFbsVal ? new ListView(typeFbsVal) : null;
    } else if (typeTy === FbsType.LargeListView) {
      const typeFbsVal = fbs.type(new FbsLargeListView());
      this._type = typeFbsVal ? new LargeListView(typeFbsVal) : null;
    } else {
      this._type = null;
    }
  }

  get data(): Buffer {
    return this._data;
  }

  set data(value: Buffer) {
    this._data = value;
  }

  get nonZeroLength(): bigint {
    return this._nonZeroLength;
  }

  set nonZeroLength(value: bigint) {
    this._nonZeroLength = value;
  }

  get shape(): TensorDim[] {
    return this._shape;
  }

  set shape(value: TensorDim[]) {
    this._shape = value;
  }

  get sparseIndex(): SparseTensorIndex | null {
    return this._sparseIndex;
  }

  set sparseIndex(value: SparseTensorIndex | null) {
    this._sparseIndex = value;
  }

  get type(): Type | null {
    return this._type;
  }

  set type(value: Type | null) {
    this._type = value;
  }

  toFbsT(): FbsSparseTensorT {
    const t = new FbsSparseTensorT();
    t.data = this._data.toFbsT();
    t.nonZeroLength = this._nonZeroLength;
    t.shape = this._shape.map(item => item.toFbsT());
    if (this._sparseIndex instanceof SparseTensorIndexCOO) {
      t.sparseIndexType = FbsSparseTensorIndex.SparseTensorIndexCOO;
      t.sparseIndex = this._sparseIndex.toFbsT();
    } else if (this._sparseIndex instanceof SparseMatrixIndexCSX) {
      t.sparseIndexType = FbsSparseTensorIndex.SparseMatrixIndexCSX;
      t.sparseIndex = this._sparseIndex.toFbsT();
    } else if (this._sparseIndex instanceof SparseTensorIndexCSF) {
      t.sparseIndexType = FbsSparseTensorIndex.SparseTensorIndexCSF;
      t.sparseIndex = this._sparseIndex.toFbsT();
    }
    if (this._type instanceof Null) {
      t.typeType = FbsType.Null;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Int) {
      t.typeType = FbsType.Int;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof FloatingPoint) {
      t.typeType = FbsType.FloatingPoint;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Binary) {
      t.typeType = FbsType.Binary;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Utf8) {
      t.typeType = FbsType.Utf8;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Bool) {
      t.typeType = FbsType.Bool;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Decimal) {
      t.typeType = FbsType.Decimal;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Date) {
      t.typeType = FbsType.Date;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Time) {
      t.typeType = FbsType.Time;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Timestamp) {
      t.typeType = FbsType.Timestamp;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Interval) {
      t.typeType = FbsType.Interval;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof List) {
      t.typeType = FbsType.List;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Struct_) {
      t.typeType = FbsType.Struct_;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Union) {
      t.typeType = FbsType.Union;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof FixedSizeBinary) {
      t.typeType = FbsType.FixedSizeBinary;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof FixedSizeList) {
      t.typeType = FbsType.FixedSizeList;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Map) {
      t.typeType = FbsType.Map;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Duration) {
      t.typeType = FbsType.Duration;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof LargeBinary) {
      t.typeType = FbsType.LargeBinary;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof LargeUtf8) {
      t.typeType = FbsType.LargeUtf8;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof LargeList) {
      t.typeType = FbsType.LargeList;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof RunEndEncoded) {
      t.typeType = FbsType.RunEndEncoded;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof BinaryView) {
      t.typeType = FbsType.BinaryView;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof Utf8View) {
      t.typeType = FbsType.Utf8View;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof ListView) {
      t.typeType = FbsType.ListView;
      t.type = this._type.toFbsT();
    } else if (this._type instanceof LargeListView) {
      t.typeType = FbsType.LargeListView;
      t.type = this._type.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
