// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { EdgeList as FbsEdgeList, EdgeListT as FbsEdgeListT } from './generated/edge-list';
import { EdgeQuery as FbsEdgeQuery, EdgeQueryT as FbsEdgeQueryT } from './generated/edge-query';
import { Geom as FbsGeom, GeomT as FbsGeomT } from './generated/geom';
import { GeomOp as FbsGeomOp, GeomOpT as FbsGeomOpT } from './generated/geom-op';
import { GraphQuery as FbsGraphQuery, GraphQueryT as FbsGraphQueryT } from './generated/graph-query';
import { NodeIdPair as FbsNodeIdPair, NodeIdPairT as FbsNodeIdPairT } from './generated/node-id-pair';
import { NodeList as FbsNodeList, NodeListT as FbsNodeListT } from './generated/node-list';
import { NodeQuery as FbsNodeQuery, NodeQueryT as FbsNodeQueryT } from './generated/node-query';
import { OrderBy as FbsOrderBy, OrderByT as FbsOrderByT } from './generated/order-by';
import { Projection as FbsProjection, ProjectionT as FbsProjectionT } from './generated/projection';
import { QueryPathElement as FbsQueryPathElement, QueryPathElementT as FbsQueryPathElementT } from './generated/query-path-element';
import { QueryPathElementUnion as FbsQueryPathElementUnion } from './generated/query-path-element-union';

import { SortOrder } from './api';
import { SortOrder as FbsSortOrder } from './generated/sort-order';
import { EdgeTy, EntityTy, Geometry, GraphEdge, GraphNode, Line, MultiLine, MultiPoint, MultiPolygon, NodeTy, Point, Polygon } from './entity';
import { EdgeTy as FbsEdgeTy } from './generated/edge-ty';
import { EntityTy as FbsEntityTy } from './generated/entity-ty';
import { Geometry as FbsGeometry } from './generated/geometry';
import { GraphEdge as FbsGraphEdge } from './generated/graph-edge';
import { GraphNode as FbsGraphNode } from './generated/graph-node';
import { Line as FbsLine } from './generated/line';
import { MultiLine as FbsMultiLine } from './generated/multi-line';
import { MultiPoint as FbsMultiPoint } from './generated/multi-point';
import { MultiPolygon as FbsMultiPolygon } from './generated/multi-polygon';
import { NodeTy as FbsNodeTy } from './generated/node-ty';
import { Point as FbsPoint } from './generated/point';
import { Polygon as FbsPolygon } from './generated/polygon';
import { Fn } from './fun';
import { Fn as FbsFn } from './generated/fn';
import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { StreamId as FbsStreamId } from './generated/stream-id';

export { Predicate } from './generated/predicate';

export class GeomOp {
  private _geoms!: Geom[];

  private _op!: number;

  private _predicate!: number;

  constructor(arg?: FbsGeomOp | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGeomOp.getSizePrefixedRootAsGeomOp(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGeomOp) {
      this._initFromFbs(arg);
    } else {
      this._geoms = [];
      this._op = 0;
      this._predicate = 0;
    }
  }

  private _initFromFbs(fbs: FbsGeomOp): void {
    this._geoms = Array.from({ length: fbs.geomsLength() }, (_, i) => {
      const item = fbs.geoms(i);
      return item ? new Geom(item) : new Geom();
    });
    this._op = fbs.op();
    this._predicate = fbs.predicate();
  }

  get geoms(): Geom[] {
    return this._geoms;
  }

  set geoms(value: Geom[]) {
    this._geoms = value;
  }

  get op(): number {
    return this._op;
  }

  set op(value: number) {
    this._op = value;
  }

  get predicate(): number {
    return this._predicate;
  }

  set predicate(value: number) {
    this._predicate = value;
  }

  toFbsT(): FbsGeomOpT {
    const t = new FbsGeomOpT();
    t.geoms = this._geoms.map(item => item.toFbsT());
    t.op = this._op;
    t.predicate = this._predicate;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class NodeQuery {
/**
 *  If descriptions are provided here, then results will be ordered by their string similarity to the
 *  descriptions here. This ordering is secondary to the any top-level order_by that might be provided.
 */
  private _descriptions!: string[] | null;

  private _entityTys!: number[] | null;

  private _geomOp!: GeomOp | null;

  private _nodeIds!: NodeIdPair[] | null;

  private _projections!: Projection[] | null;

  private _streamIds!: ObjectId[] | null;

  constructor(arg?: FbsNodeQuery | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNodeQuery.getSizePrefixedRootAsNodeQuery(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNodeQuery) {
      this._initFromFbs(arg);
    } else {
      this._descriptions = null;
      this._entityTys = null;
      this._geomOp = null;
      this._nodeIds = null;
      this._projections = null;
      this._streamIds = null;
    }
  }

  private _initFromFbs(fbs: FbsNodeQuery): void {
    if (fbs.descriptionsLength() > 0) {
      this._descriptions = Array.from({ length: fbs.descriptionsLength() }, (_, i) => fbs.descriptions(i) ?? '');
    } else {
      this._descriptions = null;
    }
    if (fbs.entityTysLength() > 0) {
      this._entityTys = Array.from({ length: fbs.entityTysLength() }, (_, i) => fbs.entityTys(i)!);
    } else {
      this._entityTys = null;
    }
    const geomOpVal = fbs.geomOp();
    this._geomOp = geomOpVal ? new GeomOp(geomOpVal) : null;
    if (fbs.nodeIdsLength() > 0) {
      this._nodeIds = Array.from({ length: fbs.nodeIdsLength() }, (_, i) => {
        const item = fbs.nodeIds(i);
        return item ? new NodeIdPair(item) : new NodeIdPair();
      });
    } else {
      this._nodeIds = null;
    }
    if (fbs.projectionsLength() > 0) {
      this._projections = Array.from({ length: fbs.projectionsLength() }, (_, i) => {
        const item = fbs.projections(i);
        return item ? new Projection(item) : new Projection();
      });
    } else {
      this._projections = null;
    }
    if (fbs.streamIdsLength() > 0) {
      this._streamIds = Array.from({ length: fbs.streamIdsLength() }, (_, i) => {
        const item = fbs.streamIds(i);
        return item ? new ObjectId(item) : new ObjectId();
      });
    } else {
      this._streamIds = null;
    }
  }

  get descriptions(): string[] | null {
    return this._descriptions;
  }

  set descriptions(value: string[] | null) {
    this._descriptions = value;
  }

  get entityTys(): number[] | null {
    return this._entityTys;
  }

  set entityTys(value: number[] | null) {
    this._entityTys = value;
  }

  get geomOp(): GeomOp | null {
    return this._geomOp;
  }

  set geomOp(value: GeomOp | null) {
    this._geomOp = value;
  }

  get nodeIds(): NodeIdPair[] | null {
    return this._nodeIds;
  }

  set nodeIds(value: NodeIdPair[] | null) {
    this._nodeIds = value;
  }

  get projections(): Projection[] | null {
    return this._projections;
  }

  set projections(value: Projection[] | null) {
    this._projections = value;
  }

  get streamIds(): ObjectId[] | null {
    return this._streamIds;
  }

  set streamIds(value: ObjectId[] | null) {
    this._streamIds = value;
  }

  toFbsT(): FbsNodeQueryT {
    const t = new FbsNodeQueryT();
    t.descriptions = this._descriptions ?? [];
    t.entityTys = this._entityTys ?? [];
    t.geomOp = this._geomOp ? this._geomOp.toFbsT() : null;
    t.nodeIds = this._nodeIds ? this._nodeIds.map(item => item.toFbsT()) : [];
    t.projections = this._projections ? this._projections.map(item => item.toFbsT()) : [];
    t.streamIds = this._streamIds ? this._streamIds.map(item => item.toFbsT()) : [];
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class EdgeQuery {
  private _edgeTy!: number;

  constructor(arg?: FbsEdgeQuery | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsEdgeQuery.getSizePrefixedRootAsEdgeQuery(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsEdgeQuery) {
      this._initFromFbs(arg);
    } else {
      this._edgeTy = 0;
    }
  }

  private _initFromFbs(fbs: FbsEdgeQuery): void {
    this._edgeTy = fbs.edgeTy();
  }

  get edgeTy(): number {
    return this._edgeTy;
  }

  set edgeTy(value: number) {
    this._edgeTy = value;
  }

  toFbsT(): FbsEdgeQueryT {
    const t = new FbsEdgeQueryT();
    t.edgeTy = this._edgeTy;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type QueryPathElementUnion = NodeQuery | EdgeQuery;

export { ValueTransform } from './generated/value-transform';

export class EdgeList {
  private _edges!: GraphEdge[];

  constructor(arg?: FbsEdgeList | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsEdgeList.getSizePrefixedRootAsEdgeList(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsEdgeList) {
      this._initFromFbs(arg);
    } else {
      this._edges = [];
    }
  }

  private _initFromFbs(fbs: FbsEdgeList): void {
    this._edges = Array.from({ length: fbs.edgesLength() }, (_, i) => {
      const item = fbs.edges(i);
      return item ? new GraphEdge(item) : new GraphEdge();
    });
  }

  get edges(): GraphEdge[] {
    return this._edges;
  }

  set edges(value: GraphEdge[]) {
    this._edges = value;
  }

  toFbsT(): FbsEdgeListT {
    const t = new FbsEdgeListT();
    t.edges = this._edges.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Geom {
  private _geom!: Geometry | null;

  constructor(arg?: FbsGeom | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGeom.getSizePrefixedRootAsGeom(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGeom) {
      this._initFromFbs(arg);
    } else {
      this._geom = null;
    }
  }

  private _initFromFbs(fbs: FbsGeom): void {
    const geomTy = fbs.geomType();
    if (geomTy === FbsGeometry.Point) {
      const geomFbsVal = fbs.geom(new FbsPoint());
      this._geom = geomFbsVal ? new Point(geomFbsVal) : null;
    } else if (geomTy === FbsGeometry.Line) {
      const geomFbsVal = fbs.geom(new FbsLine());
      this._geom = geomFbsVal ? new Line(geomFbsVal) : null;
    } else if (geomTy === FbsGeometry.MultiLine) {
      const geomFbsVal = fbs.geom(new FbsMultiLine());
      this._geom = geomFbsVal ? new MultiLine(geomFbsVal) : null;
    } else if (geomTy === FbsGeometry.Polygon) {
      const geomFbsVal = fbs.geom(new FbsPolygon());
      this._geom = geomFbsVal ? new Polygon(geomFbsVal) : null;
    } else if (geomTy === FbsGeometry.MultiPolygon) {
      const geomFbsVal = fbs.geom(new FbsMultiPolygon());
      this._geom = geomFbsVal ? new MultiPolygon(geomFbsVal) : null;
    } else if (geomTy === FbsGeometry.MultiPoint) {
      const geomFbsVal = fbs.geom(new FbsMultiPoint());
      this._geom = geomFbsVal ? new MultiPoint(geomFbsVal) : null;
    } else {
      this._geom = null;
    }
  }

  get geom(): Geometry | null {
    return this._geom;
  }

  set geom(value: Geometry | null) {
    this._geom = value;
  }

  toFbsT(): FbsGeomT {
    const t = new FbsGeomT();
    if (this._geom instanceof Point) {
      t.geomType = FbsGeometry.Point;
      t.geom = this._geom.toFbsT();
    } else if (this._geom instanceof Line) {
      t.geomType = FbsGeometry.Line;
      t.geom = this._geom.toFbsT();
    } else if (this._geom instanceof MultiLine) {
      t.geomType = FbsGeometry.MultiLine;
      t.geom = this._geom.toFbsT();
    } else if (this._geom instanceof Polygon) {
      t.geomType = FbsGeometry.Polygon;
      t.geom = this._geom.toFbsT();
    } else if (this._geom instanceof MultiPolygon) {
      t.geomType = FbsGeometry.MultiPolygon;
      t.geom = this._geom.toFbsT();
    } else if (this._geom instanceof MultiPoint) {
      t.geomType = FbsGeometry.MultiPoint;
      t.geom = this._geom.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  The GraphQuery encapsulates the entire world graph query.
 */
export class GraphQuery {
  private _limit!: number;

  private _orderBy!: OrderBy[] | null;

  private _path!: QueryPathElement[];

  constructor(arg?: FbsGraphQuery | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsGraphQuery.getSizePrefixedRootAsGraphQuery(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsGraphQuery) {
      this._initFromFbs(arg);
    } else {
      this._limit = 0;
      this._orderBy = null;
      this._path = [];
    }
  }

  private _initFromFbs(fbs: FbsGraphQuery): void {
    this._limit = fbs.limit();
    if (fbs.orderByLength() > 0) {
      this._orderBy = Array.from({ length: fbs.orderByLength() }, (_, i) => {
        const item = fbs.orderBy(i);
        return item ? new OrderBy(item) : new OrderBy();
      });
    } else {
      this._orderBy = null;
    }
    this._path = Array.from({ length: fbs.pathLength() }, (_, i) => {
      const item = fbs.path(i);
      return item ? new QueryPathElement(item) : new QueryPathElement();
    });
  }

  get limit(): number {
    return this._limit;
  }

  set limit(value: number) {
    this._limit = value;
  }

  get orderBy(): OrderBy[] | null {
    return this._orderBy;
  }

  set orderBy(value: OrderBy[] | null) {
    this._orderBy = value;
  }

  get path(): QueryPathElement[] {
    return this._path;
  }

  set path(value: QueryPathElement[]) {
    this._path = value;
  }

  toFbsT(): FbsGraphQueryT {
    const t = new FbsGraphQueryT();
    t.limit = this._limit;
    t.orderBy = this._orderBy ? this._orderBy.map(item => item.toFbsT()) : [];
    t.path = this._path.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class NodeIdPair {
  private _nodeId!: GraphNodeId;

  private _streamId!: ObjectId | null;

  constructor(arg?: FbsNodeIdPair | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNodeIdPair.getSizePrefixedRootAsNodeIdPair(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNodeIdPair) {
      this._initFromFbs(arg);
    } else {
      this._nodeId = new GraphNodeId();
      this._streamId = null;
    }
  }

  private _initFromFbs(fbs: FbsNodeIdPair): void {
    const nodeIdVal = fbs.nodeId();
    this._nodeId = nodeIdVal ? new GraphNodeId(nodeIdVal) : new GraphNodeId();
    const streamIdVal = fbs.streamId();
    this._streamId = streamIdVal ? new ObjectId(streamIdVal) : null;
  }

  get nodeId(): GraphNodeId {
    return this._nodeId;
  }

  set nodeId(value: GraphNodeId) {
    this._nodeId = value;
  }

  get streamId(): ObjectId | null {
    return this._streamId;
  }

  set streamId(value: ObjectId | null) {
    this._streamId = value;
  }

  toFbsT(): FbsNodeIdPairT {
    const t = new FbsNodeIdPairT();
    t.nodeId = this._nodeId.toFbsT();
    t.streamId = this._streamId ? this._streamId.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class NodeList {
  private _nodes!: GraphNode[];

  constructor(arg?: FbsNodeList | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNodeList.getSizePrefixedRootAsNodeList(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNodeList) {
      this._initFromFbs(arg);
    } else {
      this._nodes = [];
    }
  }

  private _initFromFbs(fbs: FbsNodeList): void {
    this._nodes = Array.from({ length: fbs.nodesLength() }, (_, i) => {
      const item = fbs.nodes(i);
      return item ? new GraphNode(item) : new GraphNode();
    });
  }

  get nodes(): GraphNode[] {
    return this._nodes;
  }

  set nodes(value: GraphNode[]) {
    this._nodes = value;
  }

  toFbsT(): FbsNodeListT {
    const t = new FbsNodeListT();
    t.nodes = this._nodes.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class OrderBy {
  private _field!: string;

  private _nullsFirst!: boolean;

  private _sort!: number;

  private _transform!: number;

  constructor(arg?: FbsOrderBy | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsOrderBy.getSizePrefixedRootAsOrderBy(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsOrderBy) {
      this._initFromFbs(arg);
    } else {
      this._field = '';
      this._nullsFirst = false;
      this._sort = 0;
      this._transform = 0;
    }
  }

  private _initFromFbs(fbs: FbsOrderBy): void {
    this._field = fbs.field() ?? '';
    this._nullsFirst = fbs.nullsFirst();
    this._sort = fbs.sort();
    this._transform = fbs.transform();
  }

  get field(): string {
    return this._field;
  }

  set field(value: string) {
    this._field = value;
  }

  get nullsFirst(): boolean {
    return this._nullsFirst;
  }

  set nullsFirst(value: boolean) {
    this._nullsFirst = value;
  }

  get sort(): number {
    return this._sort;
  }

  set sort(value: number) {
    this._sort = value;
  }

  get transform(): number {
    return this._transform;
  }

  set transform(value: number) {
    this._transform = value;
  }

  toFbsT(): FbsOrderByT {
    const t = new FbsOrderByT();
    t.field = this._field;
    t.nullsFirst = this._nullsFirst;
    t.sort = this._sort;
    t.transform = this._transform;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Projection {
  private _alias!: string;

  private _predicate!: number;

  constructor(arg?: FbsProjection | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsProjection.getSizePrefixedRootAsProjection(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsProjection) {
      this._initFromFbs(arg);
    } else {
      this._alias = '';
      this._predicate = 0;
    }
  }

  private _initFromFbs(fbs: FbsProjection): void {
    this._alias = fbs.alias() ?? '';
    this._predicate = fbs.predicate();
  }

  get alias(): string {
    return this._alias;
  }

  set alias(value: string) {
    this._alias = value;
  }

  get predicate(): number {
    return this._predicate;
  }

  set predicate(value: number) {
    this._predicate = value;
  }

  toFbsT(): FbsProjectionT {
    const t = new FbsProjectionT();
    t.alias = this._alias;
    t.predicate = this._predicate;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class QueryPathElement {
  private _element!: QueryPathElementUnion | null;

  constructor(arg?: FbsQueryPathElement | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsQueryPathElement.getSizePrefixedRootAsQueryPathElement(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsQueryPathElement) {
      this._initFromFbs(arg);
    } else {
      this._element = null;
    }
  }

  private _initFromFbs(fbs: FbsQueryPathElement): void {
    const elementTy = fbs.elementType();
    if (elementTy === FbsQueryPathElementUnion.NodeQuery) {
      const elementFbsVal = fbs.element(new FbsNodeQuery());
      this._element = elementFbsVal ? new NodeQuery(elementFbsVal) : null;
    } else if (elementTy === FbsQueryPathElementUnion.EdgeQuery) {
      const elementFbsVal = fbs.element(new FbsEdgeQuery());
      this._element = elementFbsVal ? new EdgeQuery(elementFbsVal) : null;
    } else {
      this._element = null;
    }
  }

  get element(): QueryPathElementUnion | null {
    return this._element;
  }

  set element(value: QueryPathElementUnion | null) {
    this._element = value;
  }

  toFbsT(): FbsQueryPathElementT {
    const t = new FbsQueryPathElementT();
    if (this._element instanceof NodeQuery) {
      t.elementType = FbsQueryPathElementUnion.NodeQuery;
      t.element = this._element.toFbsT();
    } else if (this._element instanceof EdgeQuery) {
      t.elementType = FbsQueryPathElementUnion.EdgeQuery;
      t.element = this._element.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
