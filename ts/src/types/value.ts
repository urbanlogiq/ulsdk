// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { Point2D as FbsPoint2D, Point2DT as FbsPoint2DT } from './generated/point2-d';
import { Tri2D as FbsTri2D, Tri2DT as FbsTri2DT } from './generated/tri2-d';
import { VArray as FbsVArray, VArrayT as FbsVArrayT } from './generated/varray';
import { VBool as FbsVBool, VBoolT as FbsVBoolT } from './generated/vbool';
import { VBytes as FbsVBytes, VBytesT as FbsVBytesT } from './generated/vbytes';
import { VChar as FbsVChar, VCharT as FbsVCharT } from './generated/vchar';
import { VF32 as FbsVF32, VF32T as FbsVF32T } from './generated/vf32';
import { VF64 as FbsVF64, VF64T as FbsVF64T } from './generated/vf64';
import { VFixedSizeBytes as FbsVFixedSizeBytes, VFixedSizeBytesT as FbsVFixedSizeBytesT } from './generated/vfixed-size-bytes';
import { VI16 as FbsVI16, VI16T as FbsVI16T } from './generated/vi16';
import { VI32 as FbsVI32, VI32T as FbsVI32T } from './generated/vi32';
import { VI64 as FbsVI64, VI64T as FbsVI64T } from './generated/vi64';
import { VI8 as FbsVI8, VI8T as FbsVI8T } from './generated/vi8';
import { VIsize as FbsVIsize, VIsizeT as FbsVIsizeT } from './generated/visize';
import { VNull as FbsVNull, VNullT as FbsVNullT } from './generated/vnull';
import { VPlaceholder as FbsVPlaceholder, VPlaceholderT as FbsVPlaceholderT } from './generated/vplaceholder';
import { VStr as FbsVStr, VStrT as FbsVStrT } from './generated/vstr';
import { VTimestampMs as FbsVTimestampMs, VTimestampMsT as FbsVTimestampMsT } from './generated/vtimestamp-ms';
import { VTimestampMsUtc as FbsVTimestampMsUtc, VTimestampMsUtcT as FbsVTimestampMsUtcT } from './generated/vtimestamp-ms-utc';
import { VTimestampNs as FbsVTimestampNs, VTimestampNsT as FbsVTimestampNsT } from './generated/vtimestamp-ns';
import { VTimestampNsUtc as FbsVTimestampNsUtc, VTimestampNsUtcT as FbsVTimestampNsUtcT } from './generated/vtimestamp-ns-utc';
import { VTri2D as FbsVTri2D, VTri2DT as FbsVTri2DT } from './generated/vtri2-d';
import { VU16 as FbsVU16, VU16T as FbsVU16T } from './generated/vu16';
import { VU32 as FbsVU32, VU32T as FbsVU32T } from './generated/vu32';
import { VU64 as FbsVU64, VU64T as FbsVU64T } from './generated/vu64';
import { VU8 as FbsVU8, VU8T as FbsVU8T } from './generated/vu8';
import { VUnit as FbsVUnit, VUnitT as FbsVUnitT } from './generated/vunit';
import { VUsize as FbsVUsize, VUsizeT as FbsVUsizeT } from './generated/vusize';
import { Value as FbsValue } from './generated/value';
import { ValueInstance as FbsValueInstance, ValueInstanceT as FbsValueInstanceT } from './generated/value-instance';

export class VBool {
  private _v!: boolean;

  constructor(arg?: FbsVBool | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVBool.getSizePrefixedRootAsVBool(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVBool) {
      this._initFromFbs(arg);
    } else {
      this._v = false;
    }
  }

  private _initFromFbs(fbs: FbsVBool): void {
    this._v = fbs.v();
  }

  get v(): boolean {
    return this._v;
  }

  set v(value: boolean) {
    this._v = value;
  }

  toFbsT(): FbsVBoolT {
    const t = new FbsVBoolT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VUnit {
  constructor(arg?: FbsVUnit | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVUnit.getSizePrefixedRootAsVUnit(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVUnit) {
      this._initFromFbs(arg);
    } else {
    }
  }

  private _initFromFbs(fbs: FbsVUnit): void {
  }

  toFbsT(): FbsVUnitT {
    const t = new FbsVUnitT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VChar {
  private _v!: number;

  constructor(arg?: FbsVChar | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVChar.getSizePrefixedRootAsVChar(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVChar) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVChar): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVCharT {
    const t = new FbsVCharT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VNull {
  constructor(arg?: FbsVNull | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVNull.getSizePrefixedRootAsVNull(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVNull) {
      this._initFromFbs(arg);
    } else {
    }
  }

  private _initFromFbs(fbs: FbsVNull): void {
  }

  toFbsT(): FbsVNullT {
    const t = new FbsVNullT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VI8 {
  private _v!: number;

  constructor(arg?: FbsVI8 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVI8.getSizePrefixedRootAsVI8(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVI8) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVI8): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVI8T {
    const t = new FbsVI8T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VU8 {
  private _v!: number;

  constructor(arg?: FbsVU8 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVU8.getSizePrefixedRootAsVU8(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVU8) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVU8): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVU8T {
    const t = new FbsVU8T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VI16 {
  private _v!: number;

  constructor(arg?: FbsVI16 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVI16.getSizePrefixedRootAsVI16(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVI16) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVI16): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVI16T {
    const t = new FbsVI16T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VU16 {
  private _v!: number;

  constructor(arg?: FbsVU16 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVU16.getSizePrefixedRootAsVU16(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVU16) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVU16): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVU16T {
    const t = new FbsVU16T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VI32 {
  private _v!: number;

  constructor(arg?: FbsVI32 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVI32.getSizePrefixedRootAsVI32(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVI32) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVI32): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVI32T {
    const t = new FbsVI32T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VU32 {
  private _v!: number;

  constructor(arg?: FbsVU32 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVU32.getSizePrefixedRootAsVU32(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVU32) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVU32): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVU32T {
    const t = new FbsVU32T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VF32 {
  private _v!: number;

  constructor(arg?: FbsVF32 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVF32.getSizePrefixedRootAsVF32(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVF32) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVF32): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVF32T {
    const t = new FbsVF32T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VIsize {
  private _v!: bigint;

  constructor(arg?: FbsVIsize | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVIsize.getSizePrefixedRootAsVIsize(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVIsize) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVIsize): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVIsizeT {
    const t = new FbsVIsizeT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VUsize {
  private _v!: bigint;

  constructor(arg?: FbsVUsize | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVUsize.getSizePrefixedRootAsVUsize(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVUsize) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVUsize): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVUsizeT {
    const t = new FbsVUsizeT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VI64 {
  private _v!: bigint;

  constructor(arg?: FbsVI64 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVI64.getSizePrefixedRootAsVI64(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVI64) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVI64): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVI64T {
    const t = new FbsVI64T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VU64 {
  private _v!: bigint;

  constructor(arg?: FbsVU64 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVU64.getSizePrefixedRootAsVU64(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVU64) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVU64): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVU64T {
    const t = new FbsVU64T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VF64 {
  private _v!: number;

  constructor(arg?: FbsVF64 | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVF64.getSizePrefixedRootAsVF64(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVF64) {
      this._initFromFbs(arg);
    } else {
      this._v = 0;
    }
  }

  private _initFromFbs(fbs: FbsVF64): void {
    this._v = fbs.v();
  }

  get v(): number {
    return this._v;
  }

  set v(value: number) {
    this._v = value;
  }

  toFbsT(): FbsVF64T {
    const t = new FbsVF64T();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VStr {
  private _v!: string;

  constructor(arg?: FbsVStr | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVStr.getSizePrefixedRootAsVStr(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVStr) {
      this._initFromFbs(arg);
    } else {
      this._v = '';
    }
  }

  private _initFromFbs(fbs: FbsVStr): void {
    this._v = fbs.v() ?? '';
  }

  get v(): string {
    return this._v;
  }

  set v(value: string) {
    this._v = value;
  }

  toFbsT(): FbsVStrT {
    const t = new FbsVStrT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VBytes {
  private _v!: number[];

  constructor(arg?: FbsVBytes | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVBytes.getSizePrefixedRootAsVBytes(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVBytes) {
      this._initFromFbs(arg);
    } else {
      this._v = [];
    }
  }

  private _initFromFbs(fbs: FbsVBytes): void {
    const vArr = fbs.vArray();
    this._v = vArr ? Array.from(vArr) : [];
  }

  get v(): number[] {
    return this._v;
  }

  set v(value: number[]) {
    this._v = value;
  }

  toFbsT(): FbsVBytesT {
    const t = new FbsVBytesT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VArray {
  private _v!: ValueInstance[];

  constructor(arg?: FbsVArray | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVArray.getSizePrefixedRootAsVArray(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVArray) {
      this._initFromFbs(arg);
    } else {
      this._v = [];
    }
  }

  private _initFromFbs(fbs: FbsVArray): void {
    this._v = Array.from({ length: fbs.vLength() }, (_, i) => {
      const item = fbs.v(i);
      return item ? new ValueInstance(item) : new ValueInstance();
    });
  }

  get v(): ValueInstance[] {
    return this._v;
  }

  set v(value: ValueInstance[]) {
    this._v = value;
  }

  toFbsT(): FbsVArrayT {
    const t = new FbsVArrayT();
    t.v = this._v.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Point2D {
  private _x!: number;

  private _y!: number;

  constructor(arg?: FbsPoint2D) {
    if (arg instanceof FbsPoint2D) {
      this._initFromFbs(arg);
    } else {
      this._x = 0;
      this._y = 0;
    }
  }

  private _initFromFbs(fbs: FbsPoint2D): void {
    this._x = fbs.x();
    this._y = fbs.y();
  }

  get x(): number {
    return this._x;
  }

  set x(value: number) {
    this._x = value;
  }

  get y(): number {
    return this._y;
  }

  set y(value: number) {
    this._y = value;
  }

  toFbsT(): FbsPoint2DT {
    const t = new FbsPoint2DT();
    t.x = this._x;
    t.y = this._y;
    return t;
  }

}

export class Tri2D {
  private _p0!: Point2D | null;

  private _p1!: Point2D | null;

  private _p2!: Point2D | null;

  constructor(arg?: FbsTri2D) {
    if (arg instanceof FbsTri2D) {
      this._initFromFbs(arg);
    } else {
      this._p0 = null;
      this._p1 = null;
      this._p2 = null;
    }
  }

  private _initFromFbs(fbs: FbsTri2D): void {
    const p0Val = fbs.p0();
    this._p0 = p0Val ? new Point2D(p0Val) : null;
    const p1Val = fbs.p1();
    this._p1 = p1Val ? new Point2D(p1Val) : null;
    const p2Val = fbs.p2();
    this._p2 = p2Val ? new Point2D(p2Val) : null;
  }

  get p0(): Point2D | null {
    return this._p0;
  }

  set p0(value: Point2D | null) {
    this._p0 = value;
  }

  get p1(): Point2D | null {
    return this._p1;
  }

  set p1(value: Point2D | null) {
    this._p1 = value;
  }

  get p2(): Point2D | null {
    return this._p2;
  }

  set p2(value: Point2D | null) {
    this._p2 = value;
  }

  toFbsT(): FbsTri2DT {
    const t = new FbsTri2DT();
    t.p0 = this._p0 ? this._p0.toFbsT() : null;
    t.p1 = this._p1 ? this._p1.toFbsT() : null;
    t.p2 = this._p2 ? this._p2.toFbsT() : null;
    return t;
  }

}

export class VTri2D {
  private _v!: Tri2D;

  constructor(arg?: FbsVTri2D | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVTri2D.getSizePrefixedRootAsVTri2D(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVTri2D) {
      this._initFromFbs(arg);
    } else {
      this._v = new Tri2D();
    }
  }

  private _initFromFbs(fbs: FbsVTri2D): void {
    const vVal = fbs.v();
    this._v = vVal ? new Tri2D(vVal) : new Tri2D();
  }

  get v(): Tri2D {
    return this._v;
  }

  set v(value: Tri2D) {
    this._v = value;
  }

  toFbsT(): FbsVTri2DT {
    const t = new FbsVTri2DT();
    t.v = this._v.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VFixedSizeBytes {
  private _sz!: number;

  private _v!: number[];

  constructor(arg?: FbsVFixedSizeBytes | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVFixedSizeBytes.getSizePrefixedRootAsVFixedSizeBytes(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVFixedSizeBytes) {
      this._initFromFbs(arg);
    } else {
      this._sz = 0;
      this._v = [];
    }
  }

  private _initFromFbs(fbs: FbsVFixedSizeBytes): void {
    this._sz = fbs.sz();
    const vArr = fbs.vArray();
    this._v = vArr ? Array.from(vArr) : [];
  }

  get sz(): number {
    return this._sz;
  }

  set sz(value: number) {
    this._sz = value;
  }

  get v(): number[] {
    return this._v;
  }

  set v(value: number[]) {
    this._v = value;
  }

  toFbsT(): FbsVFixedSizeBytesT {
    const t = new FbsVFixedSizeBytesT();
    t.sz = this._sz;
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VTimestampMsUtc {
  private _v!: bigint;

  constructor(arg?: FbsVTimestampMsUtc | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVTimestampMsUtc.getSizePrefixedRootAsVTimestampMsUtc(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVTimestampMsUtc) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVTimestampMsUtc): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVTimestampMsUtcT {
    const t = new FbsVTimestampMsUtcT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VTimestampMs {
  private _v!: bigint;

  constructor(arg?: FbsVTimestampMs | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVTimestampMs.getSizePrefixedRootAsVTimestampMs(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVTimestampMs) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVTimestampMs): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVTimestampMsT {
    const t = new FbsVTimestampMsT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VTimestampNsUtc {
  private _v!: bigint;

  constructor(arg?: FbsVTimestampNsUtc | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVTimestampNsUtc.getSizePrefixedRootAsVTimestampNsUtc(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVTimestampNsUtc) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVTimestampNsUtc): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVTimestampNsUtcT {
    const t = new FbsVTimestampNsUtcT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VTimestampNs {
  private _v!: bigint;

  constructor(arg?: FbsVTimestampNs | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVTimestampNs.getSizePrefixedRootAsVTimestampNs(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVTimestampNs) {
      this._initFromFbs(arg);
    } else {
      this._v = BigInt(0);
    }
  }

  private _initFromFbs(fbs: FbsVTimestampNs): void {
    this._v = fbs.v();
  }

  get v(): bigint {
    return this._v;
  }

  set v(value: bigint) {
    this._v = value;
  }

  toFbsT(): FbsVTimestampNsT {
    const t = new FbsVTimestampNsT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class VPlaceholder {
  private _name!: string;

  private _ty!: number;

  constructor(arg?: FbsVPlaceholder | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsVPlaceholder.getSizePrefixedRootAsVPlaceholder(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsVPlaceholder) {
      this._initFromFbs(arg);
    } else {
      this._name = '';
      this._ty = 0;
    }
  }

  private _initFromFbs(fbs: FbsVPlaceholder): void {
    this._name = fbs.name() ?? '';
    this._ty = fbs.ty();
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get ty(): number {
    return this._ty;
  }

  set ty(value: number) {
    this._ty = value;
  }

  toFbsT(): FbsVPlaceholderT {
    const t = new FbsVPlaceholderT();
    t.name = this._name;
    t.ty = this._ty;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type Value = VBool | VUnit | VChar | VNull | VI8 | VU8 | VI16 | VU16 | VI32 | VU32 | VF32 | VIsize | VUsize | VI64 | VU64 | VF64 | VStr | VBytes | VArray | VTri2D | VFixedSizeBytes | VTimestampMsUtc | VTimestampMs | VTimestampNsUtc | VTimestampNs | VPlaceholder;

export { ValueTy } from './generated/value-ty';

export class ValueInstance {
  private _name!: string | null;

  private _v!: Value | null;

  constructor(arg?: FbsValueInstance | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsValueInstance.getSizePrefixedRootAsValueInstance(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsValueInstance) {
      this._initFromFbs(arg);
    } else {
      this._name = null;
      this._v = null;
    }
  }

  private _initFromFbs(fbs: FbsValueInstance): void {
    this._name = fbs.name();
    const vTy = fbs.vType();
    if (vTy === FbsValue.VBool) {
      const vFbsVal = fbs.v(new FbsVBool());
      this._v = vFbsVal ? new VBool(vFbsVal) : null;
    } else if (vTy === FbsValue.VUnit) {
      const vFbsVal = fbs.v(new FbsVUnit());
      this._v = vFbsVal ? new VUnit(vFbsVal) : null;
    } else if (vTy === FbsValue.VChar) {
      const vFbsVal = fbs.v(new FbsVChar());
      this._v = vFbsVal ? new VChar(vFbsVal) : null;
    } else if (vTy === FbsValue.VNull) {
      const vFbsVal = fbs.v(new FbsVNull());
      this._v = vFbsVal ? new VNull(vFbsVal) : null;
    } else if (vTy === FbsValue.VI8) {
      const vFbsVal = fbs.v(new FbsVI8());
      this._v = vFbsVal ? new VI8(vFbsVal) : null;
    } else if (vTy === FbsValue.VU8) {
      const vFbsVal = fbs.v(new FbsVU8());
      this._v = vFbsVal ? new VU8(vFbsVal) : null;
    } else if (vTy === FbsValue.VI16) {
      const vFbsVal = fbs.v(new FbsVI16());
      this._v = vFbsVal ? new VI16(vFbsVal) : null;
    } else if (vTy === FbsValue.VU16) {
      const vFbsVal = fbs.v(new FbsVU16());
      this._v = vFbsVal ? new VU16(vFbsVal) : null;
    } else if (vTy === FbsValue.VI32) {
      const vFbsVal = fbs.v(new FbsVI32());
      this._v = vFbsVal ? new VI32(vFbsVal) : null;
    } else if (vTy === FbsValue.VU32) {
      const vFbsVal = fbs.v(new FbsVU32());
      this._v = vFbsVal ? new VU32(vFbsVal) : null;
    } else if (vTy === FbsValue.VF32) {
      const vFbsVal = fbs.v(new FbsVF32());
      this._v = vFbsVal ? new VF32(vFbsVal) : null;
    } else if (vTy === FbsValue.VIsize) {
      const vFbsVal = fbs.v(new FbsVIsize());
      this._v = vFbsVal ? new VIsize(vFbsVal) : null;
    } else if (vTy === FbsValue.VUsize) {
      const vFbsVal = fbs.v(new FbsVUsize());
      this._v = vFbsVal ? new VUsize(vFbsVal) : null;
    } else if (vTy === FbsValue.VI64) {
      const vFbsVal = fbs.v(new FbsVI64());
      this._v = vFbsVal ? new VI64(vFbsVal) : null;
    } else if (vTy === FbsValue.VU64) {
      const vFbsVal = fbs.v(new FbsVU64());
      this._v = vFbsVal ? new VU64(vFbsVal) : null;
    } else if (vTy === FbsValue.VF64) {
      const vFbsVal = fbs.v(new FbsVF64());
      this._v = vFbsVal ? new VF64(vFbsVal) : null;
    } else if (vTy === FbsValue.VStr) {
      const vFbsVal = fbs.v(new FbsVStr());
      this._v = vFbsVal ? new VStr(vFbsVal) : null;
    } else if (vTy === FbsValue.VBytes) {
      const vFbsVal = fbs.v(new FbsVBytes());
      this._v = vFbsVal ? new VBytes(vFbsVal) : null;
    } else if (vTy === FbsValue.VArray) {
      const vFbsVal = fbs.v(new FbsVArray());
      this._v = vFbsVal ? new VArray(vFbsVal) : null;
    } else if (vTy === FbsValue.VTri2D) {
      const vFbsVal = fbs.v(new FbsVTri2D());
      this._v = vFbsVal ? new VTri2D(vFbsVal) : null;
    } else if (vTy === FbsValue.VFixedSizeBytes) {
      const vFbsVal = fbs.v(new FbsVFixedSizeBytes());
      this._v = vFbsVal ? new VFixedSizeBytes(vFbsVal) : null;
    } else if (vTy === FbsValue.VTimestampMsUtc) {
      const vFbsVal = fbs.v(new FbsVTimestampMsUtc());
      this._v = vFbsVal ? new VTimestampMsUtc(vFbsVal) : null;
    } else if (vTy === FbsValue.VTimestampMs) {
      const vFbsVal = fbs.v(new FbsVTimestampMs());
      this._v = vFbsVal ? new VTimestampMs(vFbsVal) : null;
    } else if (vTy === FbsValue.VTimestampNsUtc) {
      const vFbsVal = fbs.v(new FbsVTimestampNsUtc());
      this._v = vFbsVal ? new VTimestampNsUtc(vFbsVal) : null;
    } else if (vTy === FbsValue.VTimestampNs) {
      const vFbsVal = fbs.v(new FbsVTimestampNs());
      this._v = vFbsVal ? new VTimestampNs(vFbsVal) : null;
    } else if (vTy === FbsValue.VPlaceholder) {
      const vFbsVal = fbs.v(new FbsVPlaceholder());
      this._v = vFbsVal ? new VPlaceholder(vFbsVal) : null;
    } else {
      this._v = null;
    }
  }

  get name(): string | null {
    return this._name;
  }

  set name(value: string | null) {
    this._name = value;
  }

  get v(): Value | null {
    return this._v;
  }

  set v(value: Value | null) {
    this._v = value;
  }

  toFbsT(): FbsValueInstanceT {
    const t = new FbsValueInstanceT();
    t.name = this._name;
    if (this._v instanceof VBool) {
      t.vType = FbsValue.VBool;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VUnit) {
      t.vType = FbsValue.VUnit;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VChar) {
      t.vType = FbsValue.VChar;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VNull) {
      t.vType = FbsValue.VNull;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VI8) {
      t.vType = FbsValue.VI8;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VU8) {
      t.vType = FbsValue.VU8;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VI16) {
      t.vType = FbsValue.VI16;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VU16) {
      t.vType = FbsValue.VU16;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VI32) {
      t.vType = FbsValue.VI32;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VU32) {
      t.vType = FbsValue.VU32;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VF32) {
      t.vType = FbsValue.VF32;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VIsize) {
      t.vType = FbsValue.VIsize;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VUsize) {
      t.vType = FbsValue.VUsize;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VI64) {
      t.vType = FbsValue.VI64;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VU64) {
      t.vType = FbsValue.VU64;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VF64) {
      t.vType = FbsValue.VF64;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VStr) {
      t.vType = FbsValue.VStr;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VBytes) {
      t.vType = FbsValue.VBytes;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VArray) {
      t.vType = FbsValue.VArray;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VTri2D) {
      t.vType = FbsValue.VTri2D;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VFixedSizeBytes) {
      t.vType = FbsValue.VFixedSizeBytes;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VTimestampMsUtc) {
      t.vType = FbsValue.VTimestampMsUtc;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VTimestampMs) {
      t.vType = FbsValue.VTimestampMs;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VTimestampNsUtc) {
      t.vType = FbsValue.VTimestampNsUtc;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VTimestampNs) {
      t.vType = FbsValue.VTimestampNs;
      t.v = this._v.toFbsT();
    } else if (this._v instanceof VPlaceholder) {
      t.vType = FbsValue.VPlaceholder;
      t.v = this._v.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
