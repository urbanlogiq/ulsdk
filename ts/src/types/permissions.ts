// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { AccessControlList as FbsAccessControlList, AccessControlListT as FbsAccessControlListT } from './generated/access-control-list';
import { Role as FbsRole, RoleT as FbsRoleT } from './generated/role';

import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { StreamId as FbsStreamId } from './generated/stream-id';

export { PermissionTy } from './generated/permission-ty';

export class AccessControlList {
/**
 *  The "extends" allows us to chain together ACLs without needing to copy
 *  the whole thing. For example, if want to grant Alice access to a file,
 *  we would create a new ACL for that file that has Alice in the permissions
 *  list but extends the parent directory's ACL to retain all the existing
 *  permissions. This can also be used to selectively revoke access (by
 *  adding an ACL entry with empty permissions) to an object.
 */
  private _extends_!: ObjectId | null;

  private _roles!: Role[];

  constructor(arg?: FbsAccessControlList | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsAccessControlList.getSizePrefixedRootAsAccessControlList(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsAccessControlList) {
      this._initFromFbs(arg);
    } else {
      this._extends_ = null;
      this._roles = [];
    }
  }

  private _initFromFbs(fbs: FbsAccessControlList): void {
    const extends_Val = fbs.extends_();
    this._extends_ = extends_Val ? new ObjectId(extends_Val) : null;
    this._roles = Array.from({ length: fbs.rolesLength() }, (_, i) => {
      const item = fbs.roles(i);
      return item ? new Role(item) : new Role();
    });
  }

  get extends_(): ObjectId | null {
    return this._extends_;
  }

  set extends_(value: ObjectId | null) {
    this._extends_ = value;
  }

  get roles(): Role[] {
    return this._roles;
  }

  set roles(value: Role[]) {
    this._roles = value;
  }

  toFbsT(): FbsAccessControlListT {
    const t = new FbsAccessControlListT();
    t.extends_ = this._extends_ ? this._extends_.toFbsT() : null;
    t.roles = this._roles.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Role {
  private _permission!: number;

  private _principal!: B2cId;

  constructor(arg?: FbsRole | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRole.getSizePrefixedRootAsRole(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRole) {
      this._initFromFbs(arg);
    } else {
      this._permission = 0;
      this._principal = new B2cId();
    }
  }

  private _initFromFbs(fbs: FbsRole): void {
    this._permission = fbs.permission();
    const principalVal = fbs.principal();
    this._principal = principalVal ? new B2cId(principalVal) : new B2cId();
  }

  get permission(): number {
    return this._permission;
  }

  set permission(value: number) {
    this._permission = value;
  }

  get principal(): B2cId {
    return this._principal;
  }

  set principal(value: B2cId) {
    this._principal = value;
  }

  toFbsT(): FbsRoleT {
    const t = new FbsRoleT();
    t.permission = this._permission;
    t.principal = this._principal.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
