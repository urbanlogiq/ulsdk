// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { DeprecatedRunSpec as FbsDeprecatedRunSpec, DeprecatedRunSpecT as FbsDeprecatedRunSpecT } from './generated/deprecated-run-spec';
import { DeprecatedTaskParameter as FbsDeprecatedTaskParameter, DeprecatedTaskParameterT as FbsDeprecatedTaskParameterT } from './generated/deprecated-task-parameter';
import { Edge as FbsEdge, EdgeT as FbsEdgeT } from './generated/edge';
import { EmbeddedTable as FbsEmbeddedTable, EmbeddedTableT as FbsEmbeddedTableT } from './generated/embedded-table';
import { Job as FbsJob, JobT as FbsJobT } from './generated/job';
import { Node as FbsNode, NodeT as FbsNodeT } from './generated/node';
import { ParamIndices as FbsParamIndices, ParamIndicesT as FbsParamIndicesT } from './generated/param-indices';
import { RunSpec as FbsRunSpec, RunSpecT as FbsRunSpecT } from './generated/run-spec';
import { Schematic as FbsSchematic, SchematicT as FbsSchematicT } from './generated/schematic';
import { Task as FbsTask, TaskT as FbsTaskT } from './generated/task';
import { TaskList as FbsTaskList, TaskListT as FbsTaskListT } from './generated/task-list';
import { TaskParameter as FbsTaskParameter, TaskParameterT as FbsTaskParameterT } from './generated/task-parameter';
import { TaskParameterValue as FbsTaskParameterValue } from './generated/task-parameter-value';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { Attr } from './attr';
import { Attr as FbsAttr } from './generated/attr';
import { AttributePair, BinaryYesNo, DayOfWeek, DirectionAndRoadName, DirectionAndRoadNames, DirectionTy, NamedParameter, NamedParameterFlags, OutputSchema, RoadUserTy, Source, StatisticTy, TimeGranularity, TurnTy } from './data';
import { AttributePair as FbsAttributePair } from './generated/attribute-pair';
import { BinaryYesNo as FbsBinaryYesNo } from './generated/binary-yes-no';
import { DayOfWeek as FbsDayOfWeek } from './generated/day-of-week';
import { DirectionAndRoadName as FbsDirectionAndRoadName } from './generated/direction-and-road-name';
import { DirectionAndRoadNames as FbsDirectionAndRoadNames } from './generated/direction-and-road-names';
import { DirectionTy as FbsDirectionTy } from './generated/direction-ty';
import { NamedParameter as FbsNamedParameter } from './generated/named-parameter';
import { NamedParameterFlags as FbsNamedParameterFlags } from './generated/named-parameter-flags';
import { OutputSchema as FbsOutputSchema } from './generated/output-schema';
import { RoadUserTy as FbsRoadUserTy } from './generated/road-user-ty';
import { Source as FbsSource } from './generated/source';
import { StatisticTy as FbsStatisticTy } from './generated/statistic-ty';
import { TimeGranularity as FbsTimeGranularity } from './generated/time-granularity';
import { TurnTy as FbsTurnTy } from './generated/turn-ty';
import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { StreamId as FbsStreamId } from './generated/stream-id';
import { Point2D, Tri2D, VArray, VBool, VBytes, VChar, VF32, VF64, VFixedSizeBytes, VI16, VI32, VI64, VI8, VIsize, VNull, VPlaceholder, VStr, VTimestampMs, VTimestampMsUtc, VTimestampNs, VTimestampNsUtc, VTri2D, VU16, VU32, VU64, VU8, VUnit, VUsize, Value, ValueInstance, ValueTy } from './value';
import { Point2D as FbsPoint2D } from './generated/point2-d';
import { Tri2D as FbsTri2D } from './generated/tri2-d';
import { VArray as FbsVArray } from './generated/varray';
import { VBool as FbsVBool } from './generated/vbool';
import { VBytes as FbsVBytes } from './generated/vbytes';
import { VChar as FbsVChar } from './generated/vchar';
import { VF32 as FbsVF32 } from './generated/vf32';
import { VF64 as FbsVF64 } from './generated/vf64';
import { VFixedSizeBytes as FbsVFixedSizeBytes } from './generated/vfixed-size-bytes';
import { VI16 as FbsVI16 } from './generated/vi16';
import { VI32 as FbsVI32 } from './generated/vi32';
import { VI64 as FbsVI64 } from './generated/vi64';
import { VI8 as FbsVI8 } from './generated/vi8';
import { VIsize as FbsVIsize } from './generated/visize';
import { VNull as FbsVNull } from './generated/vnull';
import { VPlaceholder as FbsVPlaceholder } from './generated/vplaceholder';
import { VStr as FbsVStr } from './generated/vstr';
import { VTimestampMs as FbsVTimestampMs } from './generated/vtimestamp-ms';
import { VTimestampMsUtc as FbsVTimestampMsUtc } from './generated/vtimestamp-ms-utc';
import { VTimestampNs as FbsVTimestampNs } from './generated/vtimestamp-ns';
import { VTimestampNsUtc as FbsVTimestampNsUtc } from './generated/vtimestamp-ns-utc';
import { VTri2D as FbsVTri2D } from './generated/vtri2-d';
import { VU16 as FbsVU16 } from './generated/vu16';
import { VU32 as FbsVU32 } from './generated/vu32';
import { VU64 as FbsVU64 } from './generated/vu64';
import { VU8 as FbsVU8 } from './generated/vu8';
import { VUnit as FbsVUnit } from './generated/vunit';
import { VUsize as FbsVUsize } from './generated/vusize';
import { Value as FbsValue } from './generated/value';
import { ValueInstance as FbsValueInstance } from './generated/value-instance';
import { ValueTy as FbsValueTy } from './generated/value-ty';

export { Status } from './generated/status';

export { TaskErrorTy } from './generated/task-error-ty';

export class EmbeddedTable {
  private _v!: number[];

  constructor(arg?: FbsEmbeddedTable | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsEmbeddedTable.getSizePrefixedRootAsEmbeddedTable(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsEmbeddedTable) {
      this._initFromFbs(arg);
    } else {
      this._v = [];
    }
  }

  private _initFromFbs(fbs: FbsEmbeddedTable): void {
    const vArr = fbs.vArray();
    this._v = vArr ? Array.from(vArr) : [];
  }

  get v(): number[] {
    return this._v;
  }

  set v(value: number[]) {
    this._v = value;
  }

  toFbsT(): FbsEmbeddedTableT {
    const t = new FbsEmbeddedTableT();
    t.v = this._v;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export type TaskParameterValue = ObjectId | EmbeddedTable | ValueInstance;

export { TaskPriority } from './generated/task-priority';

export { TaskRunFlags } from './generated/task-run-flags';

export class DeprecatedRunSpec {
  private _paramIndices!: ParamIndices[];

  private _params!: DeprecatedTaskParameter[];

  private _persist!: boolean;

  private _schematic!: ObjectId;

  constructor(arg?: FbsDeprecatedRunSpec | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDeprecatedRunSpec.getSizePrefixedRootAsDeprecatedRunSpec(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDeprecatedRunSpec) {
      this._initFromFbs(arg);
    } else {
      this._paramIndices = [];
      this._params = [];
      this._persist = false;
      this._schematic = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsDeprecatedRunSpec): void {
    this._paramIndices = Array.from({ length: fbs.paramIndicesLength() }, (_, i) => {
      const item = fbs.paramIndices(i);
      return item ? new ParamIndices(item) : new ParamIndices();
    });
    this._params = Array.from({ length: fbs.paramsLength() }, (_, i) => {
      const item = fbs.params(i);
      return item ? new DeprecatedTaskParameter(item) : new DeprecatedTaskParameter();
    });
    this._persist = fbs.persist();
    const schematicVal = fbs.schematic();
    this._schematic = schematicVal ? new ObjectId(schematicVal) : new ObjectId();
  }

  get paramIndices(): ParamIndices[] {
    return this._paramIndices;
  }

  set paramIndices(value: ParamIndices[]) {
    this._paramIndices = value;
  }

  get params(): DeprecatedTaskParameter[] {
    return this._params;
  }

  set params(value: DeprecatedTaskParameter[]) {
    this._params = value;
  }

  get persist(): boolean {
    return this._persist;
  }

  set persist(value: boolean) {
    this._persist = value;
  }

  get schematic(): ObjectId {
    return this._schematic;
  }

  set schematic(value: ObjectId) {
    this._schematic = value;
  }

  toFbsT(): FbsDeprecatedRunSpecT {
    const t = new FbsDeprecatedRunSpecT();
    t.paramIndices = this._paramIndices.map(item => item.toFbsT());
    t.params = this._params.map(item => item.toFbsT());
    t.persist = this._persist;
    t.schematic = this._schematic.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class DeprecatedTaskParameter {
  private _flags!: bigint;

  private _key!: string;

  private _obj!: ObjectId | null;

  private _value!: number[] | null;

  constructor(arg?: FbsDeprecatedTaskParameter | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDeprecatedTaskParameter.getSizePrefixedRootAsDeprecatedTaskParameter(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDeprecatedTaskParameter) {
      this._initFromFbs(arg);
    } else {
      this._flags = BigInt(0);
      this._key = '';
      this._obj = null;
      this._value = null;
    }
  }

  private _initFromFbs(fbs: FbsDeprecatedTaskParameter): void {
    this._flags = fbs.flags();
    this._key = fbs.key() ?? '';
    const objVal = fbs.obj();
    this._obj = objVal ? new ObjectId(objVal) : null;
    if (fbs.valueLength() > 0) {
      const arr = fbs.valueArray();
      this._value = arr ? Array.from(arr) : [];
    } else {
      this._value = null;
    }
  }

  get flags(): bigint {
    return this._flags;
  }

  set flags(value: bigint) {
    this._flags = value;
  }

  get key(): string {
    return this._key;
  }

  set key(value: string) {
    this._key = value;
  }

  get obj(): ObjectId | null {
    return this._obj;
  }

  set obj(value: ObjectId | null) {
    this._obj = value;
  }

  get value(): number[] | null {
    return this._value;
  }

  set value(value: number[] | null) {
    this._value = value;
  }

  toFbsT(): FbsDeprecatedTaskParameterT {
    const t = new FbsDeprecatedTaskParameterT();
    t.flags = this._flags;
    t.key = this._key;
    t.obj = this._obj ? this._obj.toFbsT() : null;
    t.value = this._value ?? [];
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Edge {
  private _from!: number;

  private _to!: number;

  constructor(arg?: FbsEdge | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsEdge.getSizePrefixedRootAsEdge(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsEdge) {
      this._initFromFbs(arg);
    } else {
      this._from = 0;
      this._to = 0;
    }
  }

  private _initFromFbs(fbs: FbsEdge): void {
    this._from = fbs.from();
    this._to = fbs.to();
  }

  get from(): number {
    return this._from;
  }

  set from(value: number) {
    this._from = value;
  }

  get to(): number {
    return this._to;
  }

  set to(value: number) {
    this._to = value;
  }

  toFbsT(): FbsEdgeT {
    const t = new FbsEdgeT();
    t.from = this._from;
    t.to = this._to;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Job {
  private _attributes!: Attr[] | null;

  private _errorTys!: number[] | null;

/**
 *  Parameters verbatim from the RunSpec
 */
  private _params!: TaskParameter[];

/**
 *  Is the job complete?
 */
  private _status!: number;

/**
 *  A list of all the tasks that constitute this job.
 */
  private _tasks!: Task[];

/**
 *  User ID who created this job.
 */
  private _userId!: ObjectId;

  constructor(arg?: FbsJob | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsJob.getSizePrefixedRootAsJob(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsJob) {
      this._initFromFbs(arg);
    } else {
      this._attributes = null;
      this._errorTys = null;
      this._params = [];
      this._status = 0;
      this._tasks = [];
      this._userId = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsJob): void {
    if (fbs.attributesLength() > 0) {
      this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
        const item = fbs.attributes(i);
        return item ? new Attr(item) : new Attr();
      });
    } else {
      this._attributes = null;
    }
    if (fbs.errorTysLength() > 0) {
      this._errorTys = Array.from({ length: fbs.errorTysLength() }, (_, i) => fbs.errorTys(i)!);
    } else {
      this._errorTys = null;
    }
    this._params = Array.from({ length: fbs.paramsLength() }, (_, i) => {
      const item = fbs.params(i);
      return item ? new TaskParameter(item) : new TaskParameter();
    });
    this._status = fbs.status();
    this._tasks = Array.from({ length: fbs.tasksLength() }, (_, i) => {
      const item = fbs.tasks(i);
      return item ? new Task(item) : new Task();
    });
    const userIdVal = fbs.userId();
    this._userId = userIdVal ? new ObjectId(userIdVal) : new ObjectId();
  }

  get attributes(): Attr[] | null {
    return this._attributes;
  }

  set attributes(value: Attr[] | null) {
    this._attributes = value;
  }

  get errorTys(): number[] | null {
    return this._errorTys;
  }

  set errorTys(value: number[] | null) {
    this._errorTys = value;
  }

  get params(): TaskParameter[] {
    return this._params;
  }

  set params(value: TaskParameter[]) {
    this._params = value;
  }

  get status(): number {
    return this._status;
  }

  set status(value: number) {
    this._status = value;
  }

  get tasks(): Task[] {
    return this._tasks;
  }

  set tasks(value: Task[]) {
    this._tasks = value;
  }

  get userId(): ObjectId {
    return this._userId;
  }

  set userId(value: ObjectId) {
    this._userId = value;
  }

  toFbsT(): FbsJobT {
    const t = new FbsJobT();
    t.attributes = this._attributes ? this._attributes.map(item => item.toFbsT()) : [];
    t.errorTys = this._errorTys ?? [];
    t.params = this._params.map(item => item.toFbsT());
    t.status = this._status;
    t.tasks = this._tasks.map(item => item.toFbsT());
    t.userId = this._userId.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Node {
  private _name!: string;

  private _obj!: ObjectId;

  constructor(arg?: FbsNode | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNode.getSizePrefixedRootAsNode(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNode) {
      this._initFromFbs(arg);
    } else {
      this._name = '';
      this._obj = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsNode): void {
    this._name = fbs.name() ?? '';
    const objVal = fbs.obj();
    this._obj = objVal ? new ObjectId(objVal) : new ObjectId();
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get obj(): ObjectId {
    return this._obj;
  }

  set obj(value: ObjectId) {
    this._obj = value;
  }

  toFbsT(): FbsNodeT {
    const t = new FbsNodeT();
    t.name = this._name;
    t.obj = this._obj.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class ParamIndices {
  private _idxs!: number[];

  constructor(arg?: FbsParamIndices | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsParamIndices.getSizePrefixedRootAsParamIndices(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsParamIndices) {
      this._initFromFbs(arg);
    } else {
      this._idxs = [];
    }
  }

  private _initFromFbs(fbs: FbsParamIndices): void {
    this._idxs = Array.from({ length: fbs.idxsLength() }, (_, i) => fbs.idxs(i)!);
  }

  get idxs(): number[] {
    return this._idxs;
  }

  set idxs(value: number[]) {
    this._idxs = value;
  }

  toFbsT(): FbsParamIndicesT {
    const t = new FbsParamIndicesT();
    t.idxs = this._idxs;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

/**
 *  A RunSpec is the data required in order to kickstart a schematic job.
 *  It includes the ID of the schematic to run along with the parameters.
 *  The param_indices list must be *exactly* as long as the nodes list in
 *  the Schematic. Each entry in the param_indices list is itself a list
 *  that points to all the parameters in the params array. This lets
 *  us reuse the task parameters across nodes (ie: if we want a shared
 *  start_date / end_date to be used in a number of calculations)
 */
export class RunSpec {
  private _attributes!: Attr[] | null;

  private _notify!: boolean;

  private _paramIndices!: ParamIndices[];

  private _params!: TaskParameter[];

  private _persist!: boolean;

  private _priority!: number;

  private _schematic!: ObjectId;

  constructor(arg?: FbsRunSpec | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsRunSpec.getSizePrefixedRootAsRunSpec(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsRunSpec) {
      this._initFromFbs(arg);
    } else {
      this._attributes = null;
      this._notify = false;
      this._paramIndices = [];
      this._params = [];
      this._persist = false;
      this._priority = 0;
      this._schematic = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsRunSpec): void {
    if (fbs.attributesLength() > 0) {
      this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
        const item = fbs.attributes(i);
        return item ? new Attr(item) : new Attr();
      });
    } else {
      this._attributes = null;
    }
    this._notify = fbs.notify();
    this._paramIndices = Array.from({ length: fbs.paramIndicesLength() }, (_, i) => {
      const item = fbs.paramIndices(i);
      return item ? new ParamIndices(item) : new ParamIndices();
    });
    this._params = Array.from({ length: fbs.paramsLength() }, (_, i) => {
      const item = fbs.params(i);
      return item ? new TaskParameter(item) : new TaskParameter();
    });
    this._persist = fbs.persist();
    this._priority = fbs.priority();
    const schematicVal = fbs.schematic();
    this._schematic = schematicVal ? new ObjectId(schematicVal) : new ObjectId();
  }

  get attributes(): Attr[] | null {
    return this._attributes;
  }

  set attributes(value: Attr[] | null) {
    this._attributes = value;
  }

  get notify(): boolean {
    return this._notify;
  }

  set notify(value: boolean) {
    this._notify = value;
  }

  get paramIndices(): ParamIndices[] {
    return this._paramIndices;
  }

  set paramIndices(value: ParamIndices[]) {
    this._paramIndices = value;
  }

  get params(): TaskParameter[] {
    return this._params;
  }

  set params(value: TaskParameter[]) {
    this._params = value;
  }

  get persist(): boolean {
    return this._persist;
  }

  set persist(value: boolean) {
    this._persist = value;
  }

  get priority(): number {
    return this._priority;
  }

  set priority(value: number) {
    this._priority = value;
  }

  get schematic(): ObjectId {
    return this._schematic;
  }

  set schematic(value: ObjectId) {
    this._schematic = value;
  }

  toFbsT(): FbsRunSpecT {
    const t = new FbsRunSpecT();
    t.attributes = this._attributes ? this._attributes.map(item => item.toFbsT()) : [];
    t.notify = this._notify;
    t.paramIndices = this._paramIndices.map(item => item.toFbsT());
    t.params = this._params.map(item => item.toFbsT());
    t.persist = this._persist;
    t.priority = this._priority;
    t.schematic = this._schematic.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Schematic {
  private _attributes!: AttributePair[];

  private _edges!: Edge[];

  private _name!: string | null;

  private _nodes!: Node[];

  constructor(arg?: FbsSchematic | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSchematic.getSizePrefixedRootAsSchematic(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSchematic) {
      this._initFromFbs(arg);
    } else {
      this._attributes = [];
      this._edges = [];
      this._name = null;
      this._nodes = [];
    }
  }

  private _initFromFbs(fbs: FbsSchematic): void {
    this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
      const item = fbs.attributes(i);
      return item ? new AttributePair(item) : new AttributePair();
    });
    this._edges = Array.from({ length: fbs.edgesLength() }, (_, i) => {
      const item = fbs.edges(i);
      return item ? new Edge(item) : new Edge();
    });
    this._name = fbs.name();
    this._nodes = Array.from({ length: fbs.nodesLength() }, (_, i) => {
      const item = fbs.nodes(i);
      return item ? new Node(item) : new Node();
    });
  }

  get attributes(): AttributePair[] {
    return this._attributes;
  }

  set attributes(value: AttributePair[]) {
    this._attributes = value;
  }

  get edges(): Edge[] {
    return this._edges;
  }

  set edges(value: Edge[]) {
    this._edges = value;
  }

  get name(): string | null {
    return this._name;
  }

  set name(value: string | null) {
    this._name = value;
  }

  get nodes(): Node[] {
    return this._nodes;
  }

  set nodes(value: Node[]) {
    this._nodes = value;
  }

  toFbsT(): FbsSchematicT {
    const t = new FbsSchematicT();
    t.attributes = this._attributes.map(item => item.toFbsT());
    t.edges = this._edges.map(item => item.toFbsT());
    t.name = this._name;
    t.nodes = this._nodes.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Task {
  private __id!: ObjectId;

  private _barrierCount!: number;

/**
 *  Task creation time, in ms-since-Unix-epoch UTC.
 */
  private _created!: bigint;

/**
 *  If false, keep this object if it's a temporary/intermediate after job
 *  creation. This must not be set if the output object above is a provided
 *  stream.
 */
  private _discard!: boolean;

/**
 *  The downstream nodes to enable once this task is complete
 */
  private _downstream!: ObjectId[];

  private _end!: bigint;

  private _errorTy!: number;

  private _flags!: number;

/**
 *  Associated Job ID
 */
  private _jobId!: ObjectId;

/**
 *  Task last poll time, in ms-since-Unix-epoch UTC.
 */
  private _lastUpdated!: bigint;

  private _lastUpdatedByPod!: string | null;

/**
 *  For errors, generic information.
 */
  private _message!: string | null;

  private _name!: string;

/**
 *  The output of this step. If the task is computational (ie: not just a
 *  data stream lookup) this is a blank object where the results will be
 *  written. If it is a lookup of an existing stream, this will be populated
 *  with the stream ID
 */
  private _output!: ObjectId;

/**
 *  Parameter indices taken from the RunSpec for this particular task step.
 */
  private _params!: ParamIndices;

  private _retries!: number;

  private _schematicId!: ObjectId | null;

/**
 *  Task start time, in ms-since-Unix-epoch UTC.
 */
  private _start!: bigint;

/**
 *  Task status
 */
  private _status!: number;

/**
 *  Task object, either a source or a stream
 */
  private _task!: ObjectId;

/**
 *  The upstream nodes that enabled this task
 */
  private _upstream!: ObjectId[];

/**
 *  User who created the job. This is the same as the user_id field in the
 *  job structure but duplicated for convenience when looking up task related
 *  information.
 */
  private _userId!: ObjectId;

  constructor(arg?: FbsTask | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsTask.getSizePrefixedRootAsTask(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsTask) {
      this._initFromFbs(arg);
    } else {
      this.__id = new ObjectId();
      this._barrierCount = 0;
      this._created = BigInt(0);
      this._discard = false;
      this._downstream = [];
      this._end = BigInt(0);
      this._errorTy = 0;
      this._flags = 0;
      this._jobId = new ObjectId();
      this._lastUpdated = BigInt(0);
      this._lastUpdatedByPod = null;
      this._message = null;
      this._name = '';
      this._output = new ObjectId();
      this._params = new ParamIndices();
      this._retries = 0;
      this._schematicId = null;
      this._start = BigInt(0);
      this._status = 0;
      this._task = new ObjectId();
      this._upstream = [];
      this._userId = new ObjectId();
    }
  }

  private _initFromFbs(fbs: FbsTask): void {
    const _idVal = fbs._Id();
    this.__id = _idVal ? new ObjectId(_idVal) : new ObjectId();
    this._barrierCount = fbs.barrierCount();
    this._created = fbs.created();
    this._discard = fbs.discard();
    this._downstream = Array.from({ length: fbs.downstreamLength() }, (_, i) => {
      const item = fbs.downstream(i);
      return item ? new ObjectId(item) : new ObjectId();
    });
    this._end = fbs.end();
    this._errorTy = fbs.errorTy();
    this._flags = fbs.flags();
    const jobIdVal = fbs.jobId();
    this._jobId = jobIdVal ? new ObjectId(jobIdVal) : new ObjectId();
    this._lastUpdated = fbs.lastUpdated();
    this._lastUpdatedByPod = fbs.lastUpdatedByPod();
    this._message = fbs.message();
    this._name = fbs.name() ?? '';
    const outputVal = fbs.output();
    this._output = outputVal ? new ObjectId(outputVal) : new ObjectId();
    const paramsVal = fbs.params();
    this._params = paramsVal ? new ParamIndices(paramsVal) : new ParamIndices();
    this._retries = fbs.retries();
    const schematicIdVal = fbs.schematicId();
    this._schematicId = schematicIdVal ? new ObjectId(schematicIdVal) : null;
    this._start = fbs.start();
    this._status = fbs.status();
    const taskVal = fbs.task();
    this._task = taskVal ? new ObjectId(taskVal) : new ObjectId();
    this._upstream = Array.from({ length: fbs.upstreamLength() }, (_, i) => {
      const item = fbs.upstream(i);
      return item ? new ObjectId(item) : new ObjectId();
    });
    const userIdVal = fbs.userId();
    this._userId = userIdVal ? new ObjectId(userIdVal) : new ObjectId();
  }

  get _id(): ObjectId {
    return this.__id;
  }

  set _id(value: ObjectId) {
    this.__id = value;
  }

  get barrierCount(): number {
    return this._barrierCount;
  }

  set barrierCount(value: number) {
    this._barrierCount = value;
  }

  get created(): bigint {
    return this._created;
  }

  set created(value: bigint) {
    this._created = value;
  }

  get discard(): boolean {
    return this._discard;
  }

  set discard(value: boolean) {
    this._discard = value;
  }

  get downstream(): ObjectId[] {
    return this._downstream;
  }

  set downstream(value: ObjectId[]) {
    this._downstream = value;
  }

  get end(): bigint {
    return this._end;
  }

  set end(value: bigint) {
    this._end = value;
  }

  get errorTy(): number {
    return this._errorTy;
  }

  set errorTy(value: number) {
    this._errorTy = value;
  }

  get flags(): number {
    return this._flags;
  }

  set flags(value: number) {
    this._flags = value;
  }

  get jobId(): ObjectId {
    return this._jobId;
  }

  set jobId(value: ObjectId) {
    this._jobId = value;
  }

  get lastUpdated(): bigint {
    return this._lastUpdated;
  }

  set lastUpdated(value: bigint) {
    this._lastUpdated = value;
  }

  get lastUpdatedByPod(): string | null {
    return this._lastUpdatedByPod;
  }

  set lastUpdatedByPod(value: string | null) {
    this._lastUpdatedByPod = value;
  }

  get message(): string | null {
    return this._message;
  }

  set message(value: string | null) {
    this._message = value;
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get output(): ObjectId {
    return this._output;
  }

  set output(value: ObjectId) {
    this._output = value;
  }

  get params(): ParamIndices {
    return this._params;
  }

  set params(value: ParamIndices) {
    this._params = value;
  }

  get retries(): number {
    return this._retries;
  }

  set retries(value: number) {
    this._retries = value;
  }

  get schematicId(): ObjectId | null {
    return this._schematicId;
  }

  set schematicId(value: ObjectId | null) {
    this._schematicId = value;
  }

  get start(): bigint {
    return this._start;
  }

  set start(value: bigint) {
    this._start = value;
  }

  get status(): number {
    return this._status;
  }

  set status(value: number) {
    this._status = value;
  }

  get task(): ObjectId {
    return this._task;
  }

  set task(value: ObjectId) {
    this._task = value;
  }

  get upstream(): ObjectId[] {
    return this._upstream;
  }

  set upstream(value: ObjectId[]) {
    this._upstream = value;
  }

  get userId(): ObjectId {
    return this._userId;
  }

  set userId(value: ObjectId) {
    this._userId = value;
  }

  toFbsT(): FbsTaskT {
    const t = new FbsTaskT();
    t._Id = this.__id.toFbsT();
    t.barrierCount = this._barrierCount;
    t.created = this._created;
    t.discard = this._discard;
    t.downstream = this._downstream.map(item => item.toFbsT());
    t.end = this._end;
    t.errorTy = this._errorTy;
    t.flags = this._flags;
    t.jobId = this._jobId.toFbsT();
    t.lastUpdated = this._lastUpdated;
    t.lastUpdatedByPod = this._lastUpdatedByPod;
    t.message = this._message;
    t.name = this._name;
    t.output = this._output.toFbsT();
    t.params = this._params.toFbsT();
    t.retries = this._retries;
    t.schematicId = this._schematicId ? this._schematicId.toFbsT() : null;
    t.start = this._start;
    t.status = this._status;
    t.task = this._task.toFbsT();
    t.upstream = this._upstream.map(item => item.toFbsT());
    t.userId = this._userId.toFbsT();
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class TaskList {
  private _tasks!: Task[];

  constructor(arg?: FbsTaskList | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsTaskList.getSizePrefixedRootAsTaskList(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsTaskList) {
      this._initFromFbs(arg);
    } else {
      this._tasks = [];
    }
  }

  private _initFromFbs(fbs: FbsTaskList): void {
    this._tasks = Array.from({ length: fbs.tasksLength() }, (_, i) => {
      const item = fbs.tasks(i);
      return item ? new Task(item) : new Task();
    });
  }

  get tasks(): Task[] {
    return this._tasks;
  }

  set tasks(value: Task[]) {
    this._tasks = value;
  }

  toFbsT(): FbsTaskListT {
    const t = new FbsTaskListT();
    t.tasks = this._tasks.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class TaskParameter {
  private _key!: string;

  private _value!: TaskParameterValue | null;

  constructor(arg?: FbsTaskParameter | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsTaskParameter.getSizePrefixedRootAsTaskParameter(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsTaskParameter) {
      this._initFromFbs(arg);
    } else {
      this._key = '';
      this._value = null;
    }
  }

  private _initFromFbs(fbs: FbsTaskParameter): void {
    this._key = fbs.key() ?? '';
    const valueTy = fbs.valueType();
    if (valueTy === FbsTaskParameterValue.ObjectId) {
      const valueFbsVal = fbs.value(new FbsObjectId());
      this._value = valueFbsVal ? new ObjectId(valueFbsVal) : null;
    } else if (valueTy === FbsTaskParameterValue.EmbeddedTable) {
      const valueFbsVal = fbs.value(new FbsEmbeddedTable());
      this._value = valueFbsVal ? new EmbeddedTable(valueFbsVal) : null;
    } else if (valueTy === FbsTaskParameterValue.ValueInstance) {
      const valueFbsVal = fbs.value(new FbsValueInstance());
      this._value = valueFbsVal ? new ValueInstance(valueFbsVal) : null;
    } else {
      this._value = null;
    }
  }

  get key(): string {
    return this._key;
  }

  set key(value: string) {
    this._key = value;
  }

  get value(): TaskParameterValue | null {
    return this._value;
  }

  set value(value: TaskParameterValue | null) {
    this._value = value;
  }

  toFbsT(): FbsTaskParameterT {
    const t = new FbsTaskParameterT();
    t.key = this._key;
    if (this._value instanceof ObjectId) {
      t.valueType = FbsTaskParameterValue.ObjectId;
      t.value = this._value.toFbsT();
    } else if (this._value instanceof EmbeddedTable) {
      t.valueType = FbsTaskParameterValue.EmbeddedTable;
      t.value = this._value.toFbsT();
    } else if (this._value instanceof ValueInstance) {
      t.valueType = FbsTaskParameterValue.ValueInstance;
      t.value = this._value.toFbsT();
    }
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
