// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';

import { AttributePair as FbsAttributePair, AttributePairT as FbsAttributePairT } from './generated/attribute-pair';
import { DirectionAndRoadName as FbsDirectionAndRoadName, DirectionAndRoadNameT as FbsDirectionAndRoadNameT } from './generated/direction-and-road-name';
import { DirectionAndRoadNames as FbsDirectionAndRoadNames, DirectionAndRoadNamesT as FbsDirectionAndRoadNamesT } from './generated/direction-and-road-names';
import { NamedParameter as FbsNamedParameter, NamedParameterT as FbsNamedParameterT } from './generated/named-parameter';
import { OutputSchema as FbsOutputSchema, OutputSchemaT as FbsOutputSchemaT } from './generated/output-schema';
import { Source as FbsSource, SourceT as FbsSourceT } from './generated/source';

import { Binary, BinaryView, Bool, Buffer, Date, DateUnit, Decimal, DictionaryEncoding, DictionaryKind, Duration, Endianness, Feature, Field, FixedSizeBinary, FixedSizeList, FloatingPoint, Int, Interval, IntervalUnit, KeyValue, LargeBinary, LargeList, LargeListView, LargeUtf8, List, ListView, Map, MetadataVersion, Null, Precision, RunEndEncoded, Schema, Struct_, Time, TimeUnit, Timestamp, Type, Union, UnionMode, Utf8, Utf8View } from './Schema';
import { Binary as FbsBinary } from './generated/binary';
import { BinaryView as FbsBinaryView } from './generated/binary-view';
import { Bool as FbsBool } from './generated/bool';
import { Buffer as FbsBuffer } from './generated/buffer';
import { Date as FbsDate } from './generated/date';
import { DateUnit as FbsDateUnit } from './generated/date-unit';
import { Decimal as FbsDecimal } from './generated/decimal';
import { DictionaryEncoding as FbsDictionaryEncoding } from './generated/dictionary-encoding';
import { DictionaryKind as FbsDictionaryKind } from './generated/dictionary-kind';
import { Duration as FbsDuration } from './generated/duration';
import { Endianness as FbsEndianness } from './generated/endianness';
import { Feature as FbsFeature } from './generated/feature';
import { Field as FbsField } from './generated/field';
import { FixedSizeBinary as FbsFixedSizeBinary } from './generated/fixed-size-binary';
import { FixedSizeList as FbsFixedSizeList } from './generated/fixed-size-list';
import { FloatingPoint as FbsFloatingPoint } from './generated/floating-point';
import { Int as FbsInt } from './generated/int';
import { Interval as FbsInterval } from './generated/interval';
import { IntervalUnit as FbsIntervalUnit } from './generated/interval-unit';
import { KeyValue as FbsKeyValue } from './generated/key-value';
import { LargeBinary as FbsLargeBinary } from './generated/large-binary';
import { LargeList as FbsLargeList } from './generated/large-list';
import { LargeListView as FbsLargeListView } from './generated/large-list-view';
import { LargeUtf8 as FbsLargeUtf8 } from './generated/large-utf8';
import { List as FbsList } from './generated/list';
import { ListView as FbsListView } from './generated/list-view';
import { Map as FbsMap } from './generated/map';
import { MetadataVersion as FbsMetadataVersion } from './generated/metadata-version';
import { Null as FbsNull } from './generated/null';
import { Precision as FbsPrecision } from './generated/precision';
import { RunEndEncoded as FbsRunEndEncoded } from './generated/run-end-encoded';
import { Schema as FbsSchema } from './generated/schema';
import { Struct_ as FbsStruct_ } from './generated/struct-';
import { Time as FbsTime } from './generated/time';
import { TimeUnit as FbsTimeUnit } from './generated/time-unit';
import { Timestamp as FbsTimestamp } from './generated/timestamp';
import { Type as FbsType } from './generated/type';
import { Union as FbsUnion } from './generated/union';
import { UnionMode as FbsUnionMode } from './generated/union-mode';
import { Utf8 as FbsUtf8 } from './generated/utf8';
import { Utf8View as FbsUtf8View } from './generated/utf8-view';
import { B2cId, ColumnGroupId, ContentId, DataStateId, GenericId, GraphNodeId, ObjectId, ObjectNamespace, PinnedObjectId, StreamId } from './id';
import { B2cId as FbsB2cId } from './generated/b2c-id';
import { ColumnGroupId as FbsColumnGroupId } from './generated/column-group-id';
import { ContentId as FbsContentId } from './generated/content-id';
import { DataStateId as FbsDataStateId } from './generated/data-state-id';
import { GenericId as FbsGenericId } from './generated/generic-id';
import { GraphNodeId as FbsGraphNodeId } from './generated/graph-node-id';
import { ObjectId as FbsObjectId } from './generated/object-id';
import { ObjectNamespace as FbsObjectNamespace } from './generated/object-namespace';
import { PinnedObjectId as FbsPinnedObjectId } from './generated/pinned-object-id';
import { StreamId as FbsStreamId } from './generated/stream-id';

export { BinaryYesNo } from './generated/binary-yes-no';

export { DayOfWeek } from './generated/day-of-week';

export { DirectionTy } from './generated/direction-ty';

/**
 *  If no flags are set, this indicates the associated NamedParameter is required.
 */
export { NamedParameterFlags } from './generated/named-parameter-flags';

export { RoadUserTy } from './generated/road-user-ty';

export { StatisticTy } from './generated/statistic-ty';

export { TimeGranularity } from './generated/time-granularity';

export { TurnTy } from './generated/turn-ty';

export class AttributePair {
  private _key!: string | null;

  private _value!: string | null;

  constructor(arg?: FbsAttributePair | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsAttributePair.getSizePrefixedRootAsAttributePair(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsAttributePair) {
      this._initFromFbs(arg);
    } else {
      this._key = null;
      this._value = null;
    }
  }

  private _initFromFbs(fbs: FbsAttributePair): void {
    this._key = fbs.key();
    this._value = fbs.value();
  }

  get key(): string | null {
    return this._key;
  }

  set key(value: string | null) {
    this._key = value;
  }

  get value(): string | null {
    return this._value;
  }

  set value(value: string | null) {
    this._value = value;
  }

  toFbsT(): FbsAttributePairT {
    const t = new FbsAttributePairT();
    t.key = this._key;
    t.value = this._value;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class DirectionAndRoadName {
  private _direction!: string;

  private _roadName!: string;

  constructor(arg?: FbsDirectionAndRoadName | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDirectionAndRoadName.getSizePrefixedRootAsDirectionAndRoadName(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDirectionAndRoadName) {
      this._initFromFbs(arg);
    } else {
      this._direction = '';
      this._roadName = '';
    }
  }

  private _initFromFbs(fbs: FbsDirectionAndRoadName): void {
    this._direction = fbs.direction() ?? '';
    this._roadName = fbs.roadName() ?? '';
  }

  get direction(): string {
    return this._direction;
  }

  set direction(value: string) {
    this._direction = value;
  }

  get roadName(): string {
    return this._roadName;
  }

  set roadName(value: string) {
    this._roadName = value;
  }

  toFbsT(): FbsDirectionAndRoadNameT {
    const t = new FbsDirectionAndRoadNameT();
    t.direction = this._direction;
    t.roadName = this._roadName;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class DirectionAndRoadNames {
  private _directionAndRoadNames!: DirectionAndRoadName[] | null;

  constructor(arg?: FbsDirectionAndRoadNames | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsDirectionAndRoadNames.getSizePrefixedRootAsDirectionAndRoadNames(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsDirectionAndRoadNames) {
      this._initFromFbs(arg);
    } else {
      this._directionAndRoadNames = null;
    }
  }

  private _initFromFbs(fbs: FbsDirectionAndRoadNames): void {
    if (fbs.directionAndRoadNamesLength() > 0) {
      this._directionAndRoadNames = Array.from({ length: fbs.directionAndRoadNamesLength() }, (_, i) => {
        const item = fbs.directionAndRoadNames(i);
        return item ? new DirectionAndRoadName(item) : new DirectionAndRoadName();
      });
    } else {
      this._directionAndRoadNames = null;
    }
  }

  get directionAndRoadNames(): DirectionAndRoadName[] | null {
    return this._directionAndRoadNames;
  }

  set directionAndRoadNames(value: DirectionAndRoadName[] | null) {
    this._directionAndRoadNames = value;
  }

  toFbsT(): FbsDirectionAndRoadNamesT {
    const t = new FbsDirectionAndRoadNamesT();
    t.directionAndRoadNames = this._directionAndRoadNames ? this._directionAndRoadNames.map(item => item.toFbsT()) : [];
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class NamedParameter {
  private _description!: string | null;

  private _flags!: number;

  private _name!: string;

  private _schema!: Schema | null;

  constructor(arg?: FbsNamedParameter | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsNamedParameter.getSizePrefixedRootAsNamedParameter(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsNamedParameter) {
      this._initFromFbs(arg);
    } else {
      this._description = null;
      this._flags = 0;
      this._name = '';
      this._schema = null;
    }
  }

  private _initFromFbs(fbs: FbsNamedParameter): void {
    this._description = fbs.description();
    this._flags = fbs.flags();
    this._name = fbs.name() ?? '';
    const schemaVal = fbs.schema();
    this._schema = schemaVal ? new Schema(schemaVal) : null;
  }

  get description(): string | null {
    return this._description;
  }

  set description(value: string | null) {
    this._description = value;
  }

  get flags(): number {
    return this._flags;
  }

  set flags(value: number) {
    this._flags = value;
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get schema(): Schema | null {
    return this._schema;
  }

  set schema(value: Schema | null) {
    this._schema = value;
  }

  toFbsT(): FbsNamedParameterT {
    const t = new FbsNamedParameterT();
    t.description = this._description;
    t.flags = this._flags;
    t.name = this._name;
    t.schema = this._schema ? this._schema.toFbsT() : null;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class OutputSchema {
  private _attributes!: AttributePair[];

  private _schema!: Schema[];

  constructor(arg?: FbsOutputSchema | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsOutputSchema.getSizePrefixedRootAsOutputSchema(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsOutputSchema) {
      this._initFromFbs(arg);
    } else {
      this._attributes = [];
      this._schema = [];
    }
  }

  private _initFromFbs(fbs: FbsOutputSchema): void {
    this._attributes = Array.from({ length: fbs.attributesLength() }, (_, i) => {
      const item = fbs.attributes(i);
      return item ? new AttributePair(item) : new AttributePair();
    });
    this._schema = Array.from({ length: fbs.schemaLength() }, (_, i) => {
      const item = fbs.schema(i);
      return item ? new Schema(item) : new Schema();
    });
  }

  get attributes(): AttributePair[] {
    return this._attributes;
  }

  set attributes(value: AttributePair[]) {
    this._attributes = value;
  }

  get schema(): Schema[] {
    return this._schema;
  }

  set schema(value: Schema[]) {
    this._schema = value;
  }

  toFbsT(): FbsOutputSchemaT {
    const t = new FbsOutputSchemaT();
    t.attributes = this._attributes.map(item => item.toFbsT());
    t.schema = this._schema.map(item => item.toFbsT());
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}

export class Source {
  private _metadata!: ObjectId | null;

  private _metadataRevision!: ContentId | null;

  private _name!: string;

  private _namedParameters!: NamedParameter[] | null;

  private _options!: number[] | null;

  private _outputSchemas!: OutputSchema[] | null;

  private _url!: string;

  constructor(arg?: FbsSource | Uint8Array) {
    if (arg instanceof Uint8Array) {
      const buf = new flatbuffers.ByteBuffer(arg);
      const fbs = FbsSource.getSizePrefixedRootAsSource(buf);
      this._initFromFbs(fbs);
    } else if (arg instanceof FbsSource) {
      this._initFromFbs(arg);
    } else {
      this._metadata = null;
      this._metadataRevision = null;
      this._name = '';
      this._namedParameters = null;
      this._options = null;
      this._outputSchemas = null;
      this._url = '';
    }
  }

  private _initFromFbs(fbs: FbsSource): void {
    const metadataVal = fbs.metadata();
    this._metadata = metadataVal ? new ObjectId(metadataVal) : null;
    const metadataRevisionVal = fbs.metadataRevision();
    this._metadataRevision = metadataRevisionVal ? new ContentId(metadataRevisionVal) : null;
    this._name = fbs.name() ?? '';
    if (fbs.namedParametersLength() > 0) {
      this._namedParameters = Array.from({ length: fbs.namedParametersLength() }, (_, i) => {
        const item = fbs.namedParameters(i);
        return item ? new NamedParameter(item) : new NamedParameter();
      });
    } else {
      this._namedParameters = null;
    }
    if (fbs.optionsLength() > 0) {
      const arr = fbs.optionsArray();
      this._options = arr ? Array.from(arr) : [];
    } else {
      this._options = null;
    }
    if (fbs.outputSchemasLength() > 0) {
      this._outputSchemas = Array.from({ length: fbs.outputSchemasLength() }, (_, i) => {
        const item = fbs.outputSchemas(i);
        return item ? new OutputSchema(item) : new OutputSchema();
      });
    } else {
      this._outputSchemas = null;
    }
    this._url = fbs.url() ?? '';
  }

  get metadata(): ObjectId | null {
    return this._metadata;
  }

  set metadata(value: ObjectId | null) {
    this._metadata = value;
  }

  get metadataRevision(): ContentId | null {
    return this._metadataRevision;
  }

  set metadataRevision(value: ContentId | null) {
    this._metadataRevision = value;
  }

  get name(): string {
    return this._name;
  }

  set name(value: string) {
    this._name = value;
  }

  get namedParameters(): NamedParameter[] | null {
    return this._namedParameters;
  }

  set namedParameters(value: NamedParameter[] | null) {
    this._namedParameters = value;
  }

  get options(): number[] | null {
    return this._options;
  }

  set options(value: number[] | null) {
    this._options = value;
  }

  get outputSchemas(): OutputSchema[] | null {
    return this._outputSchemas;
  }

  set outputSchemas(value: OutputSchema[] | null) {
    this._outputSchemas = value;
  }

  get url(): string {
    return this._url;
  }

  set url(value: string) {
    this._url = value;
  }

  toFbsT(): FbsSourceT {
    const t = new FbsSourceT();
    t.metadata = this._metadata ? this._metadata.toFbsT() : null;
    t.metadataRevision = this._metadataRevision ? this._metadataRevision.toFbsT() : null;
    t.name = this._name;
    t.namedParameters = this._namedParameters ? this._namedParameters.map(item => item.toFbsT()) : [];
    t.options = this._options ?? [];
    t.outputSchemas = this._outputSchemas ? this._outputSchemas.map(item => item.toFbsT()) : [];
    t.url = this._url;
    return t;
  }

  toBytes(): Uint8Array {
    const builder = new flatbuffers.Builder();
    const offset = this.toFbsT().pack(builder);
    builder.finishSizePrefixed(offset);
    return builder.asUint8Array();
  }
}
