// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as inbox from '../inbox';
import { TestContext } from '../../test_context';
import { ObjectIdT as ObjectId } from '../../types/generated/object-id';

describe('fetch', () => {
  it('testFetch', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    ctx.setResponse(new Uint8Array(0));
    try {
          await inbox.fetch(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('clearAllStatus', () => {
  it('testClearAllStatus', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    try {
          await inbox.clearAllStatus(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('setStatus', () => {
  it('testSetStatus', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    const p1 = new ObjectId();
    const p2 = 42;
    try {
          await inbox.setStatus(
        ctx,
        p0,
        p1,
        p2
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('clearStatus', () => {
  it('testClearStatus', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    const p1 = new ObjectId();
    try {
          await inbox.clearStatus(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
