// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as acl from '../acl';
import { TestContext } from '../../test_context';
import { AccessRequestT as AccessRequest } from '../../types/generated/access-request';
import { B2cIdT as B2cId } from '../../types/generated/b2c-id';
import { ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { ShareDetailsT as ShareDetails } from '../../types/generated/share-details';

describe('newAcl', () => {
  it('testNewAcl', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new Uint8Array(0));
    try {
          await acl.newAcl(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('newFrom', () => {
  it('testNewFrom', async () => {
    const ctx = new TestContext();
    const q0: any = null;
    ctx.setResponse(new Uint8Array(0));
    try {
          await acl.newFrom(
        ctx,
        q0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('request', () => {
  it('testRequest', async () => {
    const ctx = new TestContext();
    const body = new AccessRequest();
    try {
          await acl.request(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('share', () => {
  it('testShare', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const p2 = 42;
    try {
          await acl.share(
        ctx,
        p0,
        p1,
        p2
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('shareWithDetails', () => {
  it('testShareWithDetails', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const p2 = 42;
    const body = new ShareDetails();
    try {
          await acl.shareWithDetails(
        ctx,
        p0,
        p1,
        p2,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('shareAll', () => {
  it('testShareAll', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    try {
          await acl.shareAll(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('shareAllWithDetails', () => {
  it('testShareAllWithDetails', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const body = new ShareDetails();
    try {
          await acl.shareAllWithDetails(
        ctx,
        p0,
        p1,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('grant', () => {
  it('testGrant', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const p2 = 42;
    try {
          await acl.grant(
        ctx,
        p0,
        p1,
        p2
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('grantWithDetails', () => {
  it('testGrantWithDetails', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const p2 = 42;
    const body = new ShareDetails();
    try {
          await acl.grantWithDetails(
        ctx,
        p0,
        p1,
        p2,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('grantAll', () => {
  it('testGrantAll', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new ObjectId();
    try {
          await acl.grantAll(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('grantAllWithDetails', () => {
  it('testGrantAllWithDetails', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    const body = new ShareDetails();
    try {
          await acl.grantAllWithDetails(
        ctx,
        p0,
        p1,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('revoke', () => {
  it('testRevoke', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new B2cId();
    try {
          await acl.revoke(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getPermissions', () => {
  it('testGetPermissions', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await acl.getPermissions(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('setDefaultMode', () => {
  it('testSetDefaultMode', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = 42;
    try {
          await acl.setDefaultMode(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('set', () => {
  it('testSet', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new ObjectId();
    try {
          await acl.set(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
