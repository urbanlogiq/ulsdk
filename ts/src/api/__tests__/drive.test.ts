// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as drive from '../drive';
import { TestContext } from '../../test_context';
import { B2cIdT as B2cId } from '../../types/generated/b2c-id';
import { MoveRequestT as MoveRequest } from '../../types/generated/move-request';
import { ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { ObjectIdListT as ObjectIdList } from '../../types/generated/object-id-list';

describe('ls', () => {
  it('testLs', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    const p1 = 'tail';
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.ls(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('lsMe', () => {
  it('testLsMe', async () => {
    const ctx = new TestContext();
    const p0 = 'tail';
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.lsMe(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('lsUnion', () => {
  it('testLsUnion', async () => {
    const ctx = new TestContext();
    const p0 = 'tail';
    const q0 = true;
    const q1 = true;
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.lsUnion(
        ctx,
        p0,
        q0,
        q1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createEntry', () => {
  it('testCreateEntry', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = 'tail';
    const q0 = 'test';
    const q1 = 'test';
    const q2 = 42;
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.createEntry(
        ctx,
        p0,
        p1,
        q0,
        q1,
        q2
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getRoots', () => {
  it('testGetRoots', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.getRoots(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('postFile', () => {
  it('testPostFile', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const q0 = true;
    const body: File[] = [];
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.postFile(
        ctx,
        p0,
        q0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('unlinkList', () => {
  it('testUnlinkList', async () => {
    const ctx = new TestContext();
    const body = new ObjectIdList();
    try {
          await drive.unlinkList(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('unlink', () => {
  it('testUnlink', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    try {
          await drive.unlink(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('move', () => {
  it('testMove', async () => {
    const ctx = new TestContext();
    const body = new MoveRequest();
    try {
          await drive.move(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('copy', () => {
  it('testCopy', async () => {
    const ctx = new TestContext();
    const body = new MoveRequest();
    try {
          await drive.copy(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getFile', () => {
  it('testGetFile', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.getFile(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('putFileChunk', () => {
  it('testPutFileChunk', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = 42;
    const q0 = 'test';
    const body = new Uint8Array(0);
    try {
          await drive.putFileChunk(
        ctx,
        p0,
        p1,
        q0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getRootId', () => {
  it('testGetRootId', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await drive.getRootId(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
