// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as keys from '../keys';
import { TestContext } from '../../test_context';

describe('getKeys', () => {
  it('testGetKeys', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await keys.getKeys(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createKey', () => {
  it('testCreateKey', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await keys.createKey(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateKey', () => {
  it('testUpdateKey', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    const body = {} as any;
    try {
          await keys.updateKey(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getKey', () => {
  it('testGetKey', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await keys.getKey(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('deleteKey', () => {
  it('testDeleteKey', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    try {
          await keys.deleteKey(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
