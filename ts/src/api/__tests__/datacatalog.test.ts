// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as datacatalog from '../datacatalog';
import { TestContext } from '../../test_context';
import { ContentIdT as ContentId } from '../../types/generated/content-id';
import { DataCatalogObjectT as DataCatalogObject } from '../../types/generated/data-catalog-object';
import { DiffStreamT as DiffStream } from '../../types/generated/diff-stream';
import { GenericIdT as GenericId } from '../../types/generated/generic-id';
import { MetadataT as Metadata } from '../../types/generated/metadata';
import { NewTableT as NewTable } from '../../types/generated/new-table';
import { ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { ObjectIdListT as ObjectIdList } from '../../types/generated/object-id-list';
import { QueryT as Query } from '../../types/generated/query';

describe('getObjectAtRevision', () => {
  it('testGetObjectAtRevision', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new ContentId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getObjectAtRevision(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getAcl', () => {
  it('testGetAcl', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getAcl(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getHeadRevision', () => {
  it('testGetHeadRevision', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getHeadRevision(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getObject', () => {
  it('testGetObject', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getObject(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateObject', () => {
  it('testUpdateObject', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const body = new DataCatalogObject();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.updateObject(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateAttributes', () => {
  it('testUpdateAttributes', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const q0 = true;
    const body = {} as any;
    try {
          await datacatalog.updateAttributes(
        ctx,
        p0,
        q0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('deleteAttribute', () => {
  it('testDeleteAttribute', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = 'test';
    try {
          await datacatalog.deleteAttribute(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getObjectSummaries', () => {
  it('testGetObjectSummaries', async () => {
    const ctx = new TestContext();
    const body = new ObjectIdList();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getObjectSummaries(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('bulkFetchMetadata', () => {
  it('testBulkFetchMetadata', async () => {
    const ctx = new TestContext();
    const body = new ObjectIdList();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.bulkFetchMetadata(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('bulkFetchObjects', () => {
  it('testBulkFetchObjects', async () => {
    const ctx = new TestContext();
    const body = new ObjectIdList();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.bulkFetchObjects(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createObject', () => {
  it('testCreateObject', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.createObject(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryAggregateNumeric', () => {
  it('testQueryAggregateNumeric', async () => {
    const ctx = new TestContext();
    const q0 = 'test';
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryAggregateNumeric(
        ctx,
        q0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryAggregateString', () => {
  it('testQueryAggregateString', async () => {
    const ctx = new TestContext();
    const q0 = 'test';
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryAggregateString(
        ctx,
        q0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryAggregateHisto', () => {
  it('testQueryAggregateHisto', async () => {
    const ctx = new TestContext();
    const q0 = 42;
    const q1 = 'test';
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryAggregateHisto(
        ctx,
        q0,
        q1,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryAggregateRelativeHisto', () => {
  it('testQueryAggregateRelativeHisto', async () => {
    const ctx = new TestContext();
    const q0 = 42;
    const q1 = 'test';
    const q2 = 'test';
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryAggregateRelativeHisto(
        ctx,
        q0,
        q1,
        q2,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetArrow', () => {
  it('testStreamGetArrow', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetArrow(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetParquet', () => {
  it('testStreamGetParquet', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetParquet(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetCsv', () => {
  it('testStreamGetCsv', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetCsv(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetXlsx', () => {
  it('testStreamGetXlsx', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetXlsx(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetJson', () => {
  it('testStreamGetJson', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetJson(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetText', () => {
  it('testStreamGetText', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetText(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamGetHtml', () => {
  it('testStreamGetHtml', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.streamGetHtml(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamPutArrow', () => {
  it('testStreamPutArrow', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const body = new Uint8Array(0);
    try {
          await datacatalog.streamPutArrow(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamPutDiffstream', () => {
  it('testStreamPutDiffstream', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const body = new DiffStream();
    try {
          await datacatalog.streamPutDiffstream(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamPutJson', () => {
  it('testStreamPutJson', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const body = {} as any;
    try {
          await datacatalog.streamPutJson(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('generateMetadata', () => {
  it('testGenerateMetadata', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.generateMetadata(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateMetadata', () => {
  it('testUpdateMetadata', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const body = new Metadata();
    try {
          await datacatalog.updateMetadata(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('streamCompact', () => {
  it('testStreamCompact', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    try {
          await datacatalog.streamCompact(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('tableRowHistory', () => {
  it('testTableRowHistory', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new GenericId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.tableRowHistory(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('tableHistory', () => {
  it('testTableHistory', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.tableHistory(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getTableAttachmentsDirectory', () => {
  it('testGetTableAttachmentsDirectory', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new GenericId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getTableAttachmentsDirectory(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getOrCreateTableAttachmentsDirectory', () => {
  it('testGetOrCreateTableAttachmentsDirectory', async () => {
    const ctx = new TestContext();
    const p0 = new ObjectId();
    const p1 = new GenericId();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.getOrCreateTableAttachmentsDirectory(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createTable', () => {
  it('testCreateTable', async () => {
    const ctx = new TestContext();
    const body = new NewTable();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.createTable(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryArrow', () => {
  it('testQueryArrow', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryArrow(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryParquet', () => {
  it('testQueryParquet', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryParquet(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryCsv', () => {
  it('testQueryCsv', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryCsv(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryXlsx', () => {
  it('testQueryXlsx', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryXlsx(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryJson', () => {
  it('testQueryJson', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryJson(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryText', () => {
  it('testQueryText', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryText(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryHtml', () => {
  it('testQueryHtml', async () => {
    const ctx = new TestContext();
    const body = new Query();
    ctx.setResponse(new Uint8Array(0));
    try {
          await datacatalog.queryHtml(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
