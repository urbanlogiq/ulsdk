// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as directory from '../directory';
import { TestContext } from '../../test_context';
import { B2cIdT as B2cId } from '../../types/generated/b2c-id';

describe('getPrincipal', () => {
  it('testGetPrincipal', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await directory.getPrincipal(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getPrincipals', () => {
  it('testGetPrincipals', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.getPrincipals(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('queryPrincipals', () => {
  it('testQueryPrincipals', async () => {
    const ctx = new TestContext();
    const p0 = 'test';
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.queryPrincipals(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getUsers', () => {
  it('testGetUsers', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.getUsers(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getUsersDisplayNames', () => {
  it('testGetUsersDisplayNames', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.getUsersDisplayNames(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getCurrentUser', () => {
  it('testGetCurrentUser', async () => {
    const ctx = new TestContext();
    const q0: any = null;
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await directory.getCurrentUser(
        ctx,
        q0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createUser', () => {
  it('testCreateUser', async () => {
    const ctx = new TestContext();
    const body = {} as any;
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await directory.createUser(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateCurrentUser', () => {
  it('testUpdateCurrentUser', async () => {
    const ctx = new TestContext();
    const body = {} as any;
    try {
          await directory.updateCurrentUser(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getUser', () => {
  it('testGetUser', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    const q0: any = null;
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await directory.getUser(
        ctx,
        p0,
        q0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('updateUser', () => {
  it('testUpdateUser', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    const body = {} as any;
    try {
          await directory.updateUser(
        ctx,
        p0,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('deleteUser', () => {
  it('testDeleteUser', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    try {
          await directory.deleteUser(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getGroups', () => {
  it('testGetGroups', async () => {
    const ctx = new TestContext();
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.getGroups(
        ctx
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('createGroup', () => {
  it('testCreateGroup', async () => {
    const ctx = new TestContext();
    const body = {} as any;
    ctx.setResponse(new TextEncoder().encode('{}'));
    try {
          await directory.createGroup(
        ctx,
        body
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('getGroupMembers', () => {
  it('testGetGroupMembers', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    ctx.setResponse(new TextEncoder().encode('[]'));
    try {
          await directory.getGroupMembers(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('deleteGroup', () => {
  it('testDeleteGroup', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    try {
          await directory.deleteGroup(
        ctx,
        p0
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('addGroupMember', () => {
  it('testAddGroupMember', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    const p1 = new B2cId();
    try {
          await directory.addGroupMember(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});

describe('removeGroupMember', () => {
  it('testRemoveGroupMember', async () => {
    const ctx = new TestContext();
    const p0 = new B2cId();
    const p1 = new B2cId();
    try {
          await directory.removeGroupMember(
        ctx,
        p0,
        p1
      );
    } catch (_) {
      // Expected for mock tests with empty/invalid responses
    }
  });
});
