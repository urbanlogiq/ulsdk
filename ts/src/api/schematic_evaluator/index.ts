// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';
import { Job as FbsJob, JobT as Job } from '../../types/generated/job';
import { ObjectId as FbsObjectId, ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { RunSpec as FbsRunSpec, RunSpecT as RunSpec } from '../../types/generated/run-spec';
import { Task as FbsTask, TaskT as Task } from '../../types/generated/task';

/**
 * Create a new schematic evaluator job
 *
 * @param ctx - A request context object
 * @param job - The description of the job to create
 * @returns The ID of the newly created job
 */
export async function createJob(
  ctx: RequestContext,
  job: RunSpec
): Promise<ObjectId> {
  let path = '/v1/api/ulv2/schematicevaluator/jobs';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = job.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectId.getRootAsObjectId(buf);
  return fbs.unpack();
}

/**
 * Fetch the details of the job with the given ID. By default this will block until the job is complete.
 *
 * @param ctx - A request context object
 * @param id - The ID of the job to fetch
 * @param timeout - Timeout in milliseconds to wait before returning a response
 * @returns The details of the job
 */
export async function getJob(
  ctx: RequestContext,
  id: ObjectId,
  timeout: number | null
): Promise<Job> {
  let path = '/v1/api/ulv2/schematicevaluator/jobs/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  if (timeout != null) {
      params.push([`timeout`, timeout.toString()]);
  }

  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsJob.getRootAsJob(buf);
  return fbs.unpack();
}

/**
 * Fetch the details of the task with the given ID.
 *
 * @param ctx - A request context object
 * @param id - The ID of the task to fetch
 * @returns The details of the task
 */
export async function getTask(
  ctx: RequestContext,
  id: ObjectId
): Promise<Task> {
  let path = '/v1/api/ulv2/schematicevaluator/tasks/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsTask.getRootAsTask(buf);
  return fbs.unpack();
}
