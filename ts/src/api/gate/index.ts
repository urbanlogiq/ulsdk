// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';

export interface AdUser {
  displayName: string;
  id: string;
  userPrincipalName: string;
  otherMails?: string[];
  department?: string;
  createdDateTime: string;
}

export interface AdGroup {
  id: string;
  displayName: string;
  description?: string;
}

export interface Bootstrap {
  user: AdUser;
  groups: AdGroup[];
  clientSecrets: Record<string, unknown>;
}

/**
 * Retrieves the current user's details, groups they belong to, and secrets that are only available when authenticated.
 * @returns The current user details needed to start the UrbanLogiq web application.
 */
export async function bootstrap(
  ctx: RequestContext
): Promise<Bootstrap> {
  let path = '/v1/bootstrap/';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}
