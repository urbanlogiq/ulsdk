// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';
import { B2cId as FbsB2cId, B2cIdT as B2cId } from '../../types/generated/b2c-id';

export interface Principal {
  principalType: string;
  displayName: string;
  id: string;
  email?: string[];
  description?: string;
  department?: string;
  accountEnabled?: boolean;
}

export interface AdGroup {
  id: string;
  displayName: string;
  description?: string;
}

export interface AdUser {
  displayName: string;
  id: string;
  userPrincipalName: string;
  otherMails?: string[];
  department?: string;
  createdDateTime: string;
  groups?: AdGroup[];
  accountEnabled: boolean;
}

export interface DisplayNames {
  displayName: string;
  id: string;
}

export interface DeviceDetail {
  deviceId?: string;
  displayName?: string;
  operatingSystem?: string;
  browser?: string;
  isCompliant?: boolean;
  isManaged?: boolean;
  trustType?: string;
}

export interface Coordinates {
  latitude?: number;
  longitude?: number;
  altitude?: number;
}

export interface Location {
  city?: string;
  state?: string;
  countryOrRegion?: string;
  geoCoordinates?: Coordinates;
}

export interface AuditLogEntry {
  id?: string;
  userPrincipalName?: string;
  userId?: string;
  createdDateTime?: string;
  ipAddress?: string;
  deviceDetail?: DeviceDetail;
  location?: Location;
}

export interface AdUserWithAuditLog {
  displayName: string;
  id: string;
  userPrincipalName: string;
  otherMails?: string[];
  department?: string;
  createdDateTime: string;
  groups?: AdGroup[];
  accountEnabled: boolean;
  auditLog?: AuditLogEntry[];
}

export interface CreateUserRequest {
  displayName?: string;
  userPrincipalName?: string;
}

export interface CreateUser {
  user: AdUser;
  password: string;
}

export interface UpdateCurrentUser {
  displayName?: string;
  otherMails?: string[];
}

export interface UpdateUser {
  displayName?: string;
  otherMails?: string[];
}

export interface CreateGroup {
  displayName: string;
  description?: string;
}

export interface GroupMembership {
  id: string;
  objectType: string;
  displayName: string;
  userPrincipalName?: string;
  otherMails?: string[];
  department?: string;
  createdDateTime?: string;
}

/**
 * Retrieves a single principal by id.
 *
 * @param ctx - A request context object
 * @param id - The ID (uuid-style) of the principal to search for
 * @returns Details of the specified principal
 */
export async function getPrincipal(
  ctx: RequestContext,
  id: B2cId
): Promise<Principal> {
  let path = '/v1/api/uldirectory/v1/principal/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Retrieves a list of principals specified by a comma-separated list of ids.
 *
 * @param ctx - A request context object
 * @param ids - The comma separated list of IDs (uuid-style) of the principals to search for
 * @returns Details of all the specified principals
 */
export async function getPrincipals(
  ctx: RequestContext,
  ids: string
): Promise<Principal[]> {
  let path = '/v1/api/uldirectory/v1/principal/:ids';
  path = path.replace(':ids', ids.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Queries for principals. The query is performed as a substring match against the following fields:
 * 
 * For users:
 * - displayName
 * - id
 * - otherMails
 * 
 * For groups:
 * - displayName
 * - id
 * - description
 *
 * @param ctx - A request context object
 * @param query - The query string to search for
 * @returns A list of all matching directory principals.
 */
export async function queryPrincipals(
  ctx: RequestContext,
  query: string
): Promise<Principal[]> {
  let path = '/v1/api/uldirectory/v1/principals/:query';
  path = path.replace(':query', query.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Retrieves details of all users in the directory that are visible to the current user.
 * @returns Details of all specified users.
 */
export async function getUsers(
  ctx: RequestContext
): Promise<AdUser[]> {
  let path = '/v1/api/uldirectory/v1/users';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Retrieves the id and display name of every user in the directory.
 * @returns The list of users including their IDs and their display names
 */
export async function getUsersDisplayNames(
  ctx: RequestContext
): Promise<DisplayNames[]> {
  let path = '/v1/api/uldirectory/v1/users/display_names';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Retrieves details of the current user.
 *
 * @param ctx - A request context object
 * @param auditLog - Whether to include the user's audit log in the response.
 * @returns The complete details of the specified user, including audit log if specified.
 */
export async function getCurrentUser(
  ctx: RequestContext,
  auditLog: boolean | null
): Promise<AdUserWithAuditLog> {
  let path = '/v1/api/uldirectory/v1/user';
  const params: [string, string][] = [];
  if (auditLog != null) {
      params.push([`audit_log`, auditLog.toString()]);
  }

  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Creates a new user in the directory.
 *
 * @param ctx - A request context object
 * @param createUserRequest - The details which which to create the new user with
 * @returns The details of the user along with their temporary, one-time-use password.
 */
export async function createUser(
  ctx: RequestContext,
  createUserRequest: CreateUserRequest
): Promise<CreateUser> {
  let path = '/v1/api/uldirectory/v1/user';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(createUserRequest));
  const res = await ctx.post(path, body, 'application/json', params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Updates the current user.
 *
 * @param ctx - A request context object
 * @param updateUserRequest - The details which which to update the current user
 */
export async function updateCurrentUser(
  ctx: RequestContext,
  updateUserRequest: UpdateCurrentUser
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/user';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(updateUserRequest));
  await ctx.put(path, body, 'application/json', params, headers);
}

/**
 * Retrieves details of a user by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the user to retrieve details for
 * @param auditLog - Whether to include the user's audit log in the response.
 * @returns The complete details of the specified user, including audit log if specified.
 */
export async function getUser(
  ctx: RequestContext,
  id: B2cId,
  auditLog: boolean | null
): Promise<AdUserWithAuditLog> {
  let path = '/v1/api/uldirectory/v1/user/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  if (auditLog != null) {
      params.push([`audit_log`, auditLog.toString()]);
  }

  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Updates a user by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the user to update
 * @param updateUserRequest - The details which which to update the user
 */
export async function updateUser(
  ctx: RequestContext,
  id: B2cId,
  updateUserRequest: UpdateUser
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/user/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(updateUserRequest));
  await ctx.put(path, body, 'application/json', params, headers);
}

/**
 * Deletes a user by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the user to delete
 */
export async function deleteUser(
  ctx: RequestContext,
  id: B2cId
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/user/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  await ctx.delete(path, params, headers);
}

/**
 * Retrieves a listing of all groups in the directory.
 * @returns A list of all the groups from the directory for which the current user is allowed to see.
 */
export async function getGroups(
  ctx: RequestContext
): Promise<AdGroup[]> {
  let path = '/v1/api/uldirectory/v1/group';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Creates a new group in the directory.
 *
 * @param ctx - A request context object
 * @param createGroupRequest - The group creation request details
 * @returns Details of the created group
 */
export async function createGroup(
  ctx: RequestContext,
  createGroupRequest: CreateGroup
): Promise<AdGroup> {
  let path = '/v1/api/uldirectory/v1/group';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(createGroupRequest));
  const res = await ctx.post(path, body, 'application/json', params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Retrieves a listing of all members of a group by group id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the group to retrieve members for
 * @returns The group membership list.
 */
export async function getGroupMembers(
  ctx: RequestContext,
  id: B2cId
): Promise<GroupMembership[]> {
  let path = '/v1/api/uldirectory/v1/group/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Deletes a group by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the group to delete
 */
export async function deleteGroup(
  ctx: RequestContext,
  id: B2cId
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/group/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  await ctx.delete(path, params, headers);
}

/**
 * Adds a member to a group.
 *
 * @param ctx - A request context object
 * @param group - The ID of the group to which to add the member
 * @param member - The ID of the member to add
 */
export async function addGroupMember(
  ctx: RequestContext,
  group: B2cId,
  member: B2cId
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/group/:group/:member';
  path = path.replace(':group', group.toString());
  path = path.replace(':member', member.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.put(path, body, 'text/plain', params, headers);
}

/**
 * Removes a member from a group.
 *
 * @param ctx - A request context object
 * @param group - The ID of the group from which to remove the member
 * @param member - The ID of the member to remove
 */
export async function removeGroupMember(
  ctx: RequestContext,
  group: B2cId,
  member: B2cId
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/group/:group/:member';
  path = path.replace(':group', group.toString());
  path = path.replace(':member', member.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  await ctx.delete(path, params, headers);
}
