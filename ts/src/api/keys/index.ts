// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';

export interface Key {
  id: string;
  pubkey?: string;
  created: number;
  last?: number;
  expiry?: number;
  comment?: string;
}

export interface GetKeys {
  keys: Key[];
}

export interface CreateKey {
  id: string;
  secretKey: string;
}

export interface UpdateKey {
  comment?: string;
  expiry?: number;
}

/**
 * Retrieves a listing of the API keys associated with the current user.
 * @returns A list of all the API keys currently associated with the current user
 */
export async function getKeys(
  ctx: RequestContext
): Promise<GetKeys> {
  let path = '/v1/api/uldirectory/v1/keys/';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Creates a new API key for the current user.
 * @returns The details of the created key, including the secret key. This secret key cannot be retrieved again, if it is lost a new key must be created.
 */
export async function createKey(
  ctx: RequestContext
): Promise<CreateKey> {
  let path = '/v1/api/uldirectory/v1/keys/';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  const res = await ctx.post(path, body, 'text/plain', params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Updates an API key by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the key to update.
 * @param updateKey - The details with which to update the key.
 */
export async function updateKey(
  ctx: RequestContext,
  id: string,
  updateKey: UpdateKey
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/keys/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(updateKey));
  await ctx.put(path, body, 'application/json', params, headers);
}

/**
 * Retrieves an API key by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the key to retrieve.
 * @returns The key details. Note that the secret key is not stored and cannot be retrieved with this API.
 */
export async function getKey(
  ctx: RequestContext,
  id: string
): Promise<Key> {
  let path = '/v1/api/uldirectory/v1/keys/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const json = JSON.parse(new TextDecoder().decode(res));
  return json;
}

/**
 * Deletes an API key by id.
 *
 * @param ctx - A request context object
 * @param id - The ID of the key to delete
 */
export async function deleteKey(
  ctx: RequestContext,
  id: string
): Promise<void> {
  let path = '/v1/api/uldirectory/v1/keys/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  await ctx.delete(path, params, headers);
}
