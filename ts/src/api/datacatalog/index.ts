// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';
import { ContentId as FbsContentId, ContentIdT as ContentId } from '../../types/generated/content-id';
import { DataCatalogObject as FbsDataCatalogObject, DataCatalogObjectT as DataCatalogObject } from '../../types/generated/data-catalog-object';
import { DiffStream as FbsDiffStream, DiffStreamT as DiffStream } from '../../types/generated/diff-stream';
import { GenericId as FbsGenericId, GenericIdT as GenericId } from '../../types/generated/generic-id';
import { History as FbsHistory, HistoryT as History } from '../../types/generated/history';
import { Metadata as FbsMetadata, MetadataT as Metadata } from '../../types/generated/metadata';
import { NewTable as FbsNewTable, NewTableT as NewTable } from '../../types/generated/new-table';
import { ObjectId as FbsObjectId, ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { ObjectIdList as FbsObjectIdList, ObjectIdListT as ObjectIdList } from '../../types/generated/object-id-list';
import { ObjectIdPairList as FbsObjectIdPairList, ObjectIdPairListT as ObjectIdPairList } from '../../types/generated/object-id-pair-list';
import { ObjectSummary as FbsObjectSummary, ObjectSummaryT as ObjectSummary } from '../../types/generated/object-summary';
import { ObjectSummaryList as FbsObjectSummaryList, ObjectSummaryListT as ObjectSummaryList } from '../../types/generated/object-summary-list';
import { Query as FbsQuery, QueryT as Query } from '../../types/generated/query';

/**
 * Fetch an object at a given content ID revision
 *
 * @param ctx - A request context object
 * @param objectId - The ID of the object to fetch content from
 * @param contentId - The ID of the content to fetch
 * @returns The datacatalog object
 */
export async function getObjectAtRevision(
  ctx: RequestContext,
  objectId: ObjectId,
  contentId: ContentId
): Promise<DataCatalogObject> {
  let path = '/v1/api/ulv2/datacatalog/content/:object_id/:content_id';
  path = path.replace(':object_id', objectId.toString());
  path = path.replace(':content_id', contentId.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsDataCatalogObject.getRootAsDataCatalogObject(buf);
  return fbs.unpack();
}

/**
 * Fetch the ID of the ACL object associated with the given object ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to fetch the ACL for
 * @returns The ID of the object's ACL
 */
export async function getAcl(
  ctx: RequestContext,
  id: ObjectId
): Promise<ObjectId> {
  let path = '/v1/api/ulv2/datacatalog/object/acl/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectId.getRootAsObjectId(buf);
  return fbs.unpack();
}

/**
 * Fetch the head revision of the object with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to fetch the head revision for
 * @returns The summary for the head revision of the object
 */
export async function getHeadRevision(
  ctx: RequestContext,
  id: ObjectId
): Promise<ObjectSummary> {
  let path = '/v1/api/ulv2/datacatalog/object/head/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectSummary.getRootAsObjectSummary(buf);
  return fbs.unpack();
}

/**
 * Fetch the object with the given ID at its head revision
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to fetch
 * @returns Object content
 */
export async function getObject(
  ctx: RequestContext,
  id: ObjectId
): Promise<DataCatalogObject> {
  let path = '/v1/api/ulv2/datacatalog/object/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsDataCatalogObject.getRootAsDataCatalogObject(buf);
  return fbs.unpack();
}

/**
 * Update the object with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to update
 * @param object - Object contents with which to update the specified object
 * @returns The content ID of the updated object
 */
export async function updateObject(
  ctx: RequestContext,
  id: ObjectId,
  object: DataCatalogObject
): Promise<ContentId> {
  let path = '/v1/api/ulv2/datacatalog/object/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = object.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsContentId.getRootAsContentId(buf);
  return fbs.unpack();
}

/**
 * Update the attributes of the object with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the object whose attributes will be updated
 * @param overwrite - True to overwrite existing attributes or false to fail if there is a conflict.
 * @param attributes - A string:string json map with the new attributes
 */
export async function updateAttributes(
  ctx: RequestContext,
  id: ObjectId,
  overwrite: boolean,
  attributes: Record<string, unknown>
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/object/:id/attributes';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  params.push([`overwrite`, overwrite.toString()]);

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(attributes));
  await ctx.post(path, body, 'application/json', params, headers);
}

/**
 * Delete the attribute with the given key from the object with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the object whose attribute will be deleted
 * @param key - The key of the attribute to delete
 */
export async function deleteAttribute(
  ctx: RequestContext,
  id: ObjectId,
  key: string
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/object/:id/attribute/:key';
  path = path.replace(':id', id.toString());
  path = path.replace(':key', key.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  await ctx.delete(path, params, headers);
}

/**
 * Fetch a list of object summaries
 *
 * @param ctx - A request context object
 * @param objectIds - A list of object IDs to fetch summaries for
 * @returns A list of object summaries
 */
export async function getObjectSummaries(
  ctx: RequestContext,
  objectIds: ObjectIdList
): Promise<ObjectSummaryList> {
  let path = '/v1/api/ulv2/datacatalog/object_summaries';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = objectIds.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectSummaryList.getRootAsObjectSummaryList(buf);
  return fbs.unpack();
}

/**
 * Given a list of IDs for stream objects, fetch their metadata in bulk. Note that the returned DataCatalogObject instances only have a valid metadata `obj` field.
 *
 * @param ctx - A request context object
 * @param streamIds - A list of IDs of stream objects to fetch metadata for
 * @returns A list of metadata DataCatalogObjects
 */
export async function bulkFetchMetadata(
  ctx: RequestContext,
  streamIds: ObjectIdList
): Promise<ObjectIdPairList> {
  let path = '/v1/api/ulv2/datacatalog/stream/metadata_list';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = streamIds.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectIdPairList.getRootAsObjectIdPairList(buf);
  return fbs.unpack();
}

/**
 * Given a list of object IDs, fetch their contents in bulk
 *
 * @param ctx - A request context object
 * @param objectIds - A list of object IDs to fetch summaries for
 * @returns A list of object contents with their IDs
 */
export async function bulkFetchObjects(
  ctx: RequestContext,
  objectIds: ObjectIdList
): Promise<ObjectIdPairList> {
  let path = '/v1/api/ulv2/datacatalog/object_list';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = objectIds.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectIdPairList.getRootAsObjectIdPairList(buf);
  return fbs.unpack();
}

/**
 * Create a new, empty object
 * @returns The object summary of the newly created object
 */
export async function createObject(
  ctx: RequestContext
): Promise<ObjectSummaryList> {
  let path = '/v1/api/ulv2/datacatalog/objects';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  const res = await ctx.post(path, body, 'text/plain', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectSummaryList.getRootAsObjectSummaryList(buf);
  return fbs.unpack();
}

/**
 * INTERNAL
 *
 * @param ctx - A request context object
 * @param columns - Comma separated list of columns to aggregate
 * @param query - INTERNAL
 * @returns INTERNAL
 */
export async function queryAggregateNumeric(
  ctx: RequestContext,
  columns: string,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query/aggregate/numeric';
  const params: [string, string][] = [];
  params.push([`columns`, columns]);

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * INTERNAL
 *
 * @param ctx - A request context object
 * @param columns - Comma separated list of columns to aggregate
 * @param query - INTERNAL
 * @returns INTERNAL
 */
export async function queryAggregateString(
  ctx: RequestContext,
  columns: string,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query/aggregate/string';
  const params: [string, string][] = [];
  params.push([`columns`, columns]);

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * INTERNAL
 *
 * @param ctx - A request context object
 * @param buckets - Number of buckets
 * @param columns - Comma separated list of columns to aggregate
 * @param query - INTERNAL
 * @returns INTERNAL
 */
export async function queryAggregateHisto(
  ctx: RequestContext,
  buckets: number,
  columns: string,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query/aggregate/histo';
  const params: [string, string][] = [];
  params.push([`buckets`, buckets.toString()]);
  params.push([`columns`, columns]);

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * INTERNAL
 *
 * @param ctx - A request context object
 * @param buckets - Number of buckets
 * @param numeratorColumns - Comma separated list of columns to aggregate for the numerator value
 * @param denominatorColumns - Comma separated list of columns to aggregate for the denominator value
 * @param query - INTERNAL
 * @returns INTERNAL
 */
export async function queryAggregateRelativeHisto(
  ctx: RequestContext,
  buckets: number,
  numeratorColumns: string,
  denominatorColumns: string,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query/aggregate/relative_histo';
  const params: [string, string][] = [];
  params.push([`buckets`, buckets.toString()]);
  params.push([`numerator_columns`, numeratorColumns]);
  params.push([`denominator_columns`, denominatorColumns]);

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetArrow(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.apache.arrow.stream';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetParquet(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.apache.parquet';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetCsv(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/csv';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetXlsx(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetJson(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/json';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetText(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/plain';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Fetch the stream with the given ID
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to fetch
 * @returns Stream data as requested
 */
export async function streamGetHtml(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/html';

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Append data, in Apache Arrow format, to the specified stream
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to append data to
 * @param data - The Arrow record batches to append
 */
export async function streamPutArrow(
  ctx: RequestContext,
  id: ObjectId,
  data: Uint8Array
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = data;
  await ctx.put(path, body, 'application/vnd.apache.arrow.stream', params, headers);
}

/**
 * Append a diffstream to the specified stream
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to append data to
 * @param data - The Arrow record batches to append
 */
export async function streamPutDiffstream(
  ctx: RequestContext,
  id: ObjectId,
  data: DiffStream
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = data.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.put(path, body, 'application/octet-stream', params, headers);
}

/**
 * Append JSON data to the specified stream
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to append data to
 * @param data - The Arrow record batches to append
 */
export async function streamPutJson(
  ctx: RequestContext,
  id: ObjectId,
  data: Record<string, unknown>[]
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = new TextEncoder().encode(JSON.stringify(data));
  await ctx.put(path, body, 'application/json', params, headers);
}

/**
 * Generate metadata for the specified stream
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to generate metadata for
 * @returns The metadata as generated from the stream data
 */
export async function generateMetadata(
  ctx: RequestContext,
  id: ObjectId
): Promise<Metadata> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id/generated/metadata';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsMetadata.getRootAsMetadata(buf);
  return fbs.unpack();
}

/**
 * Given a stream ID and metadata, update the stream metadata to a combination of:
 * - the existing metadata
 * - the provided metadata
 * - the generated metadata
 * 
 * Also, update the stream object to point to the updated metadata and to have an updated schema.
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to update metadata for
 * @param metadata - The metadata to update the stream with
 */
export async function updateMetadata(
  ctx: RequestContext,
  id: ObjectId,
  metadata: Metadata | null
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id/metadata';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  if (metadata != null) {
    const __builder = new flatbuffers.Builder();
    const __offset = metadata.pack(__builder);
    __builder.finishSizePrefixed(__offset);
    body = __builder.asUint8Array();
  }
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Compact the specified stream
 *
 * @param ctx - A request context object
 * @param id - The ID of the stream to compact
 */
export async function streamCompact(
  ctx: RequestContext,
  id: ObjectId
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/stream/:id/compact';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Fetch the history of a row in a table
 *
 * @param ctx - A request context object
 * @param id - The ID of the table to fetch the row history from
 * @param row - The ID of the row to fetch the history for
 * @returns The history of the row
 */
export async function tableRowHistory(
  ctx: RequestContext,
  id: ObjectId,
  row: GenericId
): Promise<History> {
  let path = '/v1/api/ulv2/datacatalog/table/:id/history/:row';
  path = path.replace(':id', id.toString());
  path = path.replace(':row', row.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsHistory.getRootAsHistory(buf);
  return fbs.unpack();
}

/**
 * Fetch the history of a table
 *
 * @param ctx - A request context object
 * @param id - The ID of the table to fetch the history from
 * @returns The history of the table
 */
export async function tableHistory(
  ctx: RequestContext,
  id: ObjectId
): Promise<History> {
  let path = '/v1/api/ulv2/datacatalog/table/:id/history';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsHistory.getRootAsHistory(buf);
  return fbs.unpack();
}

/**
 * Fetch the directory ID for a row's file attachments location
 *
 * @param ctx - A request context object
 * @param id - The ID of the table to fetch the attachments directory ID from
 * @param row - The ID of the row to fetch the attachments directory ID for
 * @returns The directory ID for the row's attachments
 */
export async function getTableAttachmentsDirectory(
  ctx: RequestContext,
  id: ObjectId,
  row: GenericId
): Promise<ObjectId> {
  let path = '/v1/api/ulv2/datacatalog/table/:id/attachments/:row';
  path = path.replace(':id', id.toString());
  path = path.replace(':row', row.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectId.getRootAsObjectId(buf);
  return fbs.unpack();
}

/**
 * Fetch the directory ID for a row's file attachments location, creating it if it doesn't exist
 *
 * @param ctx - A request context object
 * @param id - The ID of the table to fetch the attachments directory ID from
 * @param row - The ID of the row to fetch the attachments directory ID for
 * @returns The directory ID for the row's attachments
 */
export async function getOrCreateTableAttachmentsDirectory(
  ctx: RequestContext,
  id: ObjectId,
  row: GenericId
): Promise<ObjectId> {
  let path = '/v1/api/ulv2/datacatalog/table/:id/attachments/:row';
  path = path.replace(':id', id.toString());
  path = path.replace(':row', row.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  const res = await ctx.post(path, body, 'text/plain', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectId.getRootAsObjectId(buf);
  return fbs.unpack();
}

/**
 * Create a new table
 *
 * @param ctx - A request context object
 * @param newTable - New table creation details
 * @returns The ID of the newly created table
 */
export async function createTable(
  ctx: RequestContext,
  newTable: NewTable
): Promise<ObjectId> {
  let path = '/v1/api/ulv2/datacatalog/table';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = newTable.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectId.getRootAsObjectId(buf);
  return fbs.unpack();
}

/**
 * Query the datacatalog, returning data in Apache Arrow IPC Stream format
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryArrow(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.apache.arrow.stream';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data in Apache Parquet format
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryParquet(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.apache.parquet';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data in CSV format
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryCsv(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/csv';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data in Excel (XLSX) format
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryXlsx(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data JSON
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryJson(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'application/json';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data in plain text
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryText(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/plain';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}

/**
 * Query the datacatalog, returning data in HTML
 *
 * @param ctx - A request context object
 * @param query - The query to execute
 * @returns The result of the query
 */
export async function queryHtml(
  ctx: RequestContext,
  query: Query
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/query';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};
  headers['accept'] = 'text/html';

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = query.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  const res = await ctx.post(path, body, 'application/octet-stream', params, headers);
  return res;
}
