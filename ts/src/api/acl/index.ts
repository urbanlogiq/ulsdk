// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

import * as flatbuffers from 'flatbuffers/js/flatbuffers';
import { RequestContext } from '../../request_context';
import { AccessRequest as FbsAccessRequest, AccessRequestT as AccessRequest } from '../../types/generated/access-request';
import { B2cId as FbsB2cId, B2cIdT as B2cId } from '../../types/generated/b2c-id';
import { ObjectId as FbsObjectId, ObjectIdT as ObjectId } from '../../types/generated/object-id';
import { ObjectSummaryList as FbsObjectSummaryList, ObjectSummaryListT as ObjectSummaryList } from '../../types/generated/object-summary-list';
import { ShareDetails as FbsShareDetails, ShareDetailsT as ShareDetails } from '../../types/generated/share-details';

/**
 * Create a new access control list. This ACL will be created with the current user as the owner.
 * @returns An object summary list containing a single entry with the new ACL.
 */
export async function newAcl(
  ctx: RequestContext
): Promise<ObjectSummaryList> {
  let path = '/v1/api/ulv2/datacatalog/acl/';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  const res = await ctx.post(path, body, 'text/plain', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectSummaryList.getRootAsObjectSummaryList(buf);
  return fbs.unpack();
}

/**
 * Create a new access control list that inherits from an existing ACL
 *
 * @param ctx - A request context object
 * @param extends_ - The ID of the access control list to inherit from
 * @returns An object summary list containing a single entry with the new ACL.
 */
export async function newFrom(
  ctx: RequestContext,
  extends_: ObjectId | null
): Promise<ObjectSummaryList> {
  let path = '/v1/api/ulv2/datacatalog/acl/';
  const params: [string, string][] = [];
  if (extends_ != null) {
      params.push([`extends`, extends_.toString()]);
  }

  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  const res = await ctx.post(path, body, 'text/plain', params, headers);
  const buf = new flatbuffers.ByteBuffer(res);
  buf.setPosition(buf.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  const fbs = FbsObjectSummaryList.getRootAsObjectSummaryList(buf);
  return fbs.unpack();
}

/**
 * Request access to an object
 *
 * @param ctx - A request context object
 * @param request - The access request object containing details about the permissions desired
 */
export async function request(
  ctx: RequestContext,
  request: AccessRequest
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/request';
  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = request.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Share an object to a specific access control list principal with specified permissions.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object that will be shared.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param permissionBits - The permission bitset (see the PermissionTy enum for more information).
 */
export async function share(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  permissionBits: number
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/share/:id/:to/:permission';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());
  path = path.replace(':permission', permissionBits.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Share an object to a specific access control list principal with specified permissions.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object that will be shared.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param permissionBits - The permission bitset (see the PermissionTy enum for more information).
 * @param shareDetails - A ShareDetails object containing extra information for the sharing operation, including whether or not to notify the target of the operation, and to provide a message.
 */
export async function shareWithDetails(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  permissionBits: number,
  shareDetails: ShareDetails
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/share/:id/:to/:permission';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());
  path = path.replace(':permission', permissionBits.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = shareDetails.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Share an object to a specific access control list principal with all permissions.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object that will be shared.
 * @param to - The ID of the principal who will be granted access to the object.
 */
export async function shareAll(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/share/:id/:to';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Share an object to a specific access control list principal with all permissions.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object that will be shared.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param shareDetails - A ShareDetails object containing extra information for the sharing operation, including whether or not to notify the target of the operation, and to provide a message.
 */
export async function shareAllWithDetails(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  shareDetails: ShareDetails
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/share/:id/:to';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = shareDetails.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Grant an object to a specific access control list principal with specified permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to which access will be granted.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param permissionBits - The permission bitset (see the PermissionTy enum for more information).
 */
export async function grant(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  permissionBits: number
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/grant/:id/:to/:permission';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());
  path = path.replace(':permission', permissionBits.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Grant an object to a specific access control list principal with specified permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to which access will be granted.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param permissionBits - The permission bitset (see the PermissionTy enum for more information).
 * @param grantDetails - A ShareDetails object containing extra information for the sharing operation, including whether or not to notify the target of the operation, and to provide a message.
 */
export async function grantWithDetails(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  permissionBits: number,
  grantDetails: ShareDetails
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/grant/:id/:to/:permission';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());
  path = path.replace(':permission', permissionBits.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = grantDetails.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Grant an object to a specific access control list principal with all permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to which access will be granted.
 * @param to - The ID of the principal who will be granted access to the object.
 */
export async function grantAll(
  ctx: RequestContext,
  id: ObjectId,
  to: ObjectId
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/grant/:id/:to';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Grant an object to a specific access control list principal with all permissions. Unlike the share operation, grant operations will fail with a 403 if the user performing the operation does not have the appropriate access.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to which access will be granted.
 * @param to - The ID of the principal who will be granted access to the object.
 * @param grantDetails - A ShareDetails object containing extra information for the sharing operation, including whether or not to notify the target of the operation, and to provide a message.
 */
export async function grantAllWithDetails(
  ctx: RequestContext,
  id: ObjectId,
  to: B2cId,
  grantDetails: ShareDetails
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/grant/:id/:to';
  path = path.replace(':id', id.toString());
  path = path.replace(':to', to.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  const __builder = new flatbuffers.Builder();
  const __offset = grantDetails.pack(__builder);
  __builder.finishSizePrefixed(__offset);
  body = __builder.asUint8Array();
  await ctx.post(path, body, 'application/octet-stream', params, headers);
}

/**
 * Revoke all access from a specified ACL to an object.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object from which access will be revoked.
 * @param from - The ID of the principal whose access will be revoked.
 */
export async function revoke(
  ctx: RequestContext,
  id: ObjectId,
  from: B2cId
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/revoke/:id/:from';
  path = path.replace(':id', id.toString());
  path = path.replace(':from', from.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Get the permissions the caller has on the object with the given ID.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object which will be queried for permissions.
 * @returns The 32-bit integer bitmap corresponding to the permissions the caller has on the object.
 */
export async function getPermissions(
  ctx: RequestContext,
  id: ObjectId
): Promise<Uint8Array> {
  let path = '/v1/api/ulv2/datacatalog/acl/perms/:id';
  path = path.replace(':id', id.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  const res = await ctx.get(path, params, headers);
  return res;
}

/**
 * Set the default mode of an object. This can only be done by an object's owner.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object to which access will be granted.
 * @param permission - The permission bitset (see the PermissionTy enum for more information).
 */
export async function setDefaultMode(
  ctx: RequestContext,
  id: ObjectId,
  permission: number
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/default/:id/:permission';
  path = path.replace(':id', id.toString());
  path = path.replace(':permission', permission.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}

/**
 * Forcibly set an object's ACL to another ACL object. Note that the target ACL needs to contain the exact same permissions as the current ACL otherwise this method will return 400 Bad Request. This is a safeguard to ensure the user cannot lock themselves out of an object.
 *
 * @param ctx - A request context object
 * @param id - The ID of the object which will have its ACL set.
 * @param aclId - The ID of the ACL object which will be used as the object ACL.
 */
export async function set(
  ctx: RequestContext,
  id: ObjectId,
  aclId: ObjectId
): Promise<void> {
  let path = '/v1/api/ulv2/datacatalog/acl/set/:id/:acl_id';
  path = path.replace(':id', id.toString());
  path = path.replace(':acl_id', aclId.toString());

  const params: [string, string][] = [];
  const headers: Record<string, string> = {};

  let body: Uint8Array | null = null;
  body = null;
  await ctx.post(path, body, 'text/plain', params, headers);
}
