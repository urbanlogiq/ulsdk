// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package com.urbanlogiq.ulsdk.types;

/**
 *  Where a dataset records the time its rows are observed. Declared rather
 *  than inferred: a stream's time axis is not derivable from its schema, since
 *  several of its columns may be datetimes -- a scheduled time, an actual
 *  time, an ingestion stamp -- and only one of them is the axis a consumer
 *  should filter on.
 * 
 *  Streams whose rows are valid over a window rather than at an instant
 *  (slowly-changing dimensions, carrying a validity start and end) declare
 *  NoTime. Declaring the window's start as a ColumnTime would be worse than
 *  declaring nothing: a consumer would filter validity starts as though they
 *  were observations, dropping rows whose window covers the requested range
 *  but whose start does not fall inside it. Give those streams their own
 *  variant naming both columns once a consumer needs to filter them properly.
 * 
 *  Append new variants only -- a union member's position is its wire value, so
 *  inserting one would silently reinterpret existing stored metadata.
 */
public final class TimeSource {
    Object _value;

    public TimeSource(Object value) {
        if (!(value instanceof com.urbanlogiq.ulsdk.types.NoTime)
            && !(value instanceof com.urbanlogiq.ulsdk.types.ColumnTime)) {
            throw new com.urbanlogiq.ulsdk.InvalidVariantException("TimeSource", value);
        }

        this._value = value;
    }

    public com.urbanlogiq.ulsdk.Pair<Integer, Byte> serializeTo(com.google.flatbuffers.FlatBufferBuilder builder) {
        if (this._value instanceof com.urbanlogiq.ulsdk.types.NoTime) {
            com.urbanlogiq.ulsdk.types.NoTime o = (com.urbanlogiq.ulsdk.types.NoTime)this._value;
            int offset = o.serializeTo(builder);
            byte ty = com.urbanlogiq.ulsdk.types.generated.TimeSource.NoTime;
            return new com.urbanlogiq.ulsdk.Pair<Integer, Byte>(offset, ty);
        } else if (this._value instanceof com.urbanlogiq.ulsdk.types.ColumnTime) {
            com.urbanlogiq.ulsdk.types.ColumnTime o = (com.urbanlogiq.ulsdk.types.ColumnTime)this._value;
            int offset = o.serializeTo(builder);
            byte ty = com.urbanlogiq.ulsdk.types.generated.TimeSource.ColumnTime;
            return new com.urbanlogiq.ulsdk.Pair<Integer, Byte>(offset, ty);
        } else {
            throw new RuntimeException("unreachable variant");
        }
    }

    public TimeSource(com.google.flatbuffers.Table o, byte ty) {
        if (o == null) {
            throw new NullPointerException();
        }

        if (ty == com.urbanlogiq.ulsdk.types.generated.TimeSource.NoTime) {
            this._value = new com.urbanlogiq.ulsdk.types.NoTime((com.urbanlogiq.ulsdk.types.generated.NoTime)o);
        } else if (ty == com.urbanlogiq.ulsdk.types.generated.TimeSource.ColumnTime) {
            this._value = new com.urbanlogiq.ulsdk.types.ColumnTime((com.urbanlogiq.ulsdk.types.generated.ColumnTime)o);
        }
    }

    public TimeSource() {
        this(new com.urbanlogiq.ulsdk.types.NoTime());
    }

    @Override
    public boolean equals(Object o) {
        return org.apache.commons.lang3.builder.EqualsBuilder.reflectionEquals(this, o, false, null, true);
    }
}