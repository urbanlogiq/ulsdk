// Copyright (c), CommunityLogiq Software
//
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package com.urbanlogiq.ulsdk.types.generated;
// automatically generated by the FlatBuffers compiler, do not modify

import com.google.flatbuffers.BaseVector;
import com.google.flatbuffers.BooleanVector;
import com.google.flatbuffers.ByteVector;
import com.google.flatbuffers.Constants;
import com.google.flatbuffers.DoubleVector;
import com.google.flatbuffers.FlatBufferBuilder;
import com.google.flatbuffers.FloatVector;
import com.google.flatbuffers.IntVector;
import com.google.flatbuffers.LongVector;
import com.google.flatbuffers.ShortVector;
import com.google.flatbuffers.StringVector;
import com.google.flatbuffers.Struct;
import com.google.flatbuffers.UnionVector;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * Synthetic timestamp series used to fill empty time buckets in metric queries.
 * Emits one row per bucket between `start_ms` and `end_ms`
 * (inclusive) at `interval_ms` spacing, in a single column named `output_column`.
 * All bounds are UTC unix milliseconds - to align with the
 * time_bucket UDF, callers should pass values that are already snapped to a
 * bucket boundary in the desired zone.
 * Timezone-aware generation is not yet supported.
 *
 * If `infer_bounds` is true, `start_ms` and `end_ms` are ignored and the
 * catalog planner resolves them from the WHERE filter of whichever source
 * this series is LEFT JOINed against. The joined source must produce the
 * join column via `time_bucket(width, ts)` or
 * `date_trunc(unit, [at_timezone(]ts[, tz)])` and have a closed range
 * filter on `ts`; otherwise planning errors.
 *
 * Exactly one of `interval_ms` / `interval_months` is non-zero (or both
 * zero when `infer_bounds=true` — the planner fills in whichever flavour
 * the joined source's bucket function dictates). `interval_months` is the
 * calendar-month count used to step the synthetic series; years are
 * represented as 12N months.
 *
 * `tz` is the IANA timezone name (e.g. `"America/Los_Angeles"`) used to
 * floor bounds and step the series in local calendar time. Empty means
 * UTC. This is only set when the joined source's bucket expression wraps
 * the timestamp in `at_timezone(ts, '<tz>')` — bare `date_trunc(unit, ts)`
 * stays UTC. When `tz` is non-empty, the catalog emits the series as a
 * precomputed VALUES list rather than a DataFusion `generate_series` call
 * so the local-calendar boundaries (including DST transitions) are
 * authoritative at plan time.
 */
@SuppressWarnings("unused")
public final class TimeSeries extends com.google.flatbuffers.Table {
  public static void ValidateVersion() { Constants.FLATBUFFERS_25_2_10(); }
  public static TimeSeries getRootAsTimeSeries(ByteBuffer _bb) { return getRootAsTimeSeries(_bb, new TimeSeries()); }
  public static TimeSeries getRootAsTimeSeries(ByteBuffer _bb, TimeSeries obj) { _bb.order(ByteOrder.LITTLE_ENDIAN); return (obj.__assign(_bb.getInt(_bb.position()) + _bb.position(), _bb)); }
  public void __init(int _i, ByteBuffer _bb) { __reset(_i, _bb); }
  public TimeSeries __assign(int _i, ByteBuffer _bb) { __init(_i, _bb); return this; }

  public long startMs() { int o = __offset(4); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  public long endMs() { int o = __offset(6); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  public long intervalMs() { int o = __offset(8); return o != 0 ? bb.getLong(o + bb_pos) : 0L; }
  public String outputColumn() { int o = __offset(10); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer outputColumnAsByteBuffer() { return __vector_as_bytebuffer(10, 1); }
  public ByteBuffer outputColumnInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 10, 1); }
  public boolean inferBounds() { int o = __offset(12); return o != 0 ? 0!=bb.get(o + bb_pos) : false; }
  public int intervalMonths() { int o = __offset(14); return o != 0 ? bb.getInt(o + bb_pos) : 0; }
  public String tz() { int o = __offset(16); return o != 0 ? __string(o + bb_pos) : null; }
  public ByteBuffer tzAsByteBuffer() { return __vector_as_bytebuffer(16, 1); }
  public ByteBuffer tzInByteBuffer(ByteBuffer _bb) { return __vector_in_bytebuffer(_bb, 16, 1); }

  public static int createTimeSeries(FlatBufferBuilder builder,
      long startMs,
      long endMs,
      long intervalMs,
      int outputColumnOffset,
      boolean inferBounds,
      int intervalMonths,
      int tzOffset) {
    builder.startTable(7);
    TimeSeries.addIntervalMs(builder, intervalMs);
    TimeSeries.addEndMs(builder, endMs);
    TimeSeries.addStartMs(builder, startMs);
    TimeSeries.addTz(builder, tzOffset);
    TimeSeries.addIntervalMonths(builder, intervalMonths);
    TimeSeries.addOutputColumn(builder, outputColumnOffset);
    TimeSeries.addInferBounds(builder, inferBounds);
    return TimeSeries.endTimeSeries(builder);
  }

  public static void startTimeSeries(FlatBufferBuilder builder) { builder.startTable(7); }
  public static void addStartMs(FlatBufferBuilder builder, long startMs) { builder.addLong(0, startMs, 0L); }
  public static void addEndMs(FlatBufferBuilder builder, long endMs) { builder.addLong(1, endMs, 0L); }
  public static void addIntervalMs(FlatBufferBuilder builder, long intervalMs) { builder.addLong(2, intervalMs, 0L); }
  public static void addOutputColumn(FlatBufferBuilder builder, int outputColumnOffset) { builder.addOffset(3, outputColumnOffset, 0); }
  public static void addInferBounds(FlatBufferBuilder builder, boolean inferBounds) { builder.addBoolean(4, inferBounds, false); }
  public static void addIntervalMonths(FlatBufferBuilder builder, int intervalMonths) { builder.addInt(5, intervalMonths, 0); }
  public static void addTz(FlatBufferBuilder builder, int tzOffset) { builder.addOffset(6, tzOffset, 0); }
  public static int endTimeSeries(FlatBufferBuilder builder) {
    int o = builder.endTable();
    builder.required(o, 10);  // output_column
    return o;
  }

  public static final class _Vector extends BaseVector {
    public _Vector __assign(int _vector, int _element_size, ByteBuffer _bb) { __reset(_vector, _element_size, _bb); return this; }

    public TimeSeries get(int j) { return get(new TimeSeries(), j); }
    public TimeSeries get(TimeSeries obj, int j) {  return obj.__assign(__indirect(__element(j), bb), bb); }
  }
}

