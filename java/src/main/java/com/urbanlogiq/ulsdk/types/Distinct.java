// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package com.urbanlogiq.ulsdk.types;

/**
 *  The Distinct function defined in fun.fbs is for use in cases like:
 *  SELECT COUNT(DISTINCT c0), SUM(c1) FROM t GROUP BY c2;
 *  The `distinct` field here on the query element is to be used to remove duplicate rows, like:
 *  SELECT DISTINCT * FROM t;
 *  or
 *  SELECT DISTINCT ON (c0, c1) FROM t;
 */
public final class Distinct {
    /**
     *  If `on` is unset or has length 0, then the distinct is:
     *  SELECT DISTINCT * FROM t;
     *  If `on` has length > 0, then distinct is:
     *  SELECT DISTINCT ON (c0, c1) FROM t;
     */
    com.urbanlogiq.ulsdk.types.Expr[] _on;
    public com.urbanlogiq.ulsdk.types.Expr[] getOn() {
        return this._on;
    }
    public void setOn(com.urbanlogiq.ulsdk.types.Expr[] value) {
        this._on = value;
    }

    public Distinct(com.urbanlogiq.ulsdk.types.generated.Distinct o) {
        if (o.onVector() != null) {
            com.urbanlogiq.ulsdk.types.Expr[] on = new com.urbanlogiq.ulsdk.types.Expr[o.onLength()];
            for (int i = 0; i < o.onLength(); i++) {
                com.urbanlogiq.ulsdk.types.Expr onValue = null;
                if (o.on(i) != null) {
                    onValue = new com.urbanlogiq.ulsdk.types.Expr(o.on(i));
                }
                on[i] = onValue;
            }
            this._on = on;
        }
    }

    public Distinct(byte[] data) {
        this(com.urbanlogiq.ulsdk.types.generated.Distinct.getRootAsDistinct(com.google.flatbuffers.ByteBufferUtil.removeSizePrefix(java.nio.ByteBuffer.wrap(data))));
    }

    public int serializeTo(com.google.flatbuffers.FlatBufferBuilder builder) {
        Integer onOffset = null;
        if (this._on != null) {
            int[] onOffsets = new int[this._on.length];;
            for (int i = 0; i < this._on.length; i++) {
                onOffsets[i] = this._on[i].serializeTo(builder);
            }
            com.urbanlogiq.ulsdk.types.generated.Distinct.startOnVector(builder, this._on.length);
            for (int i = onOffsets.length - 1; i >= 0; i--) {
                builder.addOffset(onOffsets[i]);
            }
            onOffset = builder.endVector();
        }
        com.urbanlogiq.ulsdk.types.generated.Distinct.startDistinct(builder);
        if (onOffset != null) {
            com.urbanlogiq.ulsdk.types.generated.Distinct.addOn(builder, onOffset);
        }
        return com.urbanlogiq.ulsdk.types.generated.Distinct.endDistinct(builder);
    }

    public byte[] toBytes() {
        com.google.flatbuffers.FlatBufferBuilder builder = new com.google.flatbuffers.FlatBufferBuilder();
        int offset = this.serializeTo(builder);
        builder.finishSizePrefixed(offset);
        return builder.sizedByteArray();
    }

    public Distinct() {
        this._on = new com.urbanlogiq.ulsdk.types.Expr[0];
    }

    @Override
    public boolean equals(Object o) {
        return org.apache.commons.lang3.builder.EqualsBuilder.reflectionEquals(this, o, false, null, true);
    }
}