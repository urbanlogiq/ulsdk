// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

package com.urbanlogiq.ulsdk.api;
import com.urbanlogiq.ulsdk.ApiKeyContext;
import com.urbanlogiq.ulsdk.Environment;
import com.urbanlogiq.ulsdk.Region;
import java.util.UUID;
import org.junit.Test;
import com.urbanlogiq.ulsdk.api.directory.AdGroup;
import com.urbanlogiq.ulsdk.api.directory.AdUser;
import com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog;
import com.urbanlogiq.ulsdk.api.directory.CreateGroup;
import com.urbanlogiq.ulsdk.api.directory.CreateUser;
import com.urbanlogiq.ulsdk.api.directory.CreateUserRequest;
import com.urbanlogiq.ulsdk.api.directory.DisplayNames;
import com.urbanlogiq.ulsdk.api.directory.GroupMembership;
import com.urbanlogiq.ulsdk.api.directory.Principal;
import com.urbanlogiq.ulsdk.api.directory.UpdateCurrentUser;
import com.urbanlogiq.ulsdk.api.directory.UpdateUser;

public final class TestApiDirectory {
    @Test
    public void testGetPrincipal() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.Principal expected = new com.urbanlogiq.ulsdk.api.directory.Principal();
        byte[] expectedBytes = new org.json.JSONObject(expected.toMap()).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.Principal result = com.urbanlogiq.ulsdk.api.directory.Directory.getPrincipal(
            ctx,
            p0
        );
        org.junit.Assert.assertTrue(result.equals(expected));
    }

    @Test
    public void testGetPrincipals() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        String p0 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
        com.urbanlogiq.ulsdk.api.directory.Principal[] expected = new com.urbanlogiq.ulsdk.api.directory.Principal[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.Principal();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.Principal[] result = com.urbanlogiq.ulsdk.api.directory.Directory.getPrincipals(
            ctx,
            p0
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testQueryPrincipals() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        String p0 = "Lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.";
        com.urbanlogiq.ulsdk.api.directory.Principal[] expected = new com.urbanlogiq.ulsdk.api.directory.Principal[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.Principal();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.Principal[] result = com.urbanlogiq.ulsdk.api.directory.Directory.queryPrincipals(
            ctx,
            p0
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testGetUsers() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.AdUser[] expected = new com.urbanlogiq.ulsdk.api.directory.AdUser[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.AdUser();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.AdUser[] result = com.urbanlogiq.ulsdk.api.directory.Directory.getUsers(
            ctx
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testGetUsersDisplayNames() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.DisplayNames[] expected = new com.urbanlogiq.ulsdk.api.directory.DisplayNames[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.DisplayNames();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.DisplayNames[] result = com.urbanlogiq.ulsdk.api.directory.Directory.getUsersDisplayNames(
            ctx
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testGetCurrentUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        boolean q0 = true;
        com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog expected = new com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog();
        byte[] expectedBytes = new org.json.JSONObject(expected.toMap()).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog result = com.urbanlogiq.ulsdk.api.directory.Directory.getCurrentUser(
            ctx,
            q0
        );
        org.junit.Assert.assertTrue(result.equals(expected));
    }

    @Test
    public void testCreateUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.CreateUserRequest body = new com.urbanlogiq.ulsdk.api.directory.CreateUserRequest();
        com.urbanlogiq.ulsdk.api.directory.CreateUser expected = new com.urbanlogiq.ulsdk.api.directory.CreateUser();
        byte[] expectedBytes = new org.json.JSONObject(expected.toMap()).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.CreateUser result = com.urbanlogiq.ulsdk.api.directory.Directory.createUser(
            ctx,
            body
        );
        org.junit.Assert.assertTrue(result.equals(expected));
    }

    @Test
    public void testUpdateCurrentUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.UpdateCurrentUser body = new com.urbanlogiq.ulsdk.api.directory.UpdateCurrentUser();
        com.urbanlogiq.ulsdk.api.directory.Directory.updateCurrentUser(
            ctx,
            body
        );
    }

    @Test
    public void testGetUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        boolean q0 = true;
        com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog expected = new com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog();
        byte[] expectedBytes = new org.json.JSONObject(expected.toMap()).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.AdUserWithAuditLog result = com.urbanlogiq.ulsdk.api.directory.Directory.getUser(
            ctx,
            p0,
            q0
        );
        org.junit.Assert.assertTrue(result.equals(expected));
    }

    @Test
    public void testUpdateUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.UpdateUser body = new com.urbanlogiq.ulsdk.api.directory.UpdateUser();
        com.urbanlogiq.ulsdk.api.directory.Directory.updateUser(
            ctx,
            p0,
            body
        );
    }

    @Test
    public void testDeleteUser() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.Directory.deleteUser(
            ctx,
            p0
        );
    }

    @Test
    public void testGetGroups() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.AdGroup[] expected = new com.urbanlogiq.ulsdk.api.directory.AdGroup[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.AdGroup();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.AdGroup[] result = com.urbanlogiq.ulsdk.api.directory.Directory.getGroups(
            ctx
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testCreateGroup() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        com.urbanlogiq.ulsdk.api.directory.CreateGroup body = new com.urbanlogiq.ulsdk.api.directory.CreateGroup();
        com.urbanlogiq.ulsdk.api.directory.AdGroup expected = new com.urbanlogiq.ulsdk.api.directory.AdGroup();
        byte[] expectedBytes = new org.json.JSONObject(expected.toMap()).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.AdGroup result = com.urbanlogiq.ulsdk.api.directory.Directory.createGroup(
            ctx,
            body
        );
        org.junit.Assert.assertTrue(result.equals(expected));
    }

    @Test
    public void testGetGroupMembers() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.GroupMembership[] expected = new com.urbanlogiq.ulsdk.api.directory.GroupMembership[5];
        for (int i = 0; i < 5; i++) {
            expected[i] = new com.urbanlogiq.ulsdk.api.directory.GroupMembership();
        }
        byte[] expectedBytes = new org.json.JSONArray(expected).toString().getBytes();
        ctx.setResponse(expectedBytes);
        com.urbanlogiq.ulsdk.api.directory.GroupMembership[] result = com.urbanlogiq.ulsdk.api.directory.Directory.getGroupMembers(
            ctx,
            p0
        );
        org.junit.Assert.assertArrayEquals(result, expected);
    }

    @Test
    public void testDeleteGroup() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.Directory.deleteGroup(
            ctx,
            p0
        );
    }

    @Test
    public void testAddGroupMember() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        java.util.UUID p1 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.Directory.addGroupMember(
            ctx,
            p0,
            p1
        );
    }

    @Test
    public void testRemoveGroupMember() throws java.net.URISyntaxException, java.io.IOException, java.lang.InterruptedException {
        String caUser = System.getenv("CA_USER");
        String caAccessKey = System.getenv("CA_ACCESS_KEY");
        String caSecretKey = System.getenv("CA_SECRET_KEY");

        if (caUser == null || caAccessKey == null || caSecretKey == null) {
            throw new RuntimeException("user / key not present, cannot run tests");
        }

        com.urbanlogiq.ulsdk.Key key = new com.urbanlogiq.ulsdk.Key(UUID.fromString(caUser), Region.CA, caAccessKey, caSecretKey);
        com.urbanlogiq.ulsdk.TestContext ctx = new com.urbanlogiq.ulsdk.TestContext(new ApiKeyContext(key, Environment.Stage));
        java.util.UUID p0 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        java.util.UUID p1 = java.util.UUID.fromString("00000000-0000-0000-0000-000000000000");
        com.urbanlogiq.ulsdk.api.directory.Directory.removeGroupMember(
            ctx,
            p0,
            p1
        );
    }
}