// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#include <stdexcept>

#include "ulsdk/types/SparseTensor.h"

namespace ul {
namespace types {

std::pair<::flatbuffers::Offset<void>, ::SparseTensorIndex>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const SparseTensorIndex &o) {
    if (std::holds_alternative<std::shared_ptr<SparseTensorIndexCOO>>(o)) {
        const std::shared_ptr<SparseTensorIndexCOO> &v = std::get<std::shared_ptr<SparseTensorIndexCOO>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::SparseTensorIndex::SparseTensorIndexCOO);
    } else if (std::holds_alternative<std::shared_ptr<SparseMatrixIndexCSX>>(o)) {
        const std::shared_ptr<SparseMatrixIndexCSX> &v = std::get<std::shared_ptr<SparseMatrixIndexCSX>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::SparseTensorIndex::SparseMatrixIndexCSX);
    } else if (std::holds_alternative<std::shared_ptr<SparseTensorIndexCSF>>(o)) {
        const std::shared_ptr<SparseTensorIndexCSF> &v = std::get<std::shared_ptr<SparseTensorIndexCSF>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::SparseTensorIndex::SparseTensorIndexCSF);
    } else { 
        throw std::runtime_error("unreachable");
    }
}

::flatbuffers::Offset<::SparseTensorIndexCOO>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const SparseTensorIndexCOO &o) {
    std::optional<decltype(builder.CreateVector(o.indicesStrides_.value()))> indicesStrides_offset = std::nullopt;
    if (o.indicesStrides_.has_value()) {
        const decltype(builder.CreateVector(o.indicesStrides_.value())) indicesStrides_offset_val = builder.CreateVector(o.indicesStrides_.value());
        indicesStrides_offset = std::make_optional(indicesStrides_offset_val);
    }
    const ::flatbuffers::Offset<::Int> indicesType_offset = serialize_to(builder, o.indicesType_);

    ::SparseTensorIndexCOOBuilder instance_builder = ::SparseTensorIndexCOOBuilder(builder);
    instance_builder.add_indicesBuffer(reinterpret_cast<const ::Buffer *>(&o.indicesBuffer_));
    if (indicesStrides_offset.has_value()) {
        instance_builder.add_indicesStrides(indicesStrides_offset.value());
    }
    instance_builder.add_indicesType(indicesType_offset);
    instance_builder.add_isCanonical(o.isCanonical_);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const SparseTensorIndexCOO &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

SparseTensorIndexCOO::SparseTensorIndexCOO()
    : indicesBuffer_()
    , indicesStrides_(std::nullopt)
    , indicesType_()
    , isCanonical_(false) {
}

SparseTensorIndexCOO::SparseTensorIndexCOO(const std::vector<uint8_t> &bytes)
    : SparseTensorIndexCOO(::flatbuffers::GetSizePrefixedRoot<::SparseTensorIndexCOO>(bytes.data())) {
}

SparseTensorIndexCOO::SparseTensorIndexCOO(const ::SparseTensorIndexCOO *root) 
    : indicesBuffer_()
    , indicesStrides_(std::nullopt)
    , indicesType_()
    , isCanonical_(false) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    const auto indicesBuffer__local = root->indicesBuffer();
    indicesBuffer_ = decltype(indicesBuffer_)(indicesBuffer__local);
    const auto &indicesStrides_vector = root->indicesStrides();
    if (indicesStrides_vector != nullptr) {
        decltype(indicesStrides_)::value_type indicesStrides__target = decltype(indicesStrides_)::value_type();
        std::copy(indicesStrides_vector->begin(), indicesStrides_vector->end(), std::back_inserter(indicesStrides__target));
        indicesStrides_ = std::make_optional(indicesStrides__target);
    }
    if (root->indicesType() != nullptr) {
        indicesType_ = decltype(indicesType_)(root->indicesType());
    }
    isCanonical_ = root->isCanonical();
}

bool
SparseTensorIndexCOO::operator==(const SparseTensorIndexCOO &rhs) const {
    if (this->indicesBuffer_ != rhs.indicesBuffer_) {
        return false;
    }
    if (this->indicesStrides_ != rhs.indicesStrides_) {
        return false;
    }
    if (this->indicesType_ != rhs.indicesType_) {
        return false;
    }
    if (this->isCanonical_ != rhs.isCanonical_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::SparseMatrixIndexCSX>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const SparseMatrixIndexCSX &o) {
    const ::flatbuffers::Offset<::Int> indicesType_offset = serialize_to(builder, o.indicesType_);
    const ::flatbuffers::Offset<::Int> indptrType_offset = serialize_to(builder, o.indptrType_);

    ::SparseMatrixIndexCSXBuilder instance_builder = ::SparseMatrixIndexCSXBuilder(builder);
    instance_builder.add_compressedAxis(o.compressedAxis_);
    instance_builder.add_indicesBuffer(reinterpret_cast<const ::Buffer *>(&o.indicesBuffer_));
    instance_builder.add_indicesType(indicesType_offset);
    instance_builder.add_indptrBuffer(reinterpret_cast<const ::Buffer *>(&o.indptrBuffer_));
    instance_builder.add_indptrType(indptrType_offset);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const SparseMatrixIndexCSX &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

SparseMatrixIndexCSX::SparseMatrixIndexCSX()
    : compressedAxis_(SparseMatrixCompressedAxis(0))
    , indicesBuffer_()
    , indicesType_()
    , indptrBuffer_()
    , indptrType_() {
}

SparseMatrixIndexCSX::SparseMatrixIndexCSX(const std::vector<uint8_t> &bytes)
    : SparseMatrixIndexCSX(::flatbuffers::GetSizePrefixedRoot<::SparseMatrixIndexCSX>(bytes.data())) {
}

SparseMatrixIndexCSX::SparseMatrixIndexCSX(const ::SparseMatrixIndexCSX *root) 
    : compressedAxis_(SparseMatrixCompressedAxis(0))
    , indicesBuffer_()
    , indicesType_()
    , indptrBuffer_()
    , indptrType_() {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    compressedAxis_ = root->compressedAxis();
    const auto indicesBuffer__local = root->indicesBuffer();
    indicesBuffer_ = decltype(indicesBuffer_)(indicesBuffer__local);
    if (root->indicesType() != nullptr) {
        indicesType_ = decltype(indicesType_)(root->indicesType());
    }
    const auto indptrBuffer__local = root->indptrBuffer();
    indptrBuffer_ = decltype(indptrBuffer_)(indptrBuffer__local);
    if (root->indptrType() != nullptr) {
        indptrType_ = decltype(indptrType_)(root->indptrType());
    }
}

bool
SparseMatrixIndexCSX::operator==(const SparseMatrixIndexCSX &rhs) const {
    if (this->compressedAxis_ != rhs.compressedAxis_) {
        return false;
    }
    if (this->indicesBuffer_ != rhs.indicesBuffer_) {
        return false;
    }
    if (this->indicesType_ != rhs.indicesType_) {
        return false;
    }
    if (this->indptrBuffer_ != rhs.indptrBuffer_) {
        return false;
    }
    if (this->indptrType_ != rhs.indptrType_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::SparseTensorIndexCSF>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const SparseTensorIndexCSF &o) {
    const decltype(builder.CreateVector(o.axisOrder_)) axisOrder_offset = builder.CreateVector(o.axisOrder_);
    std::vector<const ::Buffer *> indicesBuffers_offsets = std::vector<const ::Buffer *>();
    indicesBuffers_offsets.reserve(o.indicesBuffers_.size());
    for (size_t i = 0; i < o.indicesBuffers_.size(); ++i) {
        indicesBuffers_offsets.push_back(reinterpret_cast<const ::Buffer *>(o.indicesBuffers_.data() + i));
    }
    const ::flatbuffers::Offset<::flatbuffers::Vector<const ::Buffer *>> indicesBuffers_offset = builder.CreateVector(indicesBuffers_offsets);
    const ::flatbuffers::Offset<::Int> indicesType_offset = serialize_to(builder, o.indicesType_);
    std::vector<const ::Buffer *> indptrBuffers_offsets = std::vector<const ::Buffer *>();
    indptrBuffers_offsets.reserve(o.indptrBuffers_.size());
    for (size_t i = 0; i < o.indptrBuffers_.size(); ++i) {
        indptrBuffers_offsets.push_back(reinterpret_cast<const ::Buffer *>(o.indptrBuffers_.data() + i));
    }
    const ::flatbuffers::Offset<::flatbuffers::Vector<const ::Buffer *>> indptrBuffers_offset = builder.CreateVector(indptrBuffers_offsets);
    const ::flatbuffers::Offset<::Int> indptrType_offset = serialize_to(builder, o.indptrType_);

    ::SparseTensorIndexCSFBuilder instance_builder = ::SparseTensorIndexCSFBuilder(builder);
    instance_builder.add_axisOrder(axisOrder_offset);
    instance_builder.add_indicesBuffers(indicesBuffers_offset);
    instance_builder.add_indicesType(indicesType_offset);
    instance_builder.add_indptrBuffers(indptrBuffers_offset);
    instance_builder.add_indptrType(indptrType_offset);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const SparseTensorIndexCSF &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

SparseTensorIndexCSF::SparseTensorIndexCSF()
    : axisOrder_()
    , indicesBuffers_()
    , indicesType_()
    , indptrBuffers_()
    , indptrType_() {
}

SparseTensorIndexCSF::SparseTensorIndexCSF(const std::vector<uint8_t> &bytes)
    : SparseTensorIndexCSF(::flatbuffers::GetSizePrefixedRoot<::SparseTensorIndexCSF>(bytes.data())) {
}

SparseTensorIndexCSF::SparseTensorIndexCSF(const ::SparseTensorIndexCSF *root) 
    : axisOrder_()
    , indicesBuffers_()
    , indicesType_()
    , indptrBuffers_()
    , indptrType_() {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    const auto &axisOrder_vector = root->axisOrder();
    if (axisOrder_vector != nullptr) {
        std::copy(axisOrder_vector->begin(), axisOrder_vector->end(), std::back_inserter(axisOrder_));
    }
    const auto &indicesBuffers_vector = root->indicesBuffers();
    if (indicesBuffers_vector != nullptr) {
        indicesBuffers_.reserve(indicesBuffers_vector->size());
        for (const auto &i: *indicesBuffers_vector) {
            indicesBuffers_.emplace_back(i);
        }
    }
    if (root->indicesType() != nullptr) {
        indicesType_ = decltype(indicesType_)(root->indicesType());
    }
    const auto &indptrBuffers_vector = root->indptrBuffers();
    if (indptrBuffers_vector != nullptr) {
        indptrBuffers_.reserve(indptrBuffers_vector->size());
        for (const auto &i: *indptrBuffers_vector) {
            indptrBuffers_.emplace_back(i);
        }
    }
    if (root->indptrType() != nullptr) {
        indptrType_ = decltype(indptrType_)(root->indptrType());
    }
}

bool
SparseTensorIndexCSF::operator==(const SparseTensorIndexCSF &rhs) const {
    if (this->axisOrder_ != rhs.axisOrder_) {
        return false;
    }
    if (this->indicesBuffers_ != rhs.indicesBuffers_) {
        return false;
    }
    if (this->indicesType_ != rhs.indicesType_) {
        return false;
    }
    if (this->indptrBuffers_ != rhs.indptrBuffers_) {
        return false;
    }
    if (this->indptrType_ != rhs.indptrType_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::SparseTensor>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const SparseTensor &o) {
    std::vector<::flatbuffers::Offset<::TensorDim>> shape_offsets = std::vector<::flatbuffers::Offset<::TensorDim>>();
    shape_offsets.reserve(o.shape_.size());
    for (const auto &i: o.shape_) {
        shape_offsets.push_back(serialize_to(builder, i));
    }
    const ::flatbuffers::Offset<::flatbuffers::Vector<::flatbuffers::Offset<::TensorDim>>> shape_offset = builder.CreateVector(shape_offsets);
    const std::pair<::flatbuffers::Offset<void>, ::SparseTensorIndex> sparseIndex_offset = serialize_to(builder, o.sparseIndex_);
    const std::pair<::flatbuffers::Offset<void>, ::Type> type_offset = serialize_to(builder, o.type_);

    ::SparseTensorBuilder instance_builder = ::SparseTensorBuilder(builder);
    instance_builder.add_data(reinterpret_cast<const ::Buffer *>(&o.data_));
    instance_builder.add_non_zero_length(o.non_zero_length_);
    instance_builder.add_shape(shape_offset);
    instance_builder.add_sparseIndex(sparseIndex_offset.first);
    instance_builder.add_sparseIndex_type(sparseIndex_offset.second);
    instance_builder.add_type(type_offset.first);
    instance_builder.add_type_type(type_offset.second);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const SparseTensor &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

SparseTensor::SparseTensor()
    : data_()
    , non_zero_length_(0)
    , shape_()
    , sparseIndex_(std::make_shared<SparseTensorIndexCOO>())
    , type_(std::make_shared<Null>()) {
}

SparseTensor::SparseTensor(const std::vector<uint8_t> &bytes)
    : SparseTensor(::flatbuffers::GetSizePrefixedRoot<::SparseTensor>(bytes.data())) {
}

SparseTensor::SparseTensor(const ::SparseTensor *root) 
    : data_()
    , non_zero_length_(0)
    , shape_()
    , sparseIndex_(std::make_shared<SparseTensorIndexCOO>())
    , type_(std::make_shared<Null>()) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    const auto data__local = root->data();
    data_ = decltype(data_)(data__local);
    non_zero_length_ = root->non_zero_length();
    const auto &shape_vector = root->shape();
    if (shape_vector != nullptr) {
        shape_.reserve(shape_vector->size());
        for (const auto &i: *shape_vector) {
            shape_.emplace_back(i);
        }
    }
    if (root->sparseIndex() != nullptr) {
        switch (root->sparseIndex_type()) {
            case ::SparseTensorIndex::NONE: throw std::runtime_error("unexpected none variant");
            case ::SparseTensorIndex::SparseTensorIndexCOO: {
                const auto sparseIndex__local = static_cast<const ::SparseTensorIndexCOO *>(root->sparseIndex());
                std::shared_ptr<SparseTensorIndexCOO> sparseIndex__shared = std::make_shared<SparseTensorIndexCOO>(sparseIndex__local);
                sparseIndex_ = sparseIndex__shared;
                break;
            }
            case ::SparseTensorIndex::SparseMatrixIndexCSX: {
                const auto sparseIndex__local = static_cast<const ::SparseMatrixIndexCSX *>(root->sparseIndex());
                std::shared_ptr<SparseMatrixIndexCSX> sparseIndex__shared = std::make_shared<SparseMatrixIndexCSX>(sparseIndex__local);
                sparseIndex_ = sparseIndex__shared;
                break;
            }
            case ::SparseTensorIndex::SparseTensorIndexCSF: {
                const auto sparseIndex__local = static_cast<const ::SparseTensorIndexCSF *>(root->sparseIndex());
                std::shared_ptr<SparseTensorIndexCSF> sparseIndex__shared = std::make_shared<SparseTensorIndexCSF>(sparseIndex__local);
                sparseIndex_ = sparseIndex__shared;
                break;
            }
            default: throw std::runtime_error("unknown union variant");
        }
    }
    if (root->type() != nullptr) {
        switch (root->type_type()) {
            case ::Type::NONE: throw std::runtime_error("unexpected none variant");
            case ::Type::Null: {
                const auto type__local = static_cast<const ::Null *>(root->type());
                std::shared_ptr<Null> type__shared = std::make_shared<Null>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Int: {
                const auto type__local = static_cast<const ::Int *>(root->type());
                std::shared_ptr<Int> type__shared = std::make_shared<Int>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::FloatingPoint: {
                const auto type__local = static_cast<const ::FloatingPoint *>(root->type());
                std::shared_ptr<FloatingPoint> type__shared = std::make_shared<FloatingPoint>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Binary: {
                const auto type__local = static_cast<const ::Binary *>(root->type());
                std::shared_ptr<Binary> type__shared = std::make_shared<Binary>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Utf8: {
                const auto type__local = static_cast<const ::Utf8 *>(root->type());
                std::shared_ptr<Utf8> type__shared = std::make_shared<Utf8>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Bool: {
                const auto type__local = static_cast<const ::Bool *>(root->type());
                std::shared_ptr<Bool> type__shared = std::make_shared<Bool>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Decimal: {
                const auto type__local = static_cast<const ::Decimal *>(root->type());
                std::shared_ptr<Decimal> type__shared = std::make_shared<Decimal>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Date: {
                const auto type__local = static_cast<const ::Date *>(root->type());
                std::shared_ptr<Date> type__shared = std::make_shared<Date>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Time: {
                const auto type__local = static_cast<const ::Time *>(root->type());
                std::shared_ptr<Time> type__shared = std::make_shared<Time>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Timestamp: {
                const auto type__local = static_cast<const ::Timestamp *>(root->type());
                std::shared_ptr<Timestamp> type__shared = std::make_shared<Timestamp>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Interval: {
                const auto type__local = static_cast<const ::Interval *>(root->type());
                std::shared_ptr<Interval> type__shared = std::make_shared<Interval>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::List: {
                const auto type__local = static_cast<const ::List *>(root->type());
                std::shared_ptr<List> type__shared = std::make_shared<List>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Struct_: {
                const auto type__local = static_cast<const ::Struct_ *>(root->type());
                std::shared_ptr<Struct_> type__shared = std::make_shared<Struct_>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Union: {
                const auto type__local = static_cast<const ::Union *>(root->type());
                std::shared_ptr<Union> type__shared = std::make_shared<Union>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::FixedSizeBinary: {
                const auto type__local = static_cast<const ::FixedSizeBinary *>(root->type());
                std::shared_ptr<FixedSizeBinary> type__shared = std::make_shared<FixedSizeBinary>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::FixedSizeList: {
                const auto type__local = static_cast<const ::FixedSizeList *>(root->type());
                std::shared_ptr<FixedSizeList> type__shared = std::make_shared<FixedSizeList>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Map: {
                const auto type__local = static_cast<const ::Map *>(root->type());
                std::shared_ptr<Map> type__shared = std::make_shared<Map>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Duration: {
                const auto type__local = static_cast<const ::Duration *>(root->type());
                std::shared_ptr<Duration> type__shared = std::make_shared<Duration>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::LargeBinary: {
                const auto type__local = static_cast<const ::LargeBinary *>(root->type());
                std::shared_ptr<LargeBinary> type__shared = std::make_shared<LargeBinary>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::LargeUtf8: {
                const auto type__local = static_cast<const ::LargeUtf8 *>(root->type());
                std::shared_ptr<LargeUtf8> type__shared = std::make_shared<LargeUtf8>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::LargeList: {
                const auto type__local = static_cast<const ::LargeList *>(root->type());
                std::shared_ptr<LargeList> type__shared = std::make_shared<LargeList>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::RunEndEncoded: {
                const auto type__local = static_cast<const ::RunEndEncoded *>(root->type());
                std::shared_ptr<RunEndEncoded> type__shared = std::make_shared<RunEndEncoded>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::BinaryView: {
                const auto type__local = static_cast<const ::BinaryView *>(root->type());
                std::shared_ptr<BinaryView> type__shared = std::make_shared<BinaryView>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::Utf8View: {
                const auto type__local = static_cast<const ::Utf8View *>(root->type());
                std::shared_ptr<Utf8View> type__shared = std::make_shared<Utf8View>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::ListView: {
                const auto type__local = static_cast<const ::ListView *>(root->type());
                std::shared_ptr<ListView> type__shared = std::make_shared<ListView>(type__local);
                type_ = type__shared;
                break;
            }
            case ::Type::LargeListView: {
                const auto type__local = static_cast<const ::LargeListView *>(root->type());
                std::shared_ptr<LargeListView> type__shared = std::make_shared<LargeListView>(type__local);
                type_ = type__shared;
                break;
            }
            default: throw std::runtime_error("unknown union variant");
        }
    }
}

bool
SparseTensor::operator==(const SparseTensor &rhs) const {
    if (this->data_ != rhs.data_) {
        return false;
    }
    if (this->non_zero_length_ != rhs.non_zero_length_) {
        return false;
    }
    if (this->shape_ != rhs.shape_) {
        return false;
    }
    if (this->sparseIndex_ != rhs.sparseIndex_) {
        return false;
    }
    if (this->type_ != rhs.type_) {
        return false;
    }
    return true;
}

} // namespace types
} // namespace ul
