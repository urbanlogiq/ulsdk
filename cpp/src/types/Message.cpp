// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#include <stdexcept>

#include "ulsdk/types/Message.h"

namespace ul {
namespace types {

std::pair<::flatbuffers::Offset<void>, ::MessageHeader>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const MessageHeader &o) {
    if (std::holds_alternative<std::shared_ptr<Schema>>(o)) {
        const std::shared_ptr<Schema> &v = std::get<std::shared_ptr<Schema>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::MessageHeader::Schema);
    } else if (std::holds_alternative<std::shared_ptr<DictionaryBatch>>(o)) {
        const std::shared_ptr<DictionaryBatch> &v = std::get<std::shared_ptr<DictionaryBatch>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::MessageHeader::DictionaryBatch);
    } else if (std::holds_alternative<std::shared_ptr<RecordBatch>>(o)) {
        const std::shared_ptr<RecordBatch> &v = std::get<std::shared_ptr<RecordBatch>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::MessageHeader::RecordBatch);
    } else if (std::holds_alternative<std::shared_ptr<Tensor>>(o)) {
        const std::shared_ptr<Tensor> &v = std::get<std::shared_ptr<Tensor>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::MessageHeader::Tensor);
    } else if (std::holds_alternative<std::shared_ptr<SparseTensor>>(o)) {
        const std::shared_ptr<SparseTensor> &v = std::get<std::shared_ptr<SparseTensor>>(o);
        const auto offset = serialize_to(builder, *v);
        return std::make_pair(offset.Union(), ::MessageHeader::SparseTensor);
    } else { 
        throw std::runtime_error("unreachable");
    }
}

::flatbuffers::Offset<::BodyCompression>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const BodyCompression &o) {

    ::BodyCompressionBuilder instance_builder = ::BodyCompressionBuilder(builder);
    instance_builder.add_codec(o.codec_);
    instance_builder.add_method(o.method_);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const BodyCompression &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

BodyCompression::BodyCompression()
    : codec_(CompressionType(0))
    , method_(BodyCompressionMethod(0)) {
}

BodyCompression::BodyCompression(const std::vector<uint8_t> &bytes)
    : BodyCompression(::flatbuffers::GetSizePrefixedRoot<::BodyCompression>(bytes.data())) {
}

BodyCompression::BodyCompression(const ::BodyCompression *root) 
    : codec_(CompressionType(0))
    , method_(BodyCompressionMethod(0)) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    codec_ = root->codec();
    method_ = root->method();
}

bool
BodyCompression::operator==(const BodyCompression &rhs) const {
    if (this->codec_ != rhs.codec_) {
        return false;
    }
    if (this->method_ != rhs.method_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::RecordBatch>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const RecordBatch &o) {
    std::optional<::flatbuffers::Offset<::flatbuffers::Vector<const ::Buffer *>>> buffers_offset = std::nullopt;
    if (o.buffers_.has_value()) {
        const auto &buffers__var = o.buffers_.value();
        std::vector<const ::Buffer *> buffers_offsets = std::vector<const ::Buffer *>();
        buffers_offsets.reserve(buffers__var.size());
        for (size_t i = 0; i < buffers__var.size(); ++i) {
            buffers_offsets.push_back(reinterpret_cast<const ::Buffer *>(buffers__var.data() + i));
        }
        const ::flatbuffers::Offset<::flatbuffers::Vector<const ::Buffer *>> buffers_offset_val = builder.CreateVector(buffers_offsets);
        buffers_offset = std::make_optional(buffers_offset_val);
    }
    std::optional<::flatbuffers::Offset<::BodyCompression>> compression_offset = std::nullopt;
    if (o.compression_.has_value()) {
        const ::flatbuffers::Offset<::BodyCompression> compression_offset_val = serialize_to(builder, o.compression_.value());
        compression_offset = std::make_optional(compression_offset_val);
    }
    std::optional<::flatbuffers::Offset<::flatbuffers::Vector<const ::FieldNode *>>> nodes_offset = std::nullopt;
    if (o.nodes_.has_value()) {
        const auto &nodes__var = o.nodes_.value();
        std::vector<const ::FieldNode *> nodes_offsets = std::vector<const ::FieldNode *>();
        nodes_offsets.reserve(nodes__var.size());
        for (size_t i = 0; i < nodes__var.size(); ++i) {
            nodes_offsets.push_back(reinterpret_cast<const ::FieldNode *>(nodes__var.data() + i));
        }
        const ::flatbuffers::Offset<::flatbuffers::Vector<const ::FieldNode *>> nodes_offset_val = builder.CreateVector(nodes_offsets);
        nodes_offset = std::make_optional(nodes_offset_val);
    }
    std::optional<decltype(builder.CreateVector(o.variadicBufferCounts_.value()))> variadicBufferCounts_offset = std::nullopt;
    if (o.variadicBufferCounts_.has_value()) {
        const decltype(builder.CreateVector(o.variadicBufferCounts_.value())) variadicBufferCounts_offset_val = builder.CreateVector(o.variadicBufferCounts_.value());
        variadicBufferCounts_offset = std::make_optional(variadicBufferCounts_offset_val);
    }

    ::RecordBatchBuilder instance_builder = ::RecordBatchBuilder(builder);
    if (buffers_offset.has_value()) {
        instance_builder.add_buffers(buffers_offset.value());
    }
    if (compression_offset.has_value()) {
        instance_builder.add_compression(compression_offset.value());
    }
    instance_builder.add_length(o.length_);
    if (nodes_offset.has_value()) {
        instance_builder.add_nodes(nodes_offset.value());
    }
    if (variadicBufferCounts_offset.has_value()) {
        instance_builder.add_variadicBufferCounts(variadicBufferCounts_offset.value());
    }
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const RecordBatch &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

RecordBatch::RecordBatch()
    : buffers_(std::nullopt)
    , compression_(std::nullopt)
    , length_(0)
    , nodes_(std::nullopt)
    , variadicBufferCounts_(std::nullopt) {
}

RecordBatch::RecordBatch(const std::vector<uint8_t> &bytes)
    : RecordBatch(::flatbuffers::GetSizePrefixedRoot<::RecordBatch>(bytes.data())) {
}

RecordBatch::RecordBatch(const ::RecordBatch *root) 
    : buffers_(std::nullopt)
    , compression_(std::nullopt)
    , length_(0)
    , nodes_(std::nullopt)
    , variadicBufferCounts_(std::nullopt) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    const auto &buffers_vector = root->buffers();
    if (buffers_vector != nullptr) {
        decltype(buffers_)::value_type buffers__target = decltype(buffers_)::value_type();
        buffers__target.reserve(buffers_vector->size());
        for (const auto &i: *buffers_vector) {
            buffers__target.emplace_back(i);
        }
        buffers_ = std::make_optional(buffers__target);
    }
    if (root->compression() != nullptr) {
        compression_ = decltype(compression_)(root->compression());
    }
    length_ = root->length();
    const auto &nodes_vector = root->nodes();
    if (nodes_vector != nullptr) {
        decltype(nodes_)::value_type nodes__target = decltype(nodes_)::value_type();
        nodes__target.reserve(nodes_vector->size());
        for (const auto &i: *nodes_vector) {
            nodes__target.emplace_back(i);
        }
        nodes_ = std::make_optional(nodes__target);
    }
    const auto &variadicBufferCounts_vector = root->variadicBufferCounts();
    if (variadicBufferCounts_vector != nullptr) {
        decltype(variadicBufferCounts_)::value_type variadicBufferCounts__target = decltype(variadicBufferCounts_)::value_type();
        std::copy(variadicBufferCounts_vector->begin(), variadicBufferCounts_vector->end(), std::back_inserter(variadicBufferCounts__target));
        variadicBufferCounts_ = std::make_optional(variadicBufferCounts__target);
    }
}

bool
RecordBatch::operator==(const RecordBatch &rhs) const {
    if (this->buffers_ != rhs.buffers_) {
        return false;
    }
    if (this->compression_ != rhs.compression_) {
        return false;
    }
    if (this->length_ != rhs.length_) {
        return false;
    }
    if (this->nodes_ != rhs.nodes_) {
        return false;
    }
    if (this->variadicBufferCounts_ != rhs.variadicBufferCounts_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::DictionaryBatch>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const DictionaryBatch &o) {
    std::optional<::flatbuffers::Offset<::RecordBatch>> data_offset = std::nullopt;
    if (o.data_.has_value()) {
        const ::flatbuffers::Offset<::RecordBatch> data_offset_val = serialize_to(builder, o.data_.value());
        data_offset = std::make_optional(data_offset_val);
    }

    ::DictionaryBatchBuilder instance_builder = ::DictionaryBatchBuilder(builder);
    if (data_offset.has_value()) {
        instance_builder.add_data(data_offset.value());
    }
    instance_builder.add_id(o.id_);
    instance_builder.add_isDelta(o.isDelta_);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const DictionaryBatch &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

DictionaryBatch::DictionaryBatch()
    : data_(std::nullopt)
    , id_(0)
    , isDelta_(false) {
}

DictionaryBatch::DictionaryBatch(const std::vector<uint8_t> &bytes)
    : DictionaryBatch(::flatbuffers::GetSizePrefixedRoot<::DictionaryBatch>(bytes.data())) {
}

DictionaryBatch::DictionaryBatch(const ::DictionaryBatch *root) 
    : data_(std::nullopt)
    , id_(0)
    , isDelta_(false) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    if (root->data() != nullptr) {
        data_ = decltype(data_)(root->data());
    }
    id_ = root->id();
    isDelta_ = root->isDelta();
}

bool
DictionaryBatch::operator==(const DictionaryBatch &rhs) const {
    if (this->data_ != rhs.data_) {
        return false;
    }
    if (this->id_ != rhs.id_) {
        return false;
    }
    if (this->isDelta_ != rhs.isDelta_) {
        return false;
    }
    return true;
}

FieldNode::FieldNode()
    : length_(0)
    , null_count_(0) {
}

FieldNode::FieldNode(const ::FieldNode *root) 
    : length_(0)
    , null_count_(0) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    length_ = root->length();
    null_count_ = root->null_count();
}

bool
FieldNode::operator==(const FieldNode &rhs) const {
    if (this->length_ != rhs.length_) {
        return false;
    }
    if (this->null_count_ != rhs.null_count_) {
        return false;
    }
    return true;
}

::flatbuffers::Offset<::Message>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const Message &o) {
    std::optional<::flatbuffers::Offset<::flatbuffers::Vector<::flatbuffers::Offset<::KeyValue>>>> custom_metadata_offset = std::nullopt;
    if (o.custom_metadata_.has_value()) {
        const auto &custom_metadata__var = o.custom_metadata_.value();
        std::vector<::flatbuffers::Offset<::KeyValue>> custom_metadata_offsets = std::vector<::flatbuffers::Offset<::KeyValue>>();
        custom_metadata_offsets.reserve(custom_metadata__var.size());
        for (const auto &i: custom_metadata__var) {
            custom_metadata_offsets.push_back(serialize_to(builder, i));
        }
        const ::flatbuffers::Offset<::flatbuffers::Vector<::flatbuffers::Offset<::KeyValue>>> custom_metadata_offset_val = builder.CreateVector(custom_metadata_offsets);
        custom_metadata_offset = std::make_optional(custom_metadata_offset_val);
    }
    std::optional<std::pair<::flatbuffers::Offset<void>, ::MessageHeader>> header_offset = std::nullopt;
    if (o.header_.has_value()) {
        const std::pair<::flatbuffers::Offset<void>, ::MessageHeader> header_offset_val = serialize_to(builder, o.header_.value());
        header_offset = std::make_optional(header_offset_val);
    }

    ::MessageBuilder instance_builder = ::MessageBuilder(builder);
    instance_builder.add_bodyLength(o.bodyLength_);
    if (custom_metadata_offset.has_value()) {
        instance_builder.add_custom_metadata(custom_metadata_offset.value());
    }
    if (header_offset.has_value()) {
        const auto header_opt = header_offset.value();
        instance_builder.add_header(header_opt.first);
        instance_builder.add_header_type(header_opt.second);
    }
    instance_builder.add_version(o.version_);
    return instance_builder.Finish();
}

std::vector<uint8_t> to_bytes(const Message &o) {
    ::flatbuffers::FlatBufferBuilder builder;
    const auto offset = serialize_to(builder, o);
    builder.FinishSizePrefixed(offset);
    const auto span = builder.GetBufferSpan();
    return std::vector<uint8_t>(span.begin(), span.end());
}

Message::Message()
    : bodyLength_(0)
    , custom_metadata_(std::nullopt)
    , header_(std::nullopt)
    , version_(MetadataVersion(0)) {
}

Message::Message(const std::vector<uint8_t> &bytes)
    : Message(::flatbuffers::GetSizePrefixedRoot<::Message>(bytes.data())) {
}

Message::Message(const ::Message *root) 
    : bodyLength_(0)
    , custom_metadata_(std::nullopt)
    , header_(std::nullopt)
    , version_(MetadataVersion(0)) {
    if (root == nullptr) {
        throw std::runtime_error("cannot deserialize flatbuffer type");
    }

    bodyLength_ = root->bodyLength();
    const auto &custom_metadata_vector = root->custom_metadata();
    if (custom_metadata_vector != nullptr) {
        decltype(custom_metadata_)::value_type custom_metadata__target = decltype(custom_metadata_)::value_type();
        custom_metadata__target.reserve(custom_metadata_vector->size());
        for (const auto &i: *custom_metadata_vector) {
            custom_metadata__target.emplace_back(i);
        }
        custom_metadata_ = std::make_optional(custom_metadata__target);
    }
    if (root->header() != nullptr) {
        switch (root->header_type()) {
            case ::MessageHeader::NONE: throw std::runtime_error("unexpected none variant");
            case ::MessageHeader::Schema: {
                const auto header__local = static_cast<const ::Schema *>(root->header());
                std::shared_ptr<Schema> header__shared = std::make_shared<Schema>(header__local);
                header_ = header__shared;
                break;
            }
            case ::MessageHeader::DictionaryBatch: {
                const auto header__local = static_cast<const ::DictionaryBatch *>(root->header());
                std::shared_ptr<DictionaryBatch> header__shared = std::make_shared<DictionaryBatch>(header__local);
                header_ = header__shared;
                break;
            }
            case ::MessageHeader::RecordBatch: {
                const auto header__local = static_cast<const ::RecordBatch *>(root->header());
                std::shared_ptr<RecordBatch> header__shared = std::make_shared<RecordBatch>(header__local);
                header_ = header__shared;
                break;
            }
            case ::MessageHeader::Tensor: {
                const auto header__local = static_cast<const ::Tensor *>(root->header());
                std::shared_ptr<Tensor> header__shared = std::make_shared<Tensor>(header__local);
                header_ = header__shared;
                break;
            }
            case ::MessageHeader::SparseTensor: {
                const auto header__local = static_cast<const ::SparseTensor *>(root->header());
                std::shared_ptr<SparseTensor> header__shared = std::make_shared<SparseTensor>(header__local);
                header_ = header__shared;
                break;
            }
            default: throw std::runtime_error("unknown union variant");
        }
    }
    version_ = root->version();
}

bool
Message::operator==(const Message &rhs) const {
    if (this->bodyLength_ != rhs.bodyLength_) {
        return false;
    }
    if (this->custom_metadata_ != rhs.custom_metadata_) {
        return false;
    }
    if (this->header_ != rhs.header_) {
        return false;
    }
    if (this->version_ != rhs.version_) {
        return false;
    }
    return true;
}

} // namespace types
} // namespace ul
