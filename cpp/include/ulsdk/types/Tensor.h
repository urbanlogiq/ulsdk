// Copyright (c), CommunityLogiq Software
// 
// THIS FILE IS AUTOGENERATED, DO NOT EDIT

#pragma once

#include <cstdint>
#include <memory>
#include <optional>
#include <variant>
#include <vector>

#include "flatbuffers/flatbuffers.h"
#include "ulsdk/ulsdk.h"
#include "ulsdk/types/Schema.h"
#include "ulsdk/types/generated/Tensor_generated.h"

namespace ul {
namespace types {

struct Tensor;
struct TensorDim;

struct Tensor {
    Buffer data_;
    std::vector<TensorDim> shape_;
    std::optional<std::vector<int64_t>> strides_;
    Type type_;

    Tensor();
    Tensor(const ::Tensor *root);
    Tensor(const std::vector<uint8_t> &bytes);
    bool operator==(const Tensor &rhs) const;
    bool operator!=(const Tensor &rhs) const {
        return !(*this == rhs);
    }
};

///
/// ----------------------------------------------------------------------
/// Data structures for dense tensors
/// Shape data for a single axis in a tensor
///
struct TensorDim {
    std::optional<std::string> name_;
    int64_t size_;

    TensorDim();
    TensorDim(const ::TensorDim *root);
    TensorDim(const std::vector<uint8_t> &bytes);
    bool operator==(const TensorDim &rhs) const;
    bool operator!=(const TensorDim &rhs) const {
        return !(*this == rhs);
    }
};

::flatbuffers::Offset<::Tensor>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const Tensor &);

::flatbuffers::Offset<::TensorDim>
serialize_to(::flatbuffers::FlatBufferBuilder &builder, const TensorDim &);


std::vector<uint8_t>
to_bytes(const Tensor &o);

std::vector<uint8_t>
to_bytes(const TensorDim &o);


} // namespace types
} // namespace ul
